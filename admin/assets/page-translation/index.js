/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
/******/ 	var __webpack_modules__ = ({

/***/ "./assets/logo/chrome.js":
/*!*******************************!*\
  !*** ./assets/logo/chrome.js ***!
  \*******************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   ChromeIcon: () => (/* binding */ ChromeIcon)
/* harmony export */ });
const ChromeIcon = ({
  className
}) => /*#__PURE__*/React.createElement("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 48 48",
  className: className
}, /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("linearGradient", {
  id: "chrome-a",
  x1: "3.2173",
  y1: "15",
  x2: "44.7812",
  y2: "15",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/React.createElement("stop", {
  offset: "0",
  stopColor: "#d93025"
}), /*#__PURE__*/React.createElement("stop", {
  offset: "1",
  stopColor: "#ea4335"
})), /*#__PURE__*/React.createElement("linearGradient", {
  id: "chrome-b",
  x1: "20.7219",
  y1: "47.6791",
  x2: "41.5039",
  y2: "11.6837",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/React.createElement("stop", {
  offset: "0",
  stopColor: "#fcc934"
}), /*#__PURE__*/React.createElement("stop", {
  offset: "1",
  stopColor: "#fbbc04"
})), /*#__PURE__*/React.createElement("linearGradient", {
  id: "chrome-c",
  x1: "26.5981",
  y1: "46.5015",
  x2: "5.8161",
  y2: "10.506",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/React.createElement("stop", {
  offset: "0",
  stopColor: "#1e8e3e"
}), /*#__PURE__*/React.createElement("stop", {
  offset: "1",
  stopColor: "#34a853"
}))), /*#__PURE__*/React.createElement("circle", {
  cx: "24",
  cy: "23.9947",
  r: "12",
  fill: "#fff"
}), /*#__PURE__*/React.createElement("path", {
  d: "M3.2154,36A24,24,0,1,0,12,3.2154,24,24,0,0,0,3.2154,36ZM34.3923,18A12,12,0,1,1,18,13.6077,12,12,0,0,1,34.3923,18Z",
  fill: "none"
}), /*#__PURE__*/React.createElement("path", {
  d: "M24,12H44.7812a23.9939,23.9939,0,0,0-41.5639.0029L13.6079,30l.0093-.0024A11.9852,11.9852,0,0,1,24,12Z",
  fill: "url(#chrome-a)"
}), /*#__PURE__*/React.createElement("circle", {
  cx: "24",
  cy: "24",
  r: "9.5",
  fill: "#1a73e8"
}), /*#__PURE__*/React.createElement("path", {
  d: "M34.3913,30.0029,24.0007,48A23.994,23.994,0,0,0,44.78,12.0031H23.9989l-.0025.0093A11.985,11.985,0,0,1,34.3913,30.0029Z",
  fill: "url(#chrome-b)"
}), /*#__PURE__*/React.createElement("path", {
  d: "M13.6086,30.0031,3.218,12.006A23.994,23.994,0,0,0,24.0025,48L34.3931,30.0029l-.0067-.0068a11.9852,11.9852,0,0,1-20.7778.007Z",
  fill: "url(#chrome-c)"
}));

/***/ }),

/***/ "./assets/logo/edge.js":
/*!*****************************!*\
  !*** ./assets/logo/edge.js ***!
  \*****************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   EdgeIcon: () => (/* binding */ EdgeIcon)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");

const EdgeIcon = ({
  className
}) => /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  viewBox: "0 0 64 64",
  className: className,
  "aria-label": "Edge"
}, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("radialGradient", {
  id: "edge-a",
  cx: "276.913",
  cy: "346.817",
  r: "121.663",
  fx: "276.913",
  fy: "346.817",
  gradientTransform: "matrix(.1753 0 0 .156 -10.664 -9.52)",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: ".8",
  stopColor: "#159"
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: "1",
  stopColor: "#148"
})), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("radialGradient", {
  id: "edge-b",
  cx: "205.352",
  cy: "330.898",
  r: "190.057",
  fx: "205.352",
  fy: "330.898",
  gradientTransform: "matrix(.15338 0 0 .1783 -10.664 -9.52)",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: ".8",
  stopColor: "#18d"
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: "1",
  stopColor: "#07a"
})), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("radialGradient", {
  id: "edge-c",
  cx: "83.629",
  cy: "141.15",
  r: "313.665",
  fx: "83.629",
  fy: "141.15",
  gradientTransform: "matrix(.20404 0 0 .13403 -10.664 -9.52)",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: ".4",
  stopColor: "#3ce"
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: "1",
  stopColor: "#3c5"
})), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("radialGradient", {
  id: "edge-d",
  cx: "473.163",
  cy: "166.08",
  r: "113.777",
  fx: "473.163",
  fy: "166.08",
  gradientTransform: "matrix(.15025 0 0 .182 -10.664 -9.52)",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: "0",
  stopColor: "#6e7"
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("stop", {
  offset: "1",
  stopColor: "#6e7",
  stopOpacity: "0"
})), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
  fill: "url(#edge-a)",
  d: "M28.159 25.603s-4.166 1.97-4.166 6.894a22.16 18.382 0 0 0 33.658 15.427 1 .985 0 0 1 1.5 1.15C41.987 74.84 4.331 64.993 20.493 29.377",
  style: {
    fill: 'url(#edge-a)',
    strokeWidth: '.165368'
  }
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
  fill: "url(#edge-b)",
  d: "M39.322 15.592H5a31.991 31.512 0 0 0 36.656 46.94 19.745 19.327 86.587 0 1-12.33-37.421 7.998 8.042 0 0 1 10.664 6.73",
  style: {
    fill: 'url(#edge-b)',
    strokeWidth: '.165368'
  }
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
  fill: "url(#edge-c)",
  d: "M0 32.004c.666-18.053 30.991-22.649 38.823-5.088 4.332 11.489-5.666 10.668.5 14.443 11.83 4.924 26.492-1.149 24.493-16.74C55.984-9.027 1.833-6.893 0 32.004",
  style: {
    fill: 'url(#edge-c)',
    strokeWidth: '.165368'
  }
}), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("path", {
  fill: "url(#edge-d)",
  d: "M29.658 0c41.822 2.298 43.655 47.596 14.33 40.703",
  style: {
    fill: 'url(#edge-d)',
    strokeWidth: '.165368'
  }
}));

/***/ }),

/***/ "./assets/logo/gemini.js":
/*!*******************************!*\
  !*** ./assets/logo/gemini.js ***!
  \*******************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GeminiIcon: () => (/* binding */ GeminiIcon)
/* harmony export */ });
const GeminiIcon = ({
  className
}) => /*#__PURE__*/React.createElement("svg", {
  className: className,
  viewBox: "0 0 65 65",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg"
}, /*#__PURE__*/React.createElement("mask", {
  id: "maskme",
  style: {
    maskType: 'alpha'
  },
  maskUnits: "userSpaceOnUse",
  x: "0",
  y: "0",
  width: "65",
  height: "65"
}, /*#__PURE__*/React.createElement("path", {
  d: "M32.447 0c.68 0 1.273.465 1.439 1.125a38.904 38.904 0 001.999 5.905c2.152 5 5.105 9.376 8.854 13.125 3.751 3.75 8.126 6.703 13.125 8.855a38.98 38.98 0 005.906 1.999c.66.166 1.124.758 1.124 1.438 0 .68-.464 1.273-1.125 1.439a38.902 38.902 0 00-5.905 1.999c-5 2.152-9.375 5.105-13.125 8.854-3.749 3.751-6.702 8.126-8.854 13.125a38.973 38.973 0 00-2 5.906 1.485 1.485 0 01-1.438 1.124c-.68 0-1.272-.464-1.438-1.125a38.913 38.913 0 00-2-5.905c-2.151-5-5.103-9.375-8.854-13.125-3.75-3.749-8.125-6.702-13.125-8.854a38.973 38.973 0 00-5.905-2A1.485 1.485 0 010 32.448c0-.68.465-1.272 1.125-1.438a38.903 38.903 0 005.905-2c5-2.151 9.376-5.104 13.125-8.854 3.75-3.749 6.703-8.125 8.855-13.125a38.972 38.972 0 001.999-5.905A1.485 1.485 0 0132.447 0z",
  fill: "#000"
}), /*#__PURE__*/React.createElement("path", {
  d: "M32.447 0c.68 0 1.273.465 1.439 1.125a38.904 38.904 0 001.999 5.905c2.152 5 5.105 9.376 8.854 13.125 3.751 3.75 8.126 6.703 13.125 8.855a38.98 38.98 0 005.906 1.999c.66.166 1.124.758 1.124 1.438 0 .68-.464 1.273-1.125 1.439a38.902 38.902 0 00-5.905 1.999c-5 2.152-9.375 5.105-13.125 8.854-3.749 3.751-6.702 8.126-8.854 13.125a38.973 38.973 0 00-2 5.906 1.485 1.485 0 01-1.438 1.124c-.68 0-1.272-.464-1.438-1.125a38.913 38.913 0 00-2-5.905c-2.151-5-5.103-9.375-8.854-13.125-3.75-3.749-8.125-6.702-13.125-8.854a38.973 38.973 0 00-5.905-2A1.485 1.485 0 010 32.448c0-.68.465-1.272 1.125-1.438a38.903 38.903 0 005.905-2c5-2.151 9.376-5.104 13.125-8.854 3.75-3.749 6.703-8.125 8.855-13.125a38.972 38.972 0 001.999-5.905A1.485 1.485 0 0132.447 0z",
  fill: "url(#prefix__paint0_linear_2001_67)"
})), /*#__PURE__*/React.createElement("g", {
  mask: "url(#maskme)"
}, /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter0_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M-5.859 50.734c7.498 2.663 16.116-2.33 19.249-11.152 3.133-8.821-.406-18.131-7.904-20.794-7.498-2.663-16.116 2.33-19.25 11.151-3.132 8.822.407 18.132 7.905 20.795z",
  fill: "#FFE432"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter1_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M27.433 21.649c10.3 0 18.651-8.535 18.651-19.062 0-10.528-8.35-19.062-18.651-19.062S8.78-7.94 8.78 2.587c0 10.527 8.35 19.062 18.652 19.062z",
  fill: "#FC413D"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter2_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M20.184 82.608c10.753-.525 18.918-12.244 18.237-26.174-.68-13.93-9.95-24.797-20.703-24.271C6.965 32.689-1.2 44.407-.519 58.337c.681 13.93 9.95 24.797 20.703 24.271z",
  fill: "#00B95C"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter3_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M20.184 82.608c10.753-.525 18.918-12.244 18.237-26.174-.68-13.93-9.95-24.797-20.703-24.271C6.965 32.689-1.2 44.407-.519 58.337c.681 13.93 9.95 24.797 20.703 24.271z",
  fill: "#00B95C"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter4_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M30.954 74.181c9.014-5.485 11.427-17.976 5.389-27.9-6.038-9.925-18.241-13.524-27.256-8.04-9.015 5.486-11.428 17.977-5.39 27.902 6.04 9.924 18.242 13.523 27.257 8.038z",
  fill: "#00B95C"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter5_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M67.391 42.993c10.132 0 18.346-7.91 18.346-17.666 0-9.757-8.214-17.667-18.346-17.667s-18.346 7.91-18.346 17.667c0 9.757 8.214 17.666 18.346 17.666z",
  fill: "#3186FF"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter6_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M-13.065 40.944c9.33 7.094 22.959 4.869 30.442-4.972 7.483-9.84 5.987-23.569-3.343-30.663C4.704-1.786-8.924.439-16.408 10.28c-7.483 9.84-5.986 23.57 3.343 30.664z",
  fill: "#FBBC04"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter7_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M34.74 51.43c11.135 7.656 25.896 5.524 32.968-4.764 7.073-10.287 3.779-24.832-7.357-32.488C49.215 6.52 34.455 8.654 27.382 18.94c-7.072 10.288-3.779 24.833 7.357 32.49z",
  fill: "#3186FF"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter8_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M54.984-2.336c2.833 3.852-.808 11.34-8.131 16.727-7.324 5.387-15.557 6.631-18.39 2.78-2.833-3.853.807-11.342 8.13-16.728 7.324-5.387 15.558-6.631 18.39-2.78z",
  fill: "#749BFF"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter9_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M31.727 16.104C43.053 5.598 46.94-8.626 40.41-15.666c-6.53-7.04-21.006-4.232-32.332 6.274s-15.214 24.73-8.683 31.77c6.53 7.04 21.006 4.232 32.332-6.274z",
  fill: "#FC413D"
})), /*#__PURE__*/React.createElement("g", {
  filter: "url(#prefix__filter10_f_2001_67)"
}, /*#__PURE__*/React.createElement("path", {
  d: "M8.51 53.838c6.732 4.818 14.46 5.55 17.262 1.636 2.802-3.915-.384-10.994-7.116-15.812-6.731-4.818-14.46-5.55-17.261-1.636-2.802 3.915.383 10.994 7.115 15.812z",
  fill: "#FFEE48"
}))), /*#__PURE__*/React.createElement("defs", null, /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter0_f_2001_67",
  x: "-19.824",
  y: "13.152",
  width: "39.274",
  height: "43.217",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "2.46",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter1_f_2001_67",
  x: "-15.001",
  y: "-40.257",
  width: "84.868",
  height: "85.688",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "11.891",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter2_f_2001_67",
  x: "-20.776",
  y: "11.927",
  width: "79.454",
  height: "90.916",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "10.109",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter3_f_2001_67",
  x: "-20.776",
  y: "11.927",
  width: "79.454",
  height: "90.916",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "10.109",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter4_f_2001_67",
  x: "-19.845",
  y: "15.459",
  width: "79.731",
  height: "81.505",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "10.109",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter5_f_2001_67",
  x: "29.832",
  y: "-11.552",
  width: "75.117",
  height: "73.758",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "9.606",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter6_f_2001_67",
  x: "-38.583",
  y: "-16.253",
  width: "78.135",
  height: "78.758",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "8.706",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter7_f_2001_67",
  x: "8.107",
  y: "-5.966",
  width: "78.877",
  height: "77.539",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "7.775",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter8_f_2001_67",
  x: "13.587",
  y: "-18.488",
  width: "56.272",
  height: "51.81",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "6.957",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter9_f_2001_67",
  x: "-15.526",
  y: "-31.297",
  width: "70.856",
  height: "69.306",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "5.876",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("filter", {
  id: "prefix__filter10_f_2001_67",
  x: "-14.168",
  y: "20.964",
  width: "55.501",
  height: "51.571",
  filterUnits: "userSpaceOnUse",
  colorInterpolationFilters: "sRGB"
}, /*#__PURE__*/React.createElement("feFlood", {
  floodOpacity: "0",
  result: "BackgroundImageFix"
}), /*#__PURE__*/React.createElement("feBlend", {
  in: "SourceGraphic",
  in2: "BackgroundImageFix",
  result: "shape"
}), /*#__PURE__*/React.createElement("feGaussianBlur", {
  stdDeviation: "7.273",
  result: "effect1_foregroundBlur_2001_67"
})), /*#__PURE__*/React.createElement("linearGradient", {
  id: "prefix__paint0_linear_2001_67",
  x1: "18.447",
  y1: "43.42",
  x2: "52.153",
  y2: "15.004",
  gradientUnits: "userSpaceOnUse"
}, /*#__PURE__*/React.createElement("stop", {
  stopColor: "#4893FC"
}), /*#__PURE__*/React.createElement("stop", {
  offset: ".27",
  stopColor: "#4893FC"
}), /*#__PURE__*/React.createElement("stop", {
  offset: ".777",
  stopColor: "#969DFF"
}), /*#__PURE__*/React.createElement("stop", {
  offset: "1",
  stopColor: "#BD99FE"
}))));

/***/ }),

/***/ "./assets/logo/google.js":
/*!*******************************!*\
  !*** ./assets/logo/google.js ***!
  \*******************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   GoogleIcon: () => (/* binding */ GoogleIcon)
/* harmony export */ });
const GoogleIcon = ({
  className
}) => /*#__PURE__*/React.createElement("svg", {
  className: className,
  viewBox: "-3 0 262 262",
  xmlns: "http://www.w3.org/2000/svg",
  preserveAspectRatio: "xMidYMid"
}, /*#__PURE__*/React.createElement("path", {
  d: "M255.878 133.451c0-10.734-.871-18.567-2.756-26.69H130.55v48.448h71.947c-1.45 12.04-9.283 30.172-26.69 42.356l-.244 1.622 38.755 30.023 2.685.268c24.659-22.774 38.875-56.282 38.875-96.027",
  fill: "#4285F4"
}), /*#__PURE__*/React.createElement("path", {
  d: "M130.55 261.1c35.248 0 64.839-11.605 86.453-31.622l-41.196-31.913c-11.024 7.688-25.82 13.055-45.257 13.055-34.523 0-63.824-22.773-74.269-54.25l-1.531.13-40.298 31.187-.527 1.465C35.393 231.798 79.49 261.1 130.55 261.1",
  fill: "#34A853"
}), /*#__PURE__*/React.createElement("path", {
  d: "M56.281 156.37c-2.756-8.123-4.351-16.827-4.351-25.82 0-8.994 1.595-17.697 4.206-25.82l-.073-1.73L15.26 71.312l-1.335.635C5.077 89.644 0 109.517 0 130.55s5.077 40.905 13.925 58.602l42.356-32.782",
  fill: "#FBBC05"
}), /*#__PURE__*/React.createElement("path", {
  d: "M130.55 50.479c24.514 0 41.05 10.589 50.479 19.438l36.844-35.974C195.245 12.91 165.798 0 130.55 0 79.49 0 35.393 29.301 13.925 71.947l42.211 32.783c10.59-31.477 39.891-54.251 74.414-54.251",
  fill: "#EB4335"
}));

/***/ }),

/***/ "./modules/bulk-translation/src/components/translate-provider/ai-llm/api-client.js":
/*!*****************************************************************************************!*\
  !*** ./modules/bulk-translation/src/components/translate-provider/ai-llm/api-client.js ***!
  \*****************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   chunkStringMap: () => (/* binding */ chunkStringMap),
/* harmony export */   logAiTranslationError: () => (/* binding */ logAiTranslationError),
/* harmony export */   requestAiBatch: () => (/* binding */ requestAiBatch)
/* harmony export */ });
/**
 * Log AI translation failures to the browser console (matches Polylang Pro UX).
 *
 * @param {string} [message]
 * @param {{ emptyResponse?: boolean }} [opts]
 */
function logAiTranslationError(message, {
  emptyResponse
} = {}) {
  console.group('Automatic Translation Error');
  if (emptyResponse) {
    console.warn('Empty response');
  } else if (message) {
    console.log('%c' + String(message), 'color: red; font-weight: bold; font-size: 1.2rem;');
  }
  console.groupEnd();
}

/**
 * Server-side LLM batch translation (Gemini via WordPress AI Client).
 *
 * @param {Object} opts
 * @param {string} opts.provider
 * @param {number|string} opts.postId
 * @param {string} [opts.objectType]
 * @param {string} opts.sourceLang
 * @param {string} opts.targetLang
 * @param {Record<string,string>} opts.strings
 * @param {string} [opts.model] Optional model id override
 * @param {string} opts.restUrl Full REST URL (…/bulk-translate/ai-translate-batch)
 * @param {string} opts.nonce wp_rest nonce
 * @param {AbortSignal} [opts.signal] Optional abort signal (modal closed / user cancelled).
 * @returns {Promise<Record<string,string>>}
 */
async function requestAiBatch({
  provider,
  postId,
  objectType = 'post',
  sourceLang,
  targetLang,
  strings,
  model = '',
  restUrl,
  nonce,
  signal
}) {
  let res;
  try {
    res = await fetch(restUrl, {
      method: 'POST',
      credentials: 'same-origin',
      headers: {
        'Content-Type': 'application/json',
        'X-WP-Nonce': nonce
      },
      ...(signal ? {
        signal
      } : {}),
      body: JSON.stringify({
        provider,
        post_id: Number(postId),
        object_type: objectType,
        source_lang: sourceLang,
        target_lang: targetLang,
        model: model || '',
        strings
      })
    });
  } catch (error) {
    const networkMessage = error?.message || 'Network request failed';
    throw createTranslationError(`Translation request failed: ${networkMessage}`);
  }
  let data = {};
  try {
    data = await res.json();
  } catch {
    data = {};
  }
  if (!res.ok) {
    if (isQuotaExceededResponse(data, res.status)) {
      throw createTranslationError('API quota exceeded (429). Please check your plan/billing and retry later.', 'LLM_QUOTA_EXCEEDED');
    }
    const errorMessage = resolveErrorMessage(data, res.status, res.statusText);
    throw createTranslationError(errorMessage);
  }
  return data.translations && typeof data.translations === 'object' ? data.translations : {};
}
function resolveErrorMessage(data, status, statusText) {
  const messageFromBody = firstNonEmptyString(data?.message, data?.data, data?.data?.message, data?.data?.error);
  if (isQuotaExceededResponse(data, status)) {
    return 'API quota exceeded (429). Please check your plan/billing and retry later.';
  }
  if (messageFromBody) {
    return sanitizeErrorMessage(messageFromBody);
  }
  if (status === 401 || status === 403) {
    return 'You are not authorized to translate this content.';
  }
  if (status >= 500) {
    return 'AI translation service is temporarily unavailable (server returned 5xx). Please retry in a minute.';
  }
  return statusText || 'Translation request failed';
}
function createTranslationError(message, code = 'LLM_REQUEST_FAILED') {
  const error = new Error(message);
  error.code = code;
  return error;
}
function firstNonEmptyString(...values) {
  for (const value of values) {
    if (typeof value === 'string' && value.trim() !== '') {
      return value.trim();
    }
  }
  return '';
}
function sanitizeErrorMessage(message) {
  if (!message) {
    return 'Translation request failed';
  }

  // Keep only the primary line and remove verbose docs/traces.
  const firstLine = message.split(/\r?\n/).map(line => line.trim()).filter(Boolean)[0] || message;
  const compact = firstLine.replace(/\s+/g, ' ').trim();
  const trimmedAtDocs = compact.split(/\s+For more information/i)[0].trim();
  const trimmed = trimmedAtDocs || compact;
  return trimmed.length > 220 ? `${trimmed.slice(0, 217)}...` : trimmed;
}
function isQuotaExceededResponse(data, status) {
  if (status === 429) {
    return true;
  }
  const probe = [data?.message, data?.data, data?.data?.message, data?.data?.error].filter(value => typeof value === 'string').join(' ').toLowerCase();
  return probe.includes('quota exceeded') || probe.includes('too many requests') || probe.includes('rate limit') || probe.includes('free_tier');
}

/**
 * @param {Record<string,string>} map
 * @param {{maxTokens?:number,maxChars?:number,maxKeys?:number}} [opts]
 * @returns {Array<Record<string,string>>}
 */
function chunkStringMap(map, opts = {}) {
  const maxTokens = Number.isFinite(opts.maxTokens) ? Number(opts.maxTokens) : 500;
  const maxChars = Number.isFinite(opts.maxChars) ? Number(opts.maxChars) : Infinity;
  const maxKeys = Number.isFinite(opts.maxKeys) ? Number(opts.maxKeys) : 0; // 0 => unlimited

  const chunks = [];
  let current = {};
  let charBudget = 0;
  let tokenBudget = 0;
  const keys = Object.keys(map);
  for (const k of keys) {
    const v = map[k] ?? '';
    const value = String(v);
    const entryChars = k.length + value.length;
    const entryTokens = Math.ceil(value.length / 4);
    const wouldExceedKeys = maxKeys > 0 && Object.keys(current).length >= maxKeys;
    const wouldExceedChars = Number.isFinite(maxChars) && charBudget + entryChars > maxChars && Object.keys(current).length > 0;
    const wouldExceedTokens = tokenBudget + entryTokens > maxTokens && Object.keys(current).length > 0;
    if (wouldExceedKeys || wouldExceedChars || wouldExceedTokens) {
      chunks.push(current);
      current = {};
      charBudget = 0;
      tokenBudget = 0;
    }
    current[k] = value;
    charBudget += entryChars;
    tokenBudget += entryTokens;
  }
  if (Object.keys(current).length > 0) {
    chunks.push(current);
  }
  return chunks.length ? chunks : [{}];
}

/***/ }),

/***/ "./modules/page-translation/src/component/RightPopup/index.js":
/*!********************************************************************!*\
  !*** ./modules/page-translation/src/component/RightPopup/index.js ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   AddGlossaryPopup: () => (/* binding */ AddGlossaryPopup),
/* harmony export */   GlossaryPopup: () => (/* binding */ GlossaryPopup),
/* harmony export */   InfoPopup: () => (/* binding */ InfoPopup)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");

const AddGlossaryPopup = ({
  position,
  sourceLang,
  targetLang,
  selectedSourceText,
  onClose,
  setGlossaryTerms,
  setGlossaryOrignalTerms
}) => {
  const [type, setType] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)("general");
  const [sourceText, setSourceText] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(selectedSourceText || "");
  const [selectedText, setSelectedText] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(selectedSourceText || "");
  const [targetText, setTargetText] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('');
  const [description, setDescription] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const [saving, setSaving] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [error, setError] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const [successMessage, setSuccessMessage] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)("");
  const [extractedTexts, setExtractedTexts] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)([]);

  // Function to find the best matching text
  const findBestMatch = texts => {
    if (!texts || texts.length === 0) return '';

    // Try to find text that matches common patterns
    for (const text of texts) {
      const trimmed = text.trim();
      // Look for text ending with colon or starting with common label patterns
      if (trimmed.endsWith(':') || /^(Label|Title|Heading|Text):/i.test(trimmed)) {
        return trimmed;
      }
    }

    // If no pattern matches, return the first non-empty text
    return texts[0] || '';
  };

  // Function to extract text content from HTML
  const extractTextFromHtml = html => {
    // Create temporary div to parse HTML
    const temp = document.createElement('div');
    temp.innerHTML = html;

    // Get all text nodes
    const texts = [];
    const walk = document.createTreeWalker(temp, NodeFilter.SHOW_TEXT, null, false);
    let node;
    while (node = walk.nextNode()) {
      const text = node.textContent.trim();
      if (text && text.length > 1) {
        // Skip single characters
        // Get parent element to check context
        const parent = node.parentElement;
        if (parent) {
          // If text is inside a tag like <strong>, get its full content
          const fullText = parent.textContent.trim();
          // Check if it ends with punctuation like colon
          if (fullText.endsWith(':')) {
            texts.push(fullText);
          }
        }
        // Also add the text node content itself
        texts.push(text);
      }
    }

    // Remove duplicates but preserve order
    return [...new Set(texts)];
  };

  // Effect to process selectedSourceText when it changes
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (selectedSourceText) {
      if (selectedSourceText.includes('<') && selectedSourceText.includes('>')) {
        // Looks like HTML - extract text content
        const texts = extractTextFromHtml(selectedSourceText);
        setExtractedTexts(texts);
        if (texts.length > 0) {
          // Find the best match or use first text
          const bestMatch = findBestMatch(texts);
          setSelectedText(bestMatch);
          setSourceText(bestMatch);
        }
      } else {
        // Plain text - use as is
        setSelectedText(selectedSourceText);
        setSourceText(selectedSourceText);
        setExtractedTexts([]);
      }
    }
  }, [selectedSourceText]);
  const popupRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const infoPopupRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const handleClickOutside = event => {
      const popup = popupRef.current;
      const infoPopup = infoPopupRef.current || document.querySelector(".lmat-page-translation-info-popup");
      const clickedOutsidePopup = popup && !popup.contains(event.target);
      const clickedOutsideInfo = infoPopup ? !infoPopup.contains(event.target) : true;
      if (clickedOutsidePopup && clickedOutsideInfo) {
        onClose();
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return () => document.removeEventListener("mousedown", handleClickOutside);
  }, [onClose]);
  const handleSubmit = async e => {
    e.preventDefault();
    setSaving(true);
    setError("");
    if (!type || !sourceText?.trim() || !sourceLang?.trim()) {
      setError("Type, Term, and Source Language are required.");
      setSaving(false);
      return;
    }
    try {
      if (!lmatPageTranslationGlobal?.add_glossary_validate) {
        setError('Security token missing. Please refresh the page.');
        setSaving(false);
        return;
      }
      const formData = new FormData();
      formData.append("action", "lmat_add_glossary");
      formData.append("_wpnonce", lmatPageTranslationGlobal.add_glossary_validate);
      formData.append("type", type);
      formData.append("term", sourceText.trim());
      formData.append("description", description || "");
      formData.append("source_lang", sourceLang);
      if (targetText && targetText.trim() && targetLang) {
        formData.append(`translations[${targetLang}]`, targetText.trim());
      }
      const ajax = lmatPageTranslationGlobal?.ajaxurl || ajaxurl;
      const response = await fetch(ajax, {
        method: "POST",
        credentials: "same-origin",
        body: formData
      });
      let data;
      try {
        const responseText = await response.text();
        try {
          data = JSON.parse(responseText);
        } catch (e) {
          console.error("Failed to parse response:", responseText);
          throw new Error("Invalid response from server");
        }
      } catch (e) {
        console.error("Error reading response:", e);
        throw new Error("Failed to read server response");
      }
      if (data?.success) {
        if (typeof setGlossaryTerms === "function") {
          try {
            const data = {
              action: 'lmat_get_glossary',
              source_lang: sourceLang,
              target_lang: targetLang,
              _wpnonce: lmatPageTranslationGlobal.get_glossary_validate
            };
            // Add sourceLang and targetLang as query params
            const url = `${lmatPageTranslationGlobal.ajax_url}`;
            const refreshResponse = await fetch(url, {
              method: 'POST',
              headers: {
                'Content-Type': 'application/x-www-form-urlencoded; charset=UTF-8',
                'Accept': 'application/json'
              },
              credentials: 'same-origin',
              body: new URLSearchParams(data)
            });
            if (refreshResponse.ok) {
              const refreshData = await refreshResponse.json();
              if (refreshData?.success) {
                let glossaryOrignalTerms = [];
                refreshData.data.terms.forEach(term => {
                  if (term.original_term && term.original_term !== '') {
                    glossaryOrignalTerms.push(term.original_term);
                  }
                });
                setGlossaryOrignalTerms(glossaryOrignalTerms);
                setGlossaryTerms(refreshData.data?.terms || []);
                setSuccessMessage("Glossary term added successfully!");
                setTimeout(() => {
                  onClose();
                }, 3000);
              }
            }
          } catch (refreshErr) {
            console.error("Error refreshing glossary:", refreshErr);
          }
        }
      } else {
        const msg = data?.data || "Could not add glossary term";
        if (msg === "This term already exists in this language.") {
          setError("This term already exists in this language.");
        } else if (msg === "Permission denied") {
          setError("You do not have permission to add glossary terms.");
        } else if (typeof msg === "string") {
          setError(msg);
        } else {
          setError("Failed to add glossary term.");
        }
      }
    } catch (err) {
      console.error("Error saving glossary term:", err);
      setError("Failed to add glossary term. Please try again.");
    } finally {
      setSaving(false);
    }
  };

  // ----- Positioning -----
  const style = {};
  if (typeof window !== "undefined" && position?.right != null) {
    style.right = window.innerWidth - position.right;
  }
  if (typeof window !== "undefined" && position?.bottom != null) {
    style.bottom = window.innerHeight - position.bottom + 20;
    if (style.right != null) style.right = style.right - 30;
  } else if (position?.top != null) {
    style.top = position.top;
  }
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: popupRef,
    className: "lmat-page-translation-glossary-popup lmat-page-translation-add-glossary-popup",
    style: style,
    onClick: e => e.stopPropagation(),
    role: "dialog",
    "aria-modal": "true",
    "aria-labelledby": "lmat-page-translation-add-glossary-title"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-glossary-popup__header"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    id: "lmat-page-translation-add-glossary-title"
  }, "Add New Glossary Term")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("form", {
    onSubmit: handleSubmit,
    className: "lmat-page-translation-add-glossary-form"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-type"
  }, "Type:"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("select", {
    id: "lmat-page-translation-type",
    value: type,
    onChange: e => setType(e.target.value),
    required: true
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("option", {
    value: "general"
  }, "General"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("option", {
    value: "name"
  }, "Name"))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, extractedTexts.length > 0 ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-term-select"
  }, "Select Glossary Term:"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("select", {
    id: "lmat-page-translation-term-select",
    value: selectedText,
    onChange: e => {
      const selected = e.target.value;
      setSelectedText(selected);
      setSourceText(selected);
    },
    required: true
  }, extractedTexts.map((text, index) => {
    const displayText = text.length > 50 ? text.substring(0, 47) + '...' : text;
    return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("option", {
      key: index,
      value: text
    }, displayText);
  }))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group",
    style: {
      marginTop: '16px'
    }
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-term"
  }, "Glossary Term:"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("input", {
    id: "lmat-page-translation-term",
    type: "text",
    value: sourceText,
    onChange: e => setSourceText(e.target.value),
    placeholder: "Edit selected text if needed",
    required: true
  }))) : /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-term"
  }, "Glossary Term:"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("input", {
    id: "lmat-page-translation-term",
    type: "text",
    value: sourceText,
    onChange: e => setSourceText(e.target.value),
    placeholder: "Enter source term",
    required: true
  }))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-translation"
  }, "Custom Translation ", targetLang ? `(${targetLang})` : "", ":"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("input", {
    id: "lmat-page-translation-translation",
    type: "text",
    value: targetText,
    onChange: e => setTargetText(e.target.value),
    placeholder: "Enter custom translation..."
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-group"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("label", {
    htmlFor: "lmat-page-translation-description"
  }, "Description (optional):"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("textarea", {
    id: "lmat-page-translation-description",
    value: description,
    onChange: e => setDescription(e.target.value),
    placeholder: "Enter term description (optional)...",
    rows: 3
  })), error && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-error-message",
    role: "alert"
  }, error), successMessage && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-success-message",
    role: "alert"
  }, successMessage), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-form-actions"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    type: "button",
    onClick: onClose,
    className: "button button-secondary"
  }, "Cancel"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    type: "submit",
    className: "button button-primary",
    disabled: saving
  }, saving ? "Saving..." : "Save"))));
};
const InfoPopup = /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.forwardRef(({
  left,
  top,
  onClose,
  onGlossaryClick,
  onAddGlossary,
  glossaryOpen,
  glossaryCount = 0,
  onSave,
  onCopy,
  disableGlossary = false,
  showAddGlossary = false
}, ref) => {
  const imgFolder = lmatPageTranslationGlobal.lmat_url + 'assets/images/';
  react__WEBPACK_IMPORTED_MODULE_0__.useEffect(() => {
    const handleTranslateClick = () => onClose();
    const elements = [document.getElementById('lmat-page-translation-ai-translate-btn'), document.getElementsByClassName('local_ai_translator_btn')[0], document.querySelector('.goog-te-combo'), document.getElementById('yt-widget')].filter(Boolean);
    elements.forEach(el => {
      const eventType = el.tagName === 'SELECT' ? 'change' : 'click';
      el.addEventListener(eventType, handleTranslateClick);
    });
    return () => {
      elements.forEach(el => {
        const eventType = el.tagName === 'SELECT' ? 'change' : 'click';
        el.removeEventListener(eventType, handleTranslateClick);
      });
    };
  }, [onClose]);
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    ref: ref,
    className: "lmat-page-translation-info-popup",
    style: {
      left,
      top
    },
    onClick: e => e.stopPropagation()
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: "lmat-page-translation-info-popup__close",
    onClick: onClose,
    "aria-label": "Close",
    title: "Close"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'cross.png',
    alt: "Close"
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: `lmat-page-translation-info-popup__glossary${disableGlossary ? ' lmat-page-translation-disabled' : ''}`,
    onClick: disableGlossary ? null : onGlossaryClick,
    title: disableGlossary ? "No glossary terms available" : "Glossary",
    disabled: disableGlossary
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat-page-translation-bookmark-icon-wrapper"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'bookmark.png',
    alt: "Glossary",
    className: `lmat-page-translation-bookmark-icon${glossaryOpen ? ' lmat-page-translation-bookmark-icon--active' : ''}${disableGlossary ? ' lmat-page-translation-bookmark-icon--disabled' : ''}`
  }), glossaryCount > 0 && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat-page-translation-bookmark-badge"
  }, glossaryCount))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: `lmat-page-translation-info-popup__add-glossary${showAddGlossary ? ' lmat-page-translation-active' : ''}`,
    onClick: onAddGlossary,
    title: "Add to Glossary"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'plus.png',
    alt: "Add Glossary",
    className: `lmat-page-translation-add-icon${showAddGlossary ? ' lmat-page-translation-add-icon--active' : ''}`
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: "lmat-page-translation-info-popup__copy",
    onClick: onCopy,
    title: "Copy"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'copy.png',
    alt: "Copy",
    className: "lmat-page-translation-copy-icon"
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: "lmat-page-translation-info-popup__save",
    onClick: onSave,
    "aria-label": "Save",
    title: "Save"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'save.png',
    alt: "Save",
    className: "lmat-page-translation-save-icon"
  })));
});
const GlossaryPopup = ({
  position,
  terms = [],
  targetLangLabel = "Translation",
  sourceLangLabel = "English",
  onInsert
}) => {
  const style = {
    right: window.innerWidth - position.right
  };
  if (position.bottom) {
    style.bottom = window.innerHeight - position.bottom + 20;
    style.right = style.right - 30;
  } else {
    style.top = position.top;
  }
  const truncateText = (text, maxLength = 23, sliceLength = 20) => {
    if (!text) return "";
    return text.length > maxLength ? text.slice(0, sliceLength) + "…" : text;
  };
  const imgFolder = lmatPageTranslationGlobal.lmat_url + 'assets/images/';
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-glossary-popup",
    style: style,
    onClick: e => e.stopPropagation()
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-glossary-popup__header"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat-page-translation-glossary-popup__icon"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
    src: imgFolder + 'bookmark.png',
    alt: "Glossary",
    className: "lmat-page-translation-bookmark-icon"
  })), "Glossary term in this sentence", /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat-page-translation-glossary-popup__badge"
  }, terms.length)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("table", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("thead", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", null, sourceLangLabel), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", null, targetLangLabel), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", null, "Description"), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", null))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tbody", null, terms.map((term, idx) => /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", {
    key: idx
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", {
    title: term.english
  }, truncateText(term.english)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", {
    title: term.translation
  }, truncateText(term.translation)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", {
    title: term.description
  }, truncateText(term.description)), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    className: "lmat-page-translation-glossary-insert",
    onClick: () => onInsert && onInsert(term)
  }, "Insert")))))));
};


/***/ }),

/***/ "./modules/page-translation/src/component/copy-clipboard/index.js":
/*!************************************************************************!*\
  !*** ./modules/page-translation/src/component/copy-clipboard/index.js ***!
  \************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const CopyClipboard = async ({
  text = false,
  startCopyStatus = () => {},
  endCopyStatus = () => {}
}) => {
  if (!text || text === "") return;
  try {
    if (navigator?.clipboard?.writeText) {
      await navigator.clipboard.writeText(text);
    } else {
      // Fallback method if Clipboard API is not supported
      const textArea = document.createElement('textarea');
      textArea.value = text;
      document.body.appendChild(textArea);
      textArea.select();
      if (document.execCommand) {
        document.execCommand('copy');
      }
      document.body.removeChild(textArea);
    }
    startCopyStatus();
    setTimeout(() => endCopyStatus(), 800); // Reset to "Copy" after 2 seconds
  } catch (err) {
    console.error('Error copying text to clipboard:', err);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (CopyClipboard);

/***/ }),

/***/ "./modules/page-translation/src/component/error-modal-box/index.js":
/*!*************************************************************************!*\
  !*** ./modules/page-translation/src/component/error-modal-box/index.js ***!
  \*************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _copy_clipboard_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../copy-clipboard/index.js */ "./modules/page-translation/src/component/copy-clipboard/index.js");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var dompurify__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! dompurify */ "./node_modules/dompurify/dist/purify.es.mjs");



const ErrorModalBox = ({
  message,
  onClose,
  Title
}) => {
  let dummyElement = jQuery('<div>').append(message);
  const stringifiedMessage = dummyElement.html();
  dummyElement.remove();
  dummyElement = null;
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    const clipboardElements = document.querySelectorAll('.chrome-ai-translator-flags');
    if (clipboardElements.length > 0) {
      clipboardElements.forEach(element => {
        element.classList.add('lmat-page-translation-tooltip-element');
        element.addEventListener('click', e => {
          e.preventDefault();
          const toolTipExists = element.querySelector('.lmat-page-translation-tooltip');
          if (toolTipExists) {
            return;
          }
          let toolTipElement = document.createElement('span');
          toolTipElement.textContent = "Text to be copied.";
          toolTipElement.className = 'lmat-page-translation-tooltip';
          element.appendChild(toolTipElement);
          (0,_copy_clipboard_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])({
            text: element.getAttribute('data-clipboard-text'),
            startCopyStatus: () => {
              toolTipElement.classList.add('lmat-page-translation-tooltip-active');
            },
            endCopyStatus: () => {
              setTimeout(() => {
                toolTipElement.remove();
              }, 800);
            }
          });
        });
      });
      return () => {
        clipboardElements.forEach(element => {
          element.removeEventListener('click', () => {});
        });
      };
    }
  }, []);
  return /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-error-modal-box-container"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-error-modal-box"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-error-modal-box-header"
  }, /*#__PURE__*/React.createElement("span", {
    className: "lmat-page-translation-error-modal-box-close",
    onClick: onClose
  }, "\xD7"), Title && /*#__PURE__*/React.createElement("h3", null, Title)), /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-error-modal-box-body"
  }, /*#__PURE__*/React.createElement("p", {
    dangerouslySetInnerHTML: {
      __html: dompurify__WEBPACK_IMPORTED_MODULE_2__["default"].sanitize(stringifiedMessage)
    }
  })), /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-error-modal-box-footer"
  }, /*#__PURE__*/React.createElement("button", {
    className: "lmat-page-translation-error-modal-box-close button button-primary",
    onClick: onClose
  }, "Close"))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ErrorModalBox);

/***/ }),

/***/ "./modules/page-translation/src/component/filter-nested-attr/index.js":
/*!****************************************************************************!*\
  !*** ./modules/page-translation/src/component/filter-nested-attr/index.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const FilterBlockNestedAttr = (idsArr, attrObj, blockAttr, callBack) => {
  /**
   * Iterates over the keys of the filter object and calls saveTranslatedAttr for each key.
   * 
   * @param {Array} idArr - The array of IDs.
   * @param {Object} filterObj - The filter object to iterate over.
   */
  const childAttr = (idArr, filterObj) => {
    Object.keys(filterObj).map(key => {
      let filterObjType = filterObj;
      filterObjType = filterObjType[key];
      const newIdArr = new Array(...idArr, key);
      callBack(newIdArr, filterObjType);
    });
  };

  /**
   * Handles the attributes that are arrays and objects by recursively calling saveTranslatedAttr.
   * 
   * @param {Array} idArr - The array of IDs.
   * @param {Array} attrFilter - The filter attribute array.
   */
  const childAttrArray = (idArr, attrFilter) => {
    const newIdArr = new Array(...idArr);
    let dynamicBlockAttr = blockAttr;
    newIdArr.forEach(key => {
      dynamicBlockAttr = dynamicBlockAttr[key];
    });
    if ([null, undefined].includes(dynamicBlockAttr)) {
      return;
    }
    if (Object.getPrototypeOf(dynamicBlockAttr) === Object.prototype) {
      childAttr(idArr, attrFilter[0]);
      return;
    }
    if (Object.getPrototypeOf(dynamicBlockAttr) === Array.prototype) {
      dynamicBlockAttr.forEach((_, index) => {
        const nestedId = new Array();
        newIdArr.forEach(key => {
          nestedId.push(key);
        });
        nestedId.push(index);
        callBack(nestedId, attrFilter[0]);
      });
    }
    if (typeof dynamicBlockAttr === 'object') {
      childAttr(idArr, attrFilter[0]);
      return;
    }
  };
  const typeCheck = () => {
    if (Object.getPrototypeOf(attrObj) === Array.prototype) {
      childAttrArray(idsArr, attrObj);
    } else if (Object.getPrototypeOf(attrObj) === Object.prototype) {
      childAttr(idsArr, attrObj);
    }
  };
  typeCheck();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FilterBlockNestedAttr);

/***/ }),

/***/ "./modules/page-translation/src/component/filter-target-content/index.js":
/*!*******************************************************************************!*\
  !*** ./modules/page-translation/src/component/filter-target-content/index.js ***!
  \*******************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const FilterTargetContent = (props, storeUpdateContent) => {
  const {
    item,
    saveFilteredString
  } = props;
  const skipTags = props.skipTags || [];
  const OpenSpanPlaceholder = '#lmat_page_translation_open_translate_span#';
  const CloseSpanPlaceholder = '#lmat_page_translation_close_translate_span#';
  const OpenTempTagPlaceholder = '#lmat_page_translation_temp_tag_open#';
  const CloseTempTagPlaceholder = '#lmat_page_translation_temp_tag_close#';
  const LessThanSymbol = '#lmat_page_translation_less_then_symbol#';
  const GreaterThanSymbol = '#lmat_page_translation_greater_then_symbol#';
  const entityOpenPlaceholder = '#lmat_page_translation_entity_open_translate_span#';
  const entityClosePlaceholder = '#lmat_page_translation_entity_close_translate_span#';
  const lineBreakNOpenPlaceholder = '#lmat_page_translation_line_break_n_open#';
  const lineBreakNClosePlaceholder = '#lmat_page_translation_line_break_n_close#';
  const lineBreakROpenPlaceholder = '#lmat_page_translation_line_break_r_open#';
  const lineBreakRClosePlaceholder = '#lmat_page_translation_line_break_r_close#';
  const removeInnerSpanPlaceholder = content => {
    return content.replace(new RegExp(OpenSpanPlaceholder, 'g'), '').replace(new RegExp(CloseSpanPlaceholder, 'g'), '');
  };
  const setEntityPlaceholder = content => {
    const entityRegex = /&([a-zA-Z0-9#x]+);/g;
    return content.replace(entityRegex, match => `${entityOpenPlaceholder}${match.replace(/&/g, '').replace(/;/g, '')}${entityClosePlaceholder}`);
  };
  const removeEntityPlaceholder = content => {
    return content.replace(new RegExp(entityOpenPlaceholder, 'g'), '&').replace(new RegExp(entityClosePlaceholder, 'g'), ';');
  };
  const replaceLineBreakPlaceholder = content => {
    return content.replace(/(\n)/g, () => `${lineBreakNOpenPlaceholder}${lineBreakNClosePlaceholder}`).replace(/(\r)/g, () => `${lineBreakROpenPlaceholder}${lineBreakRClosePlaceholder}`);
  };
  const removeLineBreakPlaceholder = content => {
    return content.replace(new RegExp(lineBreakNOpenPlaceholder + lineBreakNClosePlaceholder, 'g'), `${OpenSpanPlaceholder}\n${CloseSpanPlaceholder}`).replace(new RegExp(lineBreakROpenPlaceholder + lineBreakRClosePlaceholder, 'g'), `${OpenSpanPlaceholder}\r${CloseSpanPlaceholder}`);
  };
  const fixHtmlTags = content => {
    if (typeof content !== 'string' || !content.trim()) return content;
    const tagRegex = /<\/?([a-zA-Z0-9]+)(\s[^>]*)?>/g;
    const stack = [];
    let result = '';
    let lastIndex = 0;
    let match;
    while ((match = tagRegex.exec(content)) !== null) {
      const [fullMatch, tagName] = match;
      const isClosingTag = fullMatch.startsWith('</');
      const currentIndex = match.index;

      // Append content before this tag
      if (currentIndex > lastIndex) {
        result += content.slice(lastIndex, currentIndex);
      }
      if (!isClosingTag) {
        // Opening tag: push to stack
        stack.push({
          tag: tagName
        });
        result += fullMatch;
      } else {
        // Closing tag
        const openIndex = stack.findIndex(t => t.tag === tagName);
        if (openIndex !== -1) {
          // Match found: remove opening from stack
          stack.splice(openIndex, 1);
          result += fullMatch;
        } else {
          // No opening tag: insert missing opening tag before closing
          result += `${OpenTempTagPlaceholder}<${tagName}>${CloseTempTagPlaceholder}` + fullMatch;
        }
      }
      lastIndex = tagRegex.lastIndex;
    }

    // Append any remaining content after last tag
    if (lastIndex < content.length) {
      result += content.slice(lastIndex);
    }

    // Add missing closing tags at the end
    for (let i = stack.length - 1; i >= 0; i--) {
      const {
        tag
      } = stack[i];
      result += `${OpenTempTagPlaceholder}</${tag}>${CloseTempTagPlaceholder}`;
    }

    // Clear references to free memory (optional in GC-based engines, but helpful)
    match = null;
    stack.length = 0;
    content = null;
    return result;
  };

  /**
   * Wraps the first element and its matching closing tag with translation spans.
   * If no elements are found, returns the original HTML.
   * @param {string} html - The HTML string to process.
   * @returns {string} The modified HTML string with wrapped translation spans.
   */
  const wrapFirstAndMatchingClosingTag = html => {
    // Create a temporary element to parse the HTML string
    const tempElement = document.createElement('div');
    tempElement.innerHTML = html;

    // Get the first element
    const firstElement = tempElement.firstElementChild;
    if (!firstElement) {
      return html; // If no elements, return the original HTML
    }
    let childNodes = firstElement.childNodes;
    let childNodesLength = childNodes.length;
    if (childNodesLength > 0) {
      // Sort so that nodeType 3 (Text nodes) come first
      childNodes = Array.from(childNodes).sort((a, b) => a.nodeType === 3 ? -1 : b.nodeType === 3 ? 1 : 0);
      for (let i = 0; i < childNodesLength; i++) {
        let element = childNodes[i];
        if (element.nodeType === 3) {
          let textContent = element.textContent.replace(/^\s+|^\.\s+|^\s.|\s+$|\.\s+$|\s.\+$/g, match => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
          element.textContent = textContent;
        } else if (element.nodeType === 8) {
          // let textContent = `<!--${element.textContent}-->`;
          element.textContent = element.textContent;
        } else {
          let filterContent = wrapFirstAndMatchingClosingTag(element.outerHTML);
          element.outerHTML = filterContent;
        }
      }
    }

    // Get the opening tag of the first element
    // const firstElementOpeningTag = firstElement.outerHTML.match(/^<[^>]+>/)[0];
    let firstElementOpeningTag = firstElement.outerHTML.match(/^<[^>]+>/)[0];
    const pattern = new RegExp(`${OpenSpanPlaceholder}|${CloseSpanPlaceholder}`, 'g');
    firstElementOpeningTag = firstElementOpeningTag.replace(pattern, '');

    // Check if the first element has a corresponding closing tag
    const openTagName = firstElement.tagName.toLowerCase();
    const closingTagName = new RegExp(`<\/${openTagName}>`, 'i');

    // Check if the inner HTML contains the corresponding closing tag
    const closingTagMatch = firstElement.outerHTML.match(closingTagName);

    // Wrap the style element
    if (firstElementOpeningTag === '<style>') {
      let wrappedFirstTag = `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(firstElement.outerHTML)}${CloseSpanPlaceholder}`;
      return wrappedFirstTag;
    }
    let firstElementHtml = firstElement.innerHTML;
    firstElementHtml = firstElementHtml.replace(/^\s+|^\.\s+|^\s.|\s+$|\.\s+$|\s.\+$/g, match => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
    firstElement.innerHTML = '';
    let closeTag = '';
    let openTag = '';
    let filterContent = '';
    openTag = `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(firstElementOpeningTag)}${CloseSpanPlaceholder}`;
    if (closingTagMatch) {
      closeTag = `${OpenSpanPlaceholder}</${openTagName}>${CloseSpanPlaceholder}`;
    }
    if (skipTags.includes(openTagName)) {
      // Remove the custom span markers from the HTML if the tag is in skipTags
      const pattern = new RegExp(`${OpenSpanPlaceholder}|${CloseSpanPlaceholder}`, 'g');
      firstElementHtml = firstElementHtml.replace(pattern, '');
      firstElementHtml = `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(firstElementHtml)}${CloseSpanPlaceholder}`;
    }
    if ('' !== firstElementHtml) {
      if ('' !== openTag) {
        filterContent = openTag + firstElementHtml;
      }
      if ('' !== closeTag) {
        filterContent += closeTag;
      }
    } else {
      filterContent = openTag + closeTag;
    }
    firstElement.outerHTML = filterContent;

    // Return the modified HTML
    return tempElement.innerHTML;
  };

  /**
   * Splits the content string based on a specific pattern.
   * @param {string} string - The content string to split.
   * @returns {Array} An array of strings after splitting based on the pattern.
   */
  const splitContent = string => {
    const pattern = new RegExp(`(${OpenSpanPlaceholder}[\\s\\S]*?${CloseSpanPlaceholder})|'`);
    const matches = string.split(pattern).filter(Boolean);

    // Remove empty strings from the result
    const output = matches.filter(match => match.trim() !== '');
    return output;
  };

  /**
   * Filters the SEO content.
   * @param {string} content - The SEO content to filter.
   * @returns {string} The filtered SEO content.
   */
  const filterSeoContent = content => {
    const regex = /(%{1,2}[a-zA-Z0-9_]+%{0,2})/g;

    // Replace placeholders with wrapped spans
    const output = content.replace(regex, match => {
      return `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`;
    });
    return output;
  };

  /**
   * Checks if the HTML is empty or has an unclosed tag.
   * @param {string} html - The HTML string to check.
   * @returns {boolean} True if the HTML is empty or has an unclosed tag, false otherwise.
   */
  const isEmptyOrUnclosedTag = html => {
    // Clean up whitespace
    html = html.trim();

    // Regex that matches:
    // 1️⃣ Single open tag only (<div>, <table>, <span class="x">, etc.)
    // 2️⃣ Empty tag (<div></div>, <table></table>, etc.)
    // 3️⃣ Ignores self-closing tags like <img />, <br />, etc.
    const regex = /^<([a-z][a-z0-9]*)\b[^>]*>(\s*(?:<!--.*?-->\s*)*<\/\1>)?$/i;

    // Test and return true/false
    return regex.test(html);
  };

  /**
   * Synchronizes the TR and TD tags from the first string to the second string.
   * @param {string} str1 - The first string to synchronize.
   * @param {string} str2 - The second string to synchronize.
   * @returns {string} The synchronized string.
   */
  const syncTRTDFromFirstToSecond = (str1, str2) => {
    str1 = str1.trim();

    // 1️⃣ Check if first string has any tr or td
    if (!/<\/?(tr|td|th)\b[^>]*>/i.test(str1)) {
      return str2; // no tr/td → skip
    }

    // 2️⃣ Skip if second string already contains tr or td
    if (/<\/?(tr|td|th)\b[^>]*>/i.test(str2)) {
      return str2;
    }
    str2 = str2.trim();

    // 3️⃣ Extract tags (if present)
    const startTagMatch = str1.match(/^<(tr|td|th)\b[^>]*>/i); // opening tag at start
    const endTagMatch = str1.match(/<\/(tr|td|th)>\s*$/i); // closing tag at end

    // 4️⃣ Build new string using only what exists
    let newString = str2;
    if (startTagMatch) {
      newString = `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(startTagMatch[0])}${CloseSpanPlaceholder}` + newString;
    }
    if (endTagMatch) {
      newString = newString + `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(endTagMatch[0])}${CloseSpanPlaceholder}`;
    }
    return newString;
  };

  /**
   * Replaces the inner text of HTML elements with span elements for translation.
   * @param {string} string - The HTML content string to process.
   * @returns {Array} An array of strings after splitting based on the pattern.
   */
  const filterSourceData = string => {
    const isSeoContent = /^(_yoast_wpseo_|rank_math_|_seopress_)/.test(props.contentKey.trim());
    if (isSeoContent) {
      string = filterSeoContent(string);
    }

    // Replace line break with placeholder
    string = replaceLineBreakPlaceholder(string);

    // Filter shortcode content
    const shortcodePattern = /\[(.*?)\]/g;
    const shortcodeMatches = typeof string === 'string' ? string.match(shortcodePattern) : false;
    if (shortcodeMatches) {
      string = string.replace(shortcodePattern, match => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
    }
    function replaceInnerTextWithSpan(doc) {
      let childElements = doc.childNodes;
      const childElementsReplace = index => {
        if (childElements.length > index) {
          let element = childElements[index];
          let textNode = null;
          if (element.nodeType === 3) {
            const textContent = element.textContent.replace(/^\s+|^\.\s+|^\s.|\s+$|\.\s+$|\s.\+$|(\r?\n){2,}/g, match => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
            textNode = document.createTextNode(textContent);
          } else if (element.nodeType === 8) {
            textNode = document.createTextNode(`${OpenSpanPlaceholder}${LessThanSymbol}!--${removeInnerSpanPlaceholder(element.textContent)}--${GreaterThanSymbol}${CloseSpanPlaceholder}`);
          } else if (element.nodeType === 1) {
            const childNodes = element.childNodes;
            const trimmed = element.outerHTML.trim();

            // Match the outer opening tag dynamically
            const match = trimmed.match(/^<([a-zA-Z0-9]+)(\s[^>]*)?>/i);
            if (!match) return trimmed; // no valid HTML tag found

            const tagName = match[1];
            const attrs = match[2] || "";
            const hasClosingTag = new RegExp(`<\\/${tagName}>\\s*$`, "i").test(trimmed);
            if (childNodes.length > 0) {
              replaceInnerTextWithSpan(element);
            }
            let filterHtml = `${OpenSpanPlaceholder}${LessThanSymbol}${tagName}${removeInnerSpanPlaceholder(attrs)}${GreaterThanSymbol}${CloseSpanPlaceholder}${setEntityPlaceholder(element.innerHTML)}`;
            if (hasClosingTag) {
              filterHtml += `${OpenSpanPlaceholder}${LessThanSymbol}/${tagName}${GreaterThanSymbol}${CloseSpanPlaceholder}`;
            }
            textNode = document.createTextNode(filterHtml);
          } else {
            let filterHtml = element.outerHTML;
            filterHtml = filterHtml.replace(/<!--([\s\S]*?)-->/g, (match, inner) => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
            let filterContent = wrapFirstAndMatchingClosingTag(filterHtml);
            textNode = document.createTextNode(filterContent);
          }
          element.replaceWith(textNode);
          index++;
          childElementsReplace(index);
        }
      };
      childElementsReplace(0);
      return doc;
    }
    let content = string;
    if (isEmptyOrUnclosedTag(string)) {
      content = string.replace(/<([a-z][a-z0-9]*)\b[^>]*>(\s*(?:<!--.*?-->\s*)*<\/\1>)?/gi, match => `${OpenSpanPlaceholder}${removeInnerSpanPlaceholder(match)}${CloseSpanPlaceholder}`);
    } else {
      const tempElement = document.createElement('div');
      tempElement.innerHTML = fixHtmlTags(string);
      replaceInnerTextWithSpan(tempElement);
      content = tempElement.innerText;
      content = content.replace(new RegExp(LessThanSymbol, 'g'), '<').replace(new RegExp(GreaterThanSymbol, 'g'), '>');
      content = syncTRTDFromFirstToSecond(string, content);
    }

    // remoove all the ${OpenTempTagPlaceholder} and ${CloseTempTagPlaceholder}
    const tempTagPattern = new RegExp(`${OpenTempTagPlaceholder}([\\s\\S]*?)(${CloseTempTagPlaceholder})`, 'g');
    content = content.replace(tempTagPattern, '');
    content = removeLineBreakPlaceholder(content);
    content = removeEntityPlaceholder(content);
    return splitContent(content);
  };

  /**
   * Regular expression pattern to match the span elements that should not be translated.
   */
  const notTranslatePattern = new RegExp(`${OpenSpanPlaceholder}[\\s\\S]*?${CloseSpanPlaceholder}`);

  /**
   * Regular expression pattern to replace the placeholder span elements.
   */
  const replacePlaceholderPattern = new RegExp(`${OpenSpanPlaceholder}|${CloseSpanPlaceholder}`, 'g');
  const filterContent = content => {
    const updatedContent = content.replace(replacePlaceholderPattern, '');
    return updatedContent;
  };
  const getFilteredString = () => {
    const shouldFilterForService = ['google', 'localAiTranslator', 'edgeLocalAiTranslator'].includes(props.service);
    if (shouldFilterForService && saveFilteredString && item && item.id && item.type && item.source && !item.filteredString) {
      const filteredString = filterSourceData(item.source);
      const filteredStringContent = filteredString.map((data, index) => {
        const notTranslate = notTranslatePattern.test(data);
        if (notTranslate) {
          const pTemp = document.createElement('p');
          pTemp.innerText = filterContent(data);
          return `<span class="notranslate lmat-page-translation-notraslate-tag" translate="no">${pTemp.innerHTML}</span>`;
        } else {
          return data;
        }
      }).join('');
      saveFilteredString(item.type, item.id, filteredStringContent);
    }
  };
  getFilteredString();

  /**
   * The content to be filtered based on the service type.
   * If the service is 'google' or 'localAiTranslator', the content is filtered
   * using filterSourceData; otherwise, the content remains unchanged.
   */
  const content = ['google', 'localAiTranslator', 'edgeLocalAiTranslator'].includes(props.service) ? filterSourceData(props.content) : props.content;
  return /*#__PURE__*/React.createElement(React.Fragment, null, ['localAiTranslator', 'edgeLocalAiTranslator', 'google'].includes(props.service) ? content.map((data, index) => {
    const notTranslate = notTranslatePattern.test(data);
    if (notTranslate) {
      return /*#__PURE__*/React.createElement("span", {
        key: index,
        className: "notranslate lmat-page-translation-notraslate-tag",
        translate: "no"
      }, filterContent(data));
    } else {
      return data;
    }
  }) : content);
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FilterTargetContent);

/***/ }),

/***/ "./modules/page-translation/src/component/format-number-count/index.js":
/*!*****************************************************************************!*\
  !*** ./modules/page-translation/src/component/format-number-count/index.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const FormatNumberCount = ({
  number
}) => {
  if (number >= 1000000) {
    return (number / 1000000).toFixed(1) + 'M';
  } else if (number >= 1000) {
    return (number / 1000).toFixed(1) + 'K';
  }
  return number;
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (FormatNumberCount);

/***/ }),

/***/ "./modules/page-translation/src/component/glossary-count/index.js":
/*!************************************************************************!*\
  !*** ./modules/page-translation/src/component/glossary-count/index.js ***!
  \************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const GlossaryCount = ({
  string,
  glossary
}) => {
  let count = 0;
  glossary.forEach(term => {
    if (string.includes(term)) {
      count++;
    }
  });
  if (!count || count < 1) return null;
  const bookmarkIcon = lmatPageTranslationGlobal.lmat_url + 'assets/images/bookmark.png';
  return /*#__PURE__*/React.createElement("span", {
    className: "lmat-page-translation-glossary-count"
  }, /*#__PURE__*/React.createElement("img", {
    src: bookmarkIcon,
    alt: "Glossary",
    className: "lmat-page-translation-glossary-count-icon"
  }), /*#__PURE__*/React.createElement("span", {
    className: "lmat-page-translation-glossary-count-badge"
  }, count));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GlossaryCount);

/***/ }),

/***/ "./modules/page-translation/src/component/notice/index.js":
/*!****************************************************************!*\
  !*** ./modules/page-translation/src/component/notice/index.js ***!
  \****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");

const Notice = props => {
  const updateNoticeWrapperHeight = () => {
    const parentNoticeWrapper = document.querySelector('.lmat-page-translation-body-notice-wrapper');
    if (parentNoticeWrapper) {
      const height = parentNoticeWrapper.offsetHeight + parentNoticeWrapper.offsetTop;
      parentNoticeWrapper.closest('.modal-body').style.setProperty('--lmat-page-translation-notice-wrapper-height', `${height}px`);
    }
  };
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (props.lastNotice) {
      updateNoticeWrapperHeight();
      window.addEventListener('resize', updateNoticeWrapperHeight);
    }
    return () => {
      window.removeEventListener('resize', updateNoticeWrapperHeight);
    };
  }, [props.lastNotice]);
  return /*#__PURE__*/React.createElement("div", {
    className: props.className
  }, props.children);
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Notice);

/***/ }),

/***/ "./modules/page-translation/src/component/progress-bar/index.js":
/*!**********************************************************************!*\
  !*** ./modules/page-translation/src/component/progress-bar/index.js ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/**
 * Adds a progress bar to the container.
 * 
 * @param {HTMLElement} container - The container element for translation.
 */
const AddProgressBar = provider => {
  const providerName = {
    gemini: "Google Gemini",
    google: "Google Translate"
  };
  const progressBarSelector = "#lmat_page_translation_strings_model .lmat_page_translation_translate_progress";
  if (!document.querySelector(`#lmat-page-translation-${provider}-progress-bar`)) {
    const progressBar = jQuery(`
            <div id="lmat-page-translation-${provider}-progress-bar" class="lmat-page-translation-translate-progress-bar">
                <div class="${provider}-translator_progress_bar" style="margin: 1.5rem auto; width: 50%;">
                <div class="${provider}-translator_progress"></div>
                </div>
                <div style="display:none; color: white;" class="${provider}-translator-strings-count hidden">
                    Wahooo! You have saved your valuable time via auto translating 
                    <strong class="totalChars"></strong> characters using 
                    <strong>
                        ${providerName[provider]}
                    </strong>
                </div>
            </div>
        `);
    jQuery(progressBarSelector).append(progressBar); // Append the progress bar to the specified selector
  } else {
    jQuery(`.${provider}-translator_progress`).css('width', '0%');
    jQuery(`.${provider}-translator-strings-count`).hide();
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (AddProgressBar);

/***/ }),

/***/ "./modules/page-translation/src/component/progress-bar/show-string-count.js":
/*!**********************************************************************************!*\
  !*** ./modules/page-translation/src/component/progress-bar/show-string-count.js ***!
  \**********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _format_number_count_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../format-number-count/index.js */ "./modules/page-translation/src/component/format-number-count/index.js");


const ShowStringCount = (provider, status = 'none', characterCount = false) => {
  if (false === characterCount) {
    characterCount = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslationInfo().sourceCharacterCount;
  }
  const stringCount = document.querySelector(`.${provider}-translator-strings-count`);
  if (stringCount) {
    stringCount.style.display = status;
    stringCount.querySelector('.totalChars').textContent = (0,_format_number_count_index_js__WEBPACK_IMPORTED_MODULE_1__["default"])({
      number: characterCount
    });
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ShowStringCount);

/***/ }),

/***/ "./modules/page-translation/src/component/store-time-taken/index.js":
/*!**************************************************************************!*\
  !*** ./modules/page-translation/src/component/store-time-taken/index.js ***!
  \**************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const StoreTimeTaken = ({
  prefix = false,
  start = false,
  end = false,
  translateStatus = false
}) => {
  const timeTaken = (end - start) / 1000; // Convert milliseconds to seconds
  const data = {};
  if (prefix) {
    data.provider = prefix;
    if (start && end) {
      const oldTimeTaken = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslationInfo().translateData[prefix]?.timeTaken || 0;
      data.timeTaken = timeTaken + oldTimeTaken;
    }
    if (translateStatus) {
      data.translateStatus = true;
    }
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').translationInfo(data);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (StoreTimeTaken);

/***/ }),

/***/ "./modules/page-translation/src/component/store-translated-string/index.js":
/*!*********************************************************************************!*\
  !*** ./modules/page-translation/src/component/store-translated-string/index.js ***!
  \*********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");


/**
 * Saves the translation data by updating the translation content based on the provided translate object and data.
 * @param {Object} translateData - The data containing translation information.
 */
const SaveTranslation = ({
  type,
  key,
  translateContent,
  source,
  provider,
  AllowedMetaFields
}) => {
  if (['title', 'excerpt', 'slug'].includes(type)) {
    const action = `${type}SaveTranslate`;
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate')[action](translateContent, provider);
  } else if (['metaFields'].includes(type)) {
    if (Object.keys(AllowedMetaFields).includes(key)) {
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').metaFieldsSaveTranslate(key, translateContent, source, provider);
    }
  } else {
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentSaveTranslate(key, translateContent, source, provider);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SaveTranslation);

/***/ }),

/***/ "./modules/page-translation/src/component/string-modal-scroll/index.js":
/*!*****************************************************************************!*\
  !*** ./modules/page-translation/src/component/string-modal-scroll/index.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../store-translated-string/index.js */ "./modules/page-translation/src/component/store-translated-string/index.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../store-time-taken/index.js */ "./modules/page-translation/src/component/store-time-taken/index.js");
/* harmony import */ var _progress_bar_index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../progress-bar/index.js */ "./modules/page-translation/src/component/progress-bar/index.js");
/* harmony import */ var _progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../progress-bar/show-string-count.js */ "./modules/page-translation/src/component/progress-bar/show-string-count.js");






/**
 * Handles the scrolling animation of a specified element.
 * 
 * @param {Object} props - The properties for the scroll animation.
 * @param {HTMLElement} props.element - The element to be scrolled.
 * @param {number} props.scrollSpeed - The duration of the scroll animation in milliseconds.
 */
const ScrollAnimation = props => {
  const {
    element,
    scrollSpeed,
    provider
  } = props;
  if (element.scrollHeight - element.offsetHeight <= 0) {
    return;
  }
  const progressBar = jQuery(`.${provider}-translator_progress_bar`);
  let startTime = null;
  let startScrollTop = element.scrollTop;
  const animateScroll = () => {
    const scrollHeight = element.scrollHeight - element.offsetHeight + 100;
    const currentTime = performance.now();
    const duration = scrollSpeed;
    const scrollTarget = scrollHeight + 2000;
    if (!startTime) {
      startTime = currentTime;
    }
    const progress = (currentTime - startTime) / duration;
    const scrollPosition = startScrollTop + (scrollTarget - startScrollTop) * progress;
    var scrollTop = element.scrollTop;
    var currentScrollHeight = element.scrollHeight;
    var clientHeight = element.clientHeight;
    var scrollPercentage = scrollTop / (currentScrollHeight - clientHeight) * 100;
    progressBar.find(`.${provider}-translator_progress`).css('width', scrollPercentage + '%');
    let percentage = (Math.round(scrollPercentage * 10) / 10).toFixed(1);
    percentage = Math.min(percentage, 100).toString();
    progressBar.find(`.${provider}-translator_progress`).text(percentage + '%');
    if (scrollPosition > scrollHeight) {
      (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_4__["default"])(provider, 'block');
      return; // Stop animate scroll
    }
    element.scrollTop = scrollPosition;
    if (scrollPosition < scrollHeight) {
      setTimeout(animateScroll, 16);
    }
  };
  animateScroll();
};

/**
 * Updates the translated content in the string container based on the provided translation object.
 */
const updateTranslatedContent = ({
  provider,
  startTime,
  endTime
}) => {
  const container = document.getElementById("lmat_page_translation_strings_model");
  const stringContainer = container.querySelector('.lmat_page_translation_string_container');
  const translatedData = stringContainer.querySelectorAll('td.translate[data-string-type]:not([data-translate-status="translated"]):not(.notranslate):not([translate="no"])');
  const totalTranslatedData = translatedData.length;
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  translatedData.forEach((ele, index) => {
    const translatedText = ele.innerText;
    const key = ele.dataset.key;
    const type = ele.dataset.stringType;
    const sourceText = ele.closest('tr').querySelector('td[data-source="source_text"]').innerText;
    (0,_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])({
      type: type,
      key: key,
      translateContent: translatedText,
      source: sourceText,
      provider: provider,
      AllowedMetaFields
    });
    const translationEntry = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getTranslationInfo().translateData[provider];
    const previousTargetStringCount = translationEntry && translationEntry.targetStringCount ? translationEntry.targetStringCount : 0;
    const previousTargetWordCount = translationEntry && translationEntry.targetWordCount ? translationEntry.targetWordCount : 0;
    const previousTargetCharacterCount = translationEntry && translationEntry.targetCharacterCount ? translationEntry.targetCharacterCount : 0;
    if (translatedText.trim() !== '' && translatedText.trim().length > 0) {
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate').translationInfo({
        targetStringCount: previousTargetStringCount + sourceText.trim().split(/(?<=[.!?]+)\s+/).length,
        targetWordCount: previousTargetWordCount + sourceText.trim().split(/\s+/).filter(word => /[^\p{L}\p{N}]/.test(word)).length,
        targetCharacterCount: previousTargetCharacterCount + sourceText.trim().length,
        provider: provider
      });
    }
    if (index === totalTranslatedData - 1) {
      jQuery(`.${provider}-translator_progress`).css('width', '100%');
      (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_4__["default"])(provider, 'block');
      (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
        prefix: provider,
        start: startTime,
        end: endTime,
        translateStatus: true
      });
    }
  });
};

/**
 * Handles the completion of the translation process by enabling the save button,
 * updating the UI, and stopping the translation progress.
 * 
 * @param {HTMLElement} container - The container element for translation.
 * @param {number} startTime - The start time of the translation.
 * @param {number} endTime - The end time of the translation.
 * @param {Function} translateStatus - The function to call when the translation is complete.
 */
const onCompleteTranslation = ({
  container,
  provider,
  startTime,
  endTime,
  translateStatus,
  modalRenderId
}) => {
  const conainer = document.querySelector(`#lmat-page-translation-${provider}-strings-modal.modal-container[data-render-id="${modalRenderId}"]`);
  if (!conainer) {
    return;
  }
  container.querySelector(".lmat_page_translation_translate_progress").style.display = "none";
  container.querySelector(".lmat_page_translation_string_container").style.animation = "none";
  document.body.style.top = '0';
  const saveButton = container.querySelector('button.save_it');
  saveButton.removeAttribute('disabled');
  saveButton.classList.add('translated');
  saveButton.classList.remove('notranslate');
  updateTranslatedContent({
    provider,
    startTime,
    endTime
  });
  translateStatus(false);
};

/**
 * Automatically scrolls the string container and triggers the completion callback
 * when the bottom is reached or certain conditions are met.
 * 
 * @param {Function} translateStatus - Callback function to execute when translation is deemed complete.
 * @param {string} provider - The provider of the translation.
 */
const ModalStringScroll = (translateStatus, provider, modalRenderId) => {
  const startTime = new Date().getTime();
  let translateComplete = false;
  (0,_progress_bar_index_js__WEBPACK_IMPORTED_MODULE_3__["default"])(provider);
  const container = document.getElementById("lmat_page_translation_strings_model");
  const stringContainer = container.querySelector('.lmat_page_translation_string_container');
  stringContainer.scrollTop = 0;
  const scrollHeight = stringContainer.scrollHeight;
  if (scrollHeight !== undefined && scrollHeight > 100) {
    document.querySelector(".lmat_page_translation_translate_progress").style.display = "block";
    setTimeout(() => {
      const scrollSpeed = Math.ceil(scrollHeight / stringContainer?.offsetHeight) * 2000;
      ScrollAnimation({
        element: stringContainer,
        scrollSpeed: scrollSpeed,
        provider: provider
      });
    }, 2000);
    stringContainer.addEventListener('scroll', () => {
      var isScrolledToBottom = stringContainer.scrollTop + stringContainer.clientHeight + 50 >= stringContainer.scrollHeight;
      if (isScrolledToBottom && !translateComplete) {
        const endTime = new Date().getTime();
        setTimeout(() => {
          onCompleteTranslation({
            container,
            provider,
            startTime,
            endTime,
            translateStatus,
            modalRenderId
          });
        }, 4000);
        translateComplete = true;
      }
    });
    if (stringContainer.clientHeight + 10 >= scrollHeight) {
      jQuery(`.${provider}-translator_progress`).css('width', '100%');
      jQuery(`.${provider}-translator_progress`).text('100%');
      (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_4__["default"])(provider, 'block');
      const endTime = new Date().getTime();
      setTimeout(() => {
        onCompleteTranslation({
          container,
          provider,
          startTime,
          endTime,
          translateStatus,
          modalRenderId
        });
      }, 4000);
    }
  } else {
    jQuery(`.${provider}-translator_progress`).css('width', '100%');
    jQuery(`.${provider}-translator_progress`).text('100%');
    (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_4__["default"])(provider, 'block');
    const endTime = new Date().getTime();
    setTimeout(() => {
      onCompleteTranslation({
        container,
        provider,
        startTime,
        endTime,
        translateStatus,
        modalRenderId
      });
    }, 4000);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ModalStringScroll);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/ai-llm/index.js":
/*!***********************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/ai-llm/index.js ***!
  \***********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ createAiLlmPageTranslator)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../store-translated-string/index.js */ "./modules/page-translation/src/component/store-translated-string/index.js");
/* harmony import */ var _store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../store-time-taken/index.js */ "./modules/page-translation/src/component/store-time-taken/index.js");
/* harmony import */ var _bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../../../../bulk-translation/src/components/translate-provider/ai-llm/api-client.js */ "./modules/bulk-translation/src/components/translate-provider/ai-llm/api-client.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _progress_bar_index_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../progress-bar/index.js */ "./modules/page-translation/src/component/progress-bar/index.js");
/* harmony import */ var _progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../progress-bar/show-string-count.js */ "./modules/page-translation/src/component/progress-bar/show-string-count.js");








/**
 * @param {string} providerId gemini
 * @returns {(props: Object) => Promise<void>}
 */
function createAiLlmPageTranslator(providerId) {
  return async props => {
    const {
      sourceLang,
      targetLang,
      translateStatusHandler,
      ID,
      translateStatus,
      destroyUpdateHandler,
      modalRenderId,
      translationAbortSignal
    } = props;
    const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)("block-lmatPageTranslation/translate").getAllowedMetaFields();
    const clearErrorNotice = () => {
      document.dispatchEvent(new CustomEvent("lmat-page-translation:translation-error-clear", {
        bubbles: true
      }));
    };
    const releaseRecoverableUpdateBlock = () => {
      document.dispatchEvent(new CustomEvent("lmat-page-translation:recoverable-ai-error-resolved", {
        bubbles: true
      }));
    };
    const showErrorNotice = messageOrDetail => {
      const detail = messageOrDetail && typeof messageOrDetail === "object" ? messageOrDetail : {
        message: String(messageOrDetail || "")
      };
      document.dispatchEvent(new CustomEvent("lmat-page-translation:translation-error", {
        bubbles: true,
        detail
      }));
    };
    const escapeHtml = text => String(text ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;");
    const targetEl = ID ? document.getElementById(ID) : null;
    if (!targetEl) {
      return;
    }
    const buttonTextMap = {
      gemini: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Translate with Gemini", "translate-words")
    };

    // Render button (and avoid duplicating on re-renders)
    targetEl.innerHTML = "";
    const btn = document.createElement("button");
    btn.type = "button";
    // Reuse Chrome AI button classes for identical styling
    btn.className = "local_ai_translator_btn button button-primary";
    btn.textContent = buttonTextMap[providerId] || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Translate with %s", "translate-words"), providerId);
    if (translateStatus) {
      btn.disabled = true;
    }
    targetEl.appendChild(btn);
    const getBatchConfig = () => {
      const maxTokens = Number(lmatPageTranslationGlobal?.AIRequestMaxTokens);
      return {
        maxTokens: Number.isFinite(maxTokens) && maxTokens > 0 ? maxTokens : 500
      };
    };
    const buildRecoverableHtml = (done, total, limitExceeded = false) => {
      const totalSafe = Math.max(1, total);
      const translateAgainLabel = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Translate" after updating your API key to try again.', "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Translate" to try again.', "translate-words");
      const translateAgainPartialLabel = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Translate" after updating your API key to re-translate the remaining strings.', "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Translate" to re-translate the remaining strings.', "translate-words");
      if (done <= 0) {
        const heading = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("You’ve exceeded your current plan limit.", "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Oops! Something went wrong during translation", "translate-words");
        const intro = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("To continue, please check your plan details and update your API key.", "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("No strings were saved yet. You can retry the same step or stop here.", "translate-words");
        return `<div class="lmat_page_translation_ai_pending">
                <p class="lmat_page_translation_ai_pending_heading">${escapeHtml(heading)}</p>
                ${limitExceeded ? "" : `<p>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("To see more details, open your browser’s developer console.", "translate-words"))}</p>`}
                <p>${escapeHtml(intro)}</p>
                <p><strong>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Next Steps:", "translate-words"))}</strong></p>
                <p>${escapeHtml(translateAgainLabel)}</p>
                <p><strong>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("OR", "translate-words"))}</strong></p>
                <p>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Continue" to close this message and keep the editor as it is.', "translate-words"))}</p>
            </div>`;
      }
      const completedPercent = Math.min(100, Math.round(done / totalSafe * 100 * 10) / 10).toFixed(1);
      const notCompletedPercent = Math.min(100, Math.round((100 - done / total * 100) * 10) / 10).toFixed(1);
      const heading = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("You’ve exceeded your current plan limit.", "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Oops! Something went wrong during translation", "translate-words");
      const intro = limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("To continue, please check your plan details and update your API key.", "translate-words") : "";
      return `<div class="lmat_page_translation_ai_pending">
                <p class="lmat_page_translation_ai_pending_heading">${escapeHtml(heading)}</p>
                ${intro ? `<p>${escapeHtml(intro)}</p>` : `<p>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("To see more details, open your browser’s developer console.", "translate-words"))}</p>`}
                <p>✅ ${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("You’ve translated %s of the strings.", "translate-words"), completedPercent + "%"))}</p>
                <p>❌ ${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("%s of the strings are still not translated.", "translate-words"), notCompletedPercent + "%"))}</p>
                <p><strong>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Next Steps:", "translate-words"))}</strong></p>
                <p>${escapeHtml(translateAgainPartialLabel)}</p>
                <p><strong>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("OR", "translate-words"))}</strong></p>
                <p>${escapeHtml((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)('Click "Continue" to proceed without translating the rest of the strings.', "translate-words"))}</p>
            </div>`;
    };
    const runTranslation = async () => {
      if (btn.disabled) {
        return;
      }
      btn.disabled = true;
      translateStatusHandler(true);
      const entries = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)("block-lmatPageTranslation/translate").getTranslationEntries();
      const entryById = {};
      const strings = {};
      entries.forEach(row => {
        if (!row.source || String(row.source).trim() === "") {
          return;
        }
        entryById[row.id] = row;
        strings[row.id] = row.filteredString || row.source;
      });
      const stringContainer = jQuery("#lmat_page_translation_strings_model .modal-content .lmat_page_translation_string_container");

      /** Template uses `display:none` in CSS; it must be faded in after React mounts this node (including retries). */
      const mountPageTranslationProgressUi = () => {
        const pb = jQuery("#lmat_page_translation_strings_model .lmat_page_translation_translate_progress");
        const sc = jQuery("#lmat_page_translation_strings_model .modal-content .lmat_page_translation_string_container");
        if (!pb.length) {
          return;
        }
        (0,_progress_bar_index_js__WEBPACK_IMPORTED_MODULE_5__["default"])(providerId);
        if (sc[0] && sc[0].scrollHeight > 100) {
          pb.fadeIn("slow");
        } else {
          pb.css({
            display: "block",
            opacity: 1
          });
        }
      };
      const scheduleMountPageTranslationProgressUi = () => {
        window.setTimeout(() => {
          mountPageTranslationProgressUi();
        }, 0);
      };
      if (Object.keys(strings).length === 0) {
        clearErrorNotice();
        translateStatusHandler(false);
        btn.disabled = false;
        return;
      }
      scheduleMountPageTranslationProgressUi();
      const startTime = new Date().getTime();
      const restUrl = lmatPageTranslationGlobal.ai_batch_translate_url;
      const nonce = lmatPageTranslationGlobal.rest_nonce;
      const postId = lmatPageTranslationGlobal.current_post_id;
      const objectType = lmatPageTranslationGlobal.editor_type === "taxonomy" ? "term" : "post";

      /** When true, translation finished without error — keep translate button disabled. */
      let translationSucceeded = false;
      const finish = ({
        hideProgress
      } = {}) => {
        const pb = jQuery("#lmat_page_translation_strings_model .lmat_page_translation_translate_progress");
        if (hideProgress) {
          pb.fadeOut("slow");
        } else {
          pb.show();
        }
        translateStatusHandler(false);
        if (!translationSucceeded) {
          btn.disabled = false;
        } else {
          btn.disabled = true;
        }
      };
      try {
        clearErrorNotice();
        btn.setAttribute("aria-busy", "true");
        const {
          maxTokens
        } = getBatchConfig();
        const chunks = (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.chunkStringMap)(strings, {
          maxTokens
        });
        const totalKeys = Math.max(1, Object.keys(strings).length);
        let chunkIndex = 0;
        let doneKeys = 0;
        let totalChars = 0;
        let totalStrings = 0;
        const modelKey = "gemini_model";
        const selectedModel = lmatPageTranslationGlobal?.ai_models && lmatPageTranslationGlobal.ai_models[modelKey] ? String(lmatPageTranslationGlobal.ai_models[modelKey]) : "";
        const updateProgressUi = () => {
          const pct = Math.min(100, Math.round(doneKeys / totalKeys * 100));
          jQuery(`.${providerId}-translator_progress`).css("width", `${pct}%`).text(`${pct}%`);
          const el = stringContainer && stringContainer[0] ? stringContainer[0] : null;
          if (el && el.scrollHeight > el.clientHeight) {
            const maxScroll = el.scrollHeight - el.clientHeight;
            el.scrollTop = Math.round(pct / 100 * maxScroll);
          }
        };
        const applyChunk = async chunk => {
          const translations = await (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.requestAiBatch)({
            provider: providerId,
            postId,
            objectType,
            sourceLang,
            targetLang,
            strings: chunk,
            model: selectedModel,
            restUrl,
            nonce,
            signal: translationAbortSignal
          });
          if (Object.keys(translations).length === 0 && Object.keys(chunk).length > 0) {
            const emptyMsg = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("The AI returned an empty translation response. Please try again.", "translate-words");
            (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.logAiTranslationError)(emptyMsg, {
              emptyResponse: true
            });
            throw new Error(emptyMsg);
          }
          for (const key of Object.keys(chunk)) {
            const row = entryById[key];
            if (!row) continue;
            const t = translations[key] !== undefined ? translations[key] : chunk[key];
            (0,_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_1__["default"])({
              type: row.type,
              key: row.id,
              translateContent: t,
              source: row.source,
              provider: providerId,
              AllowedMetaFields
            });
            const src = row.source || "";
            totalChars += src.trim().length;
            totalStrings += src.trim().split(/(?<=[.!?]+)\s+/).filter(Boolean).length;
            doneKeys++;
          }
          updateProgressUi();
        };
        const showGeminiRecoverableError = limitExceeded => {
          showErrorNotice({
            recoverable: true,
            messagePlain: limitExceeded ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("You’ve exceeded your current plan limit.", "translate-words") : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Translation failed.", "translate-words"),
            html: buildRecoverableHtml(doneKeys, totalKeys, limitExceeded),
            onTranslateAgain: () => {
              clearErrorNotice();
              btn.disabled = true;
              translateStatusHandler(true);
              scheduleMountPageTranslationProgressUi();
              btn.setAttribute("aria-busy", "true");
              void (async () => {
                const r = await runChunksFromCurrentIndex();
                btn.removeAttribute("aria-busy");
                if (r === "ok") {
                  finishSuccess();
                  releaseRecoverableUpdateBlock();
                } else if (r !== "recoverable") {
                  (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
                    prefix: providerId,
                    start: startTime,
                    end: new Date().getTime(),
                    translateStatus: false
                  });
                  releaseRecoverableUpdateBlock();
                }
                translateStatusHandler(false);
                if (!translationSucceeded) {
                  btn.disabled = false;
                }
                if (translationSucceeded) {
                  setTimeout(() => finish({
                    hideProgress: true
                  }), 800);
                } else {
                  finish({
                    hideProgress: false
                  });
                }
              })();
            },
            onContinue: () => {
              releaseRecoverableUpdateBlock();
              clearErrorNotice();
              jQuery(`.${providerId}-translator_progress`).css("width", `${Math.min(100, Math.round(doneKeys / totalKeys * 100))}%`).text(`${Math.min(100, Math.round(doneKeys / totalKeys * 100))}%`);
              (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("block-lmatPageTranslation/translate").translationInfo({
                targetStringCount: totalStrings,
                targetWordCount: entries.reduce((acc, row) => {
                  const s = (row.source || "").trim();
                  return acc + s.split(/\s+/).filter(word => /[^\p{L}\p{N}]/.test(word)).length;
                }, 0),
                targetCharacterCount: totalChars,
                translateStatus: doneKeys > 0,
                provider: providerId
              });
              (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_6__["default"])(providerId, "block", totalChars);
              (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
                prefix: providerId,
                start: startTime,
                end: new Date().getTime(),
                translateStatus: doneKeys > 0
              });
              translationSucceeded = doneKeys > 0;
              translateStatusHandler(false);
              btn.disabled = !translationSucceeded;
              btn.removeAttribute("aria-busy");
              if (translationSucceeded) {
                setTimeout(() => finish({
                  hideProgress: true
                }), 800);
              } else {
                finish({
                  hideProgress: false
                });
              }
            }
          });
        };
        const finishSuccess = () => {
          (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("block-lmatPageTranslation/translate").translationInfo({
            targetStringCount: totalStrings,
            targetWordCount: entries.reduce((acc, row) => {
              const s = (row.source || "").trim();
              return acc + s.split(/\s+/).filter(word => /[^\p{L}\p{N}]/.test(word)).length;
            }, 0),
            targetCharacterCount: totalChars,
            translateStatus: true,
            provider: providerId
          });
          jQuery(`.${providerId}-translator_progress`).css("width", "100%").text("100%");
          (0,_progress_bar_show_string_count_js__WEBPACK_IMPORTED_MODULE_6__["default"])(providerId, "block", totalChars);
          (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
            prefix: providerId,
            start: startTime,
            end: new Date().getTime(),
            translateStatus: true
          });
          translationSucceeded = true;
        };
        const runChunksFromCurrentIndex = async () => {
          while (chunkIndex < chunks.length) {
            if (translationAbortSignal && translationAbortSignal.aborted) {
              return "aborted";
            }
            const chunk = chunks[chunkIndex];
            try {
              // eslint-disable-next-line no-await-in-loop
              await applyChunk(chunk);
              chunkIndex += 1;
            } catch (err) {
              if (err && err.name === "AbortError") {
                return "aborted";
              }
              const errorMessage = err?.message || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Translation failed. Please try again.", "translate-words");
              const lower = String(errorMessage || "").toLowerCase();
              const isQuota = err?.code === "LLM_QUOTA_EXCEEDED" || lower.includes("429") || lower.includes("quota") || lower.includes("rate limit") || lower.includes("resource has been exhausted");
              const totalStringKeys = Object.keys(strings).length;
              const canOfferGeminiRecovery = providerId === "gemini" && totalStringKeys > 0 && chunks.length > 0 && (isQuota || chunkIndex < chunks.length);
              if (canOfferGeminiRecovery) {
                (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.logAiTranslationError)(errorMessage);
                showGeminiRecoverableError(isQuota);
                return "recoverable";
              }
              (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.logAiTranslationError)(errorMessage);
              showErrorNotice(errorMessage);
              return "fail";
            }
          }
          return "ok";
        };
        const outcome = await runChunksFromCurrentIndex();
        btn.removeAttribute("aria-busy");
        if (outcome === "aborted") {
          releaseRecoverableUpdateBlock();
          clearErrorNotice();
          translateStatusHandler(false);
          btn.disabled = false;
          finish({
            hideProgress: false
          });
          return;
        }
        if (outcome === "ok") {
          finishSuccess();
          releaseRecoverableUpdateBlock();
        } else if (outcome === "recoverable") {
          (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
            prefix: providerId,
            start: startTime,
            end: new Date().getTime(),
            translateStatus: false
          });
        } else {
          (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
            prefix: providerId,
            start: startTime,
            end: new Date().getTime(),
            translateStatus: false
          });
          releaseRecoverableUpdateBlock();
        }
      } catch (e) {
        if (e && e.name === "AbortError") {
          releaseRecoverableUpdateBlock();
          clearErrorNotice();
          translateStatusHandler(false);
          btn.disabled = false;
          finish({
            hideProgress: false
          });
          return;
        }
        (0,_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
          prefix: providerId,
          start: startTime,
          end: new Date().getTime(),
          translateStatus: false
        });
        const errorMessage = e?.message || (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_4__.__)("Translation failed. Please try again.", "translate-words");
        const lower = String(errorMessage || "").toLowerCase();
        const isQuota = e?.code === "LLM_QUOTA_EXCEEDED" || lower.includes("429") || lower.includes("quota") || lower.includes("rate limit") || lower.includes("resource has been exhausted");
        const totalStringKeys = Object.keys(strings).length;
        (0,_bulk_translation_src_components_translate_provider_ai_llm_api_client_js__WEBPACK_IMPORTED_MODULE_3__.logAiTranslationError)(errorMessage);
        if (providerId === "gemini" && isQuota && totalStringKeys > 0) {
          showGeminiRecoverableError(true);
        } else {
          showErrorNotice(errorMessage);
          releaseRecoverableUpdateBlock();
        }
      }

      // UX: hide progress only when translation succeeds.
      if (translationSucceeded) {
        setTimeout(() => finish({
          hideProgress: true
        }), 800);
      } else {
        finish({
          hideProgress: false
        });
      }
    };
    btn.addEventListener("click", runTranslation);

    // If translation is already in progress (e.g. re-render), keep button disabled.
    if (translateStatus) {
      btn.disabled = true;
    }
    if (typeof destroyUpdateHandler === "function") {
      destroyUpdateHandler(() => {
        btn.removeEventListener("click", runTranslation);
        if (targetEl) {
          targetEl.innerHTML = "";
        }
        clearErrorNotice();
      });
    }
  };
}

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/google/google-language.js":
/*!*********************************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/google/google-language.js ***!
  \*********************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__),
/* harmony export */   mapToGoogleLanguageCode: () => (/* binding */ mapToGoogleLanguageCode)
/* harmony export */ });
const GOOGLE_LANGUAGES = ["ab", "ace", "ach", "aa", "af", "sq", "alz", "am", "ar", "hy", "as", "av", "awa", "ay", "az", "ban", "bal", "bm", "bci", "ba", "eu", "btx", "bts", "bbc", "be", "bem", "bn", "bew", "bho", "bik", "bs", "br", "bg", "bua", "yue", "ca", "ceb", "ch", "ce", "ny", "zh-CN", "zh-TW", "chk", "cv", "co", "crh", "crh-Latn", "hr", "cs", "da", "fa-AF", "dv", "din", "doi", "dov", "nl", "dyu", "dz", "en", "eo", "et", "ee", "fo", "fj", "tl", "fi", "fon", "fr", "fr-CA", "fy", "fur", "ff", "gaa", "gl", "ka", "de", "el", "gn", "gu", "ht", "cnh", "ha", "haw", "iw", "hil", "hi", "hmn", "hu", "hrx", "iba", "is", "ig", "ilo", "id", "iu-Latn", "iu", "ga", "it", "jam", "ja", "jw", "kac", "kl", "kn", "kr", "pam", "kk", "kha", "km", "cgg", "kg", "rw", "ktu", "trp", "kv", "gom", "ko", "kri", "ku", "ckb", "ky", "lo", "ltg", "la", "lv", "lij", "li", "ln", "lt", "lmo", "lg", "luo", "lb", "mk", "mad", "mai", "mak", "mg", "ms", "ms-Arab", "ml", "mt", "mam", "gv", "mi", "mr", "mh", "mwr", "mfe", "chm", "mni-Mtei", "min", "lus", "mn", "my", "nhe", "ndc-ZW", "nr", "new", "ne", "bm-Nkoo", "no", "nus", "oc", "or", "om", "os", "pag", "pap", "ps", "fa", "pl", "pt", "pt-PT", "pa", "pa-Arab", "qu", "kek", "rom", "ro", "rn", "ru", "se", "sm", "sg", "sa", "sat-Latn", "sat", "gd", "nso", "sr", "st", "crs", "shn", "sn", "scn", "szl", "sd", "si", "sk", "sl", "so", "es", "su", "sus", "sw", "ss", "sv", "ty", "tg", "ber-Latn", "ber", "ta", "tt", "te", "tet", "th", "bo", "ti", "tiv", "tpi", "to", "lua", "ts", "tn", "tcy", "tum", "tr", "tk", "tyv", "ak", "udm", "uk", "ur", "ug", "uz", "ve", "vec", "vi", "war", "cy", "wo", "xh", "sah", "yi", "yo", "yua", "zap", "zu"];

/**
 * Map Linguator slug/locale to a Google Translate language code.
 * Regional variants (pt_pt, en_gb, es_mx, …) → base code via first segment after `-`.
 *
 * @param {string} lang Linguator language slug.
 * @param {Object} languageObject Site language map with locale per slug.
 * @returns {string}
 */
function mapToGoogleLanguageCode(lang, languageObject = {}) {
  const locale = languageObject[lang]?.locale || '';
  const normalized = (locale || lang).replace(/_/g, '-');
  return normalized.split('-')[0].toLowerCase();
}
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (() => GOOGLE_LANGUAGES);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/google/index.js":
/*!***********************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/google/index.js ***!
  \***********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _string_modal_scroll_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../string-modal-scroll/index.js */ "./modules/page-translation/src/component/string-modal-scroll/index.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _google_language_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./google-language.js */ "./modules/page-translation/src/component/translate-provider/google/google-language.js");




/**
 * Initializes Google Translate functionality on specific elements based on provided data.
 * @param {Object} data - The data containing source and target languages.
 */
const GoogleTranslater = data => {
  const {
    sourceLang,
    targetLang,
    ID,
    translateStatusHandler,
    modalRenderId,
    destroyUpdateHandler
  } = data;
  const languageObject = lmatPageTranslationGlobal.languageObject || {};
  const lang = (0,_google_language_js__WEBPACK_IMPORTED_MODULE_2__.mapToGoogleLanguageCode)(targetLang, languageObject);
  const srcLang = (0,_google_language_js__WEBPACK_IMPORTED_MODULE_2__.mapToGoogleLanguageCode)(sourceLang, languageObject);
  const g = typeof window !== "undefined" ? window.google : undefined;
  const TranslateElementCtor = g?.translate?.TranslateElement;
  const reportGoogleUnavailable = () => {
    document.dispatchEvent(new CustomEvent("lmat-page-translation:translation-error", {
      bubbles: true,
      detail: {
        message: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Google Translate could not be loaded. Check your internet connection, disable ad blockers for this site, and try again.", "translate-words")
      }
    }));
    translateStatusHandler(false);
  };
  if (!TranslateElementCtor) {
    reportGoogleUnavailable();
    return;
  }
  try {
    new TranslateElementCtor({
      pageLanguage: srcLang,
      includedLanguages: lang,
      defaultLanguage: srcLang,
      multilanguagePage: true,
      autoDisplay: false
    }, ID);
  } catch {
    reportGoogleUnavailable();
    return;
  }
  const element = document.querySelector(`#${ID}`);
  if (element) {
    const translateElement = element.children;
    if (translateElement.length <= 0) {
      Object.values(TranslateElementCtor()).map(item => {
        if (item instanceof HTMLElement && item.id === 'lmat_page_translation_google_translate_element') {
          element.replaceWith(item);
        }
      });
    }
  }
  const onChange = () => {
    (0,_string_modal_scroll_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])(translateStatusHandler, 'google', modalRenderId);
  };
  const host = document.querySelector(`#${ID}`);
  if (host) {
    host.addEventListener('change', onChange);
  }
  if (typeof destroyUpdateHandler === 'function') {
    destroyUpdateHandler(() => {
      const node = document.querySelector(`#${ID}`);
      if (node) {
        node.removeEventListener('change', onChange);
        node.innerHTML = '';
      }
    });
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GoogleTranslater);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/index.js":
/*!****************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/index.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _google_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./google/index.js */ "./modules/page-translation/src/component/translate-provider/google/index.js");
/* harmony import */ var _local_ai_translator_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./local-ai-translator/index.js */ "./modules/page-translation/src/component/translate-provider/local-ai-translator/index.js");
/* harmony import */ var _ai_llm_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./ai-llm/index.js */ "./modules/page-translation/src/component/translate-provider/ai-llm/index.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _assets_logo_chrome_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../../../assets/logo/chrome.js */ "./assets/logo/chrome.js");
/* harmony import */ var _assets_logo_edge_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../../../../../assets/logo/edge.js */ "./assets/logo/edge.js");
/* harmony import */ var _assets_logo_google_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ../../../../../assets/logo/google.js */ "./assets/logo/google.js");
/* harmony import */ var _assets_logo_gemini_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../../../../../assets/logo/gemini.js */ "./assets/logo/gemini.js");









/**
 * Provides translation services using Yandex Translate.
 */
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (props => {
  props = props || {};
  const {
    Service = false,
    openErrorModalHandler = () => {}
  } = props;
  const assetsUrl = window.lmatPageTranslationGlobal.lmat_url + 'admin/assets/images/';
  const errorIcon = assetsUrl + 'error-icon.svg';
  const providers = window.lmatPageTranslationGlobal.providers;
  const Services = {
    google: {
      Provider: _google_index_js__WEBPACK_IMPORTED_MODULE_0__["default"],
      title: "Google Translate",
      SettingBtnText: "Translate",
      serviceLabel: "Google Translate",
      Docs: "https://linguator.com/docs/automatic-translation-via-google-translate-widget/?utm_source=twlmat_plugin&utm_medium=inside&utm_campaign=docs&utm_content=popup_google_pro",
      heading: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Choose Language", "translate-words"),
      BetaEnabled: false,
      ButtonDisabled: props.googleButtonDisabled,
      ErrorMessage: props.googleButtonDisabled ? /*#__PURE__*/React.createElement("div", {
        className: "lmat-page-translation-provider-error button button-primary",
        onClick: () => openErrorModalHandler("google")
      }, /*#__PURE__*/React.createElement("img", {
        src: errorIcon,
        alt: "error"
      }), " ", (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)('View Error', 'translate-words')) : /*#__PURE__*/React.createElement(React.Fragment, null),
      Logo: /*#__PURE__*/React.createElement(_assets_logo_google_js__WEBPACK_IMPORTED_MODULE_6__.GoogleIcon, {
        className: "icon-size"
      })
    },
    localAiTranslator: {
      Provider: _local_ai_translator_index_js__WEBPACK_IMPORTED_MODULE_1__["default"],
      title: "Chrome Built-in AI",
      SettingBtnText: "Translate",
      serviceLabel: "Chrome AI Translator",
      heading: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Translate Using %s", "translate-words"), "Chrome built-in API"),
      Docs: "https://linguator.com/docs/automatic-translation-via-chrome-ai/?utm_source=twlmat_plugin&utm_medium=inside&utm_campaign=docs&utm_content=popup_chrome_pro",
      BetaEnabled: true,
      ButtonDisabled: props.localAiTranslatorButtonDisabled,
      ErrorMessage: props.localAiTranslatorButtonDisabled ? /*#__PURE__*/React.createElement("div", {
        className: "lmat-page-translation-provider-error button button-primary",
        onClick: () => openErrorModalHandler("localAiTranslator")
      }, /*#__PURE__*/React.createElement("img", {
        src: errorIcon,
        alt: "error"
      }), " ", (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)('View Error', 'translate-words')) : /*#__PURE__*/React.createElement(React.Fragment, null),
      Logo: /*#__PURE__*/React.createElement(_assets_logo_chrome_js__WEBPACK_IMPORTED_MODULE_4__.ChromeIcon, {
        className: "icon-size"
      })
    },
    edgeLocalAiTranslator: {
      Provider: _local_ai_translator_index_js__WEBPACK_IMPORTED_MODULE_1__["default"],
      title: "Edge Built-in AI",
      SettingBtnText: "Translate",
      serviceLabel: "Edge AI Translator",
      heading: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Translate Using %s", "translate-words"), "Edge built-in API"),
      Docs: "https://microsoftedge.github.io/Demos/built-in-ai/playgrounds/translator-api/",
      BetaEnabled: true,
      ButtonDisabled: props.edgeLocalAiTranslatorButtonDisabled,
      ErrorMessage: props.edgeLocalAiTranslatorButtonDisabled ? /*#__PURE__*/React.createElement("div", {
        className: "lmat-page-translation-provider-error button button-primary",
        onClick: () => openErrorModalHandler("edgeLocalAiTranslator")
      }, /*#__PURE__*/React.createElement("img", {
        src: errorIcon,
        alt: "error"
      }), " ", (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)('View Error', 'translate-words')) : /*#__PURE__*/React.createElement(React.Fragment, null),
      Logo: /*#__PURE__*/React.createElement(_assets_logo_edge_js__WEBPACK_IMPORTED_MODULE_5__.EdgeIcon, {
        className: "icon-size"
      })
    },
    gemini: {
      Provider: (0,_ai_llm_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])("gemini"),
      title: "Google Gemini",
      SettingBtnText: window.lmatPageTranslationGlobal.api_keys_status?.gemini ? "Translate" : "Add API Key",
      serviceLabel: "Gemini",
      heading: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)("Translate Using Gemini", "translate-words"),
      Docs: "https://linguator.com/docs/automatic-translation-via-googe-gemini/?utm_source=twlmat_plugin&utm_medium=inside&utm_campaign=docs",
      BetaEnabled: false,
      ButtonDisabled: !window.lmatPageTranslationGlobal.api_keys_status?.gemini,
      ErrorMessage: !window.lmatPageTranslationGlobal.api_keys_status?.gemini ? /*#__PURE__*/React.createElement("a", {
        href: `${window.lmatPageTranslationGlobal.admin_url}admin.php?page=lmat_settings&tab=translation`,
        target: "_blank",
        className: "lmat-page-translation-provider-error button button-primary"
      }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__.__)('Add API Key', 'translate-words')) : /*#__PURE__*/React.createElement(React.Fragment, null),
      Logo: /*#__PURE__*/React.createElement(_assets_logo_gemini_js__WEBPACK_IMPORTED_MODULE_7__.GeminiIcon, {
        className: "icon-size"
      })
    }
  };
  const browserType = (() => {
    let type = 'Other';
    if (navigator && navigator.userAgentData && navigator.userAgentData.brands) {
      navigator.userAgentData.brands.forEach(data => {
        if (data.brand === 'Google Chrome') {
          type = 'Chrome';
        } else if (data.brand === 'Microsoft Edge') {
          type = 'Edge';
        }
      });
    } else {
      if (navigator.userAgent && navigator.userAgent.includes('Edg')) {
        type = 'Edge';
      } else if (window.hasOwnProperty('chrome')) {
        type = 'Chrome';
      }
    }
    return type;
  })();
  const validServices = {};
  providers.forEach(provider => {
    if (Services[provider]) {
      if (browserType === 'Chrome' && provider === 'edgeLocalAiTranslator') {
        return;
      }
      if (browserType === 'Edge' && provider === 'localAiTranslator') {
        return;
      }
      validServices[provider] = Services[provider];
    }
  });
  if (!Service) {
    return validServices;
  }
  return validServices[Service];
});

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/local-ai-translator/index.js":
/*!************************************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/local-ai-translator/index.js ***!
  \************************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _local_ai_translator_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./local-ai-translator.js */ "./modules/page-translation/src/component/translate-provider/local-ai-translator/local-ai-translator.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../store-translated-string/index.js */ "./modules/page-translation/src/component/store-translated-string/index.js");
/* harmony import */ var _component_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../../component/store-time-taken/index.js */ "./modules/page-translation/src/component/store-time-taken/index.js");





const localAiTranslator = async props => {
  const targetLangName = lmatPageTranslationGlobal.languageObject[props.targetLang]['name'];
  const sourceLangName = lmatPageTranslationGlobal.languageObject[props.sourceLang]['name'];
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_2__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  const {
    translateStatusHandler,
    translateStatus,
    destroyUpdateHandler
  } = props;
  let startTime = 0;
  const startTranslation = () => {
    startTime = new Date().getTime();
    const stringContainer = jQuery("#lmat_page_translation_strings_model .modal-content .lmat_page_translation_string_container");
    if (stringContainer[0].scrollHeight > 100) {
      jQuery("#lmat_page_translation_strings_model .lmat_page_translation_translate_progress").fadeIn("slow");
    }
  };
  const completeTranslation = () => {
    (0,_component_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_4__["default"])({
      prefix: 'localAiTranslator',
      start: startTime,
      end: new Date().getTime(),
      translateStatus: true
    });
    setTimeout(() => {
      translateStatusHandler(false);
      jQuery("#lmat_page_translation_strings_model .lmat_page_translation_translate_progress").fadeOut("slow");
    }, 4000);
  };
  const beforeTranslate = ele => {
    const stringContainer = jQuery("#lmat_page_translation_strings_model .modal-content .lmat_page_translation_string_container");
    if (stringContainer.length < 1) {
      TranslateProvider.stopTranslation();
      (0,_component_store_time_taken_index_js__WEBPACK_IMPORTED_MODULE_4__["default"])({
        prefix: 'localAiTranslator',
        start: startTime,
        end: new Date().getTime()
      });
      startTime = 0;
      return;
    }
    const scrollStringContainer = position => {
      stringContainer.scrollTop(position);
    };
    const stringContainerPosition = stringContainer[0].getBoundingClientRect();
    const eleTopPosition = ele.closest("tr").offsetTop;
    const containerHeight = stringContainer.height();
    if (eleTopPosition > containerHeight + stringContainerPosition.y) {
      scrollStringContainer(eleTopPosition - containerHeight + ele.offsetHeight);
    }
  };
  const afterTranslate = ele => {
    const translatedText = ele.innerText;
    const type = ele.dataset.stringType;
    const key = ele.dataset.key;
    const sourceText = ele.closest('tr').querySelector('td[data-source="source_text"]').innerText;
    const serviceKey = props.service || 'localAiTranslator';
    (0,_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_3__["default"])({
      type: type,
      key: key,
      translateContent: translatedText,
      source: sourceText,
      provider: serviceKey,
      AllowedMetaFields
    });
    const translationEntry = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_2__.select)('block-lmatPageTranslation/translate').getTranslationInfo().translateData?.[serviceKey];
    const previousTargetStringCount = translationEntry && translationEntry.targetStringCount ? translationEntry.targetStringCount : 0;
    const previousTargetWordCount = translationEntry && translationEntry.targetWordCount ? translationEntry.targetWordCount : 0;
    const previousTargetCharacterCount = translationEntry && translationEntry.targetCharacterCount ? translationEntry.targetCharacterCount : 0;
    if (translatedText.trim() !== '' && translatedText.trim().length > 0) {
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_2__.dispatch)('block-lmatPageTranslation/translate').translationInfo({
        targetStringCount: previousTargetStringCount + sourceText.trim().split(/(?<=[.!?]+)\s+/).length,
        targetWordCount: previousTargetWordCount + sourceText.trim().split(/\s+/).filter(word => /[^\p{L}\p{N}]/.test(word)).length,
        targetCharacterCount: previousTargetCharacterCount + sourceText.trim().length,
        provider: serviceKey
      });
    }
  };
  const isEdge = props.service === 'edgeLocalAiTranslator' || props.ID && props.ID.includes('edgeLocalAiTranslator');
  const TranslateProvider = await _local_ai_translator_js__WEBPACK_IMPORTED_MODULE_0__["default"].Object({
    mainWrapperSelector: "#lmat_page_translation_strings_model",
    btnSelector: `#${props.ID}`,
    btnClass: "local_ai_translator_btn",
    btnText: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Translate To", 'translate-words') + ' ' + targetLangName,
    stringSelector: ".lmat_page_translation_string_container tbody tr td.translate:not([data-translate-status='translated'])",
    progressBarSelector: "#lmat_page_translation_strings_model .lmat_page_translation_translate_progress",
    sourceLanguage: props.sourceLang,
    targetLanguage: props.targetLang,
    targetLanguageLabel: targetLangName,
    sourceLanguageLabel: sourceLangName,
    onStartTranslationProcess: startTranslation,
    onComplete: completeTranslation,
    onBeforeTranslate: beforeTranslate,
    onAfterTranslate: afterTranslate,
    onLanguageError: err => {
      console.log("onLanguageError triggered with err:", err);
      const errorHtml = err && typeof err.html === 'function' ? err.html() : err.outerHTML || String(err);
      console.log("Computed errorHtml:", errorHtml);
      document.dispatchEvent(new CustomEvent("lmat-page-translation:translation-error", {
        detail: {
          message: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Translation Error", "translate-words"),
          messagePlain: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Translation Error", "translate-words"),
          recoverable: true,
          html: errorHtml,
          onTranslateAgain: () => {
            window.location.reload();
          },
          onContinue: () => {
            document.dispatchEvent(new CustomEvent("lmat-page-translation:translation-error-clear", {
              bubbles: true
            }));
          }
        },
        bubbles: true
      }));
    },
    isEdge: isEdge
  });
  if (typeof destroyUpdateHandler === 'function') {
    destroyUpdateHandler(() => {
      if (TranslateProvider && typeof TranslateProvider.stopTranslation === 'function') {
        TranslateProvider.stopTranslation();
      }
    });
  }
  if (TranslateProvider.hasOwnProperty('init')) {
    TranslateProvider.init();
    const button = document.querySelector(`#lmat_page_translation_${props.service}_translate_element .local_ai_translator_btn`);
    if (button && translateStatus) {
      button.disabled = translateStatus;
    }
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (localAiTranslator);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-provider/local-ai-translator/local-ai-translator.js":
/*!**************************************************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-provider/local-ai-translator/local-ai-translator.js ***!
  \**************************************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
class ChromeAiTranslator {
  // Static method to create an instance of ChromeAiTranslator and return extra data
  static Object = options => {
    const selfObject = new this(options);
    return selfObject.extraData();
  };

  // Constructor to initialize the translator with options
  constructor(options) {
    this.btnSelector = options.btnSelector || false; // Selector for the button that triggers translation
    this.btnClass = options.btnClass || false; // Class for the button
    this.btnText = options.btnText || `Translate To ${options.targetLanguageLabel}`; // Text for the button
    this.stringSelector = options.stringSelector || false; // Selector for the elements containing strings to translate
    this.progressBarSelector = options.progressBarSelector || false; // Selector for the progress bar element
    this.onStartTranslationProcess = options.onStartTranslationProcess || (() => {}); // Callback for when translation starts
    this.onComplete = options.onComplete || (() => {}); // Callback for when translation completes
    this.onLanguageError = options.onLanguageError || (() => {}); // Callback for language errors
    this.onBeforeTranslate = options.onBeforeTranslate || (() => {}); // Callback for before translation
    this.onAfterTranslate = options.onAfterTranslate || (() => {}); // Callback for after translation
    this.sourceLanguage = options.sourceLanguage || "en"; // Default source language
    this.targetLanguage = options.targetLanguage || "hi"; // Default target language
    this.targetLanguageLabel = options.targetLanguageLabel || "Hindi"; // Label for the target language
    this.sourceLanguageLabel = options.sourceLanguageLabel || "English"; // Label for the source language
    this.isEdge = options.isEdge || false;
  }

  // Method to check language support and return relevant data
  extraData = async () => {
    // Check if the language is supported
    const langSupportedStatus = await ChromeAiTranslator.languageSupportedStatus(this.sourceLanguage, this.targetLanguage, this.targetLanguageLabel, this.sourceLanguageLabel, this.isEdge);
    if (langSupportedStatus !== true) {
      this.onLanguageError(langSupportedStatus); // Handle language error
      return {}; // Return empty object if language is not supported
    }
    this.defaultLang = this.targetLanguage; // Set default language

    // Return methods for translation control
    return {
      continueTranslation: this.continueTranslation,
      stopTranslation: this.stopTranslation,
      startTranslation: this.startTranslation,
      reInit: this.reInit,
      init: this.init
    };
  };

  /**
   * Checks if the specified source and target languages are supported by the Local Translator AI modal.
   */
  static languageSupportedStatus = async (sourceLanguage, targetLanguage, targetLanguageLabel, sourceLanguageLabel, isEdge = false) => {
    let supportedLanguages = ['en', 'es', 'ja', 'ar', 'de', 'bn', 'fr', 'hi', 'it', 'ko', 'nl', 'pl', 'pt', 'ru', 'th', 'tr', 'vi', 'zh', 'zh-hant', 'bg', 'cs', 'da', 'el', 'fi', 'hr', 'hu', 'id', 'iw', 'lt', 'no', 'ro', 'sk', 'sl', 'sv', 'uk', 'kn', 'ta', 'te', 'mr'].map(lang => lang.toLowerCase());
    const edgeOnlyLanguages = ["nb", "af", "is", "fo", "lb", "pt-pt", "ca", "gl", "oc", "la", "bs", "dsb", "hsb", "sr-latn", "be", "mk", "sr-cyrl", "kk", "ky", "tg", "tt", "mn-cyrl", "ba", "ce", "cv", "zh-hans", "lzh", "yue", "gu", "ml", "ur", "as", "or", "pa", "ne", "si", "my", "dv", "awa", "bho", "brx", "doi", "gom", "hne", "kha", "lus", "mag", "mai", "mni", "sat", "ms", "km", "lo", "jv", "su", "ceb", "fil", "mi", "fj", "haw", "sm", "to", "ty", "tet", "fa", "he", "ps", "prs", "ku", "ks", "sd", "ug", "ha", "ig", "yo", "zu", "xh", "sw", "sn", "st", "tn", "nso", "run", "rw", "ln", "mg", "so", "am", "ti", "nya", "et", "lv", "mt", "eu", "cy", "ga", "sq", "hy", "ka", "az", "uz", "tk", "bo", "dzo", "fr-ca", "ht", "ikt", "iu-latn", "iu", "kmr", "lug", "luo", "mn-mong", "mww", "otq", "sa", "tlh-latn", "yua"].map(lang => lang.toLowerCase());
    if (isEdge) {
      supportedLanguages = [...supportedLanguages, ...edgeOnlyLanguages];
    }
    const safeBrowser = window.location.protocol === 'https:';
    const browserContentSecure = window?.isSecureContext;
    const getBrowserType = () => {
      let type = 'Other';
      if (navigator && navigator.userAgentData && navigator.userAgentData.brands) {
        navigator.userAgentData.brands.forEach(data => {
          if (data.brand === 'Google Chrome') {
            type = 'Chrome';
          } else if (data.brand === 'Microsoft Edge') {
            type = 'Edge';
          }
        });
      } else {
        if (navigator.userAgent.includes('Edg')) {
          type = 'Edge';
        } else if (window.hasOwnProperty('chrome')) {
          type = 'Chrome';
        }
      }
      return type;
    };
    const browserType = getBrowserType();
    const scheme = isEdge ? 'edge' : 'chrome';
    const browserName = isEdge ? 'Edge' : 'Chrome';
    const docUrl = isEdge ? 'https://microsoftedge.github.io/Demos/built-in-ai/playgrounds/translator-api/' : 'https://developer.chrome.com/docs/ai/translator-api';
    const edgeLangRef = `<a href="https://microsoftedge.github.io/Demos/built-in-ai/playgrounds/translator-api/" target="_blank" rel="noopener"><strong style="color: #2271b1;">test your language on the Edge Translator API playground</strong></a>`;

    // Browser check
    if (isEdge) {
      if (browserType !== 'Edge') {
        const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                    <strong>Important Notice:</strong>
                    <ol>
                        <li>The Translator API, which leverages Edge local AI models, is designed specifically for use with the Microsoft Edge browser.</li>
                        <li>For comprehensive information about Edge built-in AI translation, <a href="${docUrl}" target="_blank" rel="noopener">open the Edge Translator API playground</a>.</li>
                    </ol>
                    <p>Please ensure you are using the Microsoft Edge browser for optimal performance and compatibility.</p>
                </span>`);
        return message;
      }
    } else {
      if (browserType !== 'Chrome') {
        const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                    <strong>Important Notice:</strong>
                    <ol>
                        <li>The Translator API, which leverages Chrome local AI models, is designed specifically for use with the Chrome browser.</li>
                        <li>For comprehensive information about the Translator API, <a href="${docUrl}" target="_blank">click here</a>.</li>
                    </ol>
                    <p>Please ensure you are using the Chrome browser for optimal performance and compatibility.</p>
                </span>`);
        return message;
      }
    }
    if (!('translation' in self && 'createTranslator' in self.translation) && !('ai' in self && 'translator' in self.ai) && !("Translator" in self && "create" in self.Translator) && !safeBrowser && !browserContentSecure) {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <strong>Important Notice:</strong>
                <ol>
                    <li>
                        The Translator API is not functioning due to an insecure connection.
                    </li>
                    <li>
                        Please switch to a secure connection (HTTPS) or add this URL to the list of insecure origins treated as secure by visiting 
                        <span data-clipboard-text="${scheme}://flags/#unsafely-treat-insecure-origin-as-secure" target="_blank" class="chrome-ai-translator-flags">
                            ${scheme}://flags/#unsafely-treat-insecure-origin-as-secure ${ChromeAiTranslator.svgIcons('copy')}
                        </span>.
                        Click on the URL to copy it, then open a new window and paste this URL to access the settings.
                    </li>
                </ol>
            </span>`);
      return message;
    }

    // Check if the translation API is available
    if (!('translation' in self && 'createTranslator' in self.translation) && !('ai' in self && 'translator' in self.ai) && !("Translator" in self && "create" in self.Translator)) {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <h4>Steps to Enable the Translator AI Modal:</h4>
                <ol>
                    <li>Open this URL in a new ${browserName} tab: <strong><span data-clipboard-text="${scheme}://flags/#translation-api" target="_blank" class="chrome-ai-translator-flags">${scheme}://flags/#translation-api ${ChromeAiTranslator.svgIcons('copy')}</span></strong>. Click on the URL to copy it, then open a new window and paste this URL to access the settings.</li>
                    <li>Ensure that the <strong>Experimental translation API</strong> option is set to <strong>Enabled</strong>.</li>
                    <li>Click on the <strong>Save</strong> button to apply the changes.</li>
                    <li>The Translator AI modal should now be enabled and ready for use.</li>
                </ol>
                <p>For more information, please refer to the <a href="${docUrl}" target="_blank">documentation</a>.</p>   
                <p>If the issue persists, please ensure that your browser is up to date and restart your browser.</p>
                <p>If you continue to experience issues after following the above steps, please <a href="https://my.coolplugins.net/account/support-tickets/" target="_blank" rel="noopener">open a support ticket</a> with our team. We are here to help you resolve any problems and ensure a smooth translation experience.</p>
            </span>`);
      return message;
    }

    // Check if the target language is supported
    if (!supportedLanguages.includes(targetLanguage.toLowerCase())) {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <strong>Language Support Information:</strong>
                <ol>
                    <li>The current version of ${browserName} AI Translator does not support the Target Language <strong>${targetLanguageLabel} (${targetLanguage})</strong></li>
                    ${isEdge ? `<li>To check if this language is available in Edge, ${edgeLangRef}.</li>` : `<li>To view the list of supported languages, please <span data-clipboard-text="${scheme}://on-device-translation-internals" target="_blank" class="chrome-ai-translator-flags">${scheme}://on-device-translation-internals ${ChromeAiTranslator.svgIcons('copy')}</span>. Click on the URL to copy it, then open a new window and paste this URL to access the settings.</li>`}
                    <li>Ensure your ${browserName} browser is updated to the latest version for optimal performance.</li>
                </ol>
            </span>`);
      return message;
    }

    // Check if the source language is supported
    if (!supportedLanguages.includes(sourceLanguage.toLowerCase())) {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <strong>Language Support Information:</strong>
                <ol>
                    <li>The current version of ${browserName} AI Translator does not support the Source Language <strong>${sourceLanguageLabel} (${sourceLanguage})</strong></li>
                    ${isEdge ? `<li>To check if this language is available in Edge, ${edgeLangRef}.</li>` : `<li>To view the list of supported languages, please <span data-clipboard-text="${scheme}://on-device-translation-internals" target="_blank" class="chrome-ai-translator-flags">${scheme}://on-device-translation-internals ${ChromeAiTranslator.svgIcons('copy')}</span>. Click on the URL to copy it, then open a new window and paste this URL to access the settings.</li>`}
                    <li>Ensure your ${browserName} browser is updated to the latest version for optimal performance.</li>
                </ol>
            </span>`);
      return message;
    }

    // Check if translation can be performed
    const status = await ChromeAiTranslator.languagePairAvality(sourceLanguage, targetLanguage);

    // Handle case for language pack after download
    if (status === "after-download" || status === "downloadable") {
      return jQuery('<span class="atlt-chromeai-download-pending"></span>').attr('data-source', sourceLanguage).attr('data-target', targetLanguage).attr('data-source-label', sourceLanguageLabel || sourceLanguage).attr('data-target-label', targetLanguageLabel || targetLanguage);
    }

    // Handle case where the language pack is unavailable (cannot be downloaded automatically)
    if (status === "unavailable") {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <h4>Installation Instructions for Language Packs:</h4>
                <ol>
                    <li>
                        To proceed, please install the language pack for <strong>${targetLanguageLabel} (${targetLanguage})</strong> or <strong>${sourceLanguageLabel} (${sourceLanguage})</strong>.
                    </li>
                    <li>
                        After installing the language pack, add this language to your browser's system languages in ${browserName} settings.<br>
                        Go to <strong>Settings &gt; Languages &gt; Add languages</strong> and add <strong>${targetLanguageLabel}</strong> or <strong>${sourceLanguageLabel}</strong> to your preferred languages list & reload the page.
                    </li>
                    <li>
                        You can install it by visiting the following link: 
                        <strong>
                            ${isEdge ? `<span data-clipboard-text="${scheme}://on-device-translation-internals" target="_blank" class="chrome-ai-translator-flags">${scheme}://on-device-translation-internals ${ChromeAiTranslator.svgIcons('copy')}</span>` : `<span data-clipboard-text="${scheme}://on-device-translation-internals" target="_blank" class="chrome-ai-translator-flags">${scheme}://on-device-translation-internals ${ChromeAiTranslator.svgIcons('copy')}</span>`}
                        </strong>. Click on the URL to copy it, then open a new window and paste this URL to access the settings.
                    </li>
                    <li>
                        Please check if both your source <strong>(<span style="color:#2271b1">${sourceLanguage}</span>)</strong> and target <strong>(<span style="color:#2271b1">${targetLanguage}</span>)</strong> languages are available in the language packs list.
                    </li>
                    <li>
                        You need to install both language packs for translation to work. You can search for each language by its language code: <strong>${sourceLanguage}</strong> and <strong>${targetLanguage}</strong>.
                    </li>
                    <li>For more help, refer to the <a href="${isEdge ? 'https://microsoftedge.github.io/Demos/built-in-ai/playgrounds/translator-api/' : 'https://developer.chrome.com/docs/ai/translator-api#supported-languages'}" target="_blank">documentation to check supported languages</a>.</li>
                </ol>
            </span>`);
      return message;
    }

    // Handle case for language pack downloadable
    if (status === "downloading") {
      return jQuery('<span class="atlt-chromeai-download-pending atlt-chromeai-downloading"></span>').attr('data-source', sourceLanguage).attr('data-target', targetLanguage).attr('data-source-label', sourceLanguageLabel || sourceLanguage).attr('data-target-label', targetLanguageLabel || targetLanguage);
    }

    // Handle case for language pack not readily available
    if (status !== 'readily' && status !== 'available') {
      const message = jQuery(`<span style="color: #ff4646; display: inline-block;">
                <h4>Language Pack Installation Required</h4>
                <ol>
                    <li>Please ensure that the language pack for <strong>${targetLanguageLabel} (${targetLanguage})</strong> or <strong>${sourceLanguageLabel} (${sourceLanguage})</strong> is installed and set as a preferred language in your browser.</li>
                    <li>To install the language pack, ${isEdge ? `visit the Edge settings to install language packs. For more info, ${edgeLangRef}.` : `visit <strong><span data-clipboard-text="${scheme}://on-device-translation-internals" target="_blank" class="chrome-ai-translator-flags">${scheme}://on-device-translation-internals ${ChromeAiTranslator.svgIcons('copy')}</span></strong>. Click on the URL to copy it, then open a new window and paste this URL to access the settings.`}</li>
                    <li>If you encounter any issues, please refer to the <a href="${isEdge ? 'https://microsoftedge.github.io/Demos/built-in-ai/playgrounds/translator-api/' : 'https://developer.chrome.com/docs/ai/translator-api#supported-languages'}" target="_blank">documentation to check supported languages</a> for further assistance.</li>
                </ol>
            </span>`);
      return message;
    }
    return true;
  };
  static languagePairAvality = async (source, target) => {
    try {
      const translator = await self.Translator.create({
        sourceLanguage: source,
        targetLanguage: target,
        monitor(m) {
          m.addEventListener('downloadprogress', e => {
            console.log(`Downloaded ${e.loaded * 100}%`);
          });
        }
      });
    } catch (err) {
      console.log('err', err);
    }
    if ('translation' in self && 'createTranslator' in self.translation) {
      const status = await self.translation.canTranslate({
        sourceLanguage: source,
        targetLanguage: target
      });
      return status;
    } else if ('ai' in self && 'translator' in self.ai) {
      const translatorCapabilities = await self.ai.translator.capabilities();
      const status = await translatorCapabilities.languagePairAvailable(source, target);
      return status;
    } else if ("Translator" in self && "create" in self.Translator) {
      const status = await self.Translator.availability({
        sourceLanguage: source,
        targetLanguage: target
      });
      return status;
    }
    return false;
  };
  static downloadLanguagePair = async (source, target, onProgress) => {
    try {
      if ('translation' in self && 'createTranslator' in self.translation) {
        await self.translation.createTranslator({
          sourceLanguage: source,
          targetLanguage: target
        });
      } else if ('ai' in self && 'translator' in self.ai) {
        await self.ai.translator.create({
          sourceLanguage: source,
          targetLanguage: target
        });
      } else if ('Translator' in self && 'create' in self.Translator) {
        await self.Translator.create({
          sourceLanguage: source,
          targetLanguage: target,
          monitor(m) {
            m.addEventListener('downloadprogress', e => {
              if (typeof onProgress === 'function') {
                onProgress(e);
              }
              console.log(`Downloaded ${e.loaded * 100}%`);
            });
          }
        });
      }
    } catch (err) {
      console.log('Language pack download error:', err);
    }

    // Re-check availability after the download attempt.
    if ('translation' in self && 'createTranslator' in self.translation) {
      return await self.translation.canTranslate({
        sourceLanguage: source,
        targetLanguage: target
      });
    } else if ('ai' in self && 'translator' in self.ai) {
      const capabilities = await self.ai.translator.capabilities();
      return await capabilities.languagePairAvailable(source, target);
    } else if ('Translator' in self && 'create' in self.Translator) {
      return await self.Translator.availability({
        sourceLanguage: source,
        targetLanguage: target
      });
    }
    return false;
  };
  static startLanguagePackDownload = async (source, target, onProgress) => {
    const finalStatus = await ChromeAiTranslator.downloadLanguagePair(source, target, onProgress);
    const ok = finalStatus === 'available' || finalStatus === 'readily' || finalStatus === true;
    return {
      ok,
      status: finalStatus
    };
  };
  AITranslator = async targetLanguage => {
    if ('translation' in self && 'createTranslator' in self.translation) {
      const translator = await self.translation.createTranslator({
        sourceLanguage: this.sourceLanguage,
        targetLanguage
      });
      return translator;
    } else if ('ai' in self && 'translator' in self.ai) {
      const translator = await self.ai.translator.create({
        sourceLanguage: this.sourceLanguage,
        targetLanguage
      });
      return translator;
    } else if ("Translator" in self && "create" in self.Translator) {
      const translator = await self.Translator.create({
        sourceLanguage: this.sourceLanguage,
        targetLanguage
      });
      return translator;
    }
    return false;
  };

  // Method to initialize the translation process
  init = async () => {
    this.appendBtn();
    this.translationStart = false; // Flag to indicate if translation has started
    this.completedTranslateIndex = 0; // Index of the last completed translation
    this.completedCharacterCount = 0; // Count of characters translated
    this.translateBtnEvents(); // Set up button events
    if (this.progressBarSelector) {
      this.addProgressBar(); // Add progress bar to the UI
    }
  };

  /**
   * Appends a translation button to the specified button selector.
   * The button is styled with primary button classes and includes
   * any additional classes specified in `this.btnClass`.
   */
  appendBtn = () => {
    this.translateBtn = jQuery(`<button class="button button-primary${this.btnClass ? ' ' + this.btnClass : ''}">${this.btnText}</button>`);
    jQuery(this.btnSelector).append(this.translateBtn);
  };

  /**
   * Formats a number by converting it to a string and removing any non-numeric characters.
   * 
   * @param {number} number - The number to format.
   * @returns returns formatted number
   */
  formatCharacterCount = number => {
    if (number >= 1000000) {
      return (number / 1000000).toFixed(1) + 'M';
    } else if (number >= 1000) {
      return (number / 1000).toFixed(1) + 'K';
    }
    return number;
  };

  // Method to set up button events for translation
  translateBtnEvents = e => {
    if (!this.btnSelector || jQuery(this.btnSelector).length === 0) return this.onLanguageError("The button selector is missing. Please provide a valid selector for the button.");
    if (!this.stringSelector || jQuery(this.stringSelector).length === 0) return this.onLanguageError("The string selector is missing. Please provide a valid selector for the strings to be translated.");
    this.translateStatus = true; // Set translation status to true
    this.translateBtn.off("click"); // Clear previous click handlers
    this.translateBtn.prop("disabled", false); // Enable the button

    // Set up click event for starting translation
    if (!this.translationStart) {
      this.translateBtn.on("click", this.startTranslationProcess);
    } else if (this.translateStringEle.length > this.completedTranslateIndex + 1) {
      this.translateBtn.on("click", () => {
        this.onStartTranslationProcess(); // Call the start translation callback
        this.stringTranslation(this.completedTranslateIndex + 1); // Start translating the next string
      });
    } else {
      this.onComplete({
        translatedStringsCount: this.completedCharacterCount
      }); // Call the complete callback
      this.translateBtn.prop("disabled", true); // Disable the button
    }
  };

  // Method to start the translation process
  startTranslationProcess = async () => {
    this.onStartTranslationProcess(); // Call the start translation callback
    const langCode = this.defaultLang; // Get the default language code

    this.translationStart = true; // Set translation start flag
    this.translateStringEle = jQuery(this.stringSelector); // Get the elements to translate

    // Calculate total character count for progress tracking
    this.totalStringCount = Array.from(this.translateStringEle).map(ele => ele.innerText.length).reduce((a, b) => a + b, 0);

    // Create a translator instance
    this.translator = await this.AITranslator(langCode);

    // Start translating if there are strings to translate
    if (this.translateStringEle.length > 0) {
      await this.stringTranslation(this.completedTranslateIndex);
    }
  };

  // Method to translate a specific string at the given index
  stringTranslation = async index => {
    if (!this.translateStatus) return; // Exit if translation is stopped
    const ele = this.translateStringEle[index]; // Get the element to translate
    this.onBeforeTranslate(ele); // Call the before translation callback
    const orignalText = ele.innerText;
    let originalString = [];
    if (ele.childNodes.length > 0 && !ele.querySelector('.notranslate')) {
      ele.childNodes.forEach(child => {
        if (child.nodeType === 3 && child.nodeValue.trim() !== '') {
          originalString.push(child);
        } else if (child.classList && !child.classList.contains('notranslate')) {
          this.filterInnerTextNodes(child, originalString);
        }
      });
    } else if (ele.querySelector('.notranslate')) {
      ele.childNodes.forEach(child => {
        if (child.nodeType === 3 && child.nodeValue.trim() !== '') {
          originalString.push(child);
        } else if (child.classList && !child.classList.contains('notranslate')) {
          this.filterInnerTextNodes(child, originalString);
        }
      });
    }
    if (originalString.length > 0) {
      await this.stringTranslationBatch(originalString, 0);
    }
    this.completedCharacterCount += orignalText.length; // Update character count
    this.completedTranslateIndex = index; // Update completed index
    if (this.progressBarSelector) {
      this.updateProgressBar(); // Update the progress bar
    }
    this.onAfterTranslate(ele); // Call the after translation callback

    // Continue translating the next string if available
    if (this.translateStringEle.length > index + 1) {
      await this.stringTranslation(this.completedTranslateIndex + 1);
    }

    // If all strings are translated, complete the process
    if (index === this.translateStringEle.length - 1) {
      this.translateBtn.prop("disabled", true); // Disable the button
      this.onComplete({
        characterCount: this.completedCharacterCount
      }); // Call the complete callback
      jQuery(this.progressBarSelector).find(".chrome-ai-translator-strings-count").show().find(".totalChars").text(this.formatCharacterCount(this.completedCharacterCount));
    }
  };
  filterInnerTextNodes = (ele, updatedArray) => {
    const childElements = ele.childNodes;
    if (ele.classList && ele.classList.contains('notranslate')) return;
    childElements.forEach(child => {
      if (child.nodeType === 3 && child.nodeValue.trim() !== '') {
        updatedArray.push(child);
      } else if (child.classList && !child.classList.contains('notranslate')) {
        this.filterInnerTextNodes(child, updatedArray);
      }
    });
  };
  stringTranslationBatch = async (originalString, index) => {
    try {
      const sourceText = originalString[index].nodeValue;
      const translatedString = await this.translator.translate(sourceText); // Translate the string

      if (translatedString && '' !== translatedString) {
        originalString[index].nodeValue = translatedString; // Set the translated string
      }
    } catch (error) {
      console.error("Error during translation in stringTranslationBatch:", error);
    }
    if (index < originalString.length - 1) {
      await this.stringTranslationBatch(originalString, index + 1);
    }
    return true;
  };

  // Method to add a progress bar to the UI
  addProgressBar = () => {
    if (!document.querySelector("#chrome-ai-translator-modal .chrome-ai-translator_progress_bar")) {
      const progressBar = jQuery(`
                <div class="chrome-ai-translator_progress_bar" style="background-color: #f3f3f3;border-radius: 10px;overflow: hidden;margin: 1.5rem auto; width: 50%;">
                <div class="chrome-ai-translator_progress" style="overflow: hidden;transition: width .5s ease-in-out; border-radius: 10px;text-align: center;width: 0%;height: 20px;box-sizing: border-box;background-color: #4caf50; color: #fff; font-weight: 600;"></div>
                </div>
                <div style="display:none; color: white;" class="chrome-ai-translator-strings-count hidden">
                    Wahooo! You have saved your valuable time via auto translating 
                    <strong class="totalChars">0</strong> characters using 
                    <strong>
                        Chrome Built-in AI
                    </strong>
                </div>
            `);
      jQuery(this.progressBarSelector).append(progressBar); // Append the progress bar to the specified selector
    }
  };

  // Method to update the progress bar based on translation progress
  updateProgressBar = () => {
    const progress = this.completedCharacterCount / this.totalStringCount * 1000 / 10; // Calculate progress percentage
    let decimalValue = progress.toString().split('.')[1] || ''; // Get decimal part of the progress
    decimalValue = decimalValue.length > 0 && decimalValue[0] !== '0' ? decimalValue[0] : ''; // Format decimal value
    let formattedProgress = parseInt(progress) + `${decimalValue !== '' ? '.' + decimalValue : ''}`; // Format progress for display
    formattedProgress = Math.min(formattedProgress, 100);
    jQuery(".chrome-ai-translator_progress").css({
      "width": `${formattedProgress}%`
    }).text(`${formattedProgress}%`); // Update progress bar width and text
  };

  // Method to stop the translation process
  stopTranslation = () => {
    this.translateStatus = false; // Set translation status to false
  };

  // Method to reinitialize button events
  reInit = () => {
    this.translateBtnEvents(); // Re-setup button events
  };

  // Method to start translation from the current index
  startTranslation = () => {
    this.translateStatus = true; // Set translation status to true
    this.startTranslationProcess(this.completedTranslateIndex + 1); // Start translation process
  };
  static svgIcons = iconName => {
    const Icons = {
      'copy': `<svg stroke="currentColor" fill="currentColor" stroke-width="0" viewBox="0 0 448 512" height="16px" width="16px" xmlns="http://www.w3.org/2000/svg" fill="#2271b1"><path d="M433.941 65.941l-51.882-51.882A48 48 0 0 0 348.118 0H176c-26.51 0-48 21.49-48 48v48H48c-26.51 0-48 21.49-48 48v320c0 26.51 21.49 48 48 48h224c26.51 0 48-21.49 48-48v-48h80c26.51 0 48-21.49 48-48V99.882a48 48 0 0 0-14.059-33.941zM266 464H54a6 6 0 0 1-6-6V150a6 6 0 0 1 6-6h74v224c0 26.51 21.49 48 48 48h96v42a6 6 0 0 1-6 6zm128-96H182a6 6 0 0 1-6-6V54a6 6 0 0 1 6-6h106v88c0 13.255 10.745 24 24 24h88v202a6 6 0 0 1-6 6zm6-256h-64V48h9.632c1.591 0 3.117.632 4.243 1.757l48.368 48.368a6 6 0 0 1 1.757 4.243V112z"></path></svg>`
    };
    return Icons[iconName] || '';
  };
}

/*
 * Example Usage of the ChromeAiTranslator.init method.
 * This method initializes the Chrome AI Translator with a comprehensive set of configuration options to facilitate the translation process.
 * 
 * Configuration Options:
 * 
 * - mainWrapperSelector: A CSS selector for the main wrapper element that encapsulates all translation-related elements.
 * - btnSelector: A CSS selector for the button that initiates the translation process.
 * - btnClass: A custom class for styling the translation button.
 * - btnText: The text displayed on the translation button.
 * - stringSelector: A CSS selector for the elements that contain the strings intended for translation.
 * - progressBarSelector: A CSS selector for the progress bar element that visually represents the translation progress.
 * - sourceLanguage: The language code representing the source language (e.g., "es" for Spanish).
 * - targetLanguage: The language code representing the target language (e.g., "fr" for French).
 * - onStartTranslationProcess: A callback function that is executed when the translation process begins.
 * - onBeforeTranslate: A callback function that is executed prior to each individual translation.
 * - onAfterTranslate: A callback function that is executed following each translation.
 * - onComplete: A callback function that is executed upon the completion of the translation process.
 * - onLanguageError: A callback function that is executed when a language-related error occurs.
 */

// Example for checking language support status
// ChromeAiTranslator.languageSupportedStatus("en", "fr", "French");

// const chromeAiTranslatorObject = ChromeAiTranslator.Object(
//     {
//         mainWrapperSelector: ".main-wrapper", // CSS selector for the main wrapper element
//         btnSelector: ".translator-container .translator-button", // CSS selector for the translation button
//         btnClass: "Btn_custom_class", // Custom class for button styling
//         btnText: "Translate To French", // Text displayed on the translation button
//         stringSelector: ".translator-body .translation-item", // CSS selector for translation string elements
//         progressBarSelector: ".translator-progress-bar", // CSS selector for the progress bar
//         sourceLanguage: "es", // Language code for the source language
//         targetLanguage: "fr", // Language code for the target language
//         onStartTranslationProcess: () => { console.log("Translation process started."); }, // Callback for translation start
//         onBeforeTranslate: () => { console.log("Before translation."); }, // Callback before each translation
//         onAfterTranslate: () => { console.log("After translation."); }, // Callback after each translation
//         onComplete: () => { console.log("Translation completed."); }, // Callback for completion
//         onLanguageError: () => { console.error("Language error occurred."); } // Callback for language errors
//     }
// );
// chromeAiTranslatorObject.init();

/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ChromeAiTranslator);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js":
/*!**************************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js ***!
  \**************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const RankMathSeo = props => {
  if (!(0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("rank-math")) {
    return;
  }
  const {
    updateKeywords,
    updateTitle,
    updateDescription,
    updateBreadcrumbTitle,
    updateFacebookTitle,
    updateFacebookDescription,
    updateTwitterTitle,
    updateTwitterDescription
  } = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("rank-math");
  const {
    key,
    value
  } = props;
  switch (key) {
    case 'rank_math_focus_keyword':
      if (updateKeywords) {
        updateKeywords(value);
      }
      break;
    case 'rank_math_title':
      if (updateTitle) {
        updateTitle(value);
      }
      break;
    case 'rank_math_description':
      if (updateDescription) {
        updateDescription(value);
      }
      break;
    case 'rank_math_breadcrumb_title':
      if (updateBreadcrumbTitle) {
        updateBreadcrumbTitle(value);
      }
      break;
    case 'rank_math_facebook_title':
      if (updateFacebookTitle) {
        updateFacebookTitle(value);
      }
      break;
    case 'rank_math_facebook_description':
      if (updateFacebookDescription) {
        updateFacebookDescription(value);
      }
      break;
    case 'rank_math_twitter_title':
      if (updateTwitterTitle) {
        updateTwitterTitle(value);
      }
      break;
    case 'rank_math_twitter_description':
      if (updateTwitterDescription) {
        updateTwitterDescription(value);
      }
      break;
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (RankMathSeo);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-seo-fields/seo-press.js":
/*!**********************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-seo-fields/seo-press.js ***!
  \**********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const SeoPressFields = async props => {
  const {
    key,
    value
  } = props;
  const inputId = key.replace(/^_/, '') + '_meta';
  if (!document.querySelector('#' + inputId)) {
    return;
  }
  switch (key) {
    case '_seopress_titles_title':
    case '_seopress_titles_desc':
    case '_seopress_social_fb_title':
    case '_seopress_social_fb_desc':
    case '_seopress_social_twitter_title':
    case '_seopress_social_twitter_desc':
      jQuery(`#${inputId}`).val(value);
      jQuery(`#${inputId}`).trigger('change');
      break;
    case '_seopress_analysis_target_kw':
      if (window.target_kw && window.target_kw instanceof window.Tagify && window.target_kw.DOM.originalInput.id === inputId) {
        window.target_kw.addTags(value);
      } else {
        jQuery('#' + inputId).val(value);
        jQuery('#' + inputId).trigger('change');
      }
      break;
    default:
      break;
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SeoPressFields);

/***/ }),

/***/ "./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js":
/*!*****************************************************************************************!*\
  !*** ./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js ***!
  \*****************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const YoastSeoFields = props => {
  if (!(0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("yoast-seo/editor")) {
    return;
  }
  const {
    updateData,
    setFocusKeyword,
    setBreadcrumbsTitle,
    setFacebookPreviewTitle,
    setFacebookPreviewDescription,
    setTwitterPreviewTitle,
    setTwitterPreviewDescription
  } = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)("yoast-seo/editor");
  const {
    key,
    value
  } = props;
  switch (key) {
    case "_yoast_wpseo_focuskw":
      if (setFocusKeyword) {
        setFocusKeyword(value);
      }
      break;
    case "_yoast_wpseo_title":
      if (updateData) {
        updateData({
          title: value
        });
      }
      break;
    case "_yoast_wpseo_metadesc":
      if (updateData) {
        updateData({
          description: value
        });
      }
      break;
    case "_yoast_wpseo_bctitle":
      if (setBreadcrumbsTitle) {
        setBreadcrumbsTitle(value);
      }
      break;
    case "_yoast_wpseo_opengraph-title":
      if (setFacebookPreviewTitle) {
        setFacebookPreviewTitle(value);
      }
      break;
    case "_yoast_wpseo_opengraph-description":
      if (setFacebookPreviewDescription) {
        setFacebookPreviewDescription(value);
      }
      break;
    case "_yoast_wpseo_twitter-title":
      if (setTwitterPreviewTitle) {
        setTwitterPreviewTitle(value);
      }
      break;
    case "_yoast_wpseo_twitter-description":
      if (setTwitterPreviewDescription) {
        setTwitterPreviewDescription(value);
      }
      break;
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (YoastSeoFields);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/classic/index.js":
/*!******************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/classic/index.js ***!
  \******************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../component/translate-seo-fields/yoast-seo-fields.js */ "./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js");
/* harmony import */ var _component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../component/translate-seo-fields/rank-math-seo.js */ "./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js");
/* harmony import */ var _component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../component/translate-seo-fields/seo-press.js */ "./modules/page-translation/src/component/translate-seo-fields/seo-press.js");
/* harmony import */ var _meta_fields_index_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../meta-fields/index.js */ "./modules/page-translation/src/create-translated-post/meta-fields/index.js");






/**
 * Translates the post content and updates the post title, excerpt, and content.
 * 
 * @param {Object} props - The properties containing post content, translation function, and block rules.
 */
const UpdateClassicPage = props => {
  const {
    modalClose,
    postContent,
    service
  } = props;
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();

  /**
   * Updates the post title and excerpt text based on translation.
   */
  const postDataUpdate = () => {
    if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate' || lmatPageTranslationGlobal.slug_translation_option === 'slug_keep') {
      let translateContent = '';
      if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
        translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('slug', postContent.slug_name, null, service);
      }
      if (lmatPageTranslationGlobal.slug_translation_option === 'slug_keep') {
        translateContent = lmatPageTranslationGlobal.slug_name;
      }
      const slugBox = document.querySelector('#slugdiv');
      const slugInput = slugBox?.querySelector('input#post_name[name="post_name"]');
      const slugLabel = slugBox?.querySelector('label');
      if (slugInput) {
        slugInput.value = translateContent;
      }
      if (slugLabel) {
        slugLabel.classList.add('screen-reader-text');
      }
    }
    if (postContent.title && postContent.title.trim() !== '') {
      const translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('title', postContent.title, null, service);
      const titleBox = document.querySelector('#titlediv');
      const titleInput = titleBox?.querySelector('input#title[name="post_title"]');
      const titleLabel = titleBox?.querySelector('label');
      if (titleInput) {
        titleInput.value = translateContent;
      }
      if (titleLabel) {
        titleLabel.classList.add('screen-reader-text');
      }
    }
    if (postContent.excerpt && postContent.excerpt.trim() !== '') {
      const translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('excerpt', postContent.excerpt, null, service);
      const excerptBox = document.querySelector('#postexcerpt.postbox textarea#excerpt');
      if (excerptBox) {
        excerptBox.value = translateContent;
      }
      if (lmatPageTranslationGlobal.post_type == 'product' && window.tinymce) {
        const excerptTinymce = tinymce.get('excerpt');
        const tinymceTextArea = document.querySelector(`textarea#excerpt`);
        if (excerptTinymce) {
          excerptTinymce.setContent(translateContent);
        }
        if (tinymceTextArea) {
          tinymceTextArea.value = translateContent;
        }
      }
    }
  };
  const updateMetaFieldsTable = () => {
    const customStuff = document.querySelector('#postcustomstuff');
    const inputFields = customStuff?.querySelectorAll('tbody#the-list td.left input[type="text"][value]');
    if (inputFields && inputFields.length > 0) {
      const inputFieldsArray = Array.from(inputFields);
      inputFieldsArray.forEach(inputField => {
        const value = inputField?.value;
        if (value && '' !== value && Object.keys(AllowedMetaFields).includes(value)) {
          let metaId = inputField?.closest('tr').id;
          metaId = metaId.replace('meta-', '');
          if (metaId && '' !== metaId) {
            const valueInputField = document.querySelector(`#meta-${metaId}-value[name="meta[${metaId}][value]"]`);
            if (valueInputField && valueInputField.value) {
              const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', valueInputField.value, value, service);
              if (translatedValue && '' !== translatedValue) {
                valueInputField.value = translatedValue;
              }
            }
          }
        }
      });
    }
  };

  /**
   * Updates the post meta fields based on translation.
   */
  const postMetaFieldsUpdate = () => {
    const metaFieldsData = postContent.metaFields;
    if (!metaFieldsData) {
      return;
    }
    Object.keys(metaFieldsData).forEach(key => {
      // Update yoast seo meta fields
      if (Object.keys(AllowedMetaFields).includes(key)) {
        const translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', metaFieldsData[key], key, service);
        if (key.startsWith('_yoast_wpseo_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__["default"])({
            key: key,
            value: translatedMetaFields
          });
        } else if (key.startsWith('rank_math_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
            key: key,
            value: translatedMetaFields
          });
        } else if (key.startsWith('_seopress_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_3__["default"])({
            key: key,
            value: translatedMetaFields
          });
        }
      }
      ;
    });
  };

  /**
   * Updates the post ACF fields based on translation.
   */
  const postAcfFieldsUpdate = () => {
    const metaFieldsData = postContent.metaFields;
    if (window.acf) {
      acf.getFields().forEach(field => {
        const fieldData = JSON.parse(JSON.stringify({
          key: field.data.key,
          type: field.data.type,
          name: field.data.name
        }));
        let repeaterField = false;
        // Update repeater fields
        if (field.$el && field.$el.closest('.acf-field.acf-field-repeater') && field.$el.closest('.acf-field.acf-field-repeater').length > 0) {
          const rowId = field.$el.closest('.acf-row').data('id');
          const repeaterItemName = field.$el.closest('.acf-field.acf-field-repeater').data('name');
          if (rowId && '' !== rowId) {
            const index = rowId.replace('row-', '');
            fieldData.name = repeaterItemName + '_' + index + '_' + fieldData.name;
            repeaterField = true;
          }
        }
        if (field.data && field.data.key && Object.keys(AllowedMetaFields).includes(fieldData.name)) {
          const fieldName = field.data.name;
          const inputType = field.data.type;
          const sourceValue = metaFieldsData[fieldName] ? metaFieldsData[fieldName] : field?.val();
          const translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', sourceValue, fieldData.name, service);
          if ('wysiwyg' === inputType && window.tinymce) {
            const editorId = field.data.id;
            const tinymceTranslatedMetaFields = translatedMetaFields.replace(/(\r\n\r\n|\r\n)/g, '</p><p>');
            tinymce.get(editorId)?.setContent(tinymceTranslatedMetaFields);
            const tinymceTextArea = document.querySelector(`textarea#${editorId}`);
            if (tinymceTextArea) {
              tinymceTextArea.value = translatedMetaFields;
            }
          } else {
            field.val(translatedMetaFields);
          }
        }
      });
    }
  };
  function splitContentWithDynamicBreaks(content) {
    const result = [];
    const regex = /(\r\n|\r|\n)/g;
    let lastIndex = 0;
    let match;
    while ((match = regex.exec(content)) !== null) {
      // Push the content before the line break
      if (match.index > lastIndex) {
        result.push(content.slice(lastIndex, match.index));
      }

      // Escape line break and wrap with marker
      const escapedBreak = match[0];
      result.push(`lmat_skip_content_open_${escapedBreak}_lmat_skip_content_end`);
      lastIndex = regex.lastIndex;
    }

    // Push remaining content after the last match
    if (lastIndex < content.length) {
      result.push(content.slice(lastIndex));
    }
    return result;
  }

  /**
   * Updates the post content based on translation.
   */
  const postContentUpdate = () => {
    const arrContent = splitContentWithDynamicBreaks(postContent.content);
    const strings = [];
    arrContent.forEach((text, index) => {
      const entity = /^&[a-zA-Z0-9#]+;$/.test(text);
      const htmlTag = /^<\/?\s*[a-zA-Z0-9#]+\s*\/?>$/.test(text);
      const isEmptyHtmlTag = /^<\s*\/?\s*[a-zA-Z0-9#]+\s*><\/\s*\/?\s*[a-zA-Z0-9#]+\s*>$/.test(text);
      const blockCommentTag = /<!--[\s\S]*?-->/g.test(text) && text.indexOf('<!--') < text.indexOf('-->');
      const plainText = !entity && !htmlTag && !isEmptyHtmlTag && !blockCommentTag;
      if (text !== '' && !text.includes('lmat_skip_content_open_') && plainText) {
        const uniqueKey = 'classic_index_' + index;
        const translatedText = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('content', text, uniqueKey, service);
        strings.push(translatedText);
      } else if (text.includes('lmat_skip_content_open_')) {
        const escapedBreak = text.replace('lmat_skip_content_open_', '').replace('_lmat_skip_content_end', '');
        strings.push(escapedBreak);
      } else {
        strings.push(text);
      }
    });
    const content = strings.join('');
    const tabWrapper = document.querySelector('#wp-content-wrap .wp-editor-tabs');
    if (tabWrapper) {
      const htmlButton = tabWrapper?.querySelector('.wp-switch-editor.switch-html');
      const tinymceButton = tabWrapper?.querySelector('.wp-switch-editor.switch-tmce');
      if (htmlButton) {
        htmlButton.click();
        const textArea = document.querySelector('textarea#content');
        if (textArea) {
          textArea.value = content;
        }
        if (tinymceButton) {
          tinymceButton.click();
          return;
        }
      }
      if (tinymceButton && window.tinymce) {
        tinymceButton.click();
        tinymce.get('content')?.setContent(content);
        return;
      }
    }
    if (window.tinymce && tinymce.get('content')) {
      tinymce.get('content')?.setContent(content);
    }
  };
  const updatePostMetaFields = () => {
    const ajaxUrl = window.lmatPageTranslationGlobal.ajax_url;
    const postId = window.lmatPageTranslationGlobal.current_post_id;
    const nonce = window.lmatPageTranslationGlobal.post_meta_fields_key;
    const action = window.lmatPageTranslationGlobal.update_post_meta_fields;
    if (!postId || !nonce || !action) {
      return;
    }
    const requestBody = {
      action: action,
      post_id: postId,
      meta_fields: JSON.stringify((0,_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_4__["default"])(postContent.metaFields, service)),
      post_meta_fields_key: nonce
    };
    fetch(ajaxUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(requestBody)
    }).then(response => response.json()).then().catch(error => {
      console.error('Error:', error);
    });
  };

  /**
   * Updates the translate status.
   */
  const updateTranslateStatus = () => {
    const ajaxUrl = window.lmatPageTranslationGlobal.ajax_url;
    const postId = window.lmatPageTranslationGlobal.current_post_id;
    const nonce = window.lmatPageTranslationGlobal.classic_status_key;
    const action = window.lmatPageTranslationGlobal.action_update_status;
    const requestBody = {
      action: action,
      post_id: postId,
      status: 'completed',
      update_translation_status_key: nonce
    };
    fetch(ajaxUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(requestBody)
    }).then(response => response.json()).then().catch(error => {
      console.error('Error:', error);
    });
  };

  // Update post title and excerpt text
  postDataUpdate();

  // Update post seo & acf fields on page
  if (lmatPageTranslationGlobal.postMetaSync === 'false') {
    // Update post meta fields
    postMetaFieldsUpdate();

    // Update post ACF fields
    postAcfFieldsUpdate();

    // Update metaFields classic editor custom fields table for prevent overwrite issue.
    updateMetaFieldsTable();
  }

  // Update post content
  postContentUpdate();

  // Close string modal box
  setTimeout(() => {
    modalClose();
  }, 500);

  // Update all translation supported post meta fields using ajax request
  if (lmatPageTranslationGlobal.postMetaSync === 'false') {
    updatePostMetaFields();
  }

  // Update translate status
  updateTranslateStatus();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (UpdateClassicPage);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/elementor/index.js":
/*!********************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/elementor/index.js ***!
  \********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../component/translate-seo-fields/yoast-seo-fields.js */ "./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js");
/* harmony import */ var _component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../component/translate-seo-fields/rank-math-seo.js */ "./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js");
/* harmony import */ var _meta_fields_index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../meta-fields/index.js */ "./modules/page-translation/src/create-translated-post/meta-fields/index.js");





// Update widget content with translations
const lmatUpdateWidgetContent = translations => {
  translations.forEach(translation => {
    // Find the model by ID using the lmatFindModelById function
    const model = lmatFindModelById(elementor.elements.models, translation.ID);
    if (model) {
      const settings = model.get('settings');

      // Check for normal fields (title, text, editor, etc.)
      if (settings.get(translation.key)) {
        settings.set(translation.key, translation.translatedContent); // Set the translated content
        model?.renderRemoteServer();
      }

      // Handle repeater fields (if any)
      const repeaterMatch = translation.key.match(/(.+)\[(\d+)\]\.(.+)/);
      if (repeaterMatch) {
        const [_, repeaterKey, index, subKey] = repeaterMatch;
        const repeaterArray = settings.get(repeaterKey);
        if (Array.isArray(repeaterArray.models) && repeaterArray.models[index]) {
          let repeaterModel = repeaterArray.models[index];
          let repeaterAttribute = repeaterModel.attributes;
          repeaterAttribute[subKey] = translation.translatedContent;
          settings.set(repeaterKey, repeaterArray); // Set the updated array back to settings
          model?.renderRemoteServer();
        }
      }
    }
  });

  // After updating content, ensure that the changes are saved or published
  $e.internal('document/save/set-is-modified', {
    status: true
  });
};
const lmatUpdateMetaFields = (metaFields, service) => {
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  if (!metaFields) {
    return;
  }
  Object.keys(metaFields).forEach(key => {
    // Update yoast seo meta fields
    if (Object.keys(AllowedMetaFields).includes(key)) {
      const translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', metaFields[key], key, service);
      if (key.startsWith('_yoast_wpseo_') && AllowedMetaFields[key].inputType === 'string') {
        (0,_component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__["default"])({
          key: key,
          value: translatedMetaFields
        });
      } else if (key.startsWith('rank_math_') && AllowedMetaFields[key].inputType === 'string') {
        (0,_component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
          key: key,
          value: translatedMetaFields
        });
      } else if (key.startsWith('_seopress_') && AllowedMetaFields[key].inputType === 'string') {
        elementor?.settings?.page?.model?.setExternalChange(key, translatedMetaFields);
      }
    }
    ;
  });
};
const lmatUpdateTitle = (title, service) => {
  if (title && '' !== title) {
    const translatedTitle = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('title', title, null, service);
    if (translatedTitle && '' !== translatedTitle) {
      elementor?.settings?.page?.model?.setExternalChange('post_title', translatedTitle);
    }
  }
};

/**
 * Updates ACF fields in the Elementor editor when ACF is loaded.   
 */
const postAcfFieldsUpdate = (postContent, service) => {
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  const metaFieldsData = postContent.metaFields;
  if (!window.acf || !metaFieldsData) {
    return;
  }
  acf.getFields().forEach(field => {
    const fieldData = JSON.parse(JSON.stringify({
      key: field.data.key,
      type: field.data.type,
      name: field.data.name
    }));
    if (field.$el && field.$el.closest('.acf-field.acf-field-repeater') && field.$el.closest('.acf-field.acf-field-repeater').length > 0) {
      const rowId = field.$el.closest('.acf-row').data('id');
      const repeaterItemName = field.$el.closest('.acf-field.acf-field-repeater').data('name');
      if (rowId && '' !== rowId) {
        const index = rowId.replace('row-', '');
        fieldData.name = repeaterItemName + '_' + index + '_' + fieldData.name;
      }
    }
    if (fieldData && fieldData.key && Object.keys(AllowedMetaFields).includes(fieldData.name)) {
      const fieldName = fieldData.name;
      const inputType = fieldData.type;
      const sourceValue = metaFieldsData[fieldName] ? metaFieldsData[fieldName] : field?.val();
      const translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', sourceValue, fieldData.name, service);
      if (!translatedMetaFields || '' === translatedMetaFields) {
        return;
      }
      if ('wysiwyg' === inputType && window.tinymce) {
        const editorId = field.data.id;
        const tinymceTranslatedMetaFields = translatedMetaFields.replace(/(\r\n\r\n|\r\n)/g, '</p><p>');
        tinymce.get(editorId)?.setContent(tinymceTranslatedMetaFields);
        const tinymceTextArea = document.querySelector(`textarea#${editorId}`);
        if (tinymceTextArea) {
          tinymceTextArea.value = translatedMetaFields;
        }
      } else {
        field.val(translatedMetaFields);
      }
    }
  });
};

/**
 * Persists translated meta fields (including ACF) via AJAX when post meta sync is disabled.
 */
const updatePostMetaFields = (postContent, service) => {
  const ajaxUrl = window.lmatPageTranslationGlobal.ajax_url;
  const postId = window.lmatPageTranslationGlobal.current_post_id;
  const nonce = window.lmatPageTranslationGlobal.post_meta_fields_key;
  const action = window.lmatPageTranslationGlobal.update_post_meta_fields;
  if (!postId || !nonce || !action) {
    return Promise.resolve();
  }
  const requestBody = {
    action: action,
    post_id: postId,
    meta_fields: JSON.stringify((0,_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_3__["default"])(postContent.metaFields, service)),
    post_meta_fields_key: nonce
  };
  return fetch(ajaxUrl, {
    method: 'POST',
    headers: {
      'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
      'Accept': 'application/json'
    },
    body: new URLSearchParams(requestBody)
  }).catch(error => {
    console.error('Error updating meta fields:', error);
  });
};

// Find Elementor model by ID
const lmatFindModelById = (elements, id) => {
  for (const model of elements) {
    if (model.get('id') === id) {
      return model;
    }
    const nestedElements = model.get('elements').models;
    const foundModel = lmatFindModelById(nestedElements, id);
    if (foundModel) {
      return foundModel;
    }
  }
  return null;
};
const updateElementorPage = ({
  postContent,
  modalClose,
  service
}) => {
  const postID = elementor.config.document.id;

  // Collect translated content
  const translations = [];

  // Define a list of properties to exclude
  const cssProperties = ['content_width', 'title_size', 'font_size', 'margin', 'padding', 'background', 'border', 'color', 'text_align', 'font_weight', 'font_family', 'line_height', 'letter_spacing', 'text_transform', 'border_radius', 'box_shadow', 'opacity', 'width', 'height', 'display', 'position', 'z_index', 'visibility', 'align', 'max_width', 'content_typography_typography', 'flex_justify_content', 'title_color', 'description_color', 'email_content'];
  const subStringsToCheck = strings => {
    const dynamicSubStrings = ['title', 'description', 'editor', 'text', 'content', 'label'];
    const staticSubStrings = ['caption', 'heading', 'sub_heading', 'testimonial_content', 'testimonial_job', 'testimonial_name', 'name'];
    return dynamicSubStrings.some(substring => strings.toLowerCase().includes(substring)) || staticSubStrings.some(substring => strings === substring);
  };
  const storeSourceStrings = (element, index, ids = []) => {
    const widgetId = element.id;
    const settings = element.settings;
    ids.push(index);

    // Check if settings is an object
    if (typeof settings === 'object' && settings !== null) {
      // Define the substrings to check for translatable content

      // Iterate through the keys in settings
      Object.keys(settings).forEach(key => {
        // Skip keys that are CSS properties
        if (cssProperties.some(substring => key.toLowerCase().includes(substring))) {
          return; // Skip this property and continue to the next one
        }

        // Check if the key includes any of the specified substrings
        if (subStringsToCheck(key) && typeof settings[key] === 'string' && settings[key].trim() !== '') {
          const uniqueKey = ids.join('_lmat_page_translation_') + '_lmat_page_translation_settings_lmat_page_translation_' + key;
          const translatedData = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('content', settings[key], uniqueKey, service);
          translations.push({
            ID: widgetId,
            key: key,
            translatedContent: translatedData
          });
        }

        // Check for arrays (possible repeater fields) within settings
        if (Array.isArray(settings[key])) {
          settings[key].forEach((item, index) => {
            if (typeof item === 'object' && item !== null) {
              // Check for translatable content in repeater fields
              Object.keys(item).forEach(repeaterKey => {
                // Skip if it's a CSS-related property
                if (cssProperties.includes(repeaterKey.toLowerCase())) {
                  return; // Skip this property
                }
                if (subStringsToCheck(repeaterKey) && typeof item[repeaterKey] === 'string' && item[repeaterKey].trim() !== '') {
                  const fieldKey = `${key}[${index}].${repeaterKey}`;
                  const uniqueKey = ids.join('_lmat_page_translation_') + '_lmat_page_translation_settings_lmat_page_translation_' + key + '_lmat_page_translation_' + index + '_lmat_page_translation_' + repeaterKey;
                  const translatedData = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('content', item[repeaterKey], uniqueKey, service);
                  translations.push({
                    ID: widgetId,
                    key: fieldKey,
                    translatedContent: translatedData
                  });
                }
              });
            }
          });
        }
      });
    }

    // If there are nested elements, process them recursively
    if (element.elements && Array.isArray(element.elements)) {
      element.elements.forEach((nestedElement, index) => {
        storeSourceStrings(nestedElement, index, [...ids, 'elements']);
      });
    }
  };
  postContent.widgetsContent.map((widget, index) => storeSourceStrings(widget, index, []));

  // Update widget content with translations
  lmatUpdateWidgetContent(translations);

  // Update Meta Fields (SEO plugins in Elementor panel)
  lmatUpdateMetaFields(postContent.metaFields, service);

  // Update ACF fields in the editor when post meta sync is disabled
  if (lmatPageTranslationGlobal.postMetaSync === 'false') {
    postAcfFieldsUpdate(postContent, service);
  }

  // Update Title
  lmatUpdateTitle(postContent.title, service);
  const replaceSourceString = () => {
    const elementorData = lmatPageTranslationGlobal.elementorData;
    const translateStrings = wp.data.select('block-lmatPageTranslation/translate').getTranslationEntries();
    translateStrings.forEach(translation => {
      const sourceString = translation.source;
      const ids = translation.id;
      const translatedContent = translation.translatedData;
      const type = translation.type;
      if (!sourceString || '' === sourceString || 'content' !== type) {
        return;
      }
      const keyArray = ids.split('_lmat_page_translation_');
      const translateValue = translatedContent[service];
      let parentElement = null;
      let parentKey = null;
      let currentElement = elementorData;
      keyArray.forEach(key => {
        parentElement = currentElement;
        parentKey = key;
        currentElement = currentElement ? currentElement[key] : null;
      });
      if (parentElement && parentKey && parentElement[parentKey] && parentElement[parentKey] === sourceString) {
        parentElement[parentKey] = translateValue;
      }
    });
    return elementorData;
  };
  const elementorData = replaceSourceString();
  const requestBody = {
    action: lmatPageTranslationGlobal.update_elementor_data,
    post_id: postID,
    elementor_data: JSON.stringify(elementorData),
    lmat_page_translation_nonce: lmatPageTranslationGlobal.ajax_nonce,
    parent_post_id: lmatPageTranslationGlobal.parent_post_id
  };
  if (postContent.slug_name && '' !== postContent.slug_name && lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
    const slug_name = postContent.slug_name;
    const translatedSlug = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('slug', slug_name, null, service);
    if (translatedSlug && '' !== translatedSlug) {
      requestBody.post_name = translatedSlug;
    }
  }
  fetch(lmatPageTranslationGlobal.ajax_url, {
    method: 'POST',
    headers: {
      'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
      'Accept': 'application/json'
    },
    body: new URLSearchParams(requestBody)
  }).then(response => response.json()).then(async data => {
    if (data.success) {
      const translateButton = document.querySelector('.lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]');
      if (translateButton) {
        translateButton.setAttribute('title', 'Translation process completed successfully.');
      }
    } else {
      console.error('Failed to update Elementor data:', data.data);
    }
    if (lmatPageTranslationGlobal.postMetaSync === 'false') {
      await updatePostMetaFields(postContent, service);
    }
    modalClose();
  }).catch(error => {
    modalClose();
    console.error('Error updating Elementor data:', error);
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (updateElementorPage);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/gutenberg/create-block.js":
/*!***************************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/gutenberg/create-block.js ***!
  \***************************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _component_filter_nested_attr_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../component/filter-nested-attr/index.js */ "./modules/page-translation/src/component/filter-nested-attr/index.js");


/**
 * Filters and translates attributes of a block based on provided rules.
 * 
 * @param {Object} block - The block to filter and translate attributes for.
 * @param {Object} blockParseRules - The rules for parsing the block.
 * @returns {Object} The updated block with translated attributes.
 */
const filterTranslateAttr = (block, blockParseRules, service) => {
  const {
    select
  } = wp.data;
  const filterAttrArr = Object.values(blockParseRules);
  const blockAttr = block.attributes;
  const blockId = block.clientId;

  // Function to update a nested attribute in the block
  const updateNestedAttribute = (obj, path, value) => {
    const newObj = {
      ...obj
    };
    let current = newObj;
    for (let i = 0; i < path.length - 1; i++) {
      if (Object.getPrototypeOf(current[path[i]]) === Array.prototype) {
        current[path[i]] = [...current[path[i]]];
      } else {
        current[path[i]] = {
          ...current[path[i]]
        }; // Create a shallow copy
      }
      current = current[path[i]];
    }
    if (current[path[path.length - 1]] instanceof wp.richText.RichTextData) {
      current[path[path.length - 1]] = value.replace(/(?<!\\)"|\\"/g, "'");
    } else {
      current[path[path.length - 1]] = value;
    }
    return newObj;
  };

  /**
   * Updates translated attributes based on provided ID array and filter attribute object.
   * 
   * @param {Array} idArr - The array of IDs to update attributes for.
   * @param {Object|Array} filterAttrObj - The filter attribute object to apply.
   */
  const updateTranslatedAttr = (idArr, filterAttrObj) => {
    if (true === filterAttrObj) {
      const newIdArr = new Array(...idArr);
      const childIdArr = new Array();
      let dynamicBlockAttr = blockAttr;
      let uniqueId = blockId;
      newIdArr.forEach(key => {
        childIdArr.push(key);
        uniqueId += `lmat_page_translation_${key}`;
        dynamicBlockAttr = dynamicBlockAttr ? dynamicBlockAttr[key] : dynamicBlockAttr;
      });
      let blockAttrContent = dynamicBlockAttr;
      if (blockAttrContent instanceof wp.richText.RichTextData) {
        blockAttrContent = blockAttrContent.originalHTML;
      }
      if (undefined !== blockAttrContent && typeof blockAttrContent === 'string' && blockAttrContent.trim() !== '') {
        let filterKey = uniqueId.replace(/[^\p{L}\p{N}]/gu, '');
        let translateContent = '';
        if (!/[\p{L}\p{N}]/gu.test(blockAttrContent)) {
          translateContent = blockAttrContent;
        } else {
          translateContent = select('block-lmatPageTranslation/translate').getTranslatedString('content', blockAttrContent, filterKey, service);
        }
        block.attributes = updateNestedAttribute(block.attributes, newIdArr, translateContent);
      }
      return;
    }
    (0,_component_filter_nested_attr_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])(idArr, filterAttrObj, blockAttr, updateTranslatedAttr);
  };
  filterAttrArr.forEach(data => {
    Object.keys(data).forEach(key => {
      const idArr = new Array(key);
      updateTranslatedAttr(idArr, data[key]);
    });
  });
  return block;
};

/**
 * Creates a translated block based on the provided block, child block, translate handler, and block rules.
 * If the block name is included in the block rules, it filters and translates the attributes accordingly.
 * 
 * @param {Object} block - The block to create a translated version of.
 * @param {Array} childBlock - The child blocks associated with the main block.
 * @param {Object} blockRules - The rules for translating blocks.
 * @param {String} service - The service to use for translation.
 * @returns {Object} The newly created translated block.
 */
const createTranslatedBlock = (block, childBlock, blockRules, service) => {
  const {
    createBlock
  } = wp.blocks;
  const {
    name: blockName,
    attributes
  } = block;
  const blockTranslateName = Object.keys(blockRules.LmatBlockParseRules);
  let attribute = {
    ...attributes
  };
  let translatedBlock = block;
  let newBlock = '';
  if (blockTranslateName.includes(block.name)) {
    translatedBlock = filterTranslateAttr(block, blockRules['LmatBlockParseRules'][block.name], service);
    attribute = translatedBlock.attributes;
  }
  newBlock = createBlock(blockName, attribute, childBlock);
  return newBlock;
};

/**
 * Creates a child block recursively by translating each inner block based on the provided block, translate handler, and block rules.
 * 
 * @param {Object} block - The block to create a child block for.
 * @param {Object} blockRules - The rules for translating blocks.
 * @returns {Object} The newly created translated child block.
 */
const cretaeChildBlock = (block, blockRules, service) => {
  let childBlock = block.innerBlocks.map(block => {
    if (block.name) {
      const childBlock = cretaeChildBlock(block, blockRules, service);
      return childBlock;
    }
  });
  const newBlock = createTranslatedBlock(block, childBlock, blockRules, service);
  return newBlock;
};

/**
 * Creates the main blocks based on the provided block, translate handler, and block rules.
 * If the block name exists, it creates the main block along with its child blocks and inserts it into the block editor.
 * 
 * @param {Object} block - The main block to create.
 * @param {Object} blockRules - The rules for translating blocks.
 */
const createBlocks = (block, service) => {
  const {
    select
  } = wp.data;
  const blockRules = select('block-lmatPageTranslation/translate').getBlockRules();
  const {
    dispatch
  } = wp.data;
  const {
    name: blockName
  } = block;
  // Create the main block
  if (blockName) {
    let childBlock = block.innerBlocks.map(block => {
      if (block.name) {
        return cretaeChildBlock(block, blockRules, service);
      }
    });
    const parentBlock = createTranslatedBlock(block, childBlock, blockRules, service);
    dispatch('core/block-editor').insertBlock(parentBlock);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (createBlocks);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/gutenberg/index.js":
/*!********************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/gutenberg/index.js ***!
  \********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _create_block_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./create-block.js */ "./modules/page-translation/src/create-translated-post/gutenberg/create-block.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../component/translate-seo-fields/yoast-seo-fields.js */ "./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js");
/* harmony import */ var _component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../component/translate-seo-fields/rank-math-seo.js */ "./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js");
/* harmony import */ var _component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../component/translate-seo-fields/seo-press.js */ "./modules/page-translation/src/component/translate-seo-fields/seo-press.js");
/* harmony import */ var _meta_fields_index_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../meta-fields/index.js */ "./modules/page-translation/src/create-translated-post/meta-fields/index.js");







/**
 * Translates the post content and updates the post title, excerpt, and content.
 * 
 * @param {Object} props - The properties containing post content, translation function, and block rules.
 */
const translatePost = props => {
  const {
    editPost
  } = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('core/editor');
  const {
    modalClose,
    postContent,
    service
  } = props;

  /**
   * Updates the post title and excerpt text based on translation.
   */
  const postDataUpdate = () => {
    const data = {};
    const editPostData = Object.keys(postContent).filter(key => ['title', 'excerpt'].includes(key));
    editPostData.forEach(key => {
      const sourceData = postContent[key];
      if (sourceData.trim() !== '') {
        const translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getTranslatedString(key, sourceData, null, service);
        data[key] = translateContent;
      }
    });
    editPost(data);
    if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
      const slugData = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getTranslatedString('slug', postContent.slug_name, null, service);
      editPost({
        slug: slugData
      });
    }
    if (lmatPageTranslationGlobal.slug_translation_option === 'slug_keep') {
      const slugData = lmatPageTranslationGlobal.slug_name;
      setTimeout(() => {
        editPost({
          slug: slugData
        });
      }, 500);
    }
  };

  /**
   * Updates the post meta fields based on translation.
   */
  const postMetaFieldsUpdate = () => {
    const metaFieldsData = postContent.metaFields;
    if (!metaFieldsData || Object.keys(metaFieldsData).length < 1) {
      return;
    }
    const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
    Object.keys(metaFieldsData).forEach(key => {
      // Update yoast seo meta fields
      if (Object.keys(AllowedMetaFields).includes(key)) {
        const translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', metaFieldsData[key], key, service);
        if (key.startsWith('_yoast_wpseo_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
            key: key,
            value: translatedMetaFields
          });
        } else if (key.startsWith('rank_math_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_3__["default"])({
            key: key,
            value: translatedMetaFields
          });
        } else if (key.startsWith('_seopress_') && AllowedMetaFields[key].inputType === 'string') {
          (0,_component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_4__["default"])({
            key: key,
            value: translatedMetaFields
          });
        }
      }
      ;
    });
  };

  /**
   * Updates the post ACF fields based on translation.
   */
  const postAcfFieldsUpdate = () => {
    const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
    const metaFieldsData = postContent.metaFields;
    if (window.acf) {
      acf.getFields().forEach(field => {
        const fieldData = JSON.parse(JSON.stringify({
          key: field.data.key,
          type: field.data.type,
          name: field.data.name
        }));
        // Update repeater fields
        if (field.$el && field.$el.closest('.acf-field.acf-field-repeater') && field.$el.closest('.acf-field.acf-field-repeater').length > 0) {
          const rowId = field.$el.closest('.acf-row').data('id');
          const repeaterItemName = field.$el.closest('.acf-field.acf-field-repeater').data('name');
          if (rowId && '' !== rowId) {
            const index = rowId.replace('row-', '');
            fieldData.name = repeaterItemName + '_' + index + '_' + fieldData.name;
          }
        }
        if (fieldData && fieldData.key && Object.keys(AllowedMetaFields).includes(fieldData.name)) {
          const fieldName = fieldData.name;
          const inputType = fieldData.type;
          let sourceValue = metaFieldsData[fieldName] ? metaFieldsData[fieldName] : field?.val();
          let translatedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', sourceValue, fieldData.name, service);
          if (!translatedMetaFields || '' === translatedMetaFields) {
            return;
          }
          if ('wysiwyg' === inputType && window.tinymce) {
            const editorId = field.data.id;
            const tinymceTranslatedMetaFields = translatedMetaFields.replace(/(\r\n\r\n|\r\n)/g, '</p><p>');
            tinymce.get(editorId)?.setContent(tinymceTranslatedMetaFields);
            const tinymceTextArea = document.querySelector(`textarea#${editorId}`);
            if (tinymceTextArea) {
              tinymceTextArea.value = translatedMetaFields;
            }
          } else {
            field.val(translatedMetaFields);
          }
        }
      });
    }
  };
  const updatePostMetaFields = () => {
    const ajaxUrl = window.lmatPageTranslationGlobal.ajax_url;
    const postId = window.lmatPageTranslationGlobal.current_post_id;
    const nonce = window.lmatPageTranslationGlobal.post_meta_fields_key;
    const action = window.lmatPageTranslationGlobal.update_post_meta_fields;
    if (!postId || !nonce || !action) {
      return;
    }
    const requestBody = {
      action: action,
      post_id: postId,
      meta_fields: JSON.stringify((0,_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_5__["default"])(postContent.metaFields, service)),
      post_meta_fields_key: nonce
    };
    fetch(ajaxUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(requestBody)
    }).then(response => response.json()).then().catch(error => {
      console.error('Error:', error);
    });
  };

  /**
   * Updates the post content based on translation.
   */
  const postContentUpdate = () => {
    const postContentData = postContent.content;
    if (postContentData.length <= 0) {
      return;
    }
    Object.values(postContentData).forEach(block => {
      (0,_create_block_js__WEBPACK_IMPORTED_MODULE_0__["default"])(block, service);
    });
  };

  // Update post title and excerpt text
  postDataUpdate();
  // Update post meta fields
  postMetaFieldsUpdate();
  // Update post ACF fields
  postAcfFieldsUpdate();
  // Update post content
  postContentUpdate();

  // Update all translation supported post meta fields using ajax request
  if (lmatPageTranslationGlobal.postMetaSync === 'false') {
    updatePostMetaFields();
  }

  // Close string modal box
  modalClose();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (translatePost);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/meta-fields/index.js":
/*!**********************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/meta-fields/index.js ***!
  \**********************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const translatedMetaFields = (sourceMetaFields, service) => {
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  const metaFields = sourceMetaFields;
  const updateFieldOnPage = (key, value, sourceValue) => {
    const selectors = [`[data-depend-id="${key}"]`, `[name="${key}"]`, `[id="${key}"]`];
    selectors.forEach(selector => {
      const fields = document.querySelectorAll(selector + ':not([type="button"]):not([type="checkbox"]):not([type="radio"])');
      fields.forEach(field => {
        if (field) {
          if (field.value === sourceValue) {
            field.value = value;
            return;
          } else {
            const allInputs = field.querySelectorAll('input:not([type="button"])');
            allInputs.forEach(input => {
              if (input.value === sourceValue) {
                input.value = value;
                return;
              }
            });
            const allTextareas = field.querySelectorAll('textarea');
            allTextareas.forEach(textarea => {
              if (textarea.value === sourceValue) {
                textarea.value = value;
                return;
              }
            });
          }
        }
      });
    });
  };
  const translateObjectMetaFields = (keys, value) => {
    Object.keys(value).forEach(key => {
      const keyArr = [...keys, key];
      const uniqueKey = keyArr.join('_lmat_page_translation_');
      if (typeof value[key] === 'string') {
        const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', value[key], uniqueKey, service);
        let currentObject = metaFields;
        if (translatedValue && '' !== translatedValue) {
          keyArr.forEach((key, index) => {
            if (index === keyArr.length - 1) {
              updateFieldOnPage(keyArr.join('_'), translatedValue, value[key]);
              updateFieldOnPage(key, translatedValue, value[key]);
              currentObject[key] = translatedValue;
              return;
            }
            if (currentObject.hasOwnProperty(key)) {
              currentObject = currentObject[key];
            } else {
              currentObject[key] = {};
              currentObject = currentObject[key];
            }
          });
        }
      }
      if (typeof value[key] === 'object' && Object.keys(value[key]).length > 0) {
        metaFields[uniqueKey] = translateObjectMetaFields(keyArr, value[key]);
      }
    });
  };
  if (sourceMetaFields && Object.keys(sourceMetaFields).length > 0) {
    Object.keys(sourceMetaFields).forEach(key => {
      if (typeof sourceMetaFields[key] === 'object' && Object.keys(sourceMetaFields[key]).length > 0) {
        translateObjectMetaFields([key], sourceMetaFields[key]);
      } else if (typeof sourceMetaFields[key] === 'string') {
        if (AllowedMetaFields[key] && AllowedMetaFields[key].status) {
          const sourceValue = sourceMetaFields[key];
          const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', sourceValue, key, service);
          if (translatedValue && '' !== translatedValue) {
            updateFieldOnPage(key, translatedValue, sourceValue);
            metaFields[key] = translatedValue;
          }
        }
      }
    });
  }
  return metaFields;
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (translatedMetaFields);

/***/ }),

/***/ "./modules/page-translation/src/create-translated-post/wpbakery/index.js":
/*!*******************************************************************************!*\
  !*** ./modules/page-translation/src/create-translated-post/wpbakery/index.js ***!
  \*******************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../component/translate-seo-fields/yoast-seo-fields.js */ "./modules/page-translation/src/component/translate-seo-fields/yoast-seo-fields.js");
/* harmony import */ var _component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../component/translate-seo-fields/rank-math-seo.js */ "./modules/page-translation/src/component/translate-seo-fields/rank-math-seo.js");
/* harmony import */ var _component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../component/translate-seo-fields/seo-press.js */ "./modules/page-translation/src/component/translate-seo-fields/seo-press.js");
/* harmony import */ var _meta_fields_index_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../meta-fields/index.js */ "./modules/page-translation/src/create-translated-post/meta-fields/index.js");






/**
 * Updates WPBakery Page Builder content with translations.
 * Handles both frontend and backend editors.
 * 
 * @param {Object} props - Properties containing post content and service info
 */
const updateWPBakeryPage = ({
  postContent,
  modalClose,
  service
}) => {
  const postID = lmatPageTranslationGlobal.current_post_id;
  const AllowedMetaFields = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getAllowedMetaFields();
  /**
   * Translate WPBakery content by replacing [lmat_val] tagged tokens with translations.
   * The PHP filters wrap translatable content in [lmat_val id="token"]content[/lmat_val] tags.
   * We extract translations and replace the tokens.
   * 
   * @param {string} content - The post content with [lmat_val] tags
   * @returns {string} - Content with translated values
   */
  const translateLmatValTags = content => {
    if (!content || content.trim() === '') {
      return content;
    }

    // Pattern to match [lmat_val id="token"]content[/lmat_val]
    // The token format is: ___LMAT_{md5_hash}___
    const lmatValRegex = /\[lmat_val[^\]]*?id=["'](___LMAT_[a-f0-9]{32}___)["'][^\]]*?\](.*?)\[\/lmat_val\]/gis;
    let translatedContent = content;
    let index = 0;

    // Find all [lmat_val] tags and process them
    let match;
    const directReplacements = []; // For content without separate tokens
    const tokenReplacements = []; // For tokens in attributes

    while ((match = lmatValRegex.exec(content)) !== null) {
      const fullMatch = match[0]; // The entire [lmat_val]...[/lmat_val]
      const token = match[1]; // The unique token ID
      const sourceText = match[2]; // The original content

      // Skip empty content
      if (!sourceText || sourceText.trim() === '') {
        continue;
      }

      // Create the same unique key used during storage
      const uniqueKey = `wpbakery_lmat_val_${index}_${token}`;

      // Get the translated value from the store
      const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('content', sourceText, uniqueKey, service);
      if (translatedValue) {
        // Check if the token exists elsewhere in the content (not just in the lmat_val tag)
        const contentWithoutThisTag = content.replace(fullMatch, '');
        const tokenExistsSeparately = contentWithoutThisTag.includes(token);
        if (tokenExistsSeparately) {
          // Pattern 1: Token is used in an attribute - replace token and remove wrapper later
          tokenReplacements.push({
            token: token,
            translatedValue: translatedValue
          });
        } else {
          // Pattern 2: Direct content wrapping - replace the entire lmat_val tag
          directReplacements.push({
            fullMatch: fullMatch,
            translatedValue: translatedValue
          });
        }
      }
      index++;
    }

    // Apply direct replacements first (Pattern 2)
    directReplacements.forEach(replacement => {
      translatedContent = translatedContent.replace(replacement.fullMatch, replacement.translatedValue);
    });

    // Apply token replacements (Pattern 1)
    tokenReplacements.forEach(replacement => {
      const tokenRegex = new RegExp(replacement.token.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'), 'g');
      translatedContent = translatedContent.replace(tokenRegex, replacement.translatedValue);
    });

    // Remove any remaining [lmat_val] tags (cleanup for Pattern 1)
    translatedContent = translatedContent.replace(/\s*\[lmat_val[^\]]*?\].*?\[\/lmat_val\]/gis, '');
    return translatedContent;
  };

  /**
   * Update post title with translation.
   */
  const updateTitle = () => {
    if (!postContent.title || postContent.title.trim() === '') {
      return;
    }
    const translatedTitle = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('title', postContent.title, null, service);
    if (!translatedTitle || translatedTitle.trim() === '') {
      return;
    }

    // Update title input field
    const titleInput = document.querySelector('input#title[name="post_title"]');
    if (titleInput) {
      titleInput.value = translatedTitle;
    }

    // Hide title label
    const titleLabel = document.querySelector('#titlediv label');
    if (titleLabel) {
      titleLabel.classList.add('screen-reader-text');
    }
  };

  /**
   * Update post slug with translation.
   */
  const updateSlug = () => {
    if (!postContent.slug_name || postContent.slug_name.trim() === '') {
      return;
    }
    let translatedSlug = '';
    if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
      translatedSlug = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('slug', postContent.slug_name, null, service);
    } else if (lmatPageTranslationGlobal.slug_translation_option === 'slug_keep') {
      translatedSlug = lmatPageTranslationGlobal.slug_name;
    }
    if (!translatedSlug || translatedSlug.trim() === '') {
      return;
    }

    // Update slug input
    const slugInput = document.querySelector('input#post_name[name="post_name"]');
    if (slugInput) {
      slugInput.value = translatedSlug;
    }

    // Hide slug label
    const slugLabel = document.querySelector('#slugdiv label');
    if (slugLabel) {
      slugLabel.classList.add('screen-reader-text');
    }
  };

  /**
   * Update excerpt with translation.
   */
  const updateExcerpt = () => {
    if (!postContent.excerpt || postContent.excerpt.trim() === '') {
      return;
    }
    const translatedExcerpt = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('excerpt', postContent.excerpt, null, service);
    if (!translatedExcerpt || translatedExcerpt.trim() === '') {
      return;
    }

    // Update excerpt textarea
    const excerptTextarea = document.querySelector('textarea#excerpt');
    if (excerptTextarea) {
      excerptTextarea.value = translatedExcerpt;
    }

    // Update TinyMCE editor if exists (for WooCommerce products)
    if (lmatPageTranslationGlobal.post_type === 'product' && window.tinymce) {
      const excerptEditor = tinymce.get('excerpt');
      if (excerptEditor) {
        excerptEditor.setContent(translatedExcerpt);
      }
    }
  };

  /**
   * Update SEO meta fields (Yoast, RankMath, SEOPress).
   */
  const updateMetaFields = () => {
    const metaFieldsData = postContent.metaFields;
    if (!metaFieldsData) {
      return;
    }
    Object.keys(metaFieldsData).forEach(key => {
      if (!Object.keys(AllowedMetaFields).includes(key)) {
        return;
      }
      const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', metaFieldsData[key], key, service);

      // Update based on SEO plugin
      if (key.startsWith('_yoast_wpseo_') && AllowedMetaFields[key].inputType === 'string') {
        (0,_component_translate_seo_fields_yoast_seo_fields_js__WEBPACK_IMPORTED_MODULE_1__["default"])({
          key: key,
          value: translatedValue
        });
      } else if (key.startsWith('rank_math_') && AllowedMetaFields[key].inputType === 'string') {
        (0,_component_translate_seo_fields_rank_math_seo_js__WEBPACK_IMPORTED_MODULE_2__["default"])({
          key: key,
          value: translatedValue
        });
      } else if (key.startsWith('_seopress_') && AllowedMetaFields[key].inputType === 'string') {
        (0,_component_translate_seo_fields_seo_press_js__WEBPACK_IMPORTED_MODULE_3__["default"])({
          key: key,
          value: translatedValue
        });
      }
    });
  };

  /**
   * Update ACF fields with translations.
   */
  const updateACFFields = () => {
    const metaFieldsData = postContent.metaFields;
    if (!window.acf || !metaFieldsData) {
      return;
    }
    acf.getFields().forEach(field => {
      const fieldData = JSON.parse(JSON.stringify({
        key: field.data.key,
        type: field.data.type,
        name: field.data.name
      }));

      // Handle repeater fields
      if (field.$el && field.$el.closest('.acf-field.acf-field-repeater').length > 0) {
        const rowId = field.$el.closest('.acf-row').data('id');
        const repeaterItemName = field.$el.closest('.acf-field.acf-field-repeater').data('name');
        if (rowId && rowId !== '') {
          const index = rowId.replace('row-', '');
          fieldData.name = repeaterItemName + '_' + index + '_' + fieldData.name;
        }
      }
      if (field.data && field.data.key && Object.keys(AllowedMetaFields).includes(fieldData.name)) {
        const sourceValue = metaFieldsData[field.data.name] ? metaFieldsData[field.data.name] : field.val();
        const translatedValue = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslatedString('metaFields', sourceValue, fieldData.name, service);

        // Handle WYSIWYG fields
        if (field.data.type === 'wysiwyg' && window.tinymce) {
          const editorId = field.data.id;
          const tinymceContent = translatedValue.replace(/(\r\n\r\n|\r\n)/g, '</p><p>');
          const editor = tinymce.get(editorId);
          if (editor) {
            editor.setContent(tinymceContent);
          }
          const textarea = document.querySelector(`textarea#${editorId}`);
          if (textarea) {
            textarea.value = translatedValue;
          }
        } else {
          field.val(translatedValue);
        }
      }
    });
  };

  /**
   * Update WPBakery content in the editor.
   * This updates the actual content in the WordPress editor (TinyMCE or Text mode)
   * and refreshes the WPBakery backend editor if it's active.
   */
  const updateWPBakeryContent = () => {
    if (!postContent.content || postContent.content.trim() === '') {
      return;
    }

    // Translate content by replacing [lmat_val] tokens with translations
    let translatedContent = translateLmatValTags(postContent.content);

    // Update in WordPress editor
    const contentWrapper = document.querySelector('#wp-content-wrap');
    if (contentWrapper) {
      // Switch to HTML mode to update raw content
      const htmlButton = contentWrapper.querySelector('.wp-switch-editor.switch-html');
      const visualButton = contentWrapper.querySelector('.wp-switch-editor.switch-tmce');
      if (htmlButton) {
        htmlButton.click();
        const textarea = document.querySelector('textarea#content');
        if (textarea) {
          textarea.value = translatedContent;
        }

        // Switch back to visual mode
        if (visualButton) {
          setTimeout(() => {
            visualButton.click();
          }, 100);
        }
      } else if (window.tinymce && tinymce.get('content')) {
        // Directly update TinyMCE if HTML mode not available
        tinymce.get('content').setContent(translatedContent);
      }
    }

    // Update textarea as fallback
    const contentTextarea = document.querySelector('textarea#content');
    if (contentTextarea) {
      contentTextarea.value = translatedContent;
    }

    // Update WPBakery Backend Editor if it's active
    updateWPBakeryBackendEditor();
  };

  /**
   * Update the WPBakery backend editor with translated content.
   * This triggers WPBakery to reload and display the updated shortcodes.
   */
  const updateWPBakeryBackendEditor = () => {
    const savebtn = document.getElementById('save-post');
    if (savebtn) {
      savebtn.click();
    }
  };

  /**
   * Save meta fields via AJAX (if sync is disabled).
   */
  const saveMetaFields = async () => {
    if (!lmatPageTranslationGlobal.update_post_meta_fields || !lmatPageTranslationGlobal.post_meta_fields_key) {
      return;
    }
    const requestBody = {
      action: lmatPageTranslationGlobal.update_post_meta_fields,
      post_id: postID,
      meta_fields: JSON.stringify((0,_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_4__["default"])(postContent.metaFields, service)),
      post_meta_fields_key: lmatPageTranslationGlobal.post_meta_fields_key
    };
    try {
      const response = await fetch(lmatPageTranslationGlobal.ajax_url, {
        method: 'POST',
        headers: {
          'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
          'Accept': 'application/json'
        },
        body: new URLSearchParams(requestBody)
      });
      const data = await response.json();
      if (!data.success) {
        throw new Error('Failed to save meta fields');
      }
    } catch (error) {
      throw new Error('Error saving meta fields');
    }
  };

  /**
   * Update translation status via AJAX.
   */
  const saveTranslationStatus = async () => {
    const requestBody = {
      action: lmatPageTranslationGlobal.action_update_status || 'lmat_update_classic_translate_status',
      post_id: postID,
      status: 'completed',
      lmat_classic_translate_nonce: lmatPageTranslationGlobal.classic_status_key
    };
    try {
      const response = await fetch(lmatPageTranslationGlobal.ajax_url, {
        method: 'POST',
        headers: {
          'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
          'Accept': 'application/json'
        },
        body: new URLSearchParams(requestBody)
      });
      const data = await response.json();
      if (data.success) {
        const translateButton = document.querySelector('button#lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]');
        if (translateButton) {
          translateButton.setAttribute('title', 'Translation process completed successfully.');
          translateButton.disabled = true;
        }
      } else {
        console.error('Failed to update translation status:', data);
      }
    } catch (error) {
      console.error('Error updating translation status:', error);
    }
  };

  // Execute translation updates
  try {
    // Update title
    updateTitle();

    // Update slug
    updateSlug();

    // Update excerpt
    updateExcerpt();

    // Update WPBakery content
    updateWPBakeryContent();

    // Update meta fields if sync is disabled
    if (lmatPageTranslationGlobal.postMetaSync === 'false') {
      updateMetaFields();
      updateACFFields();
    }

    // Save meta fields and update status via AJAX
    setTimeout(async () => {
      // Save meta fields first (if sync is disabled)
      if (lmatPageTranslationGlobal.postMetaSync === 'false') {
        await saveMetaFields();
      }

      // Then update translation status
      await saveTranslationStatus();

      // Close modal
      modalClose();
    }, 500);
  } catch (error) {
    console.error('Error during WPBakery translation:', error);
    modalClose();
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (updateWPBakeryPage);

/***/ }),

/***/ "./modules/page-translation/src/fetch-post/classic/index.js":
/*!******************************************************************!*\
  !*** ./modules/page-translation/src/fetch-post/classic/index.js ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../helper/index.js */ "./modules/page-translation/src/helper/index.js");
/* harmony import */ var _store_source_string_classic_index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../../store-source-string/classic/index.js */ "./modules/page-translation/src/store-source-string/classic/index.js");




const ClassicPostFetch = async props => {
  const apiUrl = lmatPageTranslationGlobal.ajax_url;
  const apiController = [];
  const destroyHandler = () => {
    apiController.forEach(controller => {
      controller.abort('Modal Closed');
    });
  };
  props.updateDestroyHandler(() => {
    destroyHandler();
  });
  const ContentFetch = async () => {
    const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
    if (contentFetchStatus) {
      return;
    }

    /**
    * Prepare data to send in API request.
    */
    const apiSendData = {
      postId: parseInt(props.postId),
      local: props.targetLang,
      current_local: props.sourceLang,
      lmat_page_translation_nonce: lmatPageTranslationGlobal.ajax_nonce,
      action: lmatPageTranslationGlobal.action_fetch
    };
    const contentController = new AbortController();
    apiController.push(contentController);
    const fetchSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_2__.mergeFetchAbortSignals)(props.signal, contentController);

    /**
     * useEffect hook to fetch post data from the specified API endpoint.
     * Parses the response data and updates the state accordingly.
     * Handles errors in fetching post content.
     */
    await fetch(apiUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(apiSendData),
      signal: fetchSignal
    }).then(response => response.json()).then(data => {
      const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
      if (contentFetchStatus) {
        return;
      }
      const post_data = data.data;
      (0,_store_source_string_classic_index_js__WEBPACK_IMPORTED_MODULE_3__["default"])(post_data);
      props.refPostData(post_data);
      props.updatePostDataFetch(true);
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentFetchStatus(true);
    }).catch(error => {
      if (error && error.name === 'AbortError') {
        return;
      }
      console.error('Error fetching post content:', error);
    });
  };
  await ContentFetch();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ClassicPostFetch);

/***/ }),

/***/ "./modules/page-translation/src/fetch-post/elementor/index.js":
/*!********************************************************************!*\
  !*** ./modules/page-translation/src/fetch-post/elementor/index.js ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _store_source_string_elementor_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../store-source-string/elementor/index.js */ "./modules/page-translation/src/store-source-string/elementor/index.js");

const fetchPostContent = async props => {
  const elementorPostData = lmatPageTranslationGlobal.elementorData && typeof lmatPageTranslationGlobal.elementorData === 'string' ? JSON.parse(lmatPageTranslationGlobal.elementorData) : lmatPageTranslationGlobal.elementorData;
  const content = {
    widgetsContent: elementorPostData
  };
  if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
    content.slug_name = lmatPageTranslationGlobal.slug_name;
  }
  if (lmatPageTranslationGlobal.parent_post_title && '' !== lmatPageTranslationGlobal.parent_post_title) {
    content.title = lmatPageTranslationGlobal.parent_post_title;
  }
  (0,_store_source_string_elementor_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])(content);
  props.refPostData(content);
  props.updatePostDataFetch(true);
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (fetchPostContent);

/***/ }),

/***/ "./modules/page-translation/src/fetch-post/gutenberg/index.js":
/*!********************************************************************!*\
  !*** ./modules/page-translation/src/fetch-post/gutenberg/index.js ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _store_source_string_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../store-source-string/gutenberg/index.js */ "./modules/page-translation/src/store-source-string/gutenberg/index.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _wordpress_blocks__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/blocks */ "@wordpress/blocks");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../../helper/index.js */ "./modules/page-translation/src/helper/index.js");





const GutenbergPostFetch = async props => {
  const apiUrl = lmatPageTranslationGlobal.ajax_url;
  let blockRules = wp.data.select('block-lmatPageTranslation/translate').getBlockRules() || {};
  const apiController = [];
  const destroyHandler = () => {
    apiController.forEach(controller => {
      controller.abort('Modal Closed');
    });
  };
  props.updateDestroyHandler(() => {
    destroyHandler();
  });
  const BlockParseFetch = async () => {
    if (blockRules && blockRules.LmatBlockParseRules && Object.keys(blockRules.LmatBlockParseRules).length > 0) {
      return;
    }
    const blockRulesApiSendData = {
      lmat_fetch_block_rules_key: lmatPageTranslationGlobal.fetchBlockRulesNonce,
      action: lmatPageTranslationGlobal.action_block_rules
    };
    const rulesController = new AbortController();
    apiController.push(rulesController);
    const rulesSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_4__.mergeFetchAbortSignals)(props.signal, rulesController);
    await fetch(apiUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(blockRulesApiSendData),
      signal: rulesSignal
    }).then(response => response.json()).then(data => {
      blockRules = JSON.parse(data.data.blockRules);
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate').setBlockRules(blockRules);
    }).catch(error => {
      if (error && error.name === 'AbortError') {
        return;
      }
      console.error('Error fetching post content:', error);
    });
  };
  await BlockParseFetch();
  const ContentFetch = async () => {
    const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
    if (contentFetchStatus) {
      return;
    }

    /**
    * Prepare data to send in API request.
    */
    const apiSendData = {
      postId: parseInt(props.postId),
      local: props.targetLang,
      current_local: props.sourceLang,
      lmat_page_translation_nonce: lmatPageTranslationGlobal.ajax_nonce,
      action: lmatPageTranslationGlobal.action_fetch
    };
    const contentController = new AbortController();
    apiController.push(contentController);
    const contentSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_4__.mergeFetchAbortSignals)(props.signal, contentController);

    /**
     * useEffect hook to fetch post data from the specified API endpoint.
     * Parses the response data and updates the state accordingly.
     * Handles errors in fetching post content.
     */
    await fetch(apiUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(apiSendData),
      signal: contentSignal
    }).then(response => response.json()).then(data => {
      const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
      if (contentFetchStatus) {
        return;
      }
      const post_data = data.data;
      if (post_data.content && post_data.content.trim() !== '') {
        post_data.content = (0,_wordpress_blocks__WEBPACK_IMPORTED_MODULE_2__.parse)(post_data.content);
      }
      (0,_store_source_string_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])(post_data, blockRules);
      props.refPostData(post_data);
      props.updatePostDataFetch(true);
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate').contentFetchStatus(true);
    }).catch(error => {
      if (error && error.name === 'AbortError') {
        return;
      }
      console.error('Error fetching post content:', error);
    });
  };
  await ContentFetch();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GutenbergPostFetch);

/***/ }),

/***/ "./modules/page-translation/src/fetch-post/meta-fields/index.js":
/*!**********************************************************************!*\
  !*** ./modules/page-translation/src/fetch-post/meta-fields/index.js ***!
  \**********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../helper/index.js */ "./modules/page-translation/src/helper/index.js");


const MetaFieldsFetch = async props => {
  const apiUrl = window?.lmatPageTranslationGlobal?.ajax_url;
  const apiController = [];
  const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
  if (contentFetchStatus) {
    return;
  }
  const destroyHandler = () => {
    apiController.forEach(controller => {
      controller.abort('Modal Closed');
    });
  };
  props.updateDestroyHandler(() => {
    destroyHandler();
  });
  const storeMetaFields = ({
    key,
    value,
    type,
    status,
    allowedMetaFields
  }) => {
    // Check if value is not a date or timestamp like 03/16/2023 6:02 AM
    // Simple regex to match MM/DD/YYYY and optional time
    const dateTimeRegex = /^\d{1,2}\/\d{1,2}\/\d{4}(?:\s+\d{1,2}:\d{2}\s*(?:AM|PM)?)?$/i;
    const digitsWithSymbols = /^[0-9.\- _#!$%^&*()+~`]+$/;
    if (dateTimeRegex.test(value)) {
      // If value is a date or timestamp, skip saving as translatable string
      return;
    }
    if (digitsWithSymbols.test(value)) {
      // If value is a number, skip saving as translatable string
      return;
    }
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').allowedMetaFields({
      id: key,
      type,
      status
    });
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').metaFieldsSaveSource(key, value);
  };
  const getMetaFields = async () => {
    const action = window?.lmatPageTranslationGlobal?.get_meta_fields;
    const meta_fields_key = window?.lmatPageTranslationGlobal?.meta_fields_key;
    const postId = parseInt(props.postId);
    if (!action || !meta_fields_key || !postId) {
      return;
    }
    const allowedCustomFieldsController = new AbortController();
    apiController.push(allowedCustomFieldsController);
    const metaSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_1__.mergeFetchAbortSignals)(props.signal, allowedCustomFieldsController);
    try {
      const response = await fetch(apiUrl, {
        method: 'POST',
        headers: {
          'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
          'Accept': 'application/json'
        },
        body: new URLSearchParams({
          action,
          meta_fields_key,
          postId: postId
        }),
        signal: metaSignal
      });
      const data = await response.json();
      const metaFields = data?.data?.metaFields;
      const allowedMetaFields = data?.data?.allowedMetaFields;
      if (metaFields && Object.keys(metaFields).length > 0) {
        Object.keys(metaFields).forEach(key => {
          if (allowedMetaFields[key] && allowedMetaFields[key].status) {
            if (typeof metaFields[key] === 'string') {
              const value = metaFields[key];

              // Store meta fields
              storeMetaFields({
                key,
                value,
                type: allowedMetaFields[key].type,
                status: allowedMetaFields[key].status,
                allowedMetaFields
              });
            } else if (typeof metaFields[key] === 'object' && Object.keys(metaFields[key]).length > 0) {
              // Store object meta fields
              storeObjectMetaFields([key], metaFields[key], allowedMetaFields);
            }
          }
        });
      }
      props.refPostData({
        'metaFields': metaFields
      });
    } catch (err) {
      if (err && err.name === 'AbortError') {
        return;
      }
      props.refPostData({
        metaFields: {}
      });
    }
  };
  const storeObjectMetaFields = (keys, value, allowedMetaFields) => {
    Object.keys(value).forEach(key => {
      const keyArr = [...keys, key];
      const uniqueKey = keyArr.join('_lmat_page_translation_');
      if (typeof value[key] === 'string') {
        const metaValue = value[key];

        // Store meta fields
        storeMetaFields({
          key: uniqueKey,
          value: metaValue,
          type: 'string',
          status: allowedMetaFields[keys[0]].status,
          allowedMetaFields
        });
      } else if (typeof value[key] === 'object' && Object.keys(value[key]).length > 0) {
        // Store object meta fields
        storeObjectMetaFields(keyArr, value[key], allowedMetaFields);
      }
    });
  };
  if ("false" === window?.lmatPageTranslationGlobal?.postMetaSync) {
    await getMetaFields();
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (MetaFieldsFetch);

/***/ }),

/***/ "./modules/page-translation/src/fetch-post/wpbakery/index.js":
/*!*******************************************************************!*\
  !*** ./modules/page-translation/src/fetch-post/wpbakery/index.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../../helper/index.js */ "./modules/page-translation/src/helper/index.js");
/* harmony import */ var _store_source_string_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../../store-source-string/wpbakery/index.js */ "./modules/page-translation/src/store-source-string/wpbakery/index.js");




/**
 * Fetch WPBakery post content from the server.
 * Retrieves post data and stores source strings for translation.
 * 
 * @param {Object} props - Component props containing post info and callbacks
 */
const WPBakeryPostFetch = async props => {
  const apiUrl = lmatPageTranslationGlobal.ajax_url;
  const apiController = [];

  /**
   * Abort all pending API requests when modal is closed.
   */
  const destroyHandler = () => {
    apiController.forEach(controller => {
      controller.abort('Modal Closed');
    });
  };
  props.updateDestroyHandler(() => {
    destroyHandler();
  });

  /**
   * Fetch WPBakery content from server.
   * The content is already processed by PHP filters:
   * - Base64 decoded
   * - Translatable attributes wrapped in [lmat_val] tags
   */
  const ContentFetch = async () => {
    const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
    if (contentFetchStatus) {
      return;
    }

    /**
     * Prepare data for API request.
     */
    const apiSendData = {
      postId: parseInt(props.postId),
      local: props.targetLang,
      current_local: props.sourceLang,
      lmat_page_translation_nonce: lmatPageTranslationGlobal.ajax_nonce,
      action: lmatPageTranslationGlobal.action_fetch
    };
    const contentController = new AbortController();
    apiController.push(contentController);
    const contentSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_1__.mergeFetchAbortSignals)(props.signal, contentController);

    /**
     * Fetch post data from WordPress via AJAX.
     * The server applies WPBakery-specific filters:
     * - decode_wpbakery_shortcodes (priority 10)
     * - expose_translatable_attributes (priority 20)
     */
    await fetch(apiUrl, {
      method: 'POST',
      headers: {
        'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
        'Accept': 'application/json'
      },
      body: new URLSearchParams(apiSendData),
      signal: contentSignal
    }).then(response => response.json()).then(data => {
      const contentFetchStatus = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').contentFetchStatus();
      if (contentFetchStatus) {
        return;
      }
      const post_data = data.data;

      // Store WPBakery source strings
      (0,_store_source_string_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])(post_data);

      // Pass data to parent component
      props.refPostData(post_data);
      props.updatePostDataFetch(true);

      // Mark content as fetched
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentFetchStatus(true);
    }).catch(error => {
      if (error && error.name === 'AbortError') {
        return;
      }
      console.error('Error fetching WPBakery post content:', error);
    });
  };
  await ContentFetch();
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (WPBakeryPostFetch);

/***/ }),

/***/ "./modules/page-translation/src/global-store/actions.js":
/*!**************************************************************!*\
  !*** ./modules/page-translation/src/global-store/actions.js ***!
  \**************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   allowedMetaFields: () => (/* binding */ allowedMetaFields),
/* harmony export */   contentFetchStatus: () => (/* binding */ contentFetchStatus),
/* harmony export */   contentSaveFiltered: () => (/* binding */ contentSaveFiltered),
/* harmony export */   contentSaveSource: () => (/* binding */ contentSaveSource),
/* harmony export */   contentSaveTranslate: () => (/* binding */ contentSaveTranslate),
/* harmony export */   excerptSaveFiltered: () => (/* binding */ excerptSaveFiltered),
/* harmony export */   excerptSaveSource: () => (/* binding */ excerptSaveSource),
/* harmony export */   excerptSaveTranslate: () => (/* binding */ excerptSaveTranslate),
/* harmony export */   metaFieldsSaveFiltered: () => (/* binding */ metaFieldsSaveFiltered),
/* harmony export */   metaFieldsSaveSource: () => (/* binding */ metaFieldsSaveSource),
/* harmony export */   metaFieldsSaveTranslate: () => (/* binding */ metaFieldsSaveTranslate),
/* harmony export */   setBlockRules: () => (/* binding */ setBlockRules),
/* harmony export */   slugSaveFiltered: () => (/* binding */ slugSaveFiltered),
/* harmony export */   slugSaveSource: () => (/* binding */ slugSaveSource),
/* harmony export */   slugSaveTranslate: () => (/* binding */ slugSaveTranslate),
/* harmony export */   titleSaveFiltered: () => (/* binding */ titleSaveFiltered),
/* harmony export */   titleSaveSource: () => (/* binding */ titleSaveSource),
/* harmony export */   titleSaveTranslate: () => (/* binding */ titleSaveTranslate),
/* harmony export */   translationInfo: () => (/* binding */ translationInfo)
/* harmony export */ });
/* harmony import */ var _types_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./types.js */ "./modules/page-translation/src/global-store/types.js");
 // Importing action types from the types module

/**
 * Action creator for saving the source title.
 * @param {string} data - The source title to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const titleSaveSource = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceTitle,
    // Action type for saving the source title
    text: data // The source title text
  };
};

/**
 * Action creator for saving the translated title.
 * @param {string} data - The translated title to be saved.
 * @param {string} provider - The provider of the translated title.
 * @returns {Object} The action object containing the type, text, and provider.
 */
const titleSaveTranslate = (data, provider) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedTitle,
    // Action type for saving the translated title
    text: data,
    // The translated title text
    provider: provider // The provider of the translated title
  };
};

/**
 * Action creator for saving the filtered title.
 * @param {string} data - The filtered title to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const titleSaveFiltered = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredTitleString,
    // Action type for saving the filtered title
    text: data // The filtered title text
  };
};

/**
 * Action creator for saving the source excerpt.
 * @param {string} data - The source excerpt to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const excerptSaveSource = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceExcerpt,
    // Action type for saving the source excerpt
    text: data // The source excerpt text
  };
};

/**
 * Action creator for saving the translated excerpt.
 * @param {string} data - The translated excerpt to be saved.
 * @param {string} provider - The provider of the translated excerpt.
 * @returns {Object} The action object containing the type, text, and provider.
 */
const excerptSaveTranslate = (data, provider) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedExcerpt,
    // Action type for saving the translated excerpt
    text: data,
    // The translated excerpt text
    provider: provider // The provider of the translated excerpt
  };
};

/**
 * Action creator for saving the filtered excerpt.
 * @param {string} data - The filtered excerpt to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const excerptSaveFiltered = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredExcerptString,
    // Action type for saving the filtered excerpt
    text: data // The filtered excerpt text
  };
};

/**
 * Action creator for saving the source slug.
 * @param {string} data - The source slug to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const slugSaveSource = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceSlug,
    // Action type for saving the source slug
    text: data // The source slug text
  };
};

/**
 * Action creator for saving the translated slug.
 * @param {string} data - The translated slug to be saved.
 * @param {string} provider - The provider of the translated slug.
 * @returns {Object} The action object containing the type, text, and provider.
 */
const slugSaveTranslate = (data, provider) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedSlug,
    // Action type for saving the translated slug
    text: data,
    // The translated slug text
    provider: provider // The provider of the translated slug
  };
};

/**
 * Action creator for saving the filtered slug.
 * @param {string} data - The filtered slug to be saved.
 * @returns {Object} The action object containing the type and text.
 */
const slugSaveFiltered = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredSlugString,
    // Action type for saving the filtered slug
    text: data // The filtered slug text
  };
};

/**
 * Action creator for saving the source content.
 * @param {string} id - The identifier for the content.
 * @param {string} data - The source content to be saved.
 * @returns {Object} The action object containing the type, text, and id.
 */
const contentSaveSource = (id, data) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceContent,
    // Action type for saving the source content
    text: data,
    // The source content text
    id: id // The identifier for the content
  };
};

/**
 * Action creator for saving the translated content.
 * @param {string} id - The identifier for the content.
 * @param {string} data - The translated content to be saved.
 * @param {string} source - The source of the translated content.
 * @param {string} provider - The provider of the translated content.
 * @returns {Object} The action object containing the type, text, id, source, and provider.
 */
const contentSaveTranslate = (id, data, source, provider) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedContent,
    // Action type for saving the translated content
    text: data,
    // The translated content text
    id: id,
    // The identifier for the content
    source: source,
    // The source of the translated content
    provider: provider // The provider of the translated content
  };
};

/**
 * Action creator for saving the filtered content.
 * @param {string} id - The identifier for the content.
 * @param {string} data - The filtered content to be saved.
 * @returns {Object} The action object containing the type, text, and id.
 */
const contentSaveFiltered = (data, id) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredContentString,
    // Action type for saving the filtered content
    text: data,
    // The filtered content text
    id: id // The identifier for the content
  };
};

/**
 * Action creator for saving the source meta fields.
 * @param {string} id - The identifier for the meta fields.
 * @param {Object} data - The source meta fields to be saved.
 * @returns {Object} The action object containing the type, text, and id.
 */
const metaFieldsSaveSource = (id, data) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceMetaFields,
    // Action type for saving the source meta fields
    text: data,
    // The source meta fields text
    id: id // The identifier for the meta fields
  };
};

/**
 * Action creator for saving the translated meta fields.
 * @param {string} id - The identifier for the meta fields.
 * @param {Object} data - The translated meta fields to be saved.
 * @param {string} source - The source of the translated meta fields.
 * @param {string} provider - The provider of the translated meta fields.
 * @returns {Object} The action object containing the type, text, id, source, and provider.
 */
const metaFieldsSaveTranslate = (id, data, source, provider) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedMetaFields,
    // Action type for saving the translated meta fields
    text: data,
    // The translated meta fields text
    id: id,
    // The identifier for the meta fields
    source: source,
    // The source of the translated meta fields
    provider: provider // The provider of the translated meta fields
  };
};

/**
 * Action creator for saving the filtered meta fields.
 * @param {string} id - The identifier for the meta fields.
 * @param {Object} data - The filtered meta fields to be saved.
 * @returns {Object} The action object containing the type, text, and id.
 */
const metaFieldsSaveFiltered = (data, id) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredMetaFieldsString,
    // Action type for saving the filtered meta fields
    text: data,
    // The filtered meta fields text
    id: id // The identifier for the meta fields
  };
};

/**
 * Action creator for saving the block rules.
 * @param {Object} data - The block rules to be saved.
 * @returns {Object} The action object containing the type and data.
 */
const setBlockRules = data => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].setBlockRules,
    // Action type for saving the block rules
    data: data // The block rules data
  };
};

/**
 * Action creator for saving the translation info.
 * @param {Object} data - The translation info to be saved.
 * @returns {Object} The action object containing the type and data.
 */
const translationInfo = ({
  sourceStringCount = null,
  sourceWordCount = null,
  sourceCharacterCount = null,
  timeTaken = null,
  provider = null,
  targetStringCount = null,
  targetWordCount = null,
  targetCharacterCount = null,
  translateStatus = null
}) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].translationInfo,
    // Action type for saving the translation info
    sourceStringCount: sourceStringCount,
    sourceWordCount: sourceWordCount,
    sourceCharacterCount: sourceCharacterCount,
    // The character count
    timeTaken: timeTaken,
    // The time taken
    targetStringCount: targetStringCount,
    targetWordCount: targetWordCount,
    targetCharacterCount: targetCharacterCount,
    provider: provider,
    // The provider
    translateStatus: translateStatus // The translate status
  };
};

/**
 * Action creator for saving the allowed meta fields.
 * @param {Object} data - The allowed meta fields to be saved.
 * @returns {Object} The action object containing the type and data.
 */
const allowedMetaFields = ({
  id,
  type,
  status
}) => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].allowedMetaFields,
    id: id,
    inputType: type,
    status: status
  };
};

/**
 * Action creator for saving the content fetch status.
 * @param {boolean} status - The content fetch status to be saved.
 * @returns {Object} The action object containing the type and status.
 */
const contentFetchStatus = status => {
  return {
    type: _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].contentFetchStatus,
    status: status
  };
};

/***/ }),

/***/ "./modules/page-translation/src/global-store/index.js":
/*!************************************************************!*\
  !*** ./modules/page-translation/src/global-store/index.js ***!
  \************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _reducer_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./reducer.js */ "./modules/page-translation/src/global-store/reducer.js");
/* harmony import */ var _actions_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./actions.js */ "./modules/page-translation/src/global-store/actions.js");
/* harmony import */ var _selectors_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./selectors.js */ "./modules/page-translation/src/global-store/selectors.js");
/**
 * This module sets up a Redux store for the automatic translation block.
 * It imports the necessary reducer, actions, and selectors, and then
 * creates and registers the Redux store with the WordPress data system.
 */

// Import the reducer function from the reducer module, which handles state changes.


// Import all action creators from the actions module, which define how to interact with the store.


// Import all selector functions from the selectors module, which allow us to retrieve specific pieces of state.


// Destructure the createReduxStore and register functions from the wp.data object provided by WordPress.
const {
  createReduxStore,
  register
} = wp.data;

// Create a Redux store named 'block-lmatPageTranslation/translate' with the specified reducer, actions, and selectors.
const store = createReduxStore('block-lmatPageTranslation/translate', {
  reducer: _reducer_js__WEBPACK_IMPORTED_MODULE_0__["default"],
  // The reducer function to manage state updates.
  actions: _actions_js__WEBPACK_IMPORTED_MODULE_1__,
  // The action creators for dispatching actions to the store.
  selectors: _selectors_js__WEBPACK_IMPORTED_MODULE_2__ // The selectors for accessing specific state values.
});

// Register the created store with the WordPress data system, making it available for use in the application.
register(store);

/***/ }),

/***/ "./modules/page-translation/src/global-store/reducer.js":
/*!**************************************************************!*\
  !*** ./modules/page-translation/src/global-store/reducer.js ***!
  \**************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _types_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./types.js */ "./modules/page-translation/src/global-store/types.js");
 // Importing action types from the types module

/**
 * The default state for the translation reducer.
 * This state holds the initial values for title, excerpt, content, and metaFields.
 * 
 * @type {Object}
 * @property {Object} title - Contains source and target translations for the title.
 * @property {Object} excerpt - Contains source and target translations for the excerpt.
 * @property {Array} content - An array holding content translations.
 * @property {Object} metaFields - Contains source and target translations for meta fields.
 */
const TranslateDefaultState = {
  title: {},
  // Initial state for title translations
  excerpt: {},
  // Initial state for excerpt translations
  slug: {},
  // Initial state for slug translations
  content: [],
  // Initial state for content translations
  metaFields: {},
  // Initial state for meta field translations
  allowedMetaFields: {},
  // Initial state for allowed meta fields
  contentFetchStatus: false // Initial state for content fetch status
};

/**
 * The reducer function for handling translation actions.
 * This function updates the state based on the action type received.
 * 
 * @param {Object} state - The current state of the reducer.
 * @param {Object} action - The action dispatched to the reducer.
 * @returns {Object} The new state after applying the action.
 */
const reducer = (state = TranslateDefaultState, action) => {
  switch (action.type) {
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceTitle:
      // Action to save the source title
      // Check if the action text contains any letters or numbers
      if (/[\p{L}\p{N}]/gu.test(action.text)) {
        // Update the state with the new source title
        return {
          ...state,
          title: {
            ...state.title,
            source: action.text
          }
        };
      }
      return state;
    // Return the current state if no valid text

    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedTitle:
      // Action to save the translated title
      // Update the state with the new target title
      return {
        ...state,
        title: {
          ...state.title,
          translatedData: {
            ...(state.title.translatedData || []),
            [action.provider]: action.text
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredTitleString:
      // Action to save the filtered title
      // Update the state with the new filtered title
      return {
        ...state,
        title: {
          ...state.title,
          filteredString: action.text
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceExcerpt:
      // Action to save the source excerpt
      // Check if the action text contains any letters or numbers
      if (/[\p{L}\p{N}]/gu.test(action.text)) {
        // Update the state with the new source excerpt
        return {
          ...state,
          excerpt: {
            ...state.excerpt,
            source: action.text
          }
        };
      }
      return state;
    // Return the current state if no valid text

    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedExcerpt:
      // Action to save the translated excerpt
      // Update the state with the new target excerpt
      return {
        ...state,
        excerpt: {
          ...state.excerpt,
          translatedData: {
            ...(state.excerpt.translatedData || []),
            [action.provider]: action.text
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredExcerptString:
      // Action to save the filtered excerpt
      // Update the state with the new filtered excerpt
      return {
        ...state,
        excerpt: {
          ...state.excerpt,
          filteredString: action.text
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceSlug:
      // Action to save the source slug
      // Update the state with the new source slug
      return {
        ...state,
        slug: {
          ...state.slug,
          source: action.text
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedSlug:
      // Action to save the translated slug
      // Update the state with the new target slug
      return {
        ...state,
        slug: {
          ...state.slug,
          translatedData: {
            ...(state.slug.translatedData || []),
            [action.provider]: action.text
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredSlugString:
      // Action to save the filtered slug
      // Update the state with the new filtered slug
      return {
        ...state,
        slug: {
          ...state.slug,
          filteredString: action.text
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceContent:
      // Action to save the source content
      // Check if the action text contains any letters or numbers
      if (/[\p{L}\p{N}]/gu.test(action.text)) {
        // Update the state with the new source content for the specific ID
        return {
          ...state,
          content: {
            ...state.content,
            [action.id]: {
              ...(state.content[action.id] || []),
              source: action.text
            }
          }
        };
      }
      return state;
    // Return the current state if no valid text

    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedContent:
      // Action to save the translated content
      // Check if the source of the content matches the action source
      if (state.content[action.id].source === action.source) {
        // Update the state with the new target content for the specific ID
        return {
          ...state,
          content: {
            ...state.content,
            [action.id]: {
              ...(state.content[action.id] || []),
              translatedData: {
                ...(state.content[action.id].translatedData || []),
                [action.provider]: action.text
              }
            }
          }
        };
      }
      return state;
    // Return the current state if no match

    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredContentString:
      // Action to save the filtered content
      // Update the state with the new filtered content
      return {
        ...state,
        content: {
          ...state.content,
          [action.id]: {
            ...(state.content[action.id] || []),
            filteredString: action.text
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].sourceMetaFields:
      // Action to save the source meta fields
      // Check if the action text contains any letters or numbers
      if (/[\p{L}\p{N}]/gu.test(action.text)) {
        // Update the state with the new source meta field for the specific ID
        return {
          ...state,
          metaFields: {
            ...state.metaFields,
            [action.id]: {
              ...(state.metaFields[action.id] || []),
              source: action.text
            }
          }
        };
      }
      return state;
    // Return the current state if no valid text

    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].traslatedMetaFields:
      // Action to save the translated meta fields
      // Update the state with the new target meta field for the specific ID
      return {
        ...state,
        metaFields: {
          ...state.metaFields,
          [action.id]: {
            ...(state.metaFields[action.id] || []),
            translatedData: {
              ...(state.metaFields[action.id].translatedData || []),
              [action.provider]: action.text
            }
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].filteredMetaFieldsString:
      // Action to save the filtered meta fields
      // Update the state with the new filtered meta fields
      return {
        ...state,
        metaFields: {
          ...state.metaFields,
          [action.id]: {
            ...(state.metaFields[action.id] || []),
            filteredString: action.text
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].setBlockRules:
      // Action to save the block rules
      // Update the state with the new block rules
      return {
        ...state,
        blockRules: action.data
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].translationInfo:
      // Action to save the translation info
      // Update the state with the new translation info
      const data = {};

      // Source String Count
      action.sourceStringCount && (data.sourceStringCount = action.sourceStringCount);
      // Source Word Count
      action.sourceWordCount && (data.sourceWordCount = action.sourceWordCount);
      // Source Character Count
      action.sourceCharacterCount && (data.sourceCharacterCount = action.sourceCharacterCount);

      // Save the translation info like target word count, target character count, translate status, time taken
      if ((action.targetWordCount || action.targetCharacterCount || action.translateStatus || action.timeTaken) && action.provider) {
        data.translateData = {
          ...(state.translationInfo?.translateData || {}),
          [action.provider]: {
            // If the provider already exists, update the existing provider data    
            ...(state.translationInfo?.translateData?.[action.provider] || {}),
            // Update the source string count
            ...(action.targetStringCount && {
              targetStringCount: action.targetStringCount
            }),
            // Update the target word count, target character count, translate status, time taken
            ...(action.targetWordCount && {
              targetWordCount: action.targetWordCount
            }),
            // Update the target character count
            ...(action.targetCharacterCount && {
              targetCharacterCount: action.targetCharacterCount
            }),
            // Update the translate status
            ...(action.translateStatus && {
              translateStatus: action.translateStatus
            }),
            // Update the time taken
            ...(action.timeTaken && {
              timeTaken: action.timeTaken
            })
          }
        };
      }
      return {
        ...state,
        translationInfo: {
          ...state.translationInfo,
          ...data
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].allowedMetaFields:
      // Action to save the allowed meta fields
      // Update the state with the new allowed meta fields
      return {
        ...state,
        allowedMetaFields: {
          ...state.allowedMetaFields,
          [action.id]: {
            ...(state.allowedMetaFields[action.id] || []),
            inputType: action.inputType,
            status: action.status
          }
        }
      };
    case _types_js__WEBPACK_IMPORTED_MODULE_0__["default"].contentFetchStatus:
      // Action to save the content fetch status
      // Update the state with the new content fetch status
      return {
        ...state,
        contentFetchStatus: action.status
      };
    default:
      // If the action type does not match any case
      return state;
    // Return the current state unchanged
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (reducer); // Exporting the reducer as the default export

/***/ }),

/***/ "./modules/page-translation/src/global-store/selectors.js":
/*!****************************************************************!*\
  !*** ./modules/page-translation/src/global-store/selectors.js ***!
  \****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   contentFetchStatus: () => (/* binding */ contentFetchStatus),
/* harmony export */   getAllowedMetaFields: () => (/* binding */ getAllowedMetaFields),
/* harmony export */   getBlockRules: () => (/* binding */ getBlockRules),
/* harmony export */   getTranslatedString: () => (/* binding */ getTranslatedString),
/* harmony export */   getTranslationEntries: () => (/* binding */ getTranslationEntries),
/* harmony export */   getTranslationEntry: () => (/* binding */ getTranslationEntry),
/* harmony export */   getTranslationInfo: () => (/* binding */ getTranslationInfo)
/* harmony export */ });
/**
 * Retrieves the translation entries from the given state.
 *
 * This function extracts translation entries for the title, excerpt, meta fields, and content
 * from the provided state object and returns them as an array of translation entry objects.
 *
 * @param {Object} state - The state object containing translation data.
 * @param {Object} state.title - The title object containing source and target translations.
 * @param {Object} state.excerpt - The excerpt object containing source and target translations.
 * @param {Object} state.metaFields - An object containing meta field translations, where each key is a meta field ID.
 * @param {Object} state.content - An object containing content translations, where each key is a content ID.
 * @returns {Array<Object>} An array of translation entry objects, each containing the following properties:
 *   @property {string} id - The identifier of the translation entry.
 *   @property {string} source - The source text of the translation entry.
 *   @property {string} type - The type of the translation entry (e.g., 'title', 'excerpt', 'metaFields', 'content').
 *   @property {string} translatedData - The target text of the translation entry (default is an empty string if not provided).
 */
const getTranslationEntries = state => {
  // Initialize an empty array to hold translation entries
  const translateEntry = new Array();
  if (state.title && state.title.source) {
    const titleData = {
      id: 'title',
      // Identifier for the entry
      source: state.title.source,
      // Source text for the title
      type: 'title',
      // Type of the entry
      translatedData: state.title.translatedData || {} // translated text for the title, defaulting to an empty string if not provided
    };
    if (state.title && state.title.filteredString && state.title.filteredString !== '') {
      titleData.filteredString = state.title.filteredString;
    }

    // Push the title translation entry into the array
    translateEntry.push(titleData);
  }
  if (state.excerpt && state.excerpt.source) {
    const excerptData = {
      id: 'excerpt',
      // Identifier for the entry
      source: state.excerpt.source,
      // Source text for the excerpt
      type: 'excerpt',
      // Type of the entry
      translatedData: state.excerpt.translatedData || {} // translated text for the excerpt, defaulting to an empty string if not provided
    };
    if (state.excerpt && state.excerpt.filteredString && state.excerpt.filteredString !== '') {
      excerptData.filteredString = state.excerpt.filteredString;
    }

    // Push the excerpt translation entry into the array
    translateEntry.push(excerptData);
  }
  if (state.slug && state.slug.source) {
    const slugData = {
      id: 'slug',
      // Identifier for the entry
      source: state.slug.source,
      // Source text for the slug
      type: 'slug',
      // Type of the entry
      translatedData: state.slug.translatedData || {} // translated text for the slug, defaulting to an empty string if not provided
    };
    if (state.slug && state.slug.filteredString && state.slug.filteredString !== '') {
      slugData.filteredString = state.slug.filteredString;
    }

    // Push the slug translation entry into the array
    translateEntry.push(slugData);
  }

  // Iterate over the metaFields object keys and push each translation entry into the array
  Object.keys(state.metaFields).map(key => {
    const metaFieldsData = {
      type: 'metaFields',
      // Type of the entry
      id: key,
      // Identifier for the meta field
      source: state.metaFields[key].source,
      // Source text for the meta field
      translatedData: state.metaFields[key].translatedData || {} // translated text for the meta field, defaulting to an empty string if not provided
    };
    if (state.metaFields && state.metaFields[key] && state.metaFields[key].filteredString && state.metaFields[key].filteredString !== '') {
      metaFieldsData.filteredString = state.metaFields[key].filteredString;
    }
    translateEntry.push(metaFieldsData);
  });

  // Iterate over the content object keys and push each translation entry into the array
  Object.keys(state.content).map(key => {
    const contentData = {
      type: 'content',
      // Type of the entry
      id: key,
      // Identifier for the content
      source: state.content[key].source,
      // Source text for the content
      translatedData: state.content[key].translatedData || {} // translated text for the content, defaulting to an empty string if not provided
    };
    if (state.content && state.content[key] && state.content[key].filteredString && state.content[key].filteredString !== '') {
      contentData.filteredString = state.content[key].filteredString;
    }
    translateEntry.push(contentData);
  });

  // Return the array of translation entries
  return translateEntry;
};
const getTranslationEntry = (state, data) => {
  if (['title', 'excerpt', 'slug'].includes(data.type)) {
    return state[data.type] || false;
  }
  if (data.type && data.id && state[data.type] && state[data.type][data.id]) {
    return state[data.type][data.id] || false;
  }
  return false;
};

/**
 * Retrieves the block rules from the given state.
 * @param {Object} state - The state object containing translation data.
 * @returns {Object} The block rules data.
 */
const getBlockRules = state => {
  return state.blockRules;
};

/**
 * Retrieves the translated string from the given state.
 *
 * This function extracts the translated string for a given type (title, excerpt, metaFields, or content)
 * from the provided state object and returns it.
 *
 * @param {Object} state - The state object containing translation data.
 * @param {string} type - The type of the translation entry (e.g., 'title', 'excerpt', 'metaFields', 'content').
 * @param {string} source - The source text of the translation entry.
 * @param {string} id - The identifier of the translation entry (optional, used for metaFields and content).
 * @param {string} provider - The provider of the translation (optional, used for metaFields and content).
 * @returns {string} The translated string for the given type and source, or the original source text if no translation is found.
 */
const getTranslatedString = (state, type, source, id = null, provider = null) => {
  // Check if the type is 'title' or 'excerpt' and if the source matches
  if (['title', 'excerpt'].includes(type) && state[type].source === source && state[type].translatedData && state[type].translatedData[provider]) {
    return state[type]?.translatedData[provider] || state[type]?.source; // Return the translatedData if it matches
  } else if (type === 'slug' && state.slug.source === source && state.slug.translatedData && state.slug.translatedData[provider]) {
    return undefined !== state.slug?.translatedData[provider] ? state.slug?.translatedData[provider] : state.slug?.source; // Return the translatedData if it matches
  }
  // Check if the type is 'metaFields' and if the source matches
  else if (type === 'metaFields' && state.metaFields && state.metaFields[id] && state.metaFields[id].source === source && state.metaFields[id].translatedData && state.metaFields[id].translatedData[provider]) {
    // Return the target text if it exists, otherwise return the source text
    return undefined !== state.metaFields[id]?.translatedData[provider] ? state.metaFields[id]?.translatedData[provider] : state.metaFields[id]?.source;
  }
  // Check if the type is 'content' and if the source matches
  else if (type === 'content' && state.content && state.content[id] && state.content[id].source === source && state.content[id].translatedData && state.content[id].translatedData[provider]) {
    // Return the target text if it exists, otherwise return the source text
    return undefined !== state.content[id]?.translatedData[provider] ? state.content[id]?.translatedData[provider] : state.content[id]?.source;
  }
  // If no matches, return the original source text
  return source;
};

/**
 * Retrieves the translation info from the given state.
 * @param {Object} state - The state object containing translation data.
 * @returns {Object} The translation info.
 */
const getTranslationInfo = state => {
  return {
    sourceStringCount: state?.translationInfo?.sourceStringCount || 0,
    sourceWordCount: state?.translationInfo?.sourceWordCount || 0,
    sourceCharacterCount: state?.translationInfo?.sourceCharacterCount || 0,
    translateData: state?.translationInfo?.translateData || {}
  };
};

/** 
 * Retrieves the allowed meta fields from the given state.
 * @param {Object} state - The state object containing translation data.
 * @returns {Object} The allowed meta fields.
 */
const getAllowedMetaFields = state => {
  return state.allowedMetaFields || {};
};
const contentFetchStatus = state => {
  return state.contentFetchStatus;
};

/***/ }),

/***/ "./modules/page-translation/src/global-store/types.js":
/*!************************************************************!*\
  !*** ./modules/page-translation/src/global-store/types.js ***!
  \************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/**
 * LmatActionTypes is an object that defines the action types used in the 
 * automatic translation feature of the application. Each property in this 
 * object corresponds to a specific action that can be dispatched to the 
 * global store, allowing the application to manage the state related to 
 * source and translated content effectively.
 */
const LmatActionTypes = {
  // Action type for saving the title of the source content
  sourceTitle: 'SAVE_SOURCE_TITLE',
  // Action type for saving the title of the translated content
  traslatedTitle: 'SAVE_TRANSLATE_TITLE',
  // Action type for saving the filtered title
  filteredTitleString: 'SAVE_FILTERED_TITLE',
  // Action type for saving the excerpt of the source content
  sourceExcerpt: 'SAVE_SOURCE_EXCERPT',
  // Action type for saving the excerpt of the translated content
  traslatedExcerpt: 'SAVE_TRANSLATE_EXCERPT',
  // Action type for saving the filtered excerpt
  filteredExcerptString: 'SAVE_FILTERED_EXCERPT',
  // Action type for saving the main content of the source
  sourceContent: 'SAVE_SOURCE_CONTENT',
  // Action type for saving the main content of the translated content
  traslatedContent: 'SAVE_TRANSLATE_CONTENT',
  // Action type for saving the filtered content
  filteredContentString: 'SAVE_FILTERED_CONTENT',
  // Action type for saving the meta fields of the source content
  sourceMetaFields: 'SAVE_SOURCE_META_FIELDS',
  // Action type for saving the meta fields of the translated content
  traslatedMetaFields: 'SAVE_TRANSLATE_META_FIELDS',
  // Action type for saving the filtered meta fields
  filteredMetaFieldsString: 'SAVE_FILTERED_META_FIELDS',
  // Action type for saving the block rules
  setBlockRules: 'SET_BLOCK_RULES',
  // Action type for saving the translatio info of the translated content
  translationInfo: 'SAVE_TRANSLATE_INFO',
  // Action type for saving the allowed meta fields
  allowedMetaFields: 'ALLOWED_META_FIELDS',
  // Action type for saving the slug of the source content
  sourceSlug: 'SAVE_SOURCE_SLUG',
  // Action type for saving the slug of the translated content
  traslatedSlug: 'SAVE_TRANSLATE_SLUG',
  // Action type for saving the filtered slug
  filteredSlugString: 'SAVE_FILTERED_SLUG',
  // Action type for saving the content fetch status
  contentFetchStatus: 'CONTENT_FETCH_STATUS'
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (LmatActionTypes);

/***/ }),

/***/ "./modules/page-translation/src/helper/index.js":
/*!******************************************************!*\
  !*** ./modules/page-translation/src/helper/index.js ***!
  \******************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   mergeFetchAbortSignals: () => (/* binding */ mergeFetchAbortSignals),
/* harmony export */   updateTranslateData: () => (/* binding */ updateTranslateData)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");


/**
 * Combine a modal/session abort signal with a per-request AbortController so either can cancel the fetch.
 *
 * @param {AbortSignal|undefined|null} parentSignal
 * @param {AbortController|undefined|null} childController
 * @returns {AbortSignal|undefined}
 */
function mergeFetchAbortSignals(parentSignal, childController) {
  if (!childController) {
    return parentSignal || undefined;
  }
  const childSig = childController.signal;
  if (!parentSignal) {
    return childSig;
  }
  if (typeof AbortSignal.any === 'function') {
    try {
      return AbortSignal.any([parentSignal, childSig]);
    } catch (e) {
      // fall through
    }
  }
  if (parentSignal.aborted) {
    try {
      childController.abort();
    } catch (e) {
      /* noop */
    }
    return childSig;
  }
  const onParentAbort = () => {
    try {
      childController.abort();
    } catch (e) {
      /* noop */
    }
  };
  parentSignal.addEventListener('abort', onParentAbort, {
    once: true
  });
  const onChildDone = () => {
    parentSignal.removeEventListener('abort', onParentAbort);
  };
  childSig.addEventListener('abort', onChildDone, {
    once: true
  });
  return childSig;
}
const updateTranslateData = ({
  provider,
  sourceLang,
  targetLang,
  postId
}) => {
  const translateData = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.select)('block-lmatPageTranslation/translate').getTranslationInfo();
  const totalStringCount = translateData.translateData?.[provider]?.targetStringCount || 0;
  const totalWordCount = translateData.translateData?.[provider]?.targetWordCount || 0;
  const totalCharacterCount = translateData.translateData?.[provider]?.targetCharacterCount || 0;
  const timeTaken = translateData.translateData?.[provider]?.timeTaken || 0;
  const sourceWordCount = translateData?.sourceWordCount || 0;
  const sourceCharacterCount = translateData?.sourceCharacterCount || 0;
  const sourceStringCount = translateData?.sourceStringCount || 0;
  const editorType = lmatPageTranslationGlobal.editor_type;
  const date = new Date().toISOString();
  const data = {
    provider,
    totalStringCount,
    totalWordCount,
    totalCharacterCount,
    editorType,
    date,
    sourceStringCount,
    sourceWordCount,
    sourceCharacterCount,
    sourceLang,
    targetLang,
    timeTaken,
    action: lmatPageTranslationGlobal.update_translate_data,
    update_translation_key: lmatPageTranslationGlobal.update_translation_check,
    post_id: postId
  };
  fetch(lmatPageTranslationGlobal.ajax_url, {
    method: 'POST',
    headers: {
      'content-type': 'application/x-www-form-urlencoded; charset=UTF-8',
      'Accept': 'application/json'
    },
    body: new URLSearchParams(data)
  }).then().catch(error => {
    console.error(error);
  });
};

/***/ }),

/***/ "./modules/page-translation/src/index.scss":
/*!*************************************************!*\
  !*** ./modules/page-translation/src/index.scss ***!
  \*************************************************/
/***/ ((__unused_webpack_module, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
// extracted by mini-css-extract-plugin


/***/ }),

/***/ "./modules/page-translation/src/popup-setting-modal/body.js":
/*!******************************************************************!*\
  !*** ./modules/page-translation/src/popup-setting-modal/body.js ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _providers_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./providers.js */ "./modules/page-translation/src/popup-setting-modal/providers.js");
/* harmony import */ var _component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../component/translate-provider/index.js */ "./modules/page-translation/src/component/translate-provider/index.js");
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }



const SettingModalBody = props => {
  const ServiceProviders = (0,_component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_2__["default"])();
  return /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-setting-modal-body"
  }, /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-setting-modal-notice-wrapper"
  }), /*#__PURE__*/React.createElement("table", null, /*#__PURE__*/React.createElement("thead", null, /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("th", null, "Name"), /*#__PURE__*/React.createElement("th", null, "Translate"), /*#__PURE__*/React.createElement("th", null, "Docs"))), /*#__PURE__*/React.createElement("tbody", null, Object.keys(ServiceProviders).map(provider => /*#__PURE__*/React.createElement(_providers_js__WEBPACK_IMPORTED_MODULE_1__["default"], _extends({
    key: provider
  }, props, {
    Service: provider
  }))))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SettingModalBody);

/***/ }),

/***/ "./modules/page-translation/src/popup-setting-modal/footer.js":
/*!********************************************************************!*\
  !*** ./modules/page-translation/src/popup-setting-modal/footer.js ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");

const SettingModalFooter = props => {
  const {
    targetLangName,
    postType,
    sourceLangName,
    setSettingVisibility
  } = props;
  return /*#__PURE__*/React.createElement("div", {
    className: "modal-footer"
  }, /*#__PURE__*/React.createElement("p", {
    className: "lmat-page-translation-error-message",
    style: {
      marginBottom: '.5rem'
    }
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("This will replace your current %(postType)s with a %(target)s translation of the original %(source)s content.", 'translate-words'), {
    postType: postType,
    source: sourceLangName,
    target: targetLangName
  })), /*#__PURE__*/React.createElement("button", {
    className: "lmat-page-translation-setting-close button button-primary",
    onClick: () => setSettingVisibility(false)
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Close", 'translate-words')));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SettingModalFooter);

/***/ }),

/***/ "./modules/page-translation/src/popup-setting-modal/header.js":
/*!********************************************************************!*\
  !*** ./modules/page-translation/src/popup-setting-modal/header.js ***!
  \********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");

const SettingModalHeader = ({
  setSettingVisibility
}) => {
  return /*#__PURE__*/React.createElement("div", {
    className: "modal-header"
  }, /*#__PURE__*/React.createElement("h2", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Step 1 - Select Translation Provider", 'translate-words')), /*#__PURE__*/React.createElement("span", {
    className: "close",
    onClick: () => setSettingVisibility(false)
  }, "\xD7"));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SettingModalHeader);

/***/ }),

/***/ "./modules/page-translation/src/popup-setting-modal/index.js":
/*!*******************************************************************!*\
  !*** ./modules/page-translation/src/popup-setting-modal/index.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react_dom_client__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react-dom/client */ "./node_modules/react-dom/client.js");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _popup_string_modal_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../popup-string-modal/index.js */ "./modules/page-translation/src/popup-string-modal/index.js");
/* harmony import */ var _component_translate_provider_google_google_language_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../component/translate-provider/google/google-language.js */ "./modules/page-translation/src/component/translate-provider/google/google-language.js");
/* harmony import */ var _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ../component/translate-provider/local-ai-translator/local-ai-translator.js */ "./modules/page-translation/src/component/translate-provider/local-ai-translator/local-ai-translator.js");
/* harmony import */ var _header_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./header.js */ "./modules/page-translation/src/popup-setting-modal/header.js");
/* harmony import */ var _body_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./body.js */ "./modules/page-translation/src/popup-setting-modal/body.js");
/* harmony import */ var _footer_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./footer.js */ "./modules/page-translation/src/popup-setting-modal/footer.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _component_error_modal_box_index_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../component/error-modal-box/index.js */ "./modules/page-translation/src/component/error-modal-box/index.js");
/* harmony import */ var _component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ../component/translate-provider/index.js */ "./modules/page-translation/src/component/translate-provider/index.js");
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }











const SettingModal = props => {
  const [activeService, setActiveService] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)({});
  const [modalRender, setModalRender] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(0);
  const [settingVisibility, setSettingVisibility] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const sourceLang = lmatPageTranslationGlobal.source_lang;
  const targetLang = props.targetLang;
  const sourceLangName = lmatPageTranslationGlobal.languageObject[sourceLang]['name'];
  const targetLangName = lmatPageTranslationGlobal.languageObject[targetLang]['name'];
  const imgFolder = lmatPageTranslationGlobal.lmat_url + 'admin/assets/images/';
  const googleSupport = (0,_component_translate_provider_google_google_language_js__WEBPACK_IMPORTED_MODULE_3__["default"])().includes((0,_component_translate_provider_google_google_language_js__WEBPACK_IMPORTED_MODULE_3__.mapToGoogleLanguageCode)(targetLang, lmatPageTranslationGlobal.languageObject || {}));
  const [serviceModalErrors, setServiceModalErrors] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)({});
  const [errorModalVisibility, setErrorModalVisibility] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const [chromeAiBtnDisabled, setChromeAiBtnDisabled] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const [edgeAiBtnDisabled, setEdgeAiBtnDisabled] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(false);
  const [chromeAiDownload, setChromeAiDownload] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(null);
  const [edgeAiDownload, setEdgeAiDownload] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useState)(null);
  const providers = lmatPageTranslationGlobal.providers;
  const openModalOnLoadHandler = e => {
    e.preventDefault();
    const btnElement = e.target;
    const visibility = btnElement.dataset.value;
    if (visibility === 'yes') {
      setSettingVisibility(true);
    }
    btnElement.closest('#lmat-page-translation-modal-open-warning-wrapper').remove();
  };
  const closeErrorModal = () => {
    setErrorModalVisibility(false);
  };
  const openErrorModalHandler = service => {
    setSettingVisibility(false);
    setErrorModalVisibility(service);
  };
  const openModelHandler = activeService => {
    if (serviceModalErrors && serviceModalErrors[activeService]) {
      openErrorModalHandler(activeService);
    } else {
      setActiveService(activeService);
      setModalRender(prev => prev + 1);
    }
  };
  const handleMetaFieldBtnClick = async e => {
    e.preventDefault();
    if (providers.length > 1) {
      // Perform language check upfront when modal is opened with multiple providers
      const errors = await providerErrors();
      if (errors && (errors.chromeAiDownloadPending || errors.edgeAiDownloadPending)) {
        setSettingVisibility(true);
        return;
      }

      // Show provider selection modal - errors will be shown as error messages on provider buttons
      setSettingVisibility(prev => !prev);
    } else if (providers.length < 1) {
      openErrorModalHandler('providerNotConfigured');
    } else {
      // Single provider case
      const errors = await providerErrors();
      const providerId = providers[0];

      // Check for download pending
      if (errors && (errors.chromeAiDownloadPending || errors.edgeAiDownloadPending)) {
        setSettingVisibility(true);
        return;
      }

      // Check for language support errors - open the provider modal to show error on provider button
      if (errors && errors[providerId]) {
        // Don't open error modal, let the provider modal show the error inline
        setSettingVisibility(true);
        return;
      }

      // All good, open the provider modal
      openModelHandler(providerId);
    }
  };

  /**
   * useEffect hook to set settingVisibility.
   * Triggers the setSettingVisibility only when user click on meta field Button.
  */
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    const firstRenderBtns = document.querySelectorAll('#lmat-page-translation-modal-open-warning-wrapper .modal-content div[data-value]');
    const metaFieldBtn = document.querySelector(props.translateWrpSelector);
    if (metaFieldBtn) {
      metaFieldBtn.removeEventListener('click', handleMetaFieldBtnClick);
      metaFieldBtn.addEventListener('click', handleMetaFieldBtnClick);
    }
    firstRenderBtns.forEach(ele => {
      if (ele) {
        ele.addEventListener('click', openModalOnLoadHandler);
      }
    });
    return () => {
      metaFieldBtn.removeEventListener('click', handleMetaFieldBtnClick);
    };
  }, [serviceModalErrors]);
  const providerErrors = async () => {
    let errors = {};
    const localAiSupportStatus = async () => {
      const localAiTranslatorSupport = await _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_4__["default"].languageSupportedStatus(sourceLang, targetLang, targetLangName, sourceLangName, false);
      if (localAiTranslatorSupport !== true && typeof localAiTranslatorSupport === 'object') {
        if (localAiTranslatorSupport.hasClass && localAiTranslatorSupport.hasClass('atlt-chromeai-download-pending')) {
          const isDownloading = localAiTranslatorSupport.hasClass('atlt-chromeai-downloading');
          setChromeAiDownload({
            status: isDownloading ? 'downloading' : 'downloadable',
            progress: 0,
            source: localAiTranslatorSupport.attr('data-source'),
            target: localAiTranslatorSupport.attr('data-target'),
            sourceLabel: localAiTranslatorSupport.attr('data-source-label'),
            targetLabel: localAiTranslatorSupport.attr('data-target-label')
          });
          errors.chromeAiDownloadPending = true;
        } else {
          setChromeAiBtnDisabled(true);
          errors.localAiTranslator = {
            message: localAiTranslatorSupport,
            Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Chrome AI Translator", 'translate-words')
          };
          setServiceModalErrors(prev => ({
            ...prev,
            localAiTranslator: errors.localAiTranslator
          }));
        }
      } else {
        setChromeAiBtnDisabled(false);
        setChromeAiDownload(null);
        setServiceModalErrors(prev => {
          const next = {
            ...prev
          };
          delete next.localAiTranslator;
          return next;
        });
      }
    };
    const edgeLocalAiSupportStatus = async () => {
      const edgeLocalAiTranslatorSupport = await _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_4__["default"].languageSupportedStatus(sourceLang, targetLang, targetLangName, sourceLangName, true);
      if (edgeLocalAiTranslatorSupport !== true && typeof edgeLocalAiTranslatorSupport === 'object') {
        if (edgeLocalAiTranslatorSupport.hasClass && edgeLocalAiTranslatorSupport.hasClass('atlt-chromeai-download-pending')) {
          const isDownloading = edgeLocalAiTranslatorSupport.hasClass('atlt-chromeai-downloading');
          setEdgeAiDownload({
            status: isDownloading ? 'downloading' : 'downloadable',
            progress: 0,
            source: edgeLocalAiTranslatorSupport.attr('data-source'),
            target: edgeLocalAiTranslatorSupport.attr('data-target'),
            sourceLabel: edgeLocalAiTranslatorSupport.attr('data-source-label'),
            targetLabel: edgeLocalAiTranslatorSupport.attr('data-target-label')
          });
          errors.edgeAiDownloadPending = true;
        } else {
          setEdgeAiBtnDisabled(true);
          errors.edgeLocalAiTranslator = {
            message: edgeLocalAiTranslatorSupport,
            Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Edge AI Translator", 'translate-words')
          };
          setServiceModalErrors(prev => ({
            ...prev,
            edgeLocalAiTranslator: errors.edgeLocalAiTranslator
          }));
        }
      } else {
        setEdgeAiBtnDisabled(false);
        setEdgeAiDownload(null);
        setServiceModalErrors(prev => {
          const next = {
            ...prev
          };
          delete next.edgeLocalAiTranslator;
          return next;
        });
      }
    };
    const googleSupportStatus = async () => {
      if (!googleSupport) {
        errors.google = {
          message: "<p style={{ fontSize: '1rem', color: '#ff4646' }}>" + (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Google Translate does not support the target language: %s.", 'translate-words'), "<strong>" + targetLangName + "</strong>") + "</p>",
          Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Google Translate", 'translate-words')
        };
        setServiceModalErrors(prev => ({
          ...prev,
          google: errors.google
        }));
      }
    };
    if (providers.includes('localAiTranslator')) {
      await localAiSupportStatus();
    }
    if (providers.includes('edgeLocalAiTranslator')) {
      await edgeLocalAiSupportStatus();
    }
    if (providers.includes('google')) {
      await googleSupportStatus();
    }
    return errors;
  };

  /**
   * useEffect hook to check if the local AI translator is supported.
   */
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    if (providers.length < 1) {
      let providerConfigMsg = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)('%sYou have not enabled any translation provider. Please enable at least one service provider to use automatic translation. Go to the %sTranslation Settings%s to configure a translation provider.%s', 'translate-words'), '<p>', `<strong><a href='${lmatPageTranslationGlobal.admin_url}admin.php?page=lmat_settings&tab=translation' target='_blank' rel='noopener noreferrer'>`, '</a></strong>', '</p>');
      setServiceModalErrors(prev => ({
        ...prev,
        providerNotConfigured: {
          message: providerConfigMsg,
          Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Translation Provider Not Configured", 'translate-words')
        }
      }));
      return;
    }
    if (settingVisibility) {
      providerErrors();
    }
  }, [settingVisibility]);

  /**
   * useEffect hook to handle displaying the modal and rendering the PopStringModal component.
   */
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_1__.useEffect)(() => {
    const activeServiceObject = (0,_component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_10__["default"])({
      Service: activeService,
      [activeService + "ButtonDisabled"]: false
    });
    const service = activeService;
    const serviceLabel = activeServiceObject && activeServiceObject.serviceLabel;
    const postId = props.postId;
    const parentWrp = document.getElementById("lmat_page_translation_strings_model");
    if (parentWrp) {
      // Store root instance in a ref to avoid recreating it
      if (!parentWrp._reactRoot) {
        parentWrp._reactRoot = react_dom_client__WEBPACK_IMPORTED_MODULE_0__.createRoot(parentWrp);
      }
      if (modalRender) {
        parentWrp._reactRoot.render(/*#__PURE__*/React.createElement(_popup_string_modal_index_js__WEBPACK_IMPORTED_MODULE_2__["default"], {
          currentPostId: props.currentPostId,
          postId: postId,
          service: service,
          serviceLabel: serviceLabel,
          sourceLang: sourceLang,
          targetLang: targetLang,
          modalRender: modalRender,
          pageTranslate: props.pageTranslate,
          postDataFetchStatus: props.postDataFetchStatus,
          fetchPostData: props.fetchPostData,
          translatePost: props.translatePost,
          contentLoading: props.contentLoading,
          updatePostDataFetch: props.updatePostDataFetch,
          stringModalBodyNotice: props.stringModalBodyNotice
        }));
      }
    }
  }, [props.postDataFetchStatus, modalRender]);

  /**
   * Function to handle fetching content based on the target button clicked.
   * Sets the target button and updates the fetch status to true.
   * @param {Event} e - The event object representing the button click.
   */
  const fetchContent = async e => {
    let targetElement = !e.target.classList.contains('lmat-page-translation-service-btn') ? e.target.closest('.lmat-page-translation-service-btn') : e.target;
    if (!targetElement) {
      return;
    }
    const dataService = targetElement.dataset && targetElement.dataset.service;
    const isEdge = dataService === 'edgeLocalAiTranslator';
    if (dataService === 'localAiTranslator') {
      const localAiTranslatorSupport = await _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_4__["default"].languageSupportedStatus(sourceLang, targetLang, targetLangName, sourceLangName, false);
      if (localAiTranslatorSupport !== true && typeof localAiTranslatorSupport === 'object') {
        if (localAiTranslatorSupport.hasClass && localAiTranslatorSupport.hasClass('atlt-chromeai-download-pending')) {
          const isDownloading = localAiTranslatorSupport.hasClass('atlt-chromeai-downloading');
          setChromeAiDownload({
            status: isDownloading ? 'downloading' : 'downloadable',
            progress: 0,
            source: localAiTranslatorSupport.attr('data-source'),
            target: localAiTranslatorSupport.attr('data-target'),
            sourceLabel: localAiTranslatorSupport.attr('data-source-label'),
            targetLabel: localAiTranslatorSupport.attr('data-target-label')
          });
        } else {
          setErrorModalVisibility('localAiTranslator');
          setServiceModalErrors(prev => ({
            ...prev,
            localAiTranslator: {
              message: localAiTranslatorSupport[0]?.outerHTML || localAiTranslatorSupport,
              Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Chrome AI Translator", 'translate-words')
            }
          }));
        }
        return;
      }
    }
    if (dataService === 'edgeLocalAiTranslator') {
      const edgeLocalAiTranslatorSupport = await _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_4__["default"].languageSupportedStatus(sourceLang, targetLang, targetLangName, sourceLangName, true);
      if (edgeLocalAiTranslatorSupport !== true && typeof edgeLocalAiTranslatorSupport === 'object') {
        if (edgeLocalAiTranslatorSupport.hasClass && edgeLocalAiTranslatorSupport.hasClass('atlt-chromeai-download-pending')) {
          const isDownloading = edgeLocalAiTranslatorSupport.hasClass('atlt-chromeai-downloading');
          setEdgeAiDownload({
            status: isDownloading ? 'downloading' : 'downloadable',
            progress: 0,
            source: edgeLocalAiTranslatorSupport.attr('data-source'),
            target: edgeLocalAiTranslatorSupport.attr('data-target'),
            sourceLabel: edgeLocalAiTranslatorSupport.attr('data-source-label'),
            targetLabel: edgeLocalAiTranslatorSupport.attr('data-target-label')
          });
        } else {
          setErrorModalVisibility('edgeLocalAiTranslator');
          setServiceModalErrors(prev => ({
            ...prev,
            edgeLocalAiTranslator: {
              message: edgeLocalAiTranslatorSupport[0]?.outerHTML || edgeLocalAiTranslatorSupport,
              Title: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_8__.__)("Edge AI Translator", 'translate-words')
            }
          }));
        }
        return;
      }
    }
    setSettingVisibility(false);
    setModalRender(prev => prev + 1);
    setActiveService(dataService);
  };
  const handleSettingVisibility = visibility => {
    setSettingVisibility(visibility);
  };
  return /*#__PURE__*/React.createElement(React.Fragment, null, errorModalVisibility && serviceModalErrors[errorModalVisibility] && /*#__PURE__*/React.createElement(_component_error_modal_box_index_js__WEBPACK_IMPORTED_MODULE_9__["default"], _extends({
    onClose: closeErrorModal
  }, serviceModalErrors[errorModalVisibility])), settingVisibility && (providers.length > 1 || chromeAiDownload || edgeAiDownload) && /*#__PURE__*/React.createElement("div", {
    className: "modal-container",
    style: {
      display: settingVisibility ? 'flex' : 'none'
    }
  }, /*#__PURE__*/React.createElement("div", {
    className: "lmat-page-translation-settings modal-content"
  }, /*#__PURE__*/React.createElement(_header_js__WEBPACK_IMPORTED_MODULE_5__["default"], {
    setSettingVisibility: handleSettingVisibility,
    postType: props.postType,
    sourceLangName: sourceLangName,
    targetLangName: targetLangName
  }), /*#__PURE__*/React.createElement(_body_js__WEBPACK_IMPORTED_MODULE_6__["default"], {
    googleDisabled: !googleSupport,
    fetchContent: fetchContent,
    imgFolder: imgFolder,
    targetLangName: targetLangName,
    postType: props.postType,
    sourceLangName: sourceLangName,
    localAiTranslatorDisabled: chromeAiBtnDisabled,
    edgeLocalAiTranslatorDisabled: edgeAiBtnDisabled,
    openErrorModalHandler: openErrorModalHandler,
    chromeAiDownload: chromeAiDownload,
    setChromeAiDownload: setChromeAiDownload,
    edgeAiDownload: edgeAiDownload,
    setEdgeAiDownload: setEdgeAiDownload
  }), /*#__PURE__*/React.createElement(_footer_js__WEBPACK_IMPORTED_MODULE_7__["default"], {
    targetLangName: targetLangName,
    postType: props.postType,
    sourceLangName: sourceLangName,
    setSettingVisibility: handleSettingVisibility
  }))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (SettingModal);

/***/ }),

/***/ "./modules/page-translation/src/popup-setting-modal/providers.js":
/*!***********************************************************************!*\
  !*** ./modules/page-translation/src/popup-setting-modal/providers.js ***!
  \***********************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../component/translate-provider/index.js */ "./modules/page-translation/src/component/translate-provider/index.js");
/* harmony import */ var _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../component/translate-provider/local-ai-translator/local-ai-translator.js */ "./modules/page-translation/src/component/translate-provider/local-ai-translator/local-ai-translator.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");




const Providers = props => {
  const service = props.Service;
  const buttonDisable = props[service + "Disabled"];
  const ActiveService = (0,_component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])({
    Service: service,
    [service + "ButtonDisabled"]: buttonDisable,
    openErrorModalHandler: props.openErrorModalHandler
  });
  if (!ActiveService) {
    return null;
  }
  const serviceId = service.replace(/([A-Z])/g, '-$1').toLowerCase().replace(/[^a-z0-9-]/g, '');
  const btnId = `lmat-page-translation-${serviceId}-btn`;
  const downloadState = service === 'localAiTranslator' ? props.chromeAiDownload : service === 'edgeLocalAiTranslator' ? props.edgeAiDownload : null;
  const setDownloadState = service === 'localAiTranslator' ? props.setChromeAiDownload : service === 'edgeLocalAiTranslator' ? props.setEdgeAiDownload : null;
  const handleDownload = async e => {
    if (e) {
      e.preventDefault();
      e.stopPropagation();
    }
    if (!downloadState || downloadState.status === 'downloading') return;
    setDownloadState(prev => ({
      ...prev,
      status: 'downloading',
      progress: 0
    }));
    try {
      const result = await _component_translate_provider_local_ai_translator_local_ai_translator_js__WEBPACK_IMPORTED_MODULE_1__["default"].startLanguagePackDownload(downloadState.source, downloadState.target, evt => {
        const pct = Math.round((evt && evt.loaded ? evt.loaded : 0) * 100);
        setDownloadState(prev => prev ? {
          ...prev,
          progress: pct
        } : null);
      });
      if (result && result.ok) {
        // Download successful - reset download state so button shows "Translate"
        setDownloadState(null);
      } else {
        setDownloadState(prev => prev ? {
          ...prev,
          status: 'failed'
        } : null);
      }
    } catch (err) {
      console.error("Language pack download failed:", err);
      setDownloadState(prev => prev ? {
        ...prev,
        status: 'failed'
      } : null);
    }
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_3__.useEffect)(() => {
    if (downloadState && downloadState.status === 'downloadable') {
      handleDownload();
    }
  }, [downloadState]);
  const renderDownloadButton = () => {
    if (downloadState.status === 'downloading') {
      return /*#__PURE__*/React.createElement("div", {
        className: "lmat-page-translation-service-btn button button-primary disabled lmat-page-translation-btn-downloading",
        style: {
          pointerEvents: 'none',
          opacity: 0.85,
          display: 'inline-flex',
          alignItems: 'center',
          gap: '8px'
        }
      }, /*#__PURE__*/React.createElement("span", {
        className: "lmat-loading-spinner-mini",
        style: {
          width: '12px',
          height: '12px',
          border: '2px solid rgba(255,255,255,0.3)',
          borderTop: '2px solid #fff',
          borderRadius: '50%',
          display: 'inline-block'
        }
      }), (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)('Downloading…', 'translate-words'), " ", downloadState.progress, "%");
    }
    const btnText = downloadState.status === 'failed' ? (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)('Retry Download', 'translate-words') : `${(0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)('Download', 'translate-words')} ${props.targetLangName}`;
    return /*#__PURE__*/React.createElement("div", {
      onClick: handleDownload,
      className: "lmat-page-translation-service-btn button button-primary lmat-page-translation-btn-downloadable",
      style: {
        cursor: 'pointer'
      }
    }, btnText);
  };
  return /*#__PURE__*/React.createElement("tr", null, /*#__PURE__*/React.createElement("td", {
    className: "lmat-page-translation-provider-name"
  }, ActiveService.Logo, /*#__PURE__*/React.createElement("span", null, ActiveService.title)), /*#__PURE__*/React.createElement("td", null, downloadState ? renderDownloadButton() : ActiveService.ButtonDisabled ? /*#__PURE__*/React.createElement("div", {
    onClick: () => props.openErrorModalHandler(service),
    style: {
      cursor: 'pointer'
    }
  }, ActiveService.ErrorMessage) : /*#__PURE__*/React.createElement("div", {
    id: btnId,
    onClick: props.fetchContent,
    className: "lmat-page-translation-service-btn button button-primary",
    "data-service": service,
    "data-service-label": ActiveService.serviceLabel
  }, ActiveService.SettingBtnText)), /*#__PURE__*/React.createElement("td", null, /*#__PURE__*/React.createElement("a", {
    href: ActiveService.Docs,
    target: "_blank",
    rel: "noopener noreferrer",
    className: "lmat-page-translation-doc-icon"
  }, /*#__PURE__*/React.createElement("svg", {
    width: "9",
    height: "12",
    viewBox: "0 0 9 12",
    fill: "none",
    xmlns: "http://www.w3.org/2000/svg"
  }, /*#__PURE__*/React.createElement("path", {
    d: "M2.17607 6.20533H6.82393V5.53867H2.17607V6.20533ZM2.17607 8.05133H6.82393V7.38467H2.17607V8.05133ZM2.17607 9.898H4.89536V9.23133H2.17607V9.898ZM1.03821 12C0.7425 12 0.495643 11.8973 0.297643 11.692C0.0996427 11.4867 0.000428571 11.2304 0 10.9233V1.07667C0 0.77 0.0992142 0.514 0.297643 0.308667C0.496071 0.103333 0.743143 0.000444444 1.03886 0H6.10714L9 3V10.9233C9 11.23 8.901 11.4862 8.703 11.692C8.505 11.8978 8.25771 12.0004 7.96114 12H1.03821ZM5.78571 3.33333V0.666667H1.03886C0.939857 0.666667 0.849 0.709333 0.766286 0.794666C0.683571 0.88 0.642429 0.974 0.642857 1.07667V10.9233C0.642857 11.0256 0.684 11.1196 0.766286 11.2053C0.848571 11.2911 0.939214 11.3338 1.03821 11.3333H7.96179C8.06036 11.3333 8.151 11.2907 8.23371 11.2053C8.31643 11.12 8.35757 11.0258 8.35714 10.9227V3.33333H5.78571Z",
    fill: "#6F6F6F"
  })))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (Providers);

/***/ }),

/***/ "./modules/page-translation/src/popup-string-modal/body.js":
/*!*****************************************************************!*\
  !*** ./modules/page-translation/src/popup-string-modal/body.js ***!
  \*****************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var _component_filter_target_content_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../component/filter-target-content/index.js */ "./modules/page-translation/src/component/filter-target-content/index.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ../component/translate-provider/index.js */ "./modules/page-translation/src/component/translate-provider/index.js");
/* harmony import */ var react_dom__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! react-dom */ "react-dom");
/* harmony import */ var _component_RightPopup_index_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ../component/RightPopup/index.js */ "./modules/page-translation/src/component/RightPopup/index.js");
/* harmony import */ var _component_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! ../component/store-translated-string/index.js */ "./modules/page-translation/src/component/store-translated-string/index.js");
/* harmony import */ var _component_glossary_count_index_js__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! ../component/glossary-count/index.js */ "./modules/page-translation/src/component/glossary-count/index.js");
/* harmony import */ var dompurify__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! dompurify */ "./node_modules/dompurify/dist/purify.es.mjs");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ../helper/index.js */ "./modules/page-translation/src/helper/index.js");












const StringPopUpBody = props => {
  const {
    service: service
  } = props;
  const translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_3__.select)("block-lmatPageTranslation/translate").getTranslationEntries();
  const StringModalBodyNotice = props.stringModalBodyNotice;
  const imgFolder = lmatPageTranslationGlobal.lmat_url + 'assets/images/';

  // Add refs and state for popup
  const tableRef = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [popupInfo, setPopupInfo] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [activePopupType, setActivePopupType] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [glossaryPosition, setGlossaryPosition] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)({
    left: 0,
    top: 0
  });
  const [addGlossaryPosition, setAddGlossaryPosition] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)({
    left: 0,
    top: 0
  });
  const [editingCells, setEditingCells] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)([]);
  const [editingValues, setEditingValues] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)({});
  const textareaRefs = (0,react__WEBPACK_IMPORTED_MODULE_0__.useRef)({});
  const [glossaryTerms, setGlossaryTerms] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)([]);
  const [glossaryOrignalTerms, setGlossaryOrignalTerms] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)([]);
  const [selectedSourceText, setSelectedSourceText] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)('');
  const [activePopupCell, setActivePopupCell] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [savedValues, setSavedValues] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)({});
  const [translationError, setTranslationError] = (0,react__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const showGlossary = activePopupType === 'glossary';
  const showAddGlossary = activePopupType === 'add-glossary';
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const onTranslationError = e => {
      const d = e?.detail;
      if (d?.recoverable && typeof d.onTranslateAgain === "function" && typeof d.onContinue === "function") {
        setTranslationError({
          recoverable: true,
          message: typeof d.messagePlain === "string" ? d.messagePlain : "",
          html: typeof d.html === "string" ? d.html : "",
          onTranslateAgain: d.onTranslateAgain,
          onContinue: d.onContinue
        });
        return;
      }
      const msg = d?.message;
      if (typeof msg === "string" && msg.trim() !== "") {
        setTranslationError({
          message: msg,
          link: d?.link || null
        });
      }
    };
    const onTranslationErrorClear = () => setTranslationError(null);
    document.addEventListener('lmat-page-translation:translation-error', onTranslationError);
    document.addEventListener('lmat-page-translation:translation-error-clear', onTranslationErrorClear);
    return () => {
      document.removeEventListener('lmat-page-translation:translation-error', onTranslationError);
      document.removeEventListener('lmat-page-translation:translation-error-clear', onTranslationErrorClear);
    };
  }, []);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setTranslationError(null);
  }, [props.modalRender]);

  /** While a non-recoverable error is shown, disable provider translate buttons. */
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!translationError || translationError.recoverable) {
      return undefined;
    }
    const root = document.getElementById(`lmat_page_translation_${props.service}_translate_element`);
    if (!root) {
      return undefined;
    }
    const disableButtons = () => {
      root.querySelectorAll('button').forEach(el => {
        el.disabled = true;
      });
    };
    disableButtons();
    const observer = new MutationObserver(() => disableButtons());
    observer.observe(root, {
      childList: true,
      subtree: true
    });
    return () => {
      observer.disconnect();
      root.querySelectorAll('button').forEach(el => {
        el.disabled = false;
      });
    };
  }, [translationError, props.service]);
  const saveFilteredString = (type, id, filteredString) => {
    const action = `${type}SaveFiltered`;
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_3__.dispatch)('block-lmatPageTranslation/translate')[action](filteredString, id);
  };

  // Helper function for glossary positioning (assuming fixed/viewport relative positioning for GlossaryPopup)
  const calculateGlossaryPosition = (anchorElementRect, isTdSource = false) => {
    const stringContainer = document.querySelector('.lmat_page_translation_string_container');

    // Default to positioning below the anchor if stringContainer is not found
    if (!stringContainer) {
      return {
        right: anchorElementRect.right - (isTdSource ? 4 : 0),
        top: anchorElementRect.bottom + 4
      };
    }
    const stringContainerRect = stringContainer.getBoundingClientRect();
    const position = {
      right: anchorElementRect.right - (isTdSource ? 4 : 0)
    };
    const spaceBelowInContainer = stringContainerRect.bottom - anchorElementRect.bottom;
    const spaceAboveInContainer = anchorElementRect.top - stringContainerRect.top;

    // Estimate popup height (approximately 200px for glossary popup)
    const estimatedPopupHeight = 200;

    // Check if there's enough space below the textarea
    if (spaceBelowInContainer < estimatedPopupHeight) {
      // Check if there's enough space above the textarea
      if (spaceAboveInContainer >= estimatedPopupHeight) {
        // Position above the textarea
        position.bottom = window.innerHeight - anchorElementRect.top + 4;
      } else {
        // If not enough space above either, position at the top of the container
        position.top = stringContainerRect.top + window.scrollY;
        position.right = position.right - 30;
      }
    } else {
      // Position below the textarea
      position.top = anchorElementRect.bottom + 4;
    }
    return position;
  };

  // Fetch glossary data on initial render
  const calculateAddGlossaryPosition = (anchorElementRect, isTdSource = false) => {
    const stringContainer = document.querySelector('.lmat_page_translation_string_container');
    const position = {
      right: anchorElementRect.right - (isTdSource ? 4 : 0)
    };
    if (!stringContainer) {
      return position;
    }
    const stringContainerRect = stringContainer.getBoundingClientRect();
    const spaceBelowInContainer = stringContainerRect.bottom - anchorElementRect.bottom;
    if (spaceBelowInContainer < 200) {
      position.right = position.right - 30;
    }
    return position;
  };
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const ac = new AbortController();
    const fetchSignal = (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_11__.mergeFetchAbortSignals)(props.translationAbortSignal, ac);
    async function fetchGlossary() {
      try {
        const data = {
          action: 'lmat_get_glossary',
          source_lang: props.sourceLang,
          target_lang: props.targetLang,
          _wpnonce: lmatPageTranslationGlobal.get_glossary_validate
        };
        // Add sourceLang and targetLang as query params
        const url = `${lmatPageTranslationGlobal.ajax_url}`;
        const response = await fetch(url, {
          method: 'POST',
          headers: {
            'Content-Type': 'application/x-www-form-urlencoded; charset=UTF-8',
            'Accept': 'application/json'
          },
          credentials: 'same-origin',
          body: new URLSearchParams(data),
          signal: fetchSignal
        });
        const responseData = await response.json();
        const glossaryOrignalTerms = [];
        responseData.data.terms.forEach(term => {
          if (term.original_term && term.original_term !== '') {
            glossaryOrignalTerms.push(term.original_term);
          }
        });
        setGlossaryOrignalTerms(glossaryOrignalTerms);
        setGlossaryTerms(responseData.data.terms || []);
      } catch (err) {
        if (err && err.name === 'AbortError') {
          return;
        }
        setGlossaryTerms([]); // fallback or show error
      }
    }
    fetchGlossary();
    return () => {
      ac.abort();
    };
  }, [props.sourceLang, props.targetLang, props.translationAbortSignal]);

  // Update handleTdClick to check for AI service
  const handleTdClick = (event, colIndex, rowIndex, data) => {
    const translationEntry = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_3__.select)("block-lmatPageTranslation/translate").getTranslationEntry({
      id: data.id,
      type: data.type
    });
    if (colIndex !== 2 || !translationEntry) return;
    const cellKey = `${rowIndex}_${colIndex}`;
    let translation = '';
    if (translationEntry.translatedData && translationEntry.translatedData[service]) {
      translation = translationEntry.translatedData[service];
    } else if (translationEntry.translatedData && translationEntry.translatedData[props.service]) {
      translation = translationEntry.translatedData[props.service];
    } else if (translationEntry.filteredString) {
      translation = translationEntry.filteredString;
    } else {
      translation = translationEntry.source || '';
    }

    // Only set editing state if not already editing this cell
    const isAlreadyEditing = editingCells.some(cell => cell.row === rowIndex && cell.col === colIndex);
    if (!isAlreadyEditing) {
      setEditingCells([{
        row: rowIndex,
        col: colIndex
      }]);
      setEditingValues({
        [cellKey]: translation
      });
    }
    setSelectedSourceText(data.source || '');
    setActivePopupCell({
      row: rowIndex,
      col: colIndex
    });
    if (!tableRef.current) return;
    const tdRect = event.currentTarget.getBoundingClientRect();
    const ths = tableRef.current.querySelectorAll("thead th");
    const thText = ths[colIndex] ? ths[colIndex].textContent : '';
    const stringContainer = document.querySelector('.lmat_page_translation_string_container');
    const stringContainerRect = stringContainer.getBoundingClientRect();
    const difference = stringContainerRect.bottom - tdRect.bottom;
    const differencetop = tdRect.top - stringContainerRect.top;

    // Calculate popup position relative to the clicked cell
    const newPopupInfo = {
      left: tdRect.right + window.scrollX,
      top: tdRect.top + window.scrollY - 35,
      value: data,
      header: thText
    };

    // Adjust position if near bottom of container
    if (difference < 50) {
      newPopupInfo.top = tdRect.top + window.scrollY - 130;
      newPopupInfo.bottom = 50;
    }
    // Adjust position if near top of container
    if (differencetop < 35) {
      newPopupInfo.top = stringContainerRect.top + window.scrollY;
      newPopupInfo.bottom = 35;
    }
    setPopupInfo(newPopupInfo);
    if (showGlossary) {
      const glossaryPos = calculateGlossaryPosition(tdRect, true);
      setGlossaryPosition(glossaryPos);
    }
  };

  // Update closePopup to clear all editing states
  const closePopup = () => {
    setEditingCells([]);
    setEditingValues({});
    setPopupInfo(null);
    setActivePopupCell(null);
  };
  // Helper to get matched glossary terms
  const getMatchedGlossaryTerms = sourceText => {
    if (!sourceText) return [];

    // Create a temporary div to parse HTML and get text content
    const temp = document.createElement('div');
    temp.innerHTML = sourceText;
    const textContent = temp.textContent;

    // Normalize text (lowercase + remove unnecessary punctuation)
    const normalizeText = text => {
      return text.toLowerCase().replace(/[.,/#!$%^&*;{}=\-_`~()]/g, match => {
        // Preserve colon but remove other punctuation
        return match === ':' ? ':' : '';
      }).trim();
    };

    // Escape special characters for safe regex creation
    const escapeRegex = str => str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
    const normalizedSourceText = normalizeText(textContent);
    return glossaryTerms.filter(entry => {
      if (!entry.original_term) return false;
      const glossaryTerm = entry.original_term.trim();
      const normalizedGlossaryTerm = normalizeText(glossaryTerm);
      const safeTerm = escapeRegex(normalizedGlossaryTerm);

      // Create regex to match whole words only
      const regex = new RegExp(`\\b${safeTerm}\\b`, 'i');

      // Check if it matches as a separate word
      const isMatch = regex.test(normalizedSourceText);
      if (isMatch && glossaryTerm.endsWith(':')) {
        // Ensure colon match is properly followed by space or end
        const matchIndex = normalizedSourceText.indexOf(normalizedGlossaryTerm);
        const afterMatch = normalizedSourceText[matchIndex + normalizedGlossaryTerm.length];
        return !afterMatch || afterMatch === ' ';
      }
      return isMatch;
    }).map(entry => {
      return {
        english: entry.original_term,
        translation: entry.translations?.[props.targetLang] || '',
        description: entry.description || ''
      };
    });
  };
  const matchedGlossaryTerms = getMatchedGlossaryTerms(selectedSourceText);

  // Update handleGlossaryClick to check for AI service
  const handleGlossaryClick = () => {
    if (matchedGlossaryTerms.length === 0) return;

    // Toggle glossary popup, close if already open
    setActivePopupType(prev => prev === 'glossary' ? null : 'glossary');
    if (activePopupCell) {
      const cellKey = `${activePopupCell.row}_${activePopupCell.col}`;
      const textarea = textareaRefs.current[cellKey];
      if (textarea) {
        const textareaRect = textarea.getBoundingClientRect();
        const newPos = calculateGlossaryPosition(textareaRect, true);
        setGlossaryPosition(newPos);
      }
    }
  };
  const handleAddGlossaryClick = () => {
    // Toggle add glossary popup - close if already open, open if closed
    setActivePopupType(prev => prev === 'add-glossary' ? null : 'add-glossary');

    // Only update position if we're opening the popup
    if (activePopupType !== 'add-glossary' && activePopupCell) {
      const cellKey = `${activePopupCell.row}_${activePopupCell.col}`;
      const textarea = textareaRefs.current[cellKey];
      if (textarea) {
        const textareaRect = textarea.getBoundingClientRect();
        const newPos = calculateAddGlossaryPosition(textareaRect, true);
        setAddGlossaryPosition(newPos);
      }
    }
  };

  // Update handleTextareaInput to use dynamic positioning
  const handleTextareaInput = (e, row, col) => {
    e.target.style.height = 'auto';
    e.target.style.height = e.target.scrollHeight + 'px';

    // Recalculate positions for both glossary and add glossary popups when textarea size changes
    if (activePopupCell && activePopupCell.row === row && activePopupCell.col === col) {
      const textareaRect = e.target.getBoundingClientRect();
      if (showGlossary) {
        const newGlossaryPos = calculateGlossaryPosition(textareaRect, true);
        setGlossaryPosition(newGlossaryPos);
      }
      if (showAddGlossary) {
        const newAddGlossaryPos = calculateAddGlossaryPosition(textareaRect, true);
        setAddGlossaryPosition(newAddGlossaryPos);
      }
    }
  };
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!popupInfo) {
      setActivePopupType(null);
      setEditingCells([]);
    }
  }, [popupInfo]);
  (0,react__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (['yandex', 'google'].includes(props.service)) {
      document.documentElement.setAttribute('translate', 'no');
      document.body.classList.add('notranslate');
    }

    /**
     * Calls the translate service provider based on the service type.
     * For example, it can call services like yandex Translate.
    */
    const service = props.service;
    const id = `lmat_page_translation_${service}_translate_element`;
    const translateContent = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_3__.select)('block-lmatPageTranslation/translate').getTranslationEntries();
    if (translateContent.length > 0 && props.postDataFetchStatus) {
      const ServiceSetting = (0,_component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_5__["default"])({
        Service: service
      });
      const run = ServiceSetting.Provider({
        sourceLang: props.sourceLang,
        targetLang: props.targetLang,
        translateStatusHandler: props.translateStatusHandler,
        ID: id,
        translateStatus: props.translateStatus,
        modalRenderId: props.modalRender,
        destroyUpdateHandler: props.updateDestroyHandler,
        translationAbortSignal: props.translationAbortSignal,
        service: service
      });
      if (run && typeof run.then === "function") {
        run.catch(() => {});
      }
    }
  }, [props.modalRender, props.postDataFetchStatus]);
  const udpateFilterdContent = div => {
    const tempElement = document.createElement('div');
    tempElement.innerHTML = div.innerHTML;
    const childNodes = tempElement.childNodes;
    if (childNodes.length > 0) {
      for (let i = 0; i < childNodes.length; i++) {
        const childNode = childNodes[i];
        if (childNode.nodeType === 3) {
          continue;
        } else {
          const childNodes = childNode.childNodes;
          if (childNodes.length > 0) {
            if (!childNode.classList.contains('lmat-page-translation-notraslate-tag')) {
              const textContent = udpateFilterdContent(childNode);
              childNode.outerHTML = textContent;
            } else {
              const textContent = udpateFilterdContent(childNode);
              childNode.innerHTML = textContent;
            }
          }
        }
      }
    }
    return tempElement.innerHTML;
  };
  const insertOrReplaceInContentEditable = (div, glossaryText) => {
    const selection = window.getSelection();
    if (!selection || selection.rangeCount === 0) return;
    const range = selection.getRangeAt(0);

    // Make sure the selection is inside this div
    if (!div.contains(range.startContainer)) return;
    const selectedString = range.toString();

    // Get all starting spaces (single or multiple) and ending spaces (single or multiple)
    const startingWhiteSpace = selectedString.match(/^\s+/)?.[0] || "";
    const endingWhiteSpace = selectedString.match(/\s+$/)?.[0] || "";

    // Delete selected content (if any)
    range.deleteContents();

    // Prepare HTML node
    const temp = document.createElement("div");
    temp.innerHTML = `<span class="notranslate lmat-page-translation-notraslate-tag" translate="no">${startingWhiteSpace}${glossaryText}${endingWhiteSpace}</span>`;
    const node = temp.firstChild;

    // Insert the span
    range.insertNode(node);

    // Move caret after the inserted node
    range.setStartAfter(node);
    range.collapse(true);
    selection.removeAllRanges();
    selection.addRange(range);
    const filteredContent = udpateFilterdContent(div);
    const td = div.closest('td');
    if ('' !== filteredContent && td) {
      const type = td.dataset.stringType;
      const id = td.dataset.key;
      saveFilteredString(type, id, filteredContent);
    }
  };

  // Update handleInsertGlossaryTerm to accept a cell argument
  const handleInsertGlossaryTerm = (term, cell) => {
    if (!cell) return;
    const cellKey = `${cell.row}_${cell.col}`;
    const textarea = textareaRefs.current[cellKey];
    if (textarea) {
      const insertText = term.translation || term.english || '';
      insertOrReplaceInContentEditable(textarea, insertText);
    } else {
      setEditingValues({
        ...editingValues,
        [cellKey]: (editingValues[cellKey] || '') + (term.translation || term.english || '')
      });
    }
  };

  // Add this helper function at the top level of your component
  const getTranslationProviders = service => {
    if (service) {
      return [service];
    }
  };

  // Save handler for InfoPopup
  const handleSave = () => {
    const cell = activePopupCell;
    if (cell) {
      const cellKey = `${cell.row}_${cell.col}`;
      const newValue = textareaRefs?.current[cellKey]?.innerText || '';
      const rowData = translateContent[cell.row];
      if (newValue && newValue.trim() !== '') {
        // Save non-empty values
        setSavedValues(prev => ({
          ...prev,
          [cellKey]: newValue
        }));
        if (rowData?.id) {
          const providers = getTranslationProviders(service);

          // Save translation for each provider
          providers.forEach(provider => {
            (0,_component_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_8__["default"])({
              type: rowData.type || 'content',
              key: rowData.id,
              translateContent: newValue,
              source: rowData.source,
              provider: provider,
              AllowedMetaFields: props.AllowedMetaFields || {}
            });
          });
        }
      } else {
        // For empty values, clear both local state and call SaveTranslation with empty string
        setSavedValues(prev => {
          const newSavedValues = {
            ...prev
          };
          delete newSavedValues[cellKey];
          return newSavedValues;
        });
        if (rowData?.id) {
          const providers = getTranslationProviders(service);

          // Save translation for each provider
          providers.forEach(provider => {
            (0,_component_store_translated_string_index_js__WEBPACK_IMPORTED_MODULE_8__["default"])({
              type: rowData.type || 'content',
              key: rowData.id,
              translateContent: '',
              // Save empty string to clear the translation
              source: rowData.source,
              provider: provider,
              AllowedMetaFields: props.AllowedMetaFields || {}
            });
          });
        }
      }

      // Remove the current cell from editingCells so it renders as a <td>
      setEditingCells(prev => prev.filter(c => !(c.row === cell.row && c.col === cell.col)));

      // Move to next row if exists
      const nextRow = cell.row + 1;
      if (nextRow < translateContent.length) {
        const nextRowData = translateContent[nextRow];
        if (nextRowData?.source?.trim()) {
          const nextCellKey = `${nextRow}_${cell.col}`;
          let initialNextTranslation;
          if (savedValues[nextCellKey] !== undefined) {
            initialNextTranslation = savedValues[nextCellKey];
          } else {
            let trans = '';
            if (nextRowData.translatedData) {
              if (service && nextRowData.translatedData[service]) {
                trans = nextRowData.translatedData[service];
              } else if (nextRowData.translatedData[props.service]) {
                trans = nextRowData.translatedData[props.service];
              }
            }
            initialNextTranslation = trans || nextRowData.source || '';
          }
          setEditingCells(prev => [...prev, {
            row: nextRow,
            col: cell.col
          }]);
          setEditingValues(prev => ({
            ...prev,
            [nextCellKey]: initialNextTranslation
          }));
          setSelectedSourceText(nextRowData.source || '');
          setActivePopupCell({
            row: nextRow,
            col: cell.col
          });

          // Update popup position for next row
          setTimeout(() => {
            const table = tableRef.current;
            const nextTd = table?.querySelector(`tbody tr:nth-child(${nextRow + 1}) td:nth-child(${cell.col + 1})`);
            if (nextTd) {
              const tdRect = nextTd.getBoundingClientRect();
              setPopupInfo({
                left: tdRect.right + window.scrollX,
                top: tdRect.top + window.scrollY,
                value: nextRowData,
                header: table.querySelectorAll("thead th")[cell.col]?.textContent || ''
              });
            }
          }, 0);
          return;
        }
      }
      setPopupInfo(null);
    }
  };

  // Add this helper function to get language name
  const getLanguageName = langCode => {
    const languages = window.lmatPageTranslationGlobal?.languageObject || {};
    const language = languages[langCode];
    // If language is an object, extract the name, otherwise use the langCode
    const result = typeof language === 'object' ? language.name || langCode : language || langCode;
    return result;
  };
  const getTranslation = data => {
    let originalTranslation = false;
    if (data.translatedData) {
      if (service && data.translatedData[service]) {
        originalTranslation = data.translatedData[service];
      } else if (data.translatedData[props.service]) {
        originalTranslation = data.translatedData[props.service];
      }
    }
    return originalTranslation;
  };

  // Update the cell display logic in the render section
  const getCellContent = (index, data, cellKey) => {
    const savedEdit = savedValues[cellKey];
    const translation = getTranslation(data);
    if (savedEdit !== undefined) {
      return savedEdit;
    } else if (translation) {
      return translation;
    } else {
      if (['google', 'yandex', 'localAiTranslator', 'edgeLocalAiTranslator', 'gemini'].includes(props.service)) {
        // Use FilterTargetContent for pending translations with supported services
        if (props.translatePendingStatus && !props.service.includes('_ai')) {
          if (data.filteredString) {
            return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
              dangerouslySetInnerHTML: {
                __html: dompurify__WEBPACK_IMPORTED_MODULE_10__["default"].sanitize(data.filteredString)
              },
              style: {
                whiteSpace: 'pre-wrap'
              }
            });
          }
          return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_component_filter_target_content_index_js__WEBPACK_IMPORTED_MODULE_1__["default"], {
            service: props.service,
            content: data.source || '',
            contentKey: data.id,
            item: data,
            saveFilteredString: saveFilteredString
          });
        }
        return data.source || '';
      } else {
        return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("img", {
          src: imgFolder + 'plus.png',
          alt: (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Add translation", "translate-words"),
          className: "lmat-page-translation-add-translation-icon"
        });
      }
    }
  };
  return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "modal-body"
  }, translateContent.length > 0 && props.postDataFetchStatus ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, StringModalBodyNotice && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-body-notice-wrapper"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(StringModalBodyNotice, null)), props.translatePendingStatus && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_translate_progress",
    key: props.modalRender,
    role: "status",
    "aria-live": "polite"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Automatic translation is in progress", 'translate-words'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat_page_translation_translate_progress__dots",
    "aria-hidden": "true"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, "."), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, "."), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, ".")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("br", null), (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("It will take few minutes, enjoy ☕ coffee in this time!", 'translate-words'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("br", null), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("br", null), (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Please do not leave this window or browser tab while translation is in progress", 'translate-words'), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "lmat_page_translation_translate_progress__dots",
    "aria-hidden": "true"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, "."), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, "."), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", null, "."))), translationError && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_error_popup_overlay",
    role: "alertdialog",
    "aria-modal": "true",
    "aria-labelledby": "lmat_page_translation_error_popup_title"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_error_popup_card notice notice-error"
  }, translationError.recoverable && translationError.html ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_error_popup_html",
    dangerouslySetInnerHTML: {
      __html: dompurify__WEBPACK_IMPORTED_MODULE_10__["default"].sanitize(translationError.html, {
        ADD_ATTR: ["target", "rel", "data-clipboard-text"]
      })
    }
  }) : /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("p", {
    id: "lmat_page_translation_error_popup_title",
    className: "lmat_page_translation_error_popup_message"
  }, translationError?.message || "", translationError?.link?.href && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, " ", /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("a", {
    href: translationError.link.href,
    target: "_blank",
    rel: "noreferrer"
  }, translationError?.link?.text || translationError.link.href))), translationError.recoverable && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_ai_error_buttons"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    type: "button",
    className: "button lmat_page_translation_ai_error_btn_translate",
    onClick: () => {
      if (typeof translationError.onTranslateAgain === "function") {
        translationError.onTranslateAgain();
      }
    }
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Translate", "translate-words")), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("button", {
    type: "button",
    className: "button button-primary lmat_page_translation_ai_error_btn_continue",
    onClick: () => {
      if (typeof translationError.onContinue === "function") {
        translationError.onContinue();
      }
    }
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Continue", "translate-words"))))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: `translator-widget ${service}`,
    style: {
      display: 'flex',
      alignItems: 'center'
    }
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("h3", {
    className: "choose-lang"
  }, (0,_component_translate_provider_index_js__WEBPACK_IMPORTED_MODULE_5__["default"])({
    Service: props.service
  }).heading, " ", /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("span", {
    className: "dashicons-before dashicons-translation"
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: `lmat_page_translation_translate_element_wrapper ${props.translateStatus ? 'translate-completed' : ''}`
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    id: `lmat_page_translation_${props.service}_translate_element`
  }))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_string_section"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat_page_translation_string_container"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("table", {
    className: "scrolldown",
    id: "stringTemplate",
    ref: tableRef
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("thead", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("S.No", 'translate-words')), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Source Text", 'translate-words')), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Translation", 'translate-words')))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tbody", null, props.postDataFetchStatus && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, translateContent.map((data, index) => {
    const cellKey = `${index}_2`;
    let originalTranslation = '';
    if (data.translatedData) {
      if (service && data.translatedData[service]) {
        originalTranslation = data.translatedData[service];
      } else if (data.translatedData[props.service]) {
        originalTranslation = data.translatedData[props.service];
      }
    }
    const isEditingThisCell = editingCells.some(cell => cell.row === index && cell.col === 2);
    return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_wordpress_element__WEBPACK_IMPORTED_MODULE_4__.Fragment, {
      key: index + props.translatePendingStatus
    }, undefined !== data.source && data.source.trim() !== '' && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(react__WEBPACK_IMPORTED_MODULE_0__.Fragment, null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", {
      key: index + 'tr' + props.translatePendingStatus + (data.filteredString ? 'filteredString' : '')
    }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", null, index + 1, !getTranslation(data) && !isEditingThisCell && /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_component_glossary_count_index_js__WEBPACK_IMPORTED_MODULE_9__["default"], {
      string: data.source,
      glossary: glossaryOrignalTerms,
      onClick: e => {
        handleTdClick(e, 2, index, data);
      }
    })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", {
      "data-source": "source_text"
    }, data.source), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", {
      "data-key": data.id,
      "data-string-type": data.type,
      onClick: e => {
        handleTdClick(e, 2, index, data);
      },
      style: {
        cursor: 'pointer'
      },
      translate: savedValues[cellKey] && props.service === 'google' || savedValues[cellKey] && props.service === 'yandex' ? "no" : props.translatePendingStatus && ['google', 'yandex', 'localAiTranslator', 'edgeLocalAiTranslator'].includes(props.service) && !props.service.includes('_ai') ? "yes" : 'yes',
      className: `${!isEditingThisCell && !savedValues[cellKey] && !originalTranslation ? 'lmat-page-translation-empty-translation-cell' : ''} ${savedValues[cellKey] && (props.service === 'localAiTranslator' || props.service === 'edgeLocalAiTranslator') || savedValues[cellKey] && props.service === 'google' ? 'notranslate' : props.translatePendingStatus && props.service === 'gemini' ? 'notranslate' : props.translatePendingStatus && ['google', 'yandex', 'localAiTranslator', 'edgeLocalAiTranslator'].includes(props.service) && !props.service.includes('_ai') ? 'translate' : 'translate'} ${isEditingThisCell ? 'lmat-page-translation-editing-cell' : ''}`,
      "data-translate-status": props.translatePendingStatus ? 'pending' : 'translated'
    }, isEditingThisCell ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      contentEditable: "true",
      className: "lmat-page-translation-content-editable",
      ref: el => {
        textareaRefs.current[cellKey] = el;
        if (el) {
          el.style.height = 'auto';
          el.style.height = el.scrollHeight + 'px';
        }
      },
      onFocus: () => setActivePopupCell({
        row: index,
        col: 2
      }),
      onInput: e => handleTextareaInput(e, index, 2),
      style: {
        width: '100%',
        minHeight: 40,
        resize: 'vertical'
      },
      dangerouslySetInnerHTML: {
        __html: dompurify__WEBPACK_IMPORTED_MODULE_10__["default"].sanitize(editingValues[cellKey] || '')
      }
    }) : getCellContent(index, data, cellKey)))));
  }))))))) : props.postDataFetchStatus ? /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)('No strings are available for translation', 'translate-words')) : /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-skeleton-loader-wrapper"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "translate-widget"
  }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-skeleton-loader-mini"
  }), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
    className: "lmat-page-translation-skeleton-loader-mini"
  })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("table", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("thead", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("S.No", 'translate-words')), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Source Text", 'translate-words')), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("th", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_2__.__)("Translation", 'translate-words')))), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tbody", null, [...Array(10)].map((_, index) => {
    return /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("tr", {
      key: index
    }, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      className: "lmat-page-translation-skeleton-loader-mini"
    })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      className: "lmat-page-translation-skeleton-loader-mini"
    })), /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("td", null, /*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement("div", {
      className: "lmat-page-translation-skeleton-loader-mini"
    })));
  })))), popupInfo && /*#__PURE__*/react_dom__WEBPACK_IMPORTED_MODULE_6__.createPortal(/*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_component_RightPopup_index_js__WEBPACK_IMPORTED_MODULE_7__.InfoPopup, {
    left: popupInfo.left,
    top: popupInfo.top,
    onClose: closePopup,
    onGlossaryClick: handleGlossaryClick,
    onAddGlossary: handleAddGlossaryClick,
    showAddGlossary: showAddGlossary,
    onCopy: () => {
      const cell = activePopupCell;
      if (cell) {
        const sourceText = translateContent[cell.row]?.source || '';
        setEditingValues(prev => ({
          ...prev,
          [`${cell.row}_${cell.col}`]: sourceText
        }));
        setTimeout(() => {
          const textarea = textareaRefs.current[`${cell.row}_${cell.col}`];
          if (textarea) textarea.focus();
        }, 0);
      }
    },
    glossaryOpen: showGlossary && matchedGlossaryTerms.length > 0,
    glossaryCount: matchedGlossaryTerms.length,
    onSave: handleSave,
    disableGlossary: matchedGlossaryTerms.length === 0
  }), document.body), showAddGlossary && activePopupCell && /*#__PURE__*/react_dom__WEBPACK_IMPORTED_MODULE_6__.createPortal(/*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_component_RightPopup_index_js__WEBPACK_IMPORTED_MODULE_7__.AddGlossaryPopup, {
    position: addGlossaryPosition,
    sourceLang: props.sourceLang,
    targetLang: props.targetLang,
    selectedSourceText: selectedSourceText,
    onClose: () => setActivePopupType(null),
    setGlossaryTerms: setGlossaryTerms,
    setGlossaryOrignalTerms: setGlossaryOrignalTerms
  }), document.body), showGlossary && matchedGlossaryTerms.length > 0 && /*#__PURE__*/react_dom__WEBPACK_IMPORTED_MODULE_6__.createPortal(/*#__PURE__*/react__WEBPACK_IMPORTED_MODULE_0__.createElement(_component_RightPopup_index_js__WEBPACK_IMPORTED_MODULE_7__.GlossaryPopup, {
    position: glossaryPosition,
    terms: matchedGlossaryTerms,
    sourceLangLabel: getLanguageName(props.sourceLang),
    targetLangLabel: getLanguageName(props.targetLang),
    onInsert: term => handleInsertGlossaryTerm(term, activePopupCell),
    width: 256
  }), document.body));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (StringPopUpBody);

/***/ }),

/***/ "./modules/page-translation/src/popup-string-modal/footer.js":
/*!*******************************************************************!*\
  !*** ./modules/page-translation/src/popup-string-modal/footer.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _notice_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./notice.js */ "./modules/page-translation/src/popup-string-modal/notice.js");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _component_format_number_count_index_js__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ../component/format-number-count/index.js */ "./modules/page-translation/src/component/format-number-count/index.js");



const StringPopUpFooter = props => {
  return /*#__PURE__*/React.createElement("div", {
    className: "modal-footer",
    key: props.modalRender
  }, !props.translatePendingStatus && !props.hasTranslateError && !props.updateContentDisabled && /*#__PURE__*/React.createElement(_notice_js__WEBPACK_IMPORTED_MODULE_0__["default"], {
    className: "lmat_page_translation_string_count"
  }, /*#__PURE__*/React.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('Wahooo! You have saved your valuable time via auto translating', 'translate-words'), " ", /*#__PURE__*/React.createElement("strong", null, /*#__PURE__*/React.createElement(_component_format_number_count_index_js__WEBPACK_IMPORTED_MODULE_2__["default"], {
    number: props.characterCount
  })), " ", (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('characters using', 'translate-words'), " ", /*#__PURE__*/React.createElement("strong", null, props.serviceLabel), ".", (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)(' Please share your feedback —', 'translate-words'), /*#__PURE__*/React.createElement("a", {
    href: "https://wordpress.org/support/plugin/translate-words/reviews/#new-post",
    target: "_blank",
    rel: "noopener",
    style: {
      color: 'yellow'
    }
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)('leave a quick review', 'translate-words')), "!")), /*#__PURE__*/React.createElement("div", {
    className: "save_btn_cont"
  }, /*#__PURE__*/React.createElement("button", {
    className: "notranslate save_it button button-primary",
    disabled: props.updateContentDisabled,
    onClick: props.updatePostData
  }, props.translateButtonStatus ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "updating-text"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Updating", 'translate-words'), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 2
    }
  }))) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_1__.__)("Update Content", 'translate-words'))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (StringPopUpFooter);

/***/ }),

/***/ "./modules/page-translation/src/popup-string-modal/header.js":
/*!*******************************************************************!*\
  !*** ./modules/page-translation/src/popup-string-modal/header.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");

const StringPopUpHeader = props => {
  /**
   * Function to close the popup modal.
   */
  const closeModal = () => {
    props.setPopupVisibility(false);
  };
  const translateService = lmatPageTranslationGlobal.providers;
  const serviceProvideLength = Object.keys(translateService).length;
  return /*#__PURE__*/React.createElement("div", {
    className: "modal-header",
    key: props.modalRender
  }, /*#__PURE__*/React.createElement("span", {
    className: "close",
    onClick: closeModal
  }, "\xD7"), /*#__PURE__*/React.createElement("h2", {
    className: "notranslate"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.sprintf)((0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("%sStart Automatic Translation Process", 'translate-words'), serviceProvideLength > 1 ? 'Step 2 - ' : '')), /*#__PURE__*/React.createElement("div", {
    className: "save_btn_cont"
  }, /*#__PURE__*/React.createElement("button", {
    className: "notranslate save_it button button-primary",
    disabled: props.updateContentDisabled,
    onClick: props.updatePostData
  }, props.translateButtonStatus ? /*#__PURE__*/React.createElement(React.Fragment, null, /*#__PURE__*/React.createElement("span", {
    className: "updating-text"
  }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Updating", 'translate-words'), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 0
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 1
    }
  }), /*#__PURE__*/React.createElement("span", {
    className: "dot",
    style: {
      "--i": 2
    }
  }))) : (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_0__.__)("Update Content", 'translate-words'))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (StringPopUpHeader);

/***/ }),

/***/ "./modules/page-translation/src/popup-string-modal/index.js":
/*!******************************************************************!*\
  !*** ./modules/page-translation/src/popup-string-modal/index.js ***!
  \******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_element__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/element */ "@wordpress/element");
/* harmony import */ var _helper_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ../helper/index.js */ "./modules/page-translation/src/helper/index.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _header_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./header.js */ "./modules/page-translation/src/popup-string-modal/header.js");
/* harmony import */ var _body_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./body.js */ "./modules/page-translation/src/popup-string-modal/body.js");
/* harmony import */ var _footer_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./footer.js */ "./modules/page-translation/src/popup-string-modal/footer.js");
function _extends() { return _extends = Object.assign ? Object.assign.bind() : function (n) { for (var e = 1; e < arguments.length; e++) { var t = arguments[e]; for (var r in t) ({}).hasOwnProperty.call(t, r) && (n[r] = t[r]); } return n; }, _extends.apply(null, arguments); }






const popStringModal = props => {
  let selectedService = props.service;
  const translateData = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_2__.select)('block-lmatPageTranslation/translate').getTranslationInfo().translateData[selectedService] || false;
  const translateStatus = translateData?.translateStatus || false;
  const [popupVisibility, setPopupVisibility] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(true);
  const [refPostData, setRefPostData] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)('');
  const [translatePending, setTranslatePending] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(true);
  const [hasTranslateError, setHasTranslateError] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  /** While Gemini recoverable error is open (or until resolved), block Update Content — cleared on Continue or terminal AI outcome. */
  const [recoverableAiErrorBlocksUpdate, setRecoverableAiErrorBlocksUpdate] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const [characterCount, setCharacterCount] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(translateData?.targetCharacterCount || 0);
  const destroyHandlersRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)([]);
  const sessionAbortRef = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useRef)(null);
  const [translationAbortSignal, setTranslationAbortSignal] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(null);
  const [translateButtonStatus, setTranslateButtonStatus] = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useState)(false);
  const runModalCleanup = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(() => {
    sessionAbortRef.current?.abort();
    destroyHandlersRef.current.forEach(callback => {
      if (typeof callback === 'function') {
        try {
          callback();
        } catch (e) {
          /* noop */
        }
      }
    });
  }, []);
  const updateDestroyHandler = (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useCallback)(callback => {
    destroyHandlersRef.current.push(callback);
  }, []);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    if (!popupVisibility) {
      runModalCleanup();
    }
  }, [popupVisibility, runModalCleanup]);
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const onTranslationError = e => {
      const d = e?.detail;
      if (d?.recoverable) {
        setRecoverableAiErrorBlocksUpdate(true);
      }
      const msg = d?.message;
      if (typeof msg === "string" && msg.trim() !== "") {
        setHasTranslateError(true);
      }
    };
    const onTranslationErrorClear = () => setHasTranslateError(false);
    const onRecoverableAiResolved = () => setRecoverableAiErrorBlocksUpdate(false);
    document.addEventListener('lmat-page-translation:translation-error', onTranslationError);
    document.addEventListener('lmat-page-translation:translation-error-clear', onTranslationErrorClear);
    document.addEventListener('lmat-page-translation:recoverable-ai-error-resolved', onRecoverableAiResolved);
    return () => {
      document.removeEventListener('lmat-page-translation:translation-error', onTranslationError);
      document.removeEventListener('lmat-page-translation:translation-error-clear', onTranslationErrorClear);
      document.removeEventListener('lmat-page-translation:recoverable-ai-error-resolved', onRecoverableAiResolved);
    };
  }, []);

  /**
   * Returns the label for the service provider.
   * @returns {string} The label for the service provider.
   */
  const serviceLabel = () => {
    const serviceProvider = props.service;
    if (serviceProvider === 'localAiTranslator') {
      return 'Chrome AI Translator';
    } else {
      return serviceProvider.replace(/^\w/, c => c.toUpperCase()) + ' Translate';
    }
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    const abortController = new AbortController();
    sessionAbortRef.current = abortController;
    setTranslationAbortSignal(abortController.signal);
    destroyHandlersRef.current = [];
    if (!props.postDataFetchStatus) {
      props.fetchPostData({
        postId: props.postId,
        sourceLang: props.sourceLang,
        targetLang: props.targetLang,
        updatePostDataFetch: props.updatePostDataFetch,
        refPostData: data => setRefPostData(prev => ({
          ...prev,
          ...data
        })),
        updateDestroyHandler,
        signal: abortController.signal
      });
    }
    return () => {
      try {
        abortController.abort();
      } catch (e) {
        /* noop */
      }
      destroyHandlersRef.current.forEach(callback => {
        if (typeof callback === 'function') {
          try {
            callback();
          } catch (err) {
            /* noop */
          }
        }
      });
    };
  }, [props.modalRender]);

  /**
   * Updates the post content data.
   * @param {string} data - The data to set as the post content.
   */
  const updatePostContentHandler = data => {
    setRefPostData(data);
  };

  /**
   * Updates the fetch state.
   * @param {boolean} state - The state to update the fetch with.
   */
  const setPopupVisibilityHandler = () => {
    runModalCleanup();
    if (props.service === 'google') {
      const iframe = document.querySelector('.skiptranslate iframe[id=":1.container"]');
      if (iframe) {
        const closeButton = iframe.contentDocument.querySelector('a[id=":1.close"][title="Close"] img');
        if (closeButton) {
          closeButton.click();
        }
      }
    }
    setTranslatePending(true);
    setHasTranslateError(false);
    setRecoverableAiErrorBlocksUpdate(false);
    setPopupVisibility(false);
  };
  const translateStatusHandler = status => {
    let service = props.service;
    const characterCount = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_2__.select)('block-lmatPageTranslation/translate').getTranslationInfo().translateData[service]?.targetCharacterCount || 0;
    setCharacterCount(characterCount);
    setTranslatePending(status);
  };
  const updatePostDataHandler = () => {
    if (translateButtonStatus) {
      return;
    }
    const postContent = refPostData;
    const modalClose = () => {
      setPopupVisibilityHandler();
    };
    let service = props.service;
    setTranslateButtonStatus(true);
    setTranslatePending(true);
    setHasTranslateError(false);
    setRecoverableAiErrorBlocksUpdate(false);
    props.translatePost({
      postContent: postContent,
      modalClose: modalClose,
      service: service
    });
    props.pageTranslate(true);
    (0,_helper_index_js__WEBPACK_IMPORTED_MODULE_1__.updateTranslateData)({
      provider: service,
      sourceLang: props.sourceLang,
      targetLang: props.targetLang,
      postId: props.currentPostId
    });
  };
  (0,_wordpress_element__WEBPACK_IMPORTED_MODULE_0__.useEffect)(() => {
    setPopupVisibility(true);
    setRecoverableAiErrorBlocksUpdate(false);
    if (translateStatus) {
      setCharacterCount(translateData?.targetCharacterCount || 0);
      setTranslatePending(false);
    }
    setTimeout(() => {
      const stringModal = document.querySelector('.lmat_page_translation_string_container');
      if (stringModal) {
        stringModal.scrollTop = 0;
      }
      ;
    });
  }, [props.modalRender]);
  const updateContentDisabled = translatePending || hasTranslateError || recoverableAiErrorBlocksUpdate;
  return /*#__PURE__*/React.createElement(React.Fragment, null, " ", popupVisibility && /*#__PURE__*/React.createElement("div", {
    id: `lmat-page-translation-${props.service}-strings-modal`,
    className: "modal-container",
    style: {
      display: popupVisibility ? 'flex' : 'none'
    },
    "data-render-id": props.modalRender
  }, /*#__PURE__*/React.createElement("div", {
    className: "modal-content"
  }, /*#__PURE__*/React.createElement(_header_js__WEBPACK_IMPORTED_MODULE_3__["default"], {
    modalRender: props.modalRender,
    setPopupVisibility: setPopupVisibilityHandler,
    postContent: refPostData,
    translatePendingStatus: translatePending,
    hasTranslateError: hasTranslateError,
    updateContentDisabled: updateContentDisabled,
    pageTranslate: props.pageTranslate,
    service: props.service,
    serviceLabel: serviceLabel(),
    updatePostData: updatePostDataHandler,
    characterCount: characterCount,
    translateButtonStatus: translateButtonStatus
  }), /*#__PURE__*/React.createElement(_body_js__WEBPACK_IMPORTED_MODULE_4__["default"], _extends({}, props, {
    updatePostContent: updatePostContentHandler,
    contentLoading: props.contentLoading,
    postDataFetchStatus: props.postDataFetchStatus,
    translatePendingStatus: translatePending,
    service: props.service,
    sourceLang: props.sourceLang,
    targetLang: props.targetLang,
    translateStatusHandler: translateStatusHandler,
    modalRender: props.modalRender,
    translateStatus: translateStatus,
    stringModalBodyNotice: props.stringModalBodyNotice,
    updateDestroyHandler: updateDestroyHandler,
    translationAbortSignal: translationAbortSignal
  })), /*#__PURE__*/React.createElement(_footer_js__WEBPACK_IMPORTED_MODULE_5__["default"], {
    modalRender: props.modalRender,
    setPopupVisibility: setPopupVisibilityHandler,
    postContent: refPostData,
    translatePendingStatus: translatePending,
    hasTranslateError: hasTranslateError,
    updateContentDisabled: updateContentDisabled,
    pageTranslate: props.pageTranslate,
    service: props.service,
    serviceLabel: serviceLabel(),
    updatePostData: updatePostDataHandler,
    characterCount: characterCount,
    translateButtonStatus: translateButtonStatus
  }))));
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (popStringModal);

/***/ }),

/***/ "./modules/page-translation/src/popup-string-modal/notice.js":
/*!*******************************************************************!*\
  !*** ./modules/page-translation/src/popup-string-modal/notice.js ***!
  \*******************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
const StringPopUpNotice = props => {
  return /*#__PURE__*/React.createElement("div", {
    className: `notice inline notice-info is-dismissible ${props.className}`
  }, Array.isArray(props.children) ? props.children.join(' ') : props.children);
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (StringPopUpNotice);

/***/ }),

/***/ "./modules/page-translation/src/store-source-string/classic/index.js":
/*!***************************************************************************!*\
  !*** ./modules/page-translation/src/store-source-string/classic/index.js ***!
  \***************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const ClassicSaveSource = post_data => {
  function splitContentWithDynamicBreaks(content) {
    const result = [];
    const regex = /(\r\n|\r|\n)/g;
    let lastIndex = 0;
    let match;
    while ((match = regex.exec(content)) !== null) {
      // Push the content before the line break
      if (match.index > lastIndex) {
        result.push(content.slice(lastIndex, match.index));
      }

      // Escape line break and wrap with marker
      const escapedBreak = match[0];
      result.push(`lmat_skip_content_open_${escapedBreak}_lmat_skip_content_end`);
      lastIndex = regex.lastIndex;
    }

    // Push remaining content after the last match
    if (lastIndex < content.length) {
      result.push(content.slice(lastIndex));
    }
    return result;
  }
  const fitlerClassicContent = content => {
    const arrContent = splitContentWithDynamicBreaks(content);
    arrContent.forEach((text, index) => {
      const entity = /^&[a-zA-Z0-9#]+;$/.test(text);
      const htmlTag = /^<\/?\s*[a-zA-Z0-9#]+\s*\/?>$/.test(text);
      const isEmptyHtmlTag = /^<\s*\/?\s*[a-zA-Z0-9#]+\s*><\/\s*\/?\s*[a-zA-Z0-9#]+\s*>$/.test(text);
      const blockCommentTag = /<!--[\s\S]*?-->/g.test(text) && text.indexOf('<!--') < text.indexOf('-->');
      const plainText = !entity && !htmlTag && !isEmptyHtmlTag && !blockCommentTag;
      if (text !== '' && !text.includes('lmat_skip_content_open_') && plainText) {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentSaveSource('classic_index_' + index, text);
      }
    });
  };
  Object.keys(post_data).forEach(key => {
    if (key === 'content') {
      fitlerClassicContent(post_data[key]);
    } else if (['title', 'excerpt'].includes(key)) {
      if (post_data[key] && post_data[key].trim() !== '') {
        const action = `${key}SaveSource`;
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate')[action](post_data[key]);
      }
    } else if (key === 'slug_name' && lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
      if (post_data[key] && post_data[key].trim() !== '') {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').slugSaveSource(post_data[key]);
      }
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ClassicSaveSource);

/***/ }),

/***/ "./modules/page-translation/src/store-source-string/elementor/index.js":
/*!*****************************************************************************!*\
  !*** ./modules/page-translation/src/store-source-string/elementor/index.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");

const ElementorSaveSource = content => {
  const loopCallback = (callback, loop, index) => {
    callback(loop[index], index);
    index++;
    if (index < loop.length) {
      loopCallback(callback, loop, index);
    }
  };
  const translateContent = (ids, value) => {
    if (typeof value === 'string' && value.trim() !== '' && ids.length > 0) {
      const uniqueKey = ids.join('_lmat_page_translation_');
      if (value && '' !== value) {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentSaveSource(uniqueKey, value);
      }
    }
  };
  const subStringsToCheck = strings => {
    const dynamicSubStrings = ['title', 'description', 'editor', 'text', 'content', 'label'];
    const staticSubStrings = ['caption', 'heading', 'sub_heading', 'testimonial_content', 'testimonial_job', 'testimonial_name', 'name'];
    return dynamicSubStrings.some(substring => strings.toLowerCase().includes(substring)) || staticSubStrings.some(substring => strings === substring);
  };

  // Define a list of properties to exclude
  const cssProperties = ['content_width', 'title_size', 'font_size', 'margin', 'padding', 'background', 'border', 'color', 'text_align', 'font_weight', 'font_family', 'line_height', 'letter_spacing', 'text_transform', 'border_radius', 'box_shadow', 'opacity', 'width', 'height', 'display', 'position', 'z_index', 'visibility', 'align', 'max_width', 'content_typography_typography', 'flex_justify_content', 'title_color', 'description_color', 'email_content'];
  const storeWidgetStrings = (element, index, ids = []) => {
    const settings = element.settings;
    ids.push(index);

    // Check if settings is an object
    if (typeof settings === 'object' && settings !== null && Object.keys(settings).length > 0) {
      // Define the substrings to check for translatable content

      const keysLoop = (key, index) => {
        if (cssProperties.some(substring => key.toLowerCase().includes(substring))) {
          return; // Skip this property and continue to the next one
        }
        if (subStringsToCheck(key) && typeof settings[key] === 'string' && settings[key].trim() !== '') {
          translateContent([...ids, 'settings', key], settings[key]);
        }
        if (Array.isArray(settings[key]) && settings[key].length > 0) {
          const settingsLoop = (item, index) => {
            if (typeof item === 'object' && item !== null) {
              const settingsItemsLoop = repeaterKey => {
                if (cssProperties.includes(repeaterKey.toLowerCase())) {
                  return; // Skip this property
                }
                if (subStringsToCheck(repeaterKey) && typeof item[repeaterKey] === 'string' && item[repeaterKey].trim() !== '') {
                  translateContent([...ids, 'settings', key, index, repeaterKey], item[repeaterKey]);
                }
              };
              loopCallback(settingsItemsLoop, Object.keys(item), 0);
            }
          };
          loopCallback(settingsLoop, settings[key], 0);
        }
      };
      loopCallback(keysLoop, Object.keys(settings), 0);
    }

    // If there are nested elements, process them recursively
    if (element.elements && Array.isArray(element.elements) && element.elements.length > 0) {
      const runLoop = (childElement, index) => {
        storeWidgetStrings(childElement, index, [...ids, 'elements']);
      };
      loopCallback(runLoop, element.elements, 0);
    }
  };
  if (content.widgetsContent && content.widgetsContent.length > 0) {
    const runLoop = (element, index) => {
      storeWidgetStrings(element, index, []);
    };
    loopCallback(runLoop, content.widgetsContent, 0);
  }
  if (content.title && '' !== content.title) {
    const currentPostId = lmatPageTranslationGlobal.current_post_id;
    if (currentPostId) {
      const existingTitle = elementor?.settings?.page?.model?.get('post_title');
      if (existingTitle && '' !== existingTitle && existingTitle === `Elementor #${currentPostId}`) {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').titleSaveSource(content.title);
      }
    }
  }
  if (lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
    (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').slugSaveSource(content.slug_name);
  }
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (ElementorSaveSource);

/***/ }),

/***/ "./modules/page-translation/src/store-source-string/gutenberg/index.js":
/*!*****************************************************************************!*\
  !*** ./modules/page-translation/src/store-source-string/gutenberg/index.js ***!
  \*****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _component_filter_nested_attr_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ../../component/filter-nested-attr/index.js */ "./modules/page-translation/src/component/filter-nested-attr/index.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");



/**
 * Filters and translates attributes of a block.
 * 
 * @param {string} blockId - The ID of the block.
 * @param {Object} blockAttr - The attributes of the block.
 * @param {Object} filterAttr - The attributes to filter.
 */
const filterTranslateAttr = (blockId, blockAttr, filterAttr) => {
  const filterAttrArr = Object.values(filterAttr);

  /**
   * Saves translated attributes based on the provided ID array and filter attribute object.
   * 
   * @param {Array} idArr - The array of IDs.
   * @param {Object} filterAttrObj - The filter attribute object.
   */
  const saveTranslatedAttr = (idArr, filterAttrObj) => {
    if (true === filterAttrObj) {
      const newIdArr = new Array(...idArr);
      const childIdArr = new Array();
      let dynamicBlockAttr = blockAttr;
      let uniqueId = blockId;
      newIdArr.forEach(key => {
        childIdArr.push(key);
        uniqueId += `lmat_page_translation_${key}`;
        dynamicBlockAttr = dynamicBlockAttr ? dynamicBlockAttr[key] : dynamicBlockAttr;
      });
      let blockAttrContent = dynamicBlockAttr;
      if (blockAttrContent instanceof wp.richText.RichTextData) {
        blockAttrContent = blockAttrContent.originalHTML;
      }
      if (undefined !== blockAttrContent && typeof blockAttrContent === 'string' && blockAttrContent.trim() !== '') {
        let filterKey = uniqueId.replace(/[^\p{L}\p{N}]/gu, '');
        if (!/[\p{L}\p{N}]/gu.test(blockAttrContent)) {
          return false;
        }
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate').contentSaveSource(filterKey, blockAttrContent);
      }
      return;
    }
    (0,_component_filter_nested_attr_index_js__WEBPACK_IMPORTED_MODULE_0__["default"])(idArr, filterAttrObj, blockAttr, saveTranslatedAttr);
  };
  filterAttrArr.forEach(data => {
    Object.keys(data).forEach(key => {
      const idArr = new Array(key);
      saveTranslatedAttr(idArr, data[key]);
    });
  });
};
/**
 * Retrieves the translation string for a block based on block rules and applies translation.
 * 
 * @param {Object} block - The block to translate.
 * @param {Object} blockRules - The rules for translating the block.
 */
const getTranslateString = (block, blockRules) => {
  const blockTranslateName = Object.keys(blockRules.LmatBlockParseRules);
  if (!blockTranslateName.includes(block.name)) {
    return;
  }
  filterTranslateAttr(block.clientId, block.attributes, blockRules['LmatBlockParseRules'][block.name]);
};

/**
 * Recursively processes child block attributes for translation.
 * 
 * @param {Array} blocks - The array of blocks to translate.
 * @param {Object} blockRules - The rules for translating the blocks.
 */
const childBlockAttributesContent = (blocks, blockRules) => {
  blocks.forEach(block => {
    getTranslateString(block, blockRules);
    if (block.innerBlocks) {
      childBlockAttributesContent(block.innerBlocks, blockRules);
    }
  });
};

/**
 * Processes the attributes of a block for translation.
 * 
 * @param {Object} parseBlock - The block to parse for translation.
 * @param {Object} blockRules - The rules for translating the block.
 */
const blockAttributeContent = (parseBlock, blockRules) => {
  Object.values(parseBlock).forEach(block => {
    getTranslateString(block, blockRules);
    if (block.innerBlocks) {
      childBlockAttributesContent(block.innerBlocks, blockRules);
    }
  });
};

/**
 * Saves the translation for a block based on its attributes.
 * 
 * @param {Object} block - The block to save translation for.
 * @param {Object} blockRules - The rules for translating the block.
 */
const GutenbergBlockSaveSource = (block, blockRules) => {
  Object.keys(block).forEach(key => {
    if (key === 'content') {
      blockAttributeContent(block[key], blockRules);
    } else if (['title', 'excerpt'].includes(key)) {
      if (block[key] && block[key].trim() !== '') {
        const action = `${key}SaveSource`;
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate')[action](block[key]);
      }
    }
    if (key === 'slug_name' && lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
      if (block[key] && block[key].trim() !== '') {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_1__.dispatch)('block-lmatPageTranslation/translate').slugSaveSource(block[key]);
      }
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (GutenbergBlockSaveSource);

/***/ }),

/***/ "./modules/page-translation/src/store-source-string/wpbakery/index.js":
/*!****************************************************************************!*\
  !*** ./modules/page-translation/src/store-source-string/wpbakery/index.js ***!
  \****************************************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (__WEBPACK_DEFAULT_EXPORT__)
/* harmony export */ });
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");


/**
 * Store WPBakery source strings for translation.
 * Extracts translatable content from WPBakery shortcodes.
 * 
 * @param {Object} post_data - Post data containing WPBakery content
 */
const WPBakerySaveSource = post_data => {
  /**
   * Extract and store [lmat_val] tagged content from WPBakery shortcodes.
   * These tags are added by the PHP wpbakery.php filter during content processing.
   * 
   * @param {string} content - WPBakery content with [lmat_val] tags
   */
  const extractLmatValTags = content => {
    if (!content || content.trim() === '') {
      return;
    }

    // Pattern to match [lmat_val id="token"]content[/lmat_val]
    // The token format is: ___LMAT_{md5_hash}___
    const lmatValRegex = /\[lmat_val[^\]]*?id=["'](___LMAT_[a-f0-9]{32}___)["'][^\]]*?\](.*?)\[\/lmat_val\]/gis;
    let match;
    let index = 0;
    while ((match = lmatValRegex.exec(content)) !== null) {
      const token = match[1]; // The unique token ID
      const sourceText = match[2]; // The translatable content

      // Skip empty content
      if (!sourceText || sourceText.trim() === '') {
        continue;
      }

      // Create a unique key combining index and token for better tracking
      const uniqueKey = `wpbakery_lmat_val_${index}_${token}`;

      // Store the source string in the global store
      (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').contentSaveSource(uniqueKey, sourceText);
      index++;
    }
  };

  /**
   * Process all post data fields.
   */
  Object.keys(post_data).forEach(key => {
    if (key === 'content') {
      // Extract [lmat_val] tagged content added by PHP filters
      extractLmatValTags(post_data[key]);
    } else if (['title', 'excerpt'].includes(key)) {
      // Store title and excerpt
      if (post_data[key] && post_data[key].trim() !== '') {
        const action = `${key}SaveSource`;
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate')[action](post_data[key]);
      }
    } else if (key === 'slug_name' && lmatPageTranslationGlobal.slug_translation_option === 'slug_translate') {
      // Store slug for translation
      if (post_data[key] && post_data[key].trim() !== '') {
        (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_0__.dispatch)('block-lmatPageTranslation/translate').slugSaveSource(post_data[key]);
      }
    }
  });
};
/* harmony default export */ const __WEBPACK_DEFAULT_EXPORT__ = (WPBakerySaveSource);

/***/ }),

/***/ "./node_modules/dompurify/dist/purify.es.mjs":
/*!***************************************************!*\
  !*** ./node_modules/dompurify/dist/purify.es.mjs ***!
  \***************************************************/
/***/ ((__unused_webpack___webpack_module__, __webpack_exports__, __webpack_require__) => {

__webpack_require__.r(__webpack_exports__);
/* harmony export */ __webpack_require__.d(__webpack_exports__, {
/* harmony export */   "default": () => (/* binding */ purify)
/* harmony export */ });
/*! @license DOMPurify 3.2.6 | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/3.2.6/LICENSE */

const {
  entries,
  setPrototypeOf,
  isFrozen,
  getPrototypeOf,
  getOwnPropertyDescriptor
} = Object;
let {
  freeze,
  seal,
  create
} = Object; // eslint-disable-line import/no-mutable-exports
let {
  apply,
  construct
} = typeof Reflect !== 'undefined' && Reflect;
if (!freeze) {
  freeze = function freeze(x) {
    return x;
  };
}
if (!seal) {
  seal = function seal(x) {
    return x;
  };
}
if (!apply) {
  apply = function apply(fun, thisValue, args) {
    return fun.apply(thisValue, args);
  };
}
if (!construct) {
  construct = function construct(Func, args) {
    return new Func(...args);
  };
}
const arrayForEach = unapply(Array.prototype.forEach);
const arrayLastIndexOf = unapply(Array.prototype.lastIndexOf);
const arrayPop = unapply(Array.prototype.pop);
const arrayPush = unapply(Array.prototype.push);
const arraySplice = unapply(Array.prototype.splice);
const stringToLowerCase = unapply(String.prototype.toLowerCase);
const stringToString = unapply(String.prototype.toString);
const stringMatch = unapply(String.prototype.match);
const stringReplace = unapply(String.prototype.replace);
const stringIndexOf = unapply(String.prototype.indexOf);
const stringTrim = unapply(String.prototype.trim);
const objectHasOwnProperty = unapply(Object.prototype.hasOwnProperty);
const regExpTest = unapply(RegExp.prototype.test);
const typeErrorCreate = unconstruct(TypeError);
/**
 * Creates a new function that calls the given function with a specified thisArg and arguments.
 *
 * @param func - The function to be wrapped and called.
 * @returns A new function that calls the given function with a specified thisArg and arguments.
 */
function unapply(func) {
  return function (thisArg) {
    if (thisArg instanceof RegExp) {
      thisArg.lastIndex = 0;
    }
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }
    return apply(func, thisArg, args);
  };
}
/**
 * Creates a new function that constructs an instance of the given constructor function with the provided arguments.
 *
 * @param func - The constructor function to be wrapped and called.
 * @returns A new function that constructs an instance of the given constructor function with the provided arguments.
 */
function unconstruct(func) {
  return function () {
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    return construct(func, args);
  };
}
/**
 * Add properties to a lookup table
 *
 * @param set - The set to which elements will be added.
 * @param array - The array containing elements to be added to the set.
 * @param transformCaseFunc - An optional function to transform the case of each element before adding to the set.
 * @returns The modified set with added elements.
 */
function addToSet(set, array) {
  let transformCaseFunc = arguments.length > 2 && arguments[2] !== undefined ? arguments[2] : stringToLowerCase;
  if (setPrototypeOf) {
    // Make 'in' and truthy checks like Boolean(set.constructor)
    // independent of any properties defined on Object.prototype.
    // Prevent prototype setters from intercepting set as a this value.
    setPrototypeOf(set, null);
  }
  let l = array.length;
  while (l--) {
    let element = array[l];
    if (typeof element === 'string') {
      const lcElement = transformCaseFunc(element);
      if (lcElement !== element) {
        // Config presets (e.g. tags.js, attrs.js) are immutable.
        if (!isFrozen(array)) {
          array[l] = lcElement;
        }
        element = lcElement;
      }
    }
    set[element] = true;
  }
  return set;
}
/**
 * Clean up an array to harden against CSPP
 *
 * @param array - The array to be cleaned.
 * @returns The cleaned version of the array
 */
function cleanArray(array) {
  for (let index = 0; index < array.length; index++) {
    const isPropertyExist = objectHasOwnProperty(array, index);
    if (!isPropertyExist) {
      array[index] = null;
    }
  }
  return array;
}
/**
 * Shallow clone an object
 *
 * @param object - The object to be cloned.
 * @returns A new object that copies the original.
 */
function clone(object) {
  const newObject = create(null);
  for (const [property, value] of entries(object)) {
    const isPropertyExist = objectHasOwnProperty(object, property);
    if (isPropertyExist) {
      if (Array.isArray(value)) {
        newObject[property] = cleanArray(value);
      } else if (value && typeof value === 'object' && value.constructor === Object) {
        newObject[property] = clone(value);
      } else {
        newObject[property] = value;
      }
    }
  }
  return newObject;
}
/**
 * This method automatically checks if the prop is function or getter and behaves accordingly.
 *
 * @param object - The object to look up the getter function in its prototype chain.
 * @param prop - The property name for which to find the getter function.
 * @returns The getter function found in the prototype chain or a fallback function.
 */
function lookupGetter(object, prop) {
  while (object !== null) {
    const desc = getOwnPropertyDescriptor(object, prop);
    if (desc) {
      if (desc.get) {
        return unapply(desc.get);
      }
      if (typeof desc.value === 'function') {
        return unapply(desc.value);
      }
    }
    object = getPrototypeOf(object);
  }
  function fallbackValue() {
    return null;
  }
  return fallbackValue;
}

const html$1 = freeze(['a', 'abbr', 'acronym', 'address', 'area', 'article', 'aside', 'audio', 'b', 'bdi', 'bdo', 'big', 'blink', 'blockquote', 'body', 'br', 'button', 'canvas', 'caption', 'center', 'cite', 'code', 'col', 'colgroup', 'content', 'data', 'datalist', 'dd', 'decorator', 'del', 'details', 'dfn', 'dialog', 'dir', 'div', 'dl', 'dt', 'element', 'em', 'fieldset', 'figcaption', 'figure', 'font', 'footer', 'form', 'h1', 'h2', 'h3', 'h4', 'h5', 'h6', 'head', 'header', 'hgroup', 'hr', 'html', 'i', 'img', 'input', 'ins', 'kbd', 'label', 'legend', 'li', 'main', 'map', 'mark', 'marquee', 'menu', 'menuitem', 'meter', 'nav', 'nobr', 'ol', 'optgroup', 'option', 'output', 'p', 'picture', 'pre', 'progress', 'q', 'rp', 'rt', 'ruby', 's', 'samp', 'section', 'select', 'shadow', 'small', 'source', 'spacer', 'span', 'strike', 'strong', 'style', 'sub', 'summary', 'sup', 'table', 'tbody', 'td', 'template', 'textarea', 'tfoot', 'th', 'thead', 'time', 'tr', 'track', 'tt', 'u', 'ul', 'var', 'video', 'wbr']);
const svg$1 = freeze(['svg', 'a', 'altglyph', 'altglyphdef', 'altglyphitem', 'animatecolor', 'animatemotion', 'animatetransform', 'circle', 'clippath', 'defs', 'desc', 'ellipse', 'filter', 'font', 'g', 'glyph', 'glyphref', 'hkern', 'image', 'line', 'lineargradient', 'marker', 'mask', 'metadata', 'mpath', 'path', 'pattern', 'polygon', 'polyline', 'radialgradient', 'rect', 'stop', 'style', 'switch', 'symbol', 'text', 'textpath', 'title', 'tref', 'tspan', 'view', 'vkern']);
const svgFilters = freeze(['feBlend', 'feColorMatrix', 'feComponentTransfer', 'feComposite', 'feConvolveMatrix', 'feDiffuseLighting', 'feDisplacementMap', 'feDistantLight', 'feDropShadow', 'feFlood', 'feFuncA', 'feFuncB', 'feFuncG', 'feFuncR', 'feGaussianBlur', 'feImage', 'feMerge', 'feMergeNode', 'feMorphology', 'feOffset', 'fePointLight', 'feSpecularLighting', 'feSpotLight', 'feTile', 'feTurbulence']);
// List of SVG elements that are disallowed by default.
// We still need to know them so that we can do namespace
// checks properly in case one wants to add them to
// allow-list.
const svgDisallowed = freeze(['animate', 'color-profile', 'cursor', 'discard', 'font-face', 'font-face-format', 'font-face-name', 'font-face-src', 'font-face-uri', 'foreignobject', 'hatch', 'hatchpath', 'mesh', 'meshgradient', 'meshpatch', 'meshrow', 'missing-glyph', 'script', 'set', 'solidcolor', 'unknown', 'use']);
const mathMl$1 = freeze(['math', 'menclose', 'merror', 'mfenced', 'mfrac', 'mglyph', 'mi', 'mlabeledtr', 'mmultiscripts', 'mn', 'mo', 'mover', 'mpadded', 'mphantom', 'mroot', 'mrow', 'ms', 'mspace', 'msqrt', 'mstyle', 'msub', 'msup', 'msubsup', 'mtable', 'mtd', 'mtext', 'mtr', 'munder', 'munderover', 'mprescripts']);
// Similarly to SVG, we want to know all MathML elements,
// even those that we disallow by default.
const mathMlDisallowed = freeze(['maction', 'maligngroup', 'malignmark', 'mlongdiv', 'mscarries', 'mscarry', 'msgroup', 'mstack', 'msline', 'msrow', 'semantics', 'annotation', 'annotation-xml', 'mprescripts', 'none']);
const text = freeze(['#text']);

const html = freeze(['accept', 'action', 'align', 'alt', 'autocapitalize', 'autocomplete', 'autopictureinpicture', 'autoplay', 'background', 'bgcolor', 'border', 'capture', 'cellpadding', 'cellspacing', 'checked', 'cite', 'class', 'clear', 'color', 'cols', 'colspan', 'controls', 'controlslist', 'coords', 'crossorigin', 'datetime', 'decoding', 'default', 'dir', 'disabled', 'disablepictureinpicture', 'disableremoteplayback', 'download', 'draggable', 'enctype', 'enterkeyhint', 'face', 'for', 'headers', 'height', 'hidden', 'high', 'href', 'hreflang', 'id', 'inputmode', 'integrity', 'ismap', 'kind', 'label', 'lang', 'list', 'loading', 'loop', 'low', 'max', 'maxlength', 'media', 'method', 'min', 'minlength', 'multiple', 'muted', 'name', 'nonce', 'noshade', 'novalidate', 'nowrap', 'open', 'optimum', 'pattern', 'placeholder', 'playsinline', 'popover', 'popovertarget', 'popovertargetaction', 'poster', 'preload', 'pubdate', 'radiogroup', 'readonly', 'rel', 'required', 'rev', 'reversed', 'role', 'rows', 'rowspan', 'spellcheck', 'scope', 'selected', 'shape', 'size', 'sizes', 'span', 'srclang', 'start', 'src', 'srcset', 'step', 'style', 'summary', 'tabindex', 'title', 'translate', 'type', 'usemap', 'valign', 'value', 'width', 'wrap', 'xmlns', 'slot']);
const svg = freeze(['accent-height', 'accumulate', 'additive', 'alignment-baseline', 'amplitude', 'ascent', 'attributename', 'attributetype', 'azimuth', 'basefrequency', 'baseline-shift', 'begin', 'bias', 'by', 'class', 'clip', 'clippathunits', 'clip-path', 'clip-rule', 'color', 'color-interpolation', 'color-interpolation-filters', 'color-profile', 'color-rendering', 'cx', 'cy', 'd', 'dx', 'dy', 'diffuseconstant', 'direction', 'display', 'divisor', 'dur', 'edgemode', 'elevation', 'end', 'exponent', 'fill', 'fill-opacity', 'fill-rule', 'filter', 'filterunits', 'flood-color', 'flood-opacity', 'font-family', 'font-size', 'font-size-adjust', 'font-stretch', 'font-style', 'font-variant', 'font-weight', 'fx', 'fy', 'g1', 'g2', 'glyph-name', 'glyphref', 'gradientunits', 'gradienttransform', 'height', 'href', 'id', 'image-rendering', 'in', 'in2', 'intercept', 'k', 'k1', 'k2', 'k3', 'k4', 'kerning', 'keypoints', 'keysplines', 'keytimes', 'lang', 'lengthadjust', 'letter-spacing', 'kernelmatrix', 'kernelunitlength', 'lighting-color', 'local', 'marker-end', 'marker-mid', 'marker-start', 'markerheight', 'markerunits', 'markerwidth', 'maskcontentunits', 'maskunits', 'max', 'mask', 'media', 'method', 'mode', 'min', 'name', 'numoctaves', 'offset', 'operator', 'opacity', 'order', 'orient', 'orientation', 'origin', 'overflow', 'paint-order', 'path', 'pathlength', 'patterncontentunits', 'patterntransform', 'patternunits', 'points', 'preservealpha', 'preserveaspectratio', 'primitiveunits', 'r', 'rx', 'ry', 'radius', 'refx', 'refy', 'repeatcount', 'repeatdur', 'restart', 'result', 'rotate', 'scale', 'seed', 'shape-rendering', 'slope', 'specularconstant', 'specularexponent', 'spreadmethod', 'startoffset', 'stddeviation', 'stitchtiles', 'stop-color', 'stop-opacity', 'stroke-dasharray', 'stroke-dashoffset', 'stroke-linecap', 'stroke-linejoin', 'stroke-miterlimit', 'stroke-opacity', 'stroke', 'stroke-width', 'style', 'surfacescale', 'systemlanguage', 'tabindex', 'tablevalues', 'targetx', 'targety', 'transform', 'transform-origin', 'text-anchor', 'text-decoration', 'text-rendering', 'textlength', 'type', 'u1', 'u2', 'unicode', 'values', 'viewbox', 'visibility', 'version', 'vert-adv-y', 'vert-origin-x', 'vert-origin-y', 'width', 'word-spacing', 'wrap', 'writing-mode', 'xchannelselector', 'ychannelselector', 'x', 'x1', 'x2', 'xmlns', 'y', 'y1', 'y2', 'z', 'zoomandpan']);
const mathMl = freeze(['accent', 'accentunder', 'align', 'bevelled', 'close', 'columnsalign', 'columnlines', 'columnspan', 'denomalign', 'depth', 'dir', 'display', 'displaystyle', 'encoding', 'fence', 'frame', 'height', 'href', 'id', 'largeop', 'length', 'linethickness', 'lspace', 'lquote', 'mathbackground', 'mathcolor', 'mathsize', 'mathvariant', 'maxsize', 'minsize', 'movablelimits', 'notation', 'numalign', 'open', 'rowalign', 'rowlines', 'rowspacing', 'rowspan', 'rspace', 'rquote', 'scriptlevel', 'scriptminsize', 'scriptsizemultiplier', 'selection', 'separator', 'separators', 'stretchy', 'subscriptshift', 'supscriptshift', 'symmetric', 'voffset', 'width', 'xmlns']);
const xml = freeze(['xlink:href', 'xml:id', 'xlink:title', 'xml:space', 'xmlns:xlink']);

// eslint-disable-next-line unicorn/better-regex
const MUSTACHE_EXPR = seal(/\{\{[\w\W]*|[\w\W]*\}\}/gm); // Specify template detection regex for SAFE_FOR_TEMPLATES mode
const ERB_EXPR = seal(/<%[\w\W]*|[\w\W]*%>/gm);
const TMPLIT_EXPR = seal(/\$\{[\w\W]*/gm); // eslint-disable-line unicorn/better-regex
const DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]+$/); // eslint-disable-line no-useless-escape
const ARIA_ATTR = seal(/^aria-[\-\w]+$/); // eslint-disable-line no-useless-escape
const IS_ALLOWED_URI = seal(/^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|matrix):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i // eslint-disable-line no-useless-escape
);
const IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i);
const ATTR_WHITESPACE = seal(/[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g // eslint-disable-line no-control-regex
);
const DOCTYPE_NAME = seal(/^html$/i);
const CUSTOM_ELEMENT = seal(/^[a-z][.\w]*(-[.\w]+)+$/i);

var EXPRESSIONS = /*#__PURE__*/Object.freeze({
  __proto__: null,
  ARIA_ATTR: ARIA_ATTR,
  ATTR_WHITESPACE: ATTR_WHITESPACE,
  CUSTOM_ELEMENT: CUSTOM_ELEMENT,
  DATA_ATTR: DATA_ATTR,
  DOCTYPE_NAME: DOCTYPE_NAME,
  ERB_EXPR: ERB_EXPR,
  IS_ALLOWED_URI: IS_ALLOWED_URI,
  IS_SCRIPT_OR_DATA: IS_SCRIPT_OR_DATA,
  MUSTACHE_EXPR: MUSTACHE_EXPR,
  TMPLIT_EXPR: TMPLIT_EXPR
});

/* eslint-disable @typescript-eslint/indent */
// https://developer.mozilla.org/en-US/docs/Web/API/Node/nodeType
const NODE_TYPE = {
  element: 1,
  attribute: 2,
  text: 3,
  cdataSection: 4,
  entityReference: 5,
  // Deprecated
  entityNode: 6,
  // Deprecated
  progressingInstruction: 7,
  comment: 8,
  document: 9,
  documentType: 10,
  documentFragment: 11,
  notation: 12 // Deprecated
};
const getGlobal = function getGlobal() {
  return typeof window === 'undefined' ? null : window;
};
/**
 * Creates a no-op policy for internal use only.
 * Don't export this function outside this module!
 * @param trustedTypes The policy factory.
 * @param purifyHostElement The Script element used to load DOMPurify (to determine policy name suffix).
 * @return The policy created (or null, if Trusted Types
 * are not supported or creating the policy failed).
 */
const _createTrustedTypesPolicy = function _createTrustedTypesPolicy(trustedTypes, purifyHostElement) {
  if (typeof trustedTypes !== 'object' || typeof trustedTypes.createPolicy !== 'function') {
    return null;
  }
  // Allow the callers to control the unique policy name
  // by adding a data-tt-policy-suffix to the script element with the DOMPurify.
  // Policy creation with duplicate names throws in Trusted Types.
  let suffix = null;
  const ATTR_NAME = 'data-tt-policy-suffix';
  if (purifyHostElement && purifyHostElement.hasAttribute(ATTR_NAME)) {
    suffix = purifyHostElement.getAttribute(ATTR_NAME);
  }
  const policyName = 'dompurify' + (suffix ? '#' + suffix : '');
  try {
    return trustedTypes.createPolicy(policyName, {
      createHTML(html) {
        return html;
      },
      createScriptURL(scriptUrl) {
        return scriptUrl;
      }
    });
  } catch (_) {
    // Policy creation failed (most likely another DOMPurify script has
    // already run). Skip creating the policy, as this will only cause errors
    // if TT are enforced.
    console.warn('TrustedTypes policy ' + policyName + ' could not be created.');
    return null;
  }
};
const _createHooksMap = function _createHooksMap() {
  return {
    afterSanitizeAttributes: [],
    afterSanitizeElements: [],
    afterSanitizeShadowDOM: [],
    beforeSanitizeAttributes: [],
    beforeSanitizeElements: [],
    beforeSanitizeShadowDOM: [],
    uponSanitizeAttribute: [],
    uponSanitizeElement: [],
    uponSanitizeShadowNode: []
  };
};
function createDOMPurify() {
  let window = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : getGlobal();
  const DOMPurify = root => createDOMPurify(root);
  DOMPurify.version = '3.2.6';
  DOMPurify.removed = [];
  if (!window || !window.document || window.document.nodeType !== NODE_TYPE.document || !window.Element) {
    // Not running in a browser, provide a factory function
    // so that you can pass your own Window
    DOMPurify.isSupported = false;
    return DOMPurify;
  }
  let {
    document
  } = window;
  const originalDocument = document;
  const currentScript = originalDocument.currentScript;
  const {
    DocumentFragment,
    HTMLTemplateElement,
    Node,
    Element,
    NodeFilter,
    NamedNodeMap = window.NamedNodeMap || window.MozNamedAttrMap,
    HTMLFormElement,
    DOMParser,
    trustedTypes
  } = window;
  const ElementPrototype = Element.prototype;
  const cloneNode = lookupGetter(ElementPrototype, 'cloneNode');
  const remove = lookupGetter(ElementPrototype, 'remove');
  const getNextSibling = lookupGetter(ElementPrototype, 'nextSibling');
  const getChildNodes = lookupGetter(ElementPrototype, 'childNodes');
  const getParentNode = lookupGetter(ElementPrototype, 'parentNode');
  // As per issue #47, the web-components registry is inherited by a
  // new document created via createHTMLDocument. As per the spec
  // (http://w3c.github.io/webcomponents/spec/custom/#creating-and-passing-registries)
  // a new empty registry is used when creating a template contents owner
  // document, so we use that as our parent document to ensure nothing
  // is inherited.
  if (typeof HTMLTemplateElement === 'function') {
    const template = document.createElement('template');
    if (template.content && template.content.ownerDocument) {
      document = template.content.ownerDocument;
    }
  }
  let trustedTypesPolicy;
  let emptyHTML = '';
  const {
    implementation,
    createNodeIterator,
    createDocumentFragment,
    getElementsByTagName
  } = document;
  const {
    importNode
  } = originalDocument;
  let hooks = _createHooksMap();
  /**
   * Expose whether this browser supports running the full DOMPurify.
   */
  DOMPurify.isSupported = typeof entries === 'function' && typeof getParentNode === 'function' && implementation && implementation.createHTMLDocument !== undefined;
  const {
    MUSTACHE_EXPR,
    ERB_EXPR,
    TMPLIT_EXPR,
    DATA_ATTR,
    ARIA_ATTR,
    IS_SCRIPT_OR_DATA,
    ATTR_WHITESPACE,
    CUSTOM_ELEMENT
  } = EXPRESSIONS;
  let {
    IS_ALLOWED_URI: IS_ALLOWED_URI$1
  } = EXPRESSIONS;
  /**
   * We consider the elements and attributes below to be safe. Ideally
   * don't add any new ones but feel free to remove unwanted ones.
   */
  /* allowed element names */
  let ALLOWED_TAGS = null;
  const DEFAULT_ALLOWED_TAGS = addToSet({}, [...html$1, ...svg$1, ...svgFilters, ...mathMl$1, ...text]);
  /* Allowed attribute names */
  let ALLOWED_ATTR = null;
  const DEFAULT_ALLOWED_ATTR = addToSet({}, [...html, ...svg, ...mathMl, ...xml]);
  /*
   * Configure how DOMPurify should handle custom elements and their attributes as well as customized built-in elements.
   * @property {RegExp|Function|null} tagNameCheck one of [null, regexPattern, predicate]. Default: `null` (disallow any custom elements)
   * @property {RegExp|Function|null} attributeNameCheck one of [null, regexPattern, predicate]. Default: `null` (disallow any attributes not on the allow list)
   * @property {boolean} allowCustomizedBuiltInElements allow custom elements derived from built-ins if they pass CUSTOM_ELEMENT_HANDLING.tagNameCheck. Default: `false`.
   */
  let CUSTOM_ELEMENT_HANDLING = Object.seal(create(null, {
    tagNameCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    },
    attributeNameCheck: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: null
    },
    allowCustomizedBuiltInElements: {
      writable: true,
      configurable: false,
      enumerable: true,
      value: false
    }
  }));
  /* Explicitly forbidden tags (overrides ALLOWED_TAGS/ADD_TAGS) */
  let FORBID_TAGS = null;
  /* Explicitly forbidden attributes (overrides ALLOWED_ATTR/ADD_ATTR) */
  let FORBID_ATTR = null;
  /* Decide if ARIA attributes are okay */
  let ALLOW_ARIA_ATTR = true;
  /* Decide if custom data attributes are okay */
  let ALLOW_DATA_ATTR = true;
  /* Decide if unknown protocols are okay */
  let ALLOW_UNKNOWN_PROTOCOLS = false;
  /* Decide if self-closing tags in attributes are allowed.
   * Usually removed due to a mXSS issue in jQuery 3.0 */
  let ALLOW_SELF_CLOSE_IN_ATTR = true;
  /* Output should be safe for common template engines.
   * This means, DOMPurify removes data attributes, mustaches and ERB
   */
  let SAFE_FOR_TEMPLATES = false;
  /* Output should be safe even for XML used within HTML and alike.
   * This means, DOMPurify removes comments when containing risky content.
   */
  let SAFE_FOR_XML = true;
  /* Decide if document with <html>... should be returned */
  let WHOLE_DOCUMENT = false;
  /* Track whether config is already set on this instance of DOMPurify. */
  let SET_CONFIG = false;
  /* Decide if all elements (e.g. style, script) must be children of
   * document.body. By default, browsers might move them to document.head */
  let FORCE_BODY = false;
  /* Decide if a DOM `HTMLBodyElement` should be returned, instead of a html
   * string (or a TrustedHTML object if Trusted Types are supported).
   * If `WHOLE_DOCUMENT` is enabled a `HTMLHtmlElement` will be returned instead
   */
  let RETURN_DOM = false;
  /* Decide if a DOM `DocumentFragment` should be returned, instead of a html
   * string  (or a TrustedHTML object if Trusted Types are supported) */
  let RETURN_DOM_FRAGMENT = false;
  /* Try to return a Trusted Type object instead of a string, return a string in
   * case Trusted Types are not supported  */
  let RETURN_TRUSTED_TYPE = false;
  /* Output should be free from DOM clobbering attacks?
   * This sanitizes markups named with colliding, clobberable built-in DOM APIs.
   */
  let SANITIZE_DOM = true;
  /* Achieve full DOM Clobbering protection by isolating the namespace of named
   * properties and JS variables, mitigating attacks that abuse the HTML/DOM spec rules.
   *
   * HTML/DOM spec rules that enable DOM Clobbering:
   *   - Named Access on Window (§7.3.3)
   *   - DOM Tree Accessors (§3.1.5)
   *   - Form Element Parent-Child Relations (§4.10.3)
   *   - Iframe srcdoc / Nested WindowProxies (§4.8.5)
   *   - HTMLCollection (§4.2.10.2)
   *
   * Namespace isolation is implemented by prefixing `id` and `name` attributes
   * with a constant string, i.e., `user-content-`
   */
  let SANITIZE_NAMED_PROPS = false;
  const SANITIZE_NAMED_PROPS_PREFIX = 'user-content-';
  /* Keep element content when removing element? */
  let KEEP_CONTENT = true;
  /* If a `Node` is passed to sanitize(), then performs sanitization in-place instead
   * of importing it into a new Document and returning a sanitized copy */
  let IN_PLACE = false;
  /* Allow usage of profiles like html, svg and mathMl */
  let USE_PROFILES = {};
  /* Tags to ignore content of when KEEP_CONTENT is true */
  let FORBID_CONTENTS = null;
  const DEFAULT_FORBID_CONTENTS = addToSet({}, ['annotation-xml', 'audio', 'colgroup', 'desc', 'foreignobject', 'head', 'iframe', 'math', 'mi', 'mn', 'mo', 'ms', 'mtext', 'noembed', 'noframes', 'noscript', 'plaintext', 'script', 'style', 'svg', 'template', 'thead', 'title', 'video', 'xmp']);
  /* Tags that are safe for data: URIs */
  let DATA_URI_TAGS = null;
  const DEFAULT_DATA_URI_TAGS = addToSet({}, ['audio', 'video', 'img', 'source', 'image', 'track']);
  /* Attributes safe for values like "javascript:" */
  let URI_SAFE_ATTRIBUTES = null;
  const DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, ['alt', 'class', 'for', 'id', 'label', 'name', 'pattern', 'placeholder', 'role', 'summary', 'title', 'value', 'style', 'xmlns']);
  const MATHML_NAMESPACE = 'http://www.w3.org/1998/Math/MathML';
  const SVG_NAMESPACE = 'http://www.w3.org/2000/svg';
  const HTML_NAMESPACE = 'http://www.w3.org/1999/xhtml';
  /* Document namespace */
  let NAMESPACE = HTML_NAMESPACE;
  let IS_EMPTY_INPUT = false;
  /* Allowed XHTML+XML namespaces */
  let ALLOWED_NAMESPACES = null;
  const DEFAULT_ALLOWED_NAMESPACES = addToSet({}, [MATHML_NAMESPACE, SVG_NAMESPACE, HTML_NAMESPACE], stringToString);
  let MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ['mi', 'mo', 'mn', 'ms', 'mtext']);
  let HTML_INTEGRATION_POINTS = addToSet({}, ['annotation-xml']);
  // Certain elements are allowed in both SVG and HTML
  // namespace. We need to specify them explicitly
  // so that they don't get erroneously deleted from
  // HTML namespace.
  const COMMON_SVG_AND_HTML_ELEMENTS = addToSet({}, ['title', 'style', 'font', 'a', 'script']);
  /* Parsing of strict XHTML documents */
  let PARSER_MEDIA_TYPE = null;
  const SUPPORTED_PARSER_MEDIA_TYPES = ['application/xhtml+xml', 'text/html'];
  const DEFAULT_PARSER_MEDIA_TYPE = 'text/html';
  let transformCaseFunc = null;
  /* Keep a reference to config to pass to hooks */
  let CONFIG = null;
  /* Ideally, do not touch anything below this line */
  /* ______________________________________________ */
  const formElement = document.createElement('form');
  const isRegexOrFunction = function isRegexOrFunction(testValue) {
    return testValue instanceof RegExp || testValue instanceof Function;
  };
  /**
   * _parseConfig
   *
   * @param cfg optional config literal
   */
  // eslint-disable-next-line complexity
  const _parseConfig = function _parseConfig() {
    let cfg = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    if (CONFIG && CONFIG === cfg) {
      return;
    }
    /* Shield configuration object from tampering */
    if (!cfg || typeof cfg !== 'object') {
      cfg = {};
    }
    /* Shield configuration object from prototype pollution */
    cfg = clone(cfg);
    PARSER_MEDIA_TYPE =
    // eslint-disable-next-line unicorn/prefer-includes
    SUPPORTED_PARSER_MEDIA_TYPES.indexOf(cfg.PARSER_MEDIA_TYPE) === -1 ? DEFAULT_PARSER_MEDIA_TYPE : cfg.PARSER_MEDIA_TYPE;
    // HTML tags and attributes are not case-sensitive, converting to lowercase. Keeping XHTML as is.
    transformCaseFunc = PARSER_MEDIA_TYPE === 'application/xhtml+xml' ? stringToString : stringToLowerCase;
    /* Set configuration parameters */
    ALLOWED_TAGS = objectHasOwnProperty(cfg, 'ALLOWED_TAGS') ? addToSet({}, cfg.ALLOWED_TAGS, transformCaseFunc) : DEFAULT_ALLOWED_TAGS;
    ALLOWED_ATTR = objectHasOwnProperty(cfg, 'ALLOWED_ATTR') ? addToSet({}, cfg.ALLOWED_ATTR, transformCaseFunc) : DEFAULT_ALLOWED_ATTR;
    ALLOWED_NAMESPACES = objectHasOwnProperty(cfg, 'ALLOWED_NAMESPACES') ? addToSet({}, cfg.ALLOWED_NAMESPACES, stringToString) : DEFAULT_ALLOWED_NAMESPACES;
    URI_SAFE_ATTRIBUTES = objectHasOwnProperty(cfg, 'ADD_URI_SAFE_ATTR') ? addToSet(clone(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR, transformCaseFunc) : DEFAULT_URI_SAFE_ATTRIBUTES;
    DATA_URI_TAGS = objectHasOwnProperty(cfg, 'ADD_DATA_URI_TAGS') ? addToSet(clone(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS, transformCaseFunc) : DEFAULT_DATA_URI_TAGS;
    FORBID_CONTENTS = objectHasOwnProperty(cfg, 'FORBID_CONTENTS') ? addToSet({}, cfg.FORBID_CONTENTS, transformCaseFunc) : DEFAULT_FORBID_CONTENTS;
    FORBID_TAGS = objectHasOwnProperty(cfg, 'FORBID_TAGS') ? addToSet({}, cfg.FORBID_TAGS, transformCaseFunc) : clone({});
    FORBID_ATTR = objectHasOwnProperty(cfg, 'FORBID_ATTR') ? addToSet({}, cfg.FORBID_ATTR, transformCaseFunc) : clone({});
    USE_PROFILES = objectHasOwnProperty(cfg, 'USE_PROFILES') ? cfg.USE_PROFILES : false;
    ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== false; // Default true
    ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== false; // Default true
    ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || false; // Default false
    ALLOW_SELF_CLOSE_IN_ATTR = cfg.ALLOW_SELF_CLOSE_IN_ATTR !== false; // Default true
    SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || false; // Default false
    SAFE_FOR_XML = cfg.SAFE_FOR_XML !== false; // Default true
    WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || false; // Default false
    RETURN_DOM = cfg.RETURN_DOM || false; // Default false
    RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || false; // Default false
    RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || false; // Default false
    FORCE_BODY = cfg.FORCE_BODY || false; // Default false
    SANITIZE_DOM = cfg.SANITIZE_DOM !== false; // Default true
    SANITIZE_NAMED_PROPS = cfg.SANITIZE_NAMED_PROPS || false; // Default false
    KEEP_CONTENT = cfg.KEEP_CONTENT !== false; // Default true
    IN_PLACE = cfg.IN_PLACE || false; // Default false
    IS_ALLOWED_URI$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI;
    NAMESPACE = cfg.NAMESPACE || HTML_NAMESPACE;
    MATHML_TEXT_INTEGRATION_POINTS = cfg.MATHML_TEXT_INTEGRATION_POINTS || MATHML_TEXT_INTEGRATION_POINTS;
    HTML_INTEGRATION_POINTS = cfg.HTML_INTEGRATION_POINTS || HTML_INTEGRATION_POINTS;
    CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
    if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck)) {
      CUSTOM_ELEMENT_HANDLING.tagNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck;
    }
    if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck)) {
      CUSTOM_ELEMENT_HANDLING.attributeNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck;
    }
    if (cfg.CUSTOM_ELEMENT_HANDLING && typeof cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements === 'boolean') {
      CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements = cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements;
    }
    if (SAFE_FOR_TEMPLATES) {
      ALLOW_DATA_ATTR = false;
    }
    if (RETURN_DOM_FRAGMENT) {
      RETURN_DOM = true;
    }
    /* Parse profile info */
    if (USE_PROFILES) {
      ALLOWED_TAGS = addToSet({}, text);
      ALLOWED_ATTR = [];
      if (USE_PROFILES.html === true) {
        addToSet(ALLOWED_TAGS, html$1);
        addToSet(ALLOWED_ATTR, html);
      }
      if (USE_PROFILES.svg === true) {
        addToSet(ALLOWED_TAGS, svg$1);
        addToSet(ALLOWED_ATTR, svg);
        addToSet(ALLOWED_ATTR, xml);
      }
      if (USE_PROFILES.svgFilters === true) {
        addToSet(ALLOWED_TAGS, svgFilters);
        addToSet(ALLOWED_ATTR, svg);
        addToSet(ALLOWED_ATTR, xml);
      }
      if (USE_PROFILES.mathMl === true) {
        addToSet(ALLOWED_TAGS, mathMl$1);
        addToSet(ALLOWED_ATTR, mathMl);
        addToSet(ALLOWED_ATTR, xml);
      }
    }
    /* Merge configuration parameters */
    if (cfg.ADD_TAGS) {
      if (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS) {
        ALLOWED_TAGS = clone(ALLOWED_TAGS);
      }
      addToSet(ALLOWED_TAGS, cfg.ADD_TAGS, transformCaseFunc);
    }
    if (cfg.ADD_ATTR) {
      if (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR) {
        ALLOWED_ATTR = clone(ALLOWED_ATTR);
      }
      addToSet(ALLOWED_ATTR, cfg.ADD_ATTR, transformCaseFunc);
    }
    if (cfg.ADD_URI_SAFE_ATTR) {
      addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR, transformCaseFunc);
    }
    if (cfg.FORBID_CONTENTS) {
      if (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS) {
        FORBID_CONTENTS = clone(FORBID_CONTENTS);
      }
      addToSet(FORBID_CONTENTS, cfg.FORBID_CONTENTS, transformCaseFunc);
    }
    /* Add #text in case KEEP_CONTENT is set to true */
    if (KEEP_CONTENT) {
      ALLOWED_TAGS['#text'] = true;
    }
    /* Add html, head and body to ALLOWED_TAGS in case WHOLE_DOCUMENT is true */
    if (WHOLE_DOCUMENT) {
      addToSet(ALLOWED_TAGS, ['html', 'head', 'body']);
    }
    /* Add tbody to ALLOWED_TAGS in case tables are permitted, see #286, #365 */
    if (ALLOWED_TAGS.table) {
      addToSet(ALLOWED_TAGS, ['tbody']);
      delete FORBID_TAGS.tbody;
    }
    if (cfg.TRUSTED_TYPES_POLICY) {
      if (typeof cfg.TRUSTED_TYPES_POLICY.createHTML !== 'function') {
        throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createHTML" hook.');
      }
      if (typeof cfg.TRUSTED_TYPES_POLICY.createScriptURL !== 'function') {
        throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createScriptURL" hook.');
      }
      // Overwrite existing TrustedTypes policy.
      trustedTypesPolicy = cfg.TRUSTED_TYPES_POLICY;
      // Sign local variables required by `sanitize`.
      emptyHTML = trustedTypesPolicy.createHTML('');
    } else {
      // Uninitialized policy, attempt to initialize the internal dompurify policy.
      if (trustedTypesPolicy === undefined) {
        trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, currentScript);
      }
      // If creating the internal policy succeeded sign internal variables.
      if (trustedTypesPolicy !== null && typeof emptyHTML === 'string') {
        emptyHTML = trustedTypesPolicy.createHTML('');
      }
    }
    // Prevent further manipulation of configuration.
    // Not available in IE8, Safari 5, etc.
    if (freeze) {
      freeze(cfg);
    }
    CONFIG = cfg;
  };
  /* Keep track of all possible SVG and MathML tags
   * so that we can perform the namespace checks
   * correctly. */
  const ALL_SVG_TAGS = addToSet({}, [...svg$1, ...svgFilters, ...svgDisallowed]);
  const ALL_MATHML_TAGS = addToSet({}, [...mathMl$1, ...mathMlDisallowed]);
  /**
   * @param element a DOM element whose namespace is being checked
   * @returns Return false if the element has a
   *  namespace that a spec-compliant parser would never
   *  return. Return true otherwise.
   */
  const _checkValidNamespace = function _checkValidNamespace(element) {
    let parent = getParentNode(element);
    // In JSDOM, if we're inside shadow DOM, then parentNode
    // can be null. We just simulate parent in this case.
    if (!parent || !parent.tagName) {
      parent = {
        namespaceURI: NAMESPACE,
        tagName: 'template'
      };
    }
    const tagName = stringToLowerCase(element.tagName);
    const parentTagName = stringToLowerCase(parent.tagName);
    if (!ALLOWED_NAMESPACES[element.namespaceURI]) {
      return false;
    }
    if (element.namespaceURI === SVG_NAMESPACE) {
      // The only way to switch from HTML namespace to SVG
      // is via <svg>. If it happens via any other tag, then
      // it should be killed.
      if (parent.namespaceURI === HTML_NAMESPACE) {
        return tagName === 'svg';
      }
      // The only way to switch from MathML to SVG is via`
      // svg if parent is either <annotation-xml> or MathML
      // text integration points.
      if (parent.namespaceURI === MATHML_NAMESPACE) {
        return tagName === 'svg' && (parentTagName === 'annotation-xml' || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]);
      }
      // We only allow elements that are defined in SVG
      // spec. All others are disallowed in SVG namespace.
      return Boolean(ALL_SVG_TAGS[tagName]);
    }
    if (element.namespaceURI === MATHML_NAMESPACE) {
      // The only way to switch from HTML namespace to MathML
      // is via <math>. If it happens via any other tag, then
      // it should be killed.
      if (parent.namespaceURI === HTML_NAMESPACE) {
        return tagName === 'math';
      }
      // The only way to switch from SVG to MathML is via
      // <math> and HTML integration points
      if (parent.namespaceURI === SVG_NAMESPACE) {
        return tagName === 'math' && HTML_INTEGRATION_POINTS[parentTagName];
      }
      // We only allow elements that are defined in MathML
      // spec. All others are disallowed in MathML namespace.
      return Boolean(ALL_MATHML_TAGS[tagName]);
    }
    if (element.namespaceURI === HTML_NAMESPACE) {
      // The only way to switch from SVG to HTML is via
      // HTML integration points, and from MathML to HTML
      // is via MathML text integration points
      if (parent.namespaceURI === SVG_NAMESPACE && !HTML_INTEGRATION_POINTS[parentTagName]) {
        return false;
      }
      if (parent.namespaceURI === MATHML_NAMESPACE && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) {
        return false;
      }
      // We disallow tags that are specific for MathML
      // or SVG and should never appear in HTML namespace
      return !ALL_MATHML_TAGS[tagName] && (COMMON_SVG_AND_HTML_ELEMENTS[tagName] || !ALL_SVG_TAGS[tagName]);
    }
    // For XHTML and XML documents that support custom namespaces
    if (PARSER_MEDIA_TYPE === 'application/xhtml+xml' && ALLOWED_NAMESPACES[element.namespaceURI]) {
      return true;
    }
    // The code should never reach this place (this means
    // that the element somehow got namespace that is not
    // HTML, SVG, MathML or allowed via ALLOWED_NAMESPACES).
    // Return false just in case.
    return false;
  };
  /**
   * _forceRemove
   *
   * @param node a DOM node
   */
  const _forceRemove = function _forceRemove(node) {
    arrayPush(DOMPurify.removed, {
      element: node
    });
    try {
      // eslint-disable-next-line unicorn/prefer-dom-node-remove
      getParentNode(node).removeChild(node);
    } catch (_) {
      remove(node);
    }
  };
  /**
   * _removeAttribute
   *
   * @param name an Attribute name
   * @param element a DOM node
   */
  const _removeAttribute = function _removeAttribute(name, element) {
    try {
      arrayPush(DOMPurify.removed, {
        attribute: element.getAttributeNode(name),
        from: element
      });
    } catch (_) {
      arrayPush(DOMPurify.removed, {
        attribute: null,
        from: element
      });
    }
    element.removeAttribute(name);
    // We void attribute values for unremovable "is" attributes
    if (name === 'is') {
      if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
        try {
          _forceRemove(element);
        } catch (_) {}
      } else {
        try {
          element.setAttribute(name, '');
        } catch (_) {}
      }
    }
  };
  /**
   * _initDocument
   *
   * @param dirty - a string of dirty markup
   * @return a DOM, filled with the dirty markup
   */
  const _initDocument = function _initDocument(dirty) {
    /* Create a HTML document */
    let doc = null;
    let leadingWhitespace = null;
    if (FORCE_BODY) {
      dirty = '<remove></remove>' + dirty;
    } else {
      /* If FORCE_BODY isn't used, leading whitespace needs to be preserved manually */
      const matches = stringMatch(dirty, /^[\r\n\t ]+/);
      leadingWhitespace = matches && matches[0];
    }
    if (PARSER_MEDIA_TYPE === 'application/xhtml+xml' && NAMESPACE === HTML_NAMESPACE) {
      // Root of XHTML doc must contain xmlns declaration (see https://www.w3.org/TR/xhtml1/normative.html#strict)
      dirty = '<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>' + dirty + '</body></html>';
    }
    const dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
    /*
     * Use the DOMParser API by default, fallback later if needs be
     * DOMParser not work for svg when has multiple root element.
     */
    if (NAMESPACE === HTML_NAMESPACE) {
      try {
        doc = new DOMParser().parseFromString(dirtyPayload, PARSER_MEDIA_TYPE);
      } catch (_) {}
    }
    /* Use createHTMLDocument in case DOMParser is not available */
    if (!doc || !doc.documentElement) {
      doc = implementation.createDocument(NAMESPACE, 'template', null);
      try {
        doc.documentElement.innerHTML = IS_EMPTY_INPUT ? emptyHTML : dirtyPayload;
      } catch (_) {
        // Syntax error if dirtyPayload is invalid xml
      }
    }
    const body = doc.body || doc.documentElement;
    if (dirty && leadingWhitespace) {
      body.insertBefore(document.createTextNode(leadingWhitespace), body.childNodes[0] || null);
    }
    /* Work on whole document or just its body */
    if (NAMESPACE === HTML_NAMESPACE) {
      return getElementsByTagName.call(doc, WHOLE_DOCUMENT ? 'html' : 'body')[0];
    }
    return WHOLE_DOCUMENT ? doc.documentElement : body;
  };
  /**
   * Creates a NodeIterator object that you can use to traverse filtered lists of nodes or elements in a document.
   *
   * @param root The root element or node to start traversing on.
   * @return The created NodeIterator
   */
  const _createNodeIterator = function _createNodeIterator(root) {
    return createNodeIterator.call(root.ownerDocument || root, root,
    // eslint-disable-next-line no-bitwise
    NodeFilter.SHOW_ELEMENT | NodeFilter.SHOW_COMMENT | NodeFilter.SHOW_TEXT | NodeFilter.SHOW_PROCESSING_INSTRUCTION | NodeFilter.SHOW_CDATA_SECTION, null);
  };
  /**
   * _isClobbered
   *
   * @param element element to check for clobbering attacks
   * @return true if clobbered, false if safe
   */
  const _isClobbered = function _isClobbered(element) {
    return element instanceof HTMLFormElement && (typeof element.nodeName !== 'string' || typeof element.textContent !== 'string' || typeof element.removeChild !== 'function' || !(element.attributes instanceof NamedNodeMap) || typeof element.removeAttribute !== 'function' || typeof element.setAttribute !== 'function' || typeof element.namespaceURI !== 'string' || typeof element.insertBefore !== 'function' || typeof element.hasChildNodes !== 'function');
  };
  /**
   * Checks whether the given object is a DOM node.
   *
   * @param value object to check whether it's a DOM node
   * @return true is object is a DOM node
   */
  const _isNode = function _isNode(value) {
    return typeof Node === 'function' && value instanceof Node;
  };
  function _executeHooks(hooks, currentNode, data) {
    arrayForEach(hooks, hook => {
      hook.call(DOMPurify, currentNode, data, CONFIG);
    });
  }
  /**
   * _sanitizeElements
   *
   * @protect nodeName
   * @protect textContent
   * @protect removeChild
   * @param currentNode to check for permission to exist
   * @return true if node was killed, false if left alive
   */
  const _sanitizeElements = function _sanitizeElements(currentNode) {
    let content = null;
    /* Execute a hook if present */
    _executeHooks(hooks.beforeSanitizeElements, currentNode, null);
    /* Check if element is clobbered or can clobber */
    if (_isClobbered(currentNode)) {
      _forceRemove(currentNode);
      return true;
    }
    /* Now let's check the element's type and name */
    const tagName = transformCaseFunc(currentNode.nodeName);
    /* Execute a hook if present */
    _executeHooks(hooks.uponSanitizeElement, currentNode, {
      tagName,
      allowedTags: ALLOWED_TAGS
    });
    /* Detect mXSS attempts abusing namespace confusion */
    if (SAFE_FOR_XML && currentNode.hasChildNodes() && !_isNode(currentNode.firstElementChild) && regExpTest(/<[/\w!]/g, currentNode.innerHTML) && regExpTest(/<[/\w!]/g, currentNode.textContent)) {
      _forceRemove(currentNode);
      return true;
    }
    /* Remove any occurrence of processing instructions */
    if (currentNode.nodeType === NODE_TYPE.progressingInstruction) {
      _forceRemove(currentNode);
      return true;
    }
    /* Remove any kind of possibly harmful comments */
    if (SAFE_FOR_XML && currentNode.nodeType === NODE_TYPE.comment && regExpTest(/<[/\w]/g, currentNode.data)) {
      _forceRemove(currentNode);
      return true;
    }
    /* Remove element if anything forbids its presence */
    if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
      /* Check if we have a custom element to handle */
      if (!FORBID_TAGS[tagName] && _isBasicCustomElement(tagName)) {
        if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, tagName)) {
          return false;
        }
        if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(tagName)) {
          return false;
        }
      }
      /* Keep content except for bad-listed elements */
      if (KEEP_CONTENT && !FORBID_CONTENTS[tagName]) {
        const parentNode = getParentNode(currentNode) || currentNode.parentNode;
        const childNodes = getChildNodes(currentNode) || currentNode.childNodes;
        if (childNodes && parentNode) {
          const childCount = childNodes.length;
          for (let i = childCount - 1; i >= 0; --i) {
            const childClone = cloneNode(childNodes[i], true);
            childClone.__removalCount = (currentNode.__removalCount || 0) + 1;
            parentNode.insertBefore(childClone, getNextSibling(currentNode));
          }
        }
      }
      _forceRemove(currentNode);
      return true;
    }
    /* Check whether element has a valid namespace */
    if (currentNode instanceof Element && !_checkValidNamespace(currentNode)) {
      _forceRemove(currentNode);
      return true;
    }
    /* Make sure that older browsers don't get fallback-tag mXSS */
    if ((tagName === 'noscript' || tagName === 'noembed' || tagName === 'noframes') && regExpTest(/<\/no(script|embed|frames)/i, currentNode.innerHTML)) {
      _forceRemove(currentNode);
      return true;
    }
    /* Sanitize element content to be template-safe */
    if (SAFE_FOR_TEMPLATES && currentNode.nodeType === NODE_TYPE.text) {
      /* Get the element's text content */
      content = currentNode.textContent;
      arrayForEach([MUSTACHE_EXPR, ERB_EXPR, TMPLIT_EXPR], expr => {
        content = stringReplace(content, expr, ' ');
      });
      if (currentNode.textContent !== content) {
        arrayPush(DOMPurify.removed, {
          element: currentNode.cloneNode()
        });
        currentNode.textContent = content;
      }
    }
    /* Execute a hook if present */
    _executeHooks(hooks.afterSanitizeElements, currentNode, null);
    return false;
  };
  /**
   * _isValidAttribute
   *
   * @param lcTag Lowercase tag name of containing element.
   * @param lcName Lowercase attribute name.
   * @param value Attribute value.
   * @return Returns true if `value` is valid, otherwise false.
   */
  // eslint-disable-next-line complexity
  const _isValidAttribute = function _isValidAttribute(lcTag, lcName, value) {
    /* Make sure attribute cannot clobber */
    if (SANITIZE_DOM && (lcName === 'id' || lcName === 'name') && (value in document || value in formElement)) {
      return false;
    }
    /* Allow valid data-* attributes: At least one character after "-"
        (https://html.spec.whatwg.org/multipage/dom.html#embedding-custom-non-visible-data-with-the-data-*-attributes)
        XML-compatible (https://html.spec.whatwg.org/multipage/infrastructure.html#xml-compatible and http://www.w3.org/TR/xml/#d0e804)
        We don't need to check the value; it's always URI safe. */
    if (ALLOW_DATA_ATTR && !FORBID_ATTR[lcName] && regExpTest(DATA_ATTR, lcName)) ; else if (ALLOW_ARIA_ATTR && regExpTest(ARIA_ATTR, lcName)) ; else if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
      if (
      // First condition does a very basic check if a) it's basically a valid custom element tagname AND
      // b) if the tagName passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
      // and c) if the attribute name passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.attributeNameCheck
      _isBasicCustomElement(lcTag) && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, lcTag) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(lcTag)) && (CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.attributeNameCheck, lcName) || CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.attributeNameCheck(lcName)) ||
      // Alternative, second condition checks if it's an `is`-attribute, AND
      // the value passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
      lcName === 'is' && CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, value) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(value))) ; else {
        return false;
      }
      /* Check value is safe. First, is attr inert? If so, is safe */
    } else if (URI_SAFE_ATTRIBUTES[lcName]) ; else if (regExpTest(IS_ALLOWED_URI$1, stringReplace(value, ATTR_WHITESPACE, ''))) ; else if ((lcName === 'src' || lcName === 'xlink:href' || lcName === 'href') && lcTag !== 'script' && stringIndexOf(value, 'data:') === 0 && DATA_URI_TAGS[lcTag]) ; else if (ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA, stringReplace(value, ATTR_WHITESPACE, ''))) ; else if (value) {
      return false;
    } else ;
    return true;
  };
  /**
   * _isBasicCustomElement
   * checks if at least one dash is included in tagName, and it's not the first char
   * for more sophisticated checking see https://github.com/sindresorhus/validate-element-name
   *
   * @param tagName name of the tag of the node to sanitize
   * @returns Returns true if the tag name meets the basic criteria for a custom element, otherwise false.
   */
  const _isBasicCustomElement = function _isBasicCustomElement(tagName) {
    return tagName !== 'annotation-xml' && stringMatch(tagName, CUSTOM_ELEMENT);
  };
  /**
   * _sanitizeAttributes
   *
   * @protect attributes
   * @protect nodeName
   * @protect removeAttribute
   * @protect setAttribute
   *
   * @param currentNode to sanitize
   */
  const _sanitizeAttributes = function _sanitizeAttributes(currentNode) {
    /* Execute a hook if present */
    _executeHooks(hooks.beforeSanitizeAttributes, currentNode, null);
    const {
      attributes
    } = currentNode;
    /* Check if we have attributes; if not we might have a text node */
    if (!attributes || _isClobbered(currentNode)) {
      return;
    }
    const hookEvent = {
      attrName: '',
      attrValue: '',
      keepAttr: true,
      allowedAttributes: ALLOWED_ATTR,
      forceKeepAttr: undefined
    };
    let l = attributes.length;
    /* Go backwards over all attributes; safely remove bad ones */
    while (l--) {
      const attr = attributes[l];
      const {
        name,
        namespaceURI,
        value: attrValue
      } = attr;
      const lcName = transformCaseFunc(name);
      const initValue = attrValue;
      let value = name === 'value' ? initValue : stringTrim(initValue);
      /* Execute a hook if present */
      hookEvent.attrName = lcName;
      hookEvent.attrValue = value;
      hookEvent.keepAttr = true;
      hookEvent.forceKeepAttr = undefined; // Allows developers to see this is a property they can set
      _executeHooks(hooks.uponSanitizeAttribute, currentNode, hookEvent);
      value = hookEvent.attrValue;
      /* Full DOM Clobbering protection via namespace isolation,
       * Prefix id and name attributes with `user-content-`
       */
      if (SANITIZE_NAMED_PROPS && (lcName === 'id' || lcName === 'name')) {
        // Remove the attribute with this value
        _removeAttribute(name, currentNode);
        // Prefix the value and later re-create the attribute with the sanitized value
        value = SANITIZE_NAMED_PROPS_PREFIX + value;
      }
      /* Work around a security issue with comments inside attributes */
      if (SAFE_FOR_XML && regExpTest(/((--!?|])>)|<\/(style|title)/i, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      /* Did the hooks approve of the attribute? */
      if (hookEvent.forceKeepAttr) {
        continue;
      }
      /* Did the hooks approve of the attribute? */
      if (!hookEvent.keepAttr) {
        _removeAttribute(name, currentNode);
        continue;
      }
      /* Work around a security issue in jQuery 3.0 */
      if (!ALLOW_SELF_CLOSE_IN_ATTR && regExpTest(/\/>/i, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      /* Sanitize attribute content to be template-safe */
      if (SAFE_FOR_TEMPLATES) {
        arrayForEach([MUSTACHE_EXPR, ERB_EXPR, TMPLIT_EXPR], expr => {
          value = stringReplace(value, expr, ' ');
        });
      }
      /* Is `value` valid for this attribute? */
      const lcTag = transformCaseFunc(currentNode.nodeName);
      if (!_isValidAttribute(lcTag, lcName, value)) {
        _removeAttribute(name, currentNode);
        continue;
      }
      /* Handle attributes that require Trusted Types */
      if (trustedTypesPolicy && typeof trustedTypes === 'object' && typeof trustedTypes.getAttributeType === 'function') {
        if (namespaceURI) ; else {
          switch (trustedTypes.getAttributeType(lcTag, lcName)) {
            case 'TrustedHTML':
              {
                value = trustedTypesPolicy.createHTML(value);
                break;
              }
            case 'TrustedScriptURL':
              {
                value = trustedTypesPolicy.createScriptURL(value);
                break;
              }
          }
        }
      }
      /* Handle invalid data-* attribute set by try-catching it */
      if (value !== initValue) {
        try {
          if (namespaceURI) {
            currentNode.setAttributeNS(namespaceURI, name, value);
          } else {
            /* Fallback to setAttribute() for browser-unrecognized namespaces e.g. "x-schema". */
            currentNode.setAttribute(name, value);
          }
          if (_isClobbered(currentNode)) {
            _forceRemove(currentNode);
          } else {
            arrayPop(DOMPurify.removed);
          }
        } catch (_) {
          _removeAttribute(name, currentNode);
        }
      }
    }
    /* Execute a hook if present */
    _executeHooks(hooks.afterSanitizeAttributes, currentNode, null);
  };
  /**
   * _sanitizeShadowDOM
   *
   * @param fragment to iterate over recursively
   */
  const _sanitizeShadowDOM = function _sanitizeShadowDOM(fragment) {
    let shadowNode = null;
    const shadowIterator = _createNodeIterator(fragment);
    /* Execute a hook if present */
    _executeHooks(hooks.beforeSanitizeShadowDOM, fragment, null);
    while (shadowNode = shadowIterator.nextNode()) {
      /* Execute a hook if present */
      _executeHooks(hooks.uponSanitizeShadowNode, shadowNode, null);
      /* Sanitize tags and elements */
      _sanitizeElements(shadowNode);
      /* Check attributes next */
      _sanitizeAttributes(shadowNode);
      /* Deep shadow DOM detected */
      if (shadowNode.content instanceof DocumentFragment) {
        _sanitizeShadowDOM(shadowNode.content);
      }
    }
    /* Execute a hook if present */
    _executeHooks(hooks.afterSanitizeShadowDOM, fragment, null);
  };
  // eslint-disable-next-line complexity
  DOMPurify.sanitize = function (dirty) {
    let cfg = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : {};
    let body = null;
    let importedNode = null;
    let currentNode = null;
    let returnNode = null;
    /* Make sure we have a string to sanitize.
      DO NOT return early, as this will return the wrong type if
      the user has requested a DOM object rather than a string */
    IS_EMPTY_INPUT = !dirty;
    if (IS_EMPTY_INPUT) {
      dirty = '<!-->';
    }
    /* Stringify, in case dirty is an object */
    if (typeof dirty !== 'string' && !_isNode(dirty)) {
      if (typeof dirty.toString === 'function') {
        dirty = dirty.toString();
        if (typeof dirty !== 'string') {
          throw typeErrorCreate('dirty is not a string, aborting');
        }
      } else {
        throw typeErrorCreate('toString is not a function');
      }
    }
    /* Return dirty HTML if DOMPurify cannot run */
    if (!DOMPurify.isSupported) {
      return dirty;
    }
    /* Assign config vars */
    if (!SET_CONFIG) {
      _parseConfig(cfg);
    }
    /* Clean up removed elements */
    DOMPurify.removed = [];
    /* Check if dirty is correctly typed for IN_PLACE */
    if (typeof dirty === 'string') {
      IN_PLACE = false;
    }
    if (IN_PLACE) {
      /* Do some early pre-sanitization to avoid unsafe root nodes */
      if (dirty.nodeName) {
        const tagName = transformCaseFunc(dirty.nodeName);
        if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
          throw typeErrorCreate('root node is forbidden and cannot be sanitized in-place');
        }
      }
    } else if (dirty instanceof Node) {
      /* If dirty is a DOM element, append to an empty document to avoid
         elements being stripped by the parser */
      body = _initDocument('<!---->');
      importedNode = body.ownerDocument.importNode(dirty, true);
      if (importedNode.nodeType === NODE_TYPE.element && importedNode.nodeName === 'BODY') {
        /* Node is already a body, use as is */
        body = importedNode;
      } else if (importedNode.nodeName === 'HTML') {
        body = importedNode;
      } else {
        // eslint-disable-next-line unicorn/prefer-dom-node-append
        body.appendChild(importedNode);
      }
    } else {
      /* Exit directly if we have nothing to do */
      if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT &&
      // eslint-disable-next-line unicorn/prefer-includes
      dirty.indexOf('<') === -1) {
        return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
      }
      /* Initialize the document to work on */
      body = _initDocument(dirty);
      /* Check we have a DOM node from the data */
      if (!body) {
        return RETURN_DOM ? null : RETURN_TRUSTED_TYPE ? emptyHTML : '';
      }
    }
    /* Remove first element node (ours) if FORCE_BODY is set */
    if (body && FORCE_BODY) {
      _forceRemove(body.firstChild);
    }
    /* Get node iterator */
    const nodeIterator = _createNodeIterator(IN_PLACE ? dirty : body);
    /* Now start iterating over the created document */
    while (currentNode = nodeIterator.nextNode()) {
      /* Sanitize tags and elements */
      _sanitizeElements(currentNode);
      /* Check attributes next */
      _sanitizeAttributes(currentNode);
      /* Shadow DOM detected, sanitize it */
      if (currentNode.content instanceof DocumentFragment) {
        _sanitizeShadowDOM(currentNode.content);
      }
    }
    /* If we sanitized `dirty` in-place, return it. */
    if (IN_PLACE) {
      return dirty;
    }
    /* Return sanitized string or DOM */
    if (RETURN_DOM) {
      if (RETURN_DOM_FRAGMENT) {
        returnNode = createDocumentFragment.call(body.ownerDocument);
        while (body.firstChild) {
          // eslint-disable-next-line unicorn/prefer-dom-node-append
          returnNode.appendChild(body.firstChild);
        }
      } else {
        returnNode = body;
      }
      if (ALLOWED_ATTR.shadowroot || ALLOWED_ATTR.shadowrootmode) {
        /*
          AdoptNode() is not used because internal state is not reset
          (e.g. the past names map of a HTMLFormElement), this is safe
          in theory but we would rather not risk another attack vector.
          The state that is cloned by importNode() is explicitly defined
          by the specs.
        */
        returnNode = importNode.call(originalDocument, returnNode, true);
      }
      return returnNode;
    }
    let serializedHTML = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
    /* Serialize doctype if allowed */
    if (WHOLE_DOCUMENT && ALLOWED_TAGS['!doctype'] && body.ownerDocument && body.ownerDocument.doctype && body.ownerDocument.doctype.name && regExpTest(DOCTYPE_NAME, body.ownerDocument.doctype.name)) {
      serializedHTML = '<!DOCTYPE ' + body.ownerDocument.doctype.name + '>\n' + serializedHTML;
    }
    /* Sanitize final string template-safe */
    if (SAFE_FOR_TEMPLATES) {
      arrayForEach([MUSTACHE_EXPR, ERB_EXPR, TMPLIT_EXPR], expr => {
        serializedHTML = stringReplace(serializedHTML, expr, ' ');
      });
    }
    return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(serializedHTML) : serializedHTML;
  };
  DOMPurify.setConfig = function () {
    let cfg = arguments.length > 0 && arguments[0] !== undefined ? arguments[0] : {};
    _parseConfig(cfg);
    SET_CONFIG = true;
  };
  DOMPurify.clearConfig = function () {
    CONFIG = null;
    SET_CONFIG = false;
  };
  DOMPurify.isValidAttribute = function (tag, attr, value) {
    /* Initialize shared config vars if necessary. */
    if (!CONFIG) {
      _parseConfig({});
    }
    const lcTag = transformCaseFunc(tag);
    const lcName = transformCaseFunc(attr);
    return _isValidAttribute(lcTag, lcName, value);
  };
  DOMPurify.addHook = function (entryPoint, hookFunction) {
    if (typeof hookFunction !== 'function') {
      return;
    }
    arrayPush(hooks[entryPoint], hookFunction);
  };
  DOMPurify.removeHook = function (entryPoint, hookFunction) {
    if (hookFunction !== undefined) {
      const index = arrayLastIndexOf(hooks[entryPoint], hookFunction);
      return index === -1 ? undefined : arraySplice(hooks[entryPoint], index, 1)[0];
    }
    return arrayPop(hooks[entryPoint]);
  };
  DOMPurify.removeHooks = function (entryPoint) {
    hooks[entryPoint] = [];
  };
  DOMPurify.removeAllHooks = function () {
    hooks = _createHooksMap();
  };
  return DOMPurify;
}
var purify = createDOMPurify();


//# sourceMappingURL=purify.es.mjs.map


/***/ }),

/***/ "./node_modules/react-dom/client.js":
/*!******************************************!*\
  !*** ./node_modules/react-dom/client.js ***!
  \******************************************/
/***/ ((__unused_webpack_module, exports, __webpack_require__) => {



var m = __webpack_require__(/*! react-dom */ "react-dom");
if (false) // removed by dead control flow
{} else {
  var i = m.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED;
  exports.createRoot = function(c, o) {
    i.usingClientEntryPoint = true;
    try {
      return m.createRoot(c, o);
    } finally {
      i.usingClientEntryPoint = false;
    }
  };
  exports.hydrateRoot = function(c, h, o) {
    i.usingClientEntryPoint = true;
    try {
      return m.hydrateRoot(c, h, o);
    } finally {
      i.usingClientEntryPoint = false;
    }
  };
}


/***/ }),

/***/ "@wordpress/blocks":
/*!********************************!*\
  !*** external ["wp","blocks"] ***!
  \********************************/
/***/ ((module) => {

module.exports = window["wp"]["blocks"];

/***/ }),

/***/ "@wordpress/data":
/*!******************************!*\
  !*** external ["wp","data"] ***!
  \******************************/
/***/ ((module) => {

module.exports = window["wp"]["data"];

/***/ }),

/***/ "@wordpress/element":
/*!*********************************!*\
  !*** external ["wp","element"] ***!
  \*********************************/
/***/ ((module) => {

module.exports = window["wp"]["element"];

/***/ }),

/***/ "@wordpress/i18n":
/*!******************************!*\
  !*** external ["wp","i18n"] ***!
  \******************************/
/***/ ((module) => {

module.exports = window["wp"]["i18n"];

/***/ }),

/***/ "react":
/*!************************!*\
  !*** external "React" ***!
  \************************/
/***/ ((module) => {

module.exports = window["React"];

/***/ }),

/***/ "react-dom":
/*!***************************!*\
  !*** external "ReactDOM" ***!
  \***************************/
/***/ ((module) => {

module.exports = window["ReactDOM"];

/***/ })

/******/ 	});
/************************************************************************/
/******/ 	// The module cache
/******/ 	var __webpack_module_cache__ = {};
/******/ 	
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/ 		// Check if module is in cache
/******/ 		var cachedModule = __webpack_module_cache__[moduleId];
/******/ 		if (cachedModule !== undefined) {
/******/ 			return cachedModule.exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = __webpack_module_cache__[moduleId] = {
/******/ 			// no module.id needed
/******/ 			// no module.loaded needed
/******/ 			exports: {}
/******/ 		};
/******/ 	
/******/ 		// Execute the module function
/******/ 		__webpack_modules__[moduleId](module, module.exports, __webpack_require__);
/******/ 	
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/ 	
/************************************************************************/
/******/ 	/* webpack/runtime/define property getters */
/******/ 	(() => {
/******/ 		// define getter functions for harmony exports
/******/ 		__webpack_require__.d = (exports, definition) => {
/******/ 			for(var key in definition) {
/******/ 				if(__webpack_require__.o(definition, key) && !__webpack_require__.o(exports, key)) {
/******/ 					Object.defineProperty(exports, key, { enumerable: true, get: definition[key] });
/******/ 				}
/******/ 			}
/******/ 		};
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/hasOwnProperty shorthand */
/******/ 	(() => {
/******/ 		__webpack_require__.o = (obj, prop) => (Object.prototype.hasOwnProperty.call(obj, prop))
/******/ 	})();
/******/ 	
/******/ 	/* webpack/runtime/make namespace object */
/******/ 	(() => {
/******/ 		// define __esModule on exports
/******/ 		__webpack_require__.r = (exports) => {
/******/ 			if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 				Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 			}
/******/ 			Object.defineProperty(exports, '__esModule', { value: true });
/******/ 		};
/******/ 	})();
/******/ 	
/************************************************************************/
var __webpack_exports__ = {};
// This entry needs to be wrapped in an IIFE because it needs to be isolated against other modules in the chunk.
(() => {
/*!***********************************************!*\
  !*** ./modules/page-translation/src/index.js ***!
  \***********************************************/
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var _popup_setting_modal_index_js__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./popup-setting-modal/index.js */ "./modules/page-translation/src/popup-setting-modal/index.js");
/* harmony import */ var _global_store_index_js__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./global-store/index.js */ "./modules/page-translation/src/global-store/index.js");
/* harmony import */ var react__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! react */ "react");
/* harmony import */ var _fetch_post_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./fetch-post/gutenberg/index.js */ "./modules/page-translation/src/fetch-post/gutenberg/index.js");
/* harmony import */ var _create_translated_post_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! ./create-translated-post/gutenberg/index.js */ "./modules/page-translation/src/create-translated-post/gutenberg/index.js");
/* harmony import */ var _fetch_post_classic_index_js__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! ./fetch-post/classic/index.js */ "./modules/page-translation/src/fetch-post/classic/index.js");
/* harmony import */ var _create_translated_post_classic_index_js__WEBPACK_IMPORTED_MODULE_6__ = __webpack_require__(/*! ./create-translated-post/classic/index.js */ "./modules/page-translation/src/create-translated-post/classic/index.js");
/* harmony import */ var _component_notice_index_js__WEBPACK_IMPORTED_MODULE_7__ = __webpack_require__(/*! ./component/notice/index.js */ "./modules/page-translation/src/component/notice/index.js");
/* harmony import */ var _wordpress_data__WEBPACK_IMPORTED_MODULE_8__ = __webpack_require__(/*! @wordpress/data */ "@wordpress/data");
/* harmony import */ var _wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__ = __webpack_require__(/*! @wordpress/i18n */ "@wordpress/i18n");
/* harmony import */ var _fetch_post_elementor_index_js__WEBPACK_IMPORTED_MODULE_10__ = __webpack_require__(/*! ./fetch-post/elementor/index.js */ "./modules/page-translation/src/fetch-post/elementor/index.js");
/* harmony import */ var _create_translated_post_elementor_index_js__WEBPACK_IMPORTED_MODULE_11__ = __webpack_require__(/*! ./create-translated-post/elementor/index.js */ "./modules/page-translation/src/create-translated-post/elementor/index.js");
/* harmony import */ var _fetch_post_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_12__ = __webpack_require__(/*! ./fetch-post/wpbakery/index.js */ "./modules/page-translation/src/fetch-post/wpbakery/index.js");
/* harmony import */ var _create_translated_post_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_13__ = __webpack_require__(/*! ./create-translated-post/wpbakery/index.js */ "./modules/page-translation/src/create-translated-post/wpbakery/index.js");
/* harmony import */ var react_dom_client__WEBPACK_IMPORTED_MODULE_14__ = __webpack_require__(/*! react-dom/client */ "./node_modules/react-dom/client.js");
/* harmony import */ var _fetch_post_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_15__ = __webpack_require__(/*! ./fetch-post/meta-fields/index.js */ "./modules/page-translation/src/fetch-post/meta-fields/index.js");
/* harmony import */ var _index_scss__WEBPACK_IMPORTED_MODULE_16__ = __webpack_require__(/*! ./index.scss */ "./modules/page-translation/src/index.scss");











// Elementor post fetch and update page



// WPBakery post fetch and update page





const editorType = window.lmatPageTranslationGlobal.editor_type;
const init = () => {
  let lmatModals = new Array();
  const lmatSettingModalWrp = '<!-- The Modal --><div id="lmat-page-translation-setting-modal"></div>';
  const lmatStringModalWrp = '<div id="lmat_page_translation_strings_model" class="modal lmat_page_translation_custom_model"></div>';
  lmatModals.push(lmatSettingModalWrp, lmatStringModalWrp);
  lmatModals.forEach(modal => {
    document.body.insertAdjacentHTML("beforeend", modal);
  });
};
const StringModalBodyNotice = () => {
  const notices = [];
  const postMetaSync = lmatPageTranslationGlobal.postMetaSync === "true";
  if (postMetaSync) {
    notices.push({
      className: "lmat-page-translation-notice lmat-page-translation-notice-error",
      message: /*#__PURE__*/React.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("For accurate custom field translations, please disable the Custom Fields synchronization in ", "translate-words"), /*#__PURE__*/React.createElement("a", {
        href: `${lmatPageTranslationGlobal.admin_url}admin.php?page=lmat_settings`,
        target: "_blank",
        rel: "noopener noreferrer"
      }, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Linguator settings", "translate-words")), (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)(". This may affect linked posts or pages.", "translate-words"))
    });
  }
  if (editorType === "gutenberg") {
    const blockRules = (0,_wordpress_data__WEBPACK_IMPORTED_MODULE_8__.select)("block-lmatPageTranslation/translate").getBlockRules();
    if (!blockRules.LmatBlockParseRules || Object.keys(blockRules.LmatBlockParseRules).length === 0) {
      notices.push({
        className: "lmat-page-translation-notice lmat-page-translation-notice-error",
        message: /*#__PURE__*/React.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("No block rules were found. It appears that the block-rules.JSON file could not be fetched, possibly because it is blocked by your server settings. Please check your server configuration to resolve this issue.", "translate-words"))
      });
    }
  }
  if (editorType === "classic") {
    const blockCommentTag = lmatPageTranslationGlobal.blockCommentTag === "true";
    if (blockCommentTag) {
      notices.push({
        className: "lmat-page-translation-notice lmat-page-translation-notice-error",
        message: /*#__PURE__*/React.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("This page may contain Gutenberg block content. After the translation, please review the updated content before finalizing the page update.", "translate-words"))
      });
    }
  }
  if (editorType === "wpbakery") {
    notices.push({
      className: "lmat-page-translation-notice lmat-page-translation-notice-info",
      message: /*#__PURE__*/React.createElement("p", null, (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("WPBakery Page Builder content will be translated. Please save the page after translation to apply changes.", "translate-words"))
    });
  }
  const noticeLength = notices.length;
  if (notices.length > 0) {
    return notices.map((notice, index) => /*#__PURE__*/React.createElement(_component_notice_index_js__WEBPACK_IMPORTED_MODULE_7__["default"], {
      className: notice.className,
      key: index,
      lastNotice: index === noticeLength - 1
    }, notice.message));
  }
  return;
};
const App = () => {
  const [pageTranslate, setPageTranslate] = (0,react__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const targetLang = window.lmatPageTranslationGlobal.target_lang;
  const postId = window.lmatPageTranslationGlobal.parent_post_id;
  const currentPostId = window.lmatPageTranslationGlobal.current_post_id;
  const postType = window.lmatPageTranslationGlobal.post_type;
  let translatePost, fetchPost, translateWrpSelector;
  const sourceLang = window.lmatPageTranslationGlobal.source_lang;
  // Elementor post fetch and update page
  if (editorType === "elementor") {
    translateWrpSelector = 'button.lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]';
    translatePost = _create_translated_post_elementor_index_js__WEBPACK_IMPORTED_MODULE_11__["default"];
    fetchPost = _fetch_post_elementor_index_js__WEBPACK_IMPORTED_MODULE_10__["default"];
  } else if (editorType === "gutenberg") {
    translateWrpSelector = 'input#lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]';
    translatePost = _create_translated_post_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_4__["default"];
    fetchPost = _fetch_post_gutenberg_index_js__WEBPACK_IMPORTED_MODULE_3__["default"];
  } else if (editorType === "classic") {
    translateWrpSelector = 'button#lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]';
    translatePost = _create_translated_post_classic_index_js__WEBPACK_IMPORTED_MODULE_6__["default"];
    fetchPost = _fetch_post_classic_index_js__WEBPACK_IMPORTED_MODULE_5__["default"];
  } else if (editorType === "wpbakery") {
    translateWrpSelector = 'button#lmat-page-translation-button[name="lmat_page_translation_meta_box_translate"]';
    translatePost = _create_translated_post_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_13__["default"];
    fetchPost = _fetch_post_wpbakery_index_js__WEBPACK_IMPORTED_MODULE_12__["default"];
  }
  const [postDataFetchStatus, setPostDataFetchStatus] = (0,react__WEBPACK_IMPORTED_MODULE_2__.useState)(false);
  const [loading, setLoading] = (0,react__WEBPACK_IMPORTED_MODULE_2__.useState)(true);
  const fetchPostData = async data => {
    await (0,_fetch_post_meta_fields_index_js__WEBPACK_IMPORTED_MODULE_15__["default"])(data);
    await fetchPost(data);
    const allEntries = wp.data.select("block-lmatPageTranslation/translate").getTranslationEntries();
    let totalStringCount = 0;
    let totalCharacterCount = 0;
    let totalWordCount = 0;
    allEntries.map(entries => {
      const source = entries.source ? entries.source : "";
      const stringCount = source.split(/(?<=[.!?]+)\s+/).length;
      const wordCount = source.trim().split(/\s+/).filter(word => /[^\p{L}\p{N}]/.test(word)).length;
      const characterCount = source.length;
      totalStringCount += stringCount;
      totalCharacterCount += characterCount;
      totalWordCount += wordCount;
    });
    wp.data.dispatch("block-lmatPageTranslation/translate").translationInfo({
      sourceStringCount: totalStringCount,
      sourceWordCount: totalWordCount,
      sourceCharacterCount: totalCharacterCount
    });
  };
  const updatePostDataFetch = status => {
    setPostDataFetchStatus(status);
    setLoading(false);
  };
  const handlePageTranslate = status => {
    setPageTranslate(status);
  };
  (0,react__WEBPACK_IMPORTED_MODULE_2__.useEffect)(() => {
    if (pageTranslate) {
      const metaFieldBtn = document.querySelector(translateWrpSelector);
      if (metaFieldBtn) {
        metaFieldBtn.disabled = true;
        metaFieldBtn.value = (0,_wordpress_i18n__WEBPACK_IMPORTED_MODULE_9__.__)("Already Translated", "translate-words");
      }
    }
  }, [pageTranslate]);
  if (!sourceLang || "" === sourceLang) {
    const metaFieldBtn = document.querySelector(translateWrpSelector);
    if (metaFieldBtn) {
      metaFieldBtn.title = `Parent ${window.lmatPageTranslationGlobal.post_type} may be deleted.`;
      metaFieldBtn.disabled = true;
    }
    return;
  }
  return /*#__PURE__*/React.createElement(React.Fragment, null, !pageTranslate && sourceLang && "" !== sourceLang && /*#__PURE__*/React.createElement(_popup_setting_modal_index_js__WEBPACK_IMPORTED_MODULE_0__["default"], {
    contentLoading: loading,
    updatePostDataFetch: updatePostDataFetch,
    postDataFetchStatus: postDataFetchStatus,
    pageTranslate: handlePageTranslate,
    postId: postId,
    currentPostId: currentPostId,
    targetLang: targetLang,
    postType: postType,
    fetchPostData: fetchPostData,
    translatePost: translatePost,
    translateWrpSelector: translateWrpSelector,
    stringModalBodyNotice: StringModalBodyNotice
  }));
};

/**
 * Creates a message popup based on the post type and target language.
 * @returns {HTMLElement} The created message popup element.
 */
const createMessagePopup = () => {
  const postType = window.lmatPageTranslationGlobal.post_type;
  const targetLang = window.lmatPageTranslationGlobal.target_lang;
  const targetLangName = lmatPageTranslationGlobal.languageObject[targetLang]["name"];
  const messagePopup = document.createElement("div");
  messagePopup.id = "lmat-page-translation-modal-open-warning-wrapper";
  messagePopup.innerHTML = `
    <div class="modal-container" style="display: flex">
      <div class="modal-content">
        <p>${wp.i18n.sprintf(wp.i18n.__("Would you like to duplicate your original %s content and have it automatically translated into %s?", "translate-words"), postType, targetLangName)}</p>
        <div>
          <div data-value="yes">${wp.i18n.__("Yes", "translate-words")}</div>
          <div data-value="no">${wp.i18n.__("No", "translate-words")}</div>
        </div>
      </div>
    </div>`;
  return messagePopup;
};

/**
 * Inserts the message popup into the DOM.
 */
const insertMessagePopup = () => {
  const targetElement = document.getElementById("lmat-page-translation-setting-modal");
  const messagePopup = createMessagePopup();
  document.body.insertBefore(messagePopup, targetElement);
};

/**
 * Elementor translate button append
 */
const appendElementorTranslateBtn = () => {
  const translateButtonGroup = jQuery(".MuiButtonGroup-root.MuiButtonGroup-contained").parent();
  const buttonElement = jQuery(translateButtonGroup).find(".elementor-button.lmat-page-translation-button");
  if (translateButtonGroup.length > 0 && buttonElement.length === 0) {
    const buttonHtml = '<button class="elementor-button lmat-page-translation-button" name="lmat_page_translation_meta_box_translate">Translate</button>';
    const buttonElement = jQuery(buttonHtml);
    let confirmBox = false;
    const postId = window.lmatPageTranslationGlobal.current_post_id;
    const targetLang = window.lmatPageTranslationGlobal.target_lang;
    const oldData = localStorage.getItem("lmatElementorConfirmBox");
    if (oldData && "string" === typeof oldData && "" !== oldData) {
      confirmBox = JSON.parse(oldData);
    }
    translateButtonGroup.prepend(buttonElement);
    $e.internal("document/save/set-is-modified", {
      status: true
    });
    if (!window.lmatPageTranslationGlobal.elementorData || "" === window.lmatPageTranslationGlobal.elementorData || window.lmatPageTranslationGlobal.elementorData.length < 1 || elementor.elements.length < 1) {
      if (confirmBox && confirmBox[postId + "_" + targetLang]) {
        delete confirmBox[postId + "_" + targetLang];
        if (Object.keys(confirmBox).length === 0) {
          localStorage.removeItem("lmatElementorConfirmBox");
        } else {
          localStorage.setItem("lmatElementorConfirmBox", JSON.stringify(confirmBox));
        }
      }
      buttonElement.attr("disabled", "disabled");
      buttonElement.attr("title", "Translation is not available because there is no Elementor data.");
      return;
    }
    // Append app root wrapper in body
    init();
    const root = react_dom_client__WEBPACK_IMPORTED_MODULE_14__.createRoot(document.getElementById("lmat-page-translation-setting-modal"));
    root.render(/*#__PURE__*/React.createElement(App, null));
    if (confirmBox && confirmBox[postId + "_" + targetLang]) {
      setTimeout(() => {
        buttonElement.trigger("click");
        delete confirmBox[postId + "_" + targetLang];
        if (Object.keys(confirmBox).length === 0) {
          localStorage.removeItem("lmatElementorConfirmBox");
        } else {
          localStorage.setItem("lmatElementorConfirmBox", JSON.stringify(confirmBox));
        }
      }, 100);
    }
  }
};
if (editorType === "gutenberg") {
  // Render App
  window.addEventListener("load", () => {
    // Append app root wrapper in body
    init();
    const sourceLang = window.lmatPageTranslationGlobal.source_lang;
    const providers = window.lmatPageTranslationGlobal.providers;
    if (sourceLang && "" !== sourceLang && providers.length > 0) {
      insertMessagePopup();
    }
    const root = react_dom_client__WEBPACK_IMPORTED_MODULE_14__.createRoot(document.getElementById("lmat-page-translation-setting-modal"));
    root.render(/*#__PURE__*/React.createElement(App, null));
  });
}

// Classic editor translate button append
if (editorType === "classic") {
  // Render App
  window.addEventListener("load", () => {
    // Append app root wrapper in body
    init();
    const sourceLang = window.lmatPageTranslationGlobal.source_lang;
    const providers = window.lmatPageTranslationGlobal.providers;
    if (sourceLang && "" !== sourceLang && providers.length > 0) {
      insertMessagePopup();
    }
    const root = react_dom_client__WEBPACK_IMPORTED_MODULE_14__.createRoot(document.getElementById("lmat-page-translation-setting-modal"));
    root.render(/*#__PURE__*/React.createElement(App, null));
  });
}

// Elementor translate button append
if (editorType === "elementor") {
  jQuery(window).on("elementor:init", function () {
    elementor.on("document:loaded", appendElementorTranslateBtn);
  });
}

// WPBakery translate button append
if (editorType === "wpbakery") {
  // Render App
  window.addEventListener("load", () => {
    // Append app root wrapper in body
    init();
    const sourceLang = window.lmatPageTranslationGlobal.source_lang;
    const providers = window.lmatPageTranslationGlobal.providers;
    if (sourceLang && "" !== sourceLang && providers.length > 0) {
      insertMessagePopup();
    }
    const root = react_dom_client__WEBPACK_IMPORTED_MODULE_14__.createRoot(document.getElementById("lmat-page-translation-setting-modal"));
    root.render(/*#__PURE__*/React.createElement(App, null));
  });
}
})();

/******/ })()
;
//# sourceMappingURL=index.js.map